---
phase: quick
plan: 01
subsystem: infra
tags: [whatsapp, cleanup, dead-code, config, telegram-only]

requires: []
provides:
  - Dormant WhatsApp Cloud API integration fully removed (bot is Telegram-only)
affects: [server, config, database-queries, logger]

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - src/server.ts
    - src/database/queries.ts
    - src/config.ts
    - src/utils/logger.ts
    - tests/consent.test.ts
    - tests/jest.setup.ts
    - tests/config.test.ts

key-decisions:
  - "messages DB table and its whatsapp_message_id column left in schema.ts — tests/rls-enforcement.test.ts actively depends on the table for RLS isolation testing, unrelated to the WhatsApp integration itself"
  - "src/database/schema.ts and src/conversation/router.ts comment-only historical references to WhatsApp left untouched — explicitly out of scope, not executable code"

patterns-established: []

requirements-completed: []

coverage:
  - id: D1
    description: "src/whatsapp/, src/webhooks/whatsapp.ts, src/utils/validation.ts, src/business/resolver.ts deleted; server.ts no longer mounts /webhooks/whatsapp"
    verification:
      - kind: unit
        ref: "git grep -il whatsapp -- src tests (excluding .planning/) returns only schema.ts comment, router.ts comment, rls-enforcement.test.ts"
        status: pass
    human_judgment: false
  - id: D2
    description: "queries.ts no longer exports insertOrIgnoreMessage/markMessageProcessed/findMessageByWhatsappId; config.ts/logger.ts/jest.setup.ts/config.test.ts no longer reference WhatsApp-only env vars"
    verification:
      - kind: unit
        ref: "npx tsc --noEmit"
        status: pass
    human_judgment: false
  - id: D3
    description: "Telegram path (src/telegram/, src/webhooks/telegram.ts, src/onboarding/*, function-executor.ts, src/consent/checker.ts) untouched by this change"
    verification:
      - kind: unit
        ref: "git diff --stat da88890~1..acbf908 -- src/telegram src/webhooks/telegram.ts src/onboarding src/consent (empty diff)"
        status: pass
    human_judgment: false

duration: 30min
completed: 2026-07-26
status: complete
---

# Quick Task 260726-0x3: Remove WhatsApp Integration Summary

**Deleted the dormant WhatsApp Cloud API integration (webhook route, send client, payload validation, slug resolver, config/env vars, DB helpers) — the bot has run Telegram-only since the v1.4 single-bot pivot, and this dead code was confusing to future contributors without adding runtime value.**

## Performance

- **Duration:** ~30 min (3 atomic task commits)
- **Tasks:** 3 completed
- **Files modified:** 13 (4 deleted source files, 4 deleted test files, 5 modified)

## Task Commits

Each task was committed atomically:

1. **Task 1: Delete dormant WhatsApp send/receive module + dedicated tests** - `da88890` (refactor)
2. **Task 2: Remove WhatsApp-only DB helpers from queries.ts** - `0902de2` (refactor)
3. **Task 3: Remove WhatsApp-only env vars/config fields** - `acbf908` (refactor)

## Files Created/Modified
- `src/whatsapp/client.ts` — deleted (WhatsApp Graph API send client)
- `src/webhooks/whatsapp.ts` — deleted (WhatsApp webhook route)
- `src/utils/validation.ts` — deleted (WhatsApp-payload-only signature validation)
- `src/business/resolver.ts` — deleted (WhatsApp-slug-only business resolver, unused by Telegram's per-bot routing)
- `tests/whatsapp-client.test.ts`, `tests/webhook.test.ts`, `tests/idempotency.test.ts`, `tests/business-resolver.test.ts` — deleted
- `src/server.ts` — WhatsApp route mount removed
- `src/database/queries.ts` — `insertOrIgnoreMessage`/`markMessageProcessed`/`findMessageByWhatsappId` removed
- `src/config.ts` — `APP_SECRET`, `WEBHOOK_VERIFY_TOKEN`, `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID` removed from `EnvSchema`/`Config`
- `src/utils/logger.ts` — corresponding redact-path entries removed
- `tests/consent.test.ts` — pruned to channel-agnostic `getOrCreateClientRelationship` unit tests
- `tests/jest.setup.ts`, `tests/config.test.ts` — WhatsApp env var defaults/references removed

## Decisions Made
- `messages` table kept in schema.ts (RLS test dependency), only the WhatsApp-exclusive query helpers built on top of it were removed.
- Comment-only WhatsApp references in `schema.ts` and `router.ts` left as historical documentation, per plan scope.

## Deviations from Plan

None — plan executed as written across all 3 tasks.

## Issues Encountered

None new. `tests/config.test.ts`'s pre-existing `platformBotToken`/`platformWebhookSecret` debt (documented in STATE.md) is unrelated and left untouched.

## User Setup Required

None.

## Next Phase Readiness

Self-contained cleanup, no follow-on work required. The separate `.planning/todos/pending/2026-07-07-pivot-to-per-business-whatsapp-numbers-post-poc.md` todo (WhatsApp re-introduction post-PoC) is untouched and remains a future decision, independent of this removal.

---
*Phase: quick*
*Completed: 2026-07-26*

## Self-Check: PASSED

All 4 source files and 4 test files confirmed deleted from working tree; commits `da88890`, `0902de2`, `acbf908` confirmed present in git log. `git grep -il whatsapp -- src tests` (excluding `.planning/`) returns only the 3 expected historical/structural survivors.
