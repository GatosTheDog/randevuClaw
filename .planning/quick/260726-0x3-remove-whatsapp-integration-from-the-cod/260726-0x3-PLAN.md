---
id: 260726-0x3
title: Remove dormant WhatsApp integration (Telegram-only bot)
type: execute
status: ready
date: 2026-07-26
autonomous: true
files_modified:
  - src/server.ts
  - src/whatsapp/client.ts (deleted)
  - src/webhooks/whatsapp.ts (deleted)
  - src/utils/validation.ts (deleted)
  - src/business/resolver.ts (deleted)
  - tests/whatsapp-client.test.ts (deleted)
  - tests/webhook.test.ts (deleted)
  - tests/idempotency.test.ts (deleted)
  - tests/business-resolver.test.ts (deleted)
  - tests/consent.test.ts
  - src/database/queries.ts
  - src/config.ts
  - src/utils/logger.ts
  - tests/jest.setup.ts
  - tests/config.test.ts
must_haves:
  truths:
    - "No source file imports from src/whatsapp/ or src/webhooks/whatsapp.ts"
    - "The Express app no longer mounts a /webhooks/whatsapp route"
    - "config.ts no longer requires WhatsApp-only env vars (APP_SECRET, WEBHOOK_VERIFY_TOKEN, WHATSAPP_ACCESS_TOKEN, WHATSAPP_PHONE_NUMBER_ID)"
    - "Telegram webhook handling (src/webhooks/telegram.ts) and consent logic (src/consent/checker.ts) are byte-for-byte unchanged"
    - "The messages DB table stays in schema.ts because tests/rls-enforcement.test.ts actively depends on it for RLS isolation testing — only the WhatsApp-exclusive helper functions built on top of it are removed"
  artifacts:
    - "src/whatsapp/ directory no longer exists"
    - "src/webhooks/whatsapp.ts no longer exists"
    - "src/utils/validation.ts no longer exists (was WhatsApp-payload-only)"
    - "src/business/resolver.ts no longer exists (was WhatsApp-slug-resolution-only, unused by Telegram's per-bot routing)"
  key_links:
    - "src/server.ts only wires src/webhooks/telegram.ts, no whatsapp import"
    - "src/database/queries.ts exports findBusinessBySlug still (used by scripts/setup-google-calendar.ts and multiple tests) but no longer exports insertOrIgnoreMessage/markMessageProcessed/findMessageByWhatsappId"
---

<objective>
Remove the dormant/unused WhatsApp Cloud API integration from the codebase — the bot runs Telegram-only while Meta Business Verification is pending (see .planning/PROJECT.md). Delete the WhatsApp webhook route, message-send client, WhatsApp-only payload validation, and WhatsApp-only business-slug resolver; strip the whatsapp wiring out of src/server.ts; remove WhatsApp-exclusive config/env vars and their logger-redaction entries; remove the three WhatsApp-only DB helper functions in queries.ts; and clean up every test file that exists solely to cover this dead code (or that would otherwise dangling-import a deleted module).

Purpose: WhatsApp is shelved pending an external 1-6 week Meta verification process (tracked separately in .planning/todos/pending/2026-07-07-pivot-to-per-business-whatsapp-numbers-post-poc.md — that todo is untouched by this task). The dormant code adds zero runtime value today, confuses future contributors about what's actually wired up, and — per project constraints — must be removed without touching a single line of the active Telegram booking/notification path.

Output: A codebase where `git grep -i whatsapp -- src tests` (excluding archival `.planning/milestones/` docs, which are historical records and stay as-is) returns nothing outside of the `messages` DB table's `whatsapp_message_id` column (which remains — see must_haves) and the already-existing, out-of-scope deferred-todo file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@./.claude/CLAUDE.md

# Files this plan reads/edits — already scouted during planning, no further discovery needed:
@src/whatsapp/client.ts
@src/webhooks/whatsapp.ts
@src/utils/validation.ts
@src/business/resolver.ts
@src/server.ts
@src/database/queries.ts
@src/config.ts
@src/utils/logger.ts
@tests/consent.test.ts
@tests/jest.setup.ts
@tests/config.test.ts

# NOT in scope — do not touch (active Telegram path, verified untouched by every edit in this plan):
# src/telegram/, src/webhooks/telegram.ts, src/onboarding/*, function-executor.ts, src/consent/checker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the dormant WhatsApp send/receive module and its dedicated tests</name>
  <files>
    src/whatsapp/client.ts (delete)
    src/webhooks/whatsapp.ts (delete)
    src/utils/validation.ts (delete)
    src/business/resolver.ts (delete)
    src/server.ts (modify)
    tests/whatsapp-client.test.ts (delete)
    tests/webhook.test.ts (delete)
    tests/idempotency.test.ts (delete)
    tests/business-resolver.test.ts (delete)
    tests/consent.test.ts (modify)
  </files>
  <action>
Delete four source files outright — each is exclusively used by (or only serves) the WhatsApp webhook, confirmed via repo-wide grep during planning:
- src/whatsapp/client.ts (the sendWhatsAppMessage Graph-API caller)
- src/webhooks/whatsapp.ts (the WhatsApp Cloud API webhook route: GET verify-token challenge, POST HMAC-verified message handler)
- src/utils/validation.ts (WhatsAppMessageSchema/WhatsAppWebhookPayloadSchema/validateWebhookPayload — every export here is WhatsApp-payload-shaped and imported by nothing except the file just deleted)
- src/business/resolver.ts (extractBusinessCode/extractAllBusinessCodeCandidates/normalizeBusinessCode/extractAndNormalizeBusinessCode/extractAndNormalizeAllBusinessCodeCandidates — this whole module resolves a business from a hyphenated slug embedded in a free-text message, which only the WhatsApp shared-number webhook needed; Telegram is one-bot-per-business as of v1.4 and has no caller for it anywhere in src/)

In src/server.ts, remove the line `import webhookRouter from './webhooks/whatsapp';` and the line `app.use('/webhooks/whatsapp', webhookRouter);`. Leave the telegramWebhookRouter import/mount, the /healthz route, the error-handling middleware, and every poller-startup line completely untouched.

Delete four test files that exist solely to cover the deleted module:
- tests/whatsapp-client.test.ts (tests sendWhatsAppMessage directly)
- tests/webhook.test.ts (supertest integration tests hitting GET/POST /webhooks/whatsapp)
- tests/idempotency.test.ts (supertest integration tests for WhatsApp message-dedup behavior via the same route)
- tests/business-resolver.test.ts (unit tests for src/business/resolver.ts)

Edit tests/consent.test.ts — this file mixes two concerns and only one is in scope. Keep the `describe('getOrCreateClientRelationship unit tests', ...)` block exactly as-is (it directly unit-tests src/consent/checker.ts's getOrCreateClientRelationship against mocked src/database/queries functions — channel-agnostic, used by both Telegram and the now-deleted WhatsApp route). Remove everything else: the `describe('Test 3 (webhook level): consent notice in reply', ...)` block (it posts to the now-deleted /webhooks/whatsapp route), and every import/mock/helper that exists only to support it — `import request from 'supertest'`, `import crypto from 'crypto'`, `import app from '../src/server'`, `import * as whatsappClient from '../src/whatsapp/client'`, the `import { CONSENT_NOTICE_GREEK_TEMPLATE }` named import, `jest.mock('../src/whatsapp/client')`, the `KNOWN_BUSINESS` constant, `mockedFindBusinessBySlug`, `mockedInsertOrIgnoreMessage`, `mockedMarkMessageProcessed`, `mockedSendWhatsAppMessage`, the `APP_SECRET` constant, and the `signPayload`/`makeTextPayload`/`postWebhook` helper functions. The reduced file keeps only: `import * as queries from '../src/database/queries'`, `import * as checker from '../src/consent/checker'`, `jest.mock('../src/database/queries')`, and the single surviving describe block with its `mockedFindCBR`/`mockedInsertCBR`/`mockRow` setup and its two `it()` cases.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>src/whatsapp/, src/webhooks/whatsapp.ts, src/utils/validation.ts, and src/business/resolver.ts no longer exist on disk; src/server.ts mounts only the Telegram webhook router; tests/consent.test.ts contains only the getOrCreateClientRelationship unit-test describe block; `npx tsc --noEmit` compiles src/ with zero errors; a repo-wide search for `whatsapp/client` or `webhooks/whatsapp` import paths across src/ and tests/ returns nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the WhatsApp-exclusive DB helper functions from queries.ts</name>
  <files>src/database/queries.ts</files>
  <action>
Remove three functions from src/database/queries.ts — insertOrIgnoreMessage, markMessageProcessed, and findMessageByWhatsappId. All three exist only to support the WhatsApp message-dedup flow in the now-deleted src/webhooks/whatsapp.ts; confirmed via grep during planning that no other src/ file or surviving test calls any of them. Remove the `messages` entry from the schema import at the top of the file (the `import { businesses, clientBusinessRelationships, messages, services, ... } from './schema'` line) since it becomes unused once these three functions are gone — do not remove any other name from that import.

Do NOT touch src/database/schema.ts or the `messages` table definition. Unlike the other WhatsApp-only code in this task, the `messages` table itself is not safe to drop: tests/rls-enforcement.test.ts actively inserts into and selects from `messages` (via db.insert(messages)/appDb.select().from(messages)) as its vehicle for testing multi-tenant RLS isolation (BOT-05, D-10) — a real, unrelated, active security test. Leave schema.ts, that test file, and the physical DB table completely untouched; only the three now-orphaned query-layer functions come out.

Do NOT touch findBusinessBySlug — despite living in the same file and being used by the deleted whatsapp.ts, it is also called by scripts/setup-google-calendar.ts and mocked in multiple surviving test files (tests/telegram-webhook.test.ts among them). It stays exactly as-is.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>insertOrIgnoreMessage, markMessageProcessed, and findMessageByWhatsappId no longer exist in src/database/queries.ts; the `messages` schema table and tests/rls-enforcement.test.ts are unmodified; `npx tsc --noEmit` compiles with zero errors; `findBusinessBySlug` is unchanged and still exported.</done>
</task>

<task type="auto">
  <name>Task 3: Remove WhatsApp-exclusive env vars/config fields and their logger-redaction entries</name>
  <files>
    src/config.ts
    src/utils/logger.ts
    tests/jest.setup.ts
    tests/config.test.ts
  </files>
  <action>
In src/config.ts, remove four env vars from EnvSchema (APP_SECRET, WEBHOOK_VERIFY_TOKEN, WHATSAPP_ACCESS_TOKEN, WHATSAPP_PHONE_NUMBER_ID) — confirmed via grep during planning that config.appSecret and config.webhookVerifyToken are read only inside the now-deleted src/webhooks/whatsapp.ts (Meta HMAC signature check and hub.verify_token challenge respectively), and whatsappAccessToken/whatsappPhoneNumberId only inside the now-deleted src/whatsapp/client.ts. Remove the matching four fields from the exported Config interface (appSecret, webhookVerifyToken, whatsappAccessToken, whatsappPhoneNumberId) and the matching four assignment lines in the exported config object literal. Leave every other env var, the DATABASE_APP_URL/WEBHOOK_BASE_URL optionality comments, and the rest of the file untouched.

In src/utils/logger.ts, remove the now-dead redact-path entries: `appSecret`, `whatsappAccessToken`, `webhookVerifyToken` from the top-level paths array, `*.appSecret`, `*.whatsappAccessToken`, `*.webhookVerifyToken` from the nested-path array, and `config.appSecret`, `config.whatsappAccessToken`, `config.webhookVerifyToken` from the config-namespace array. Leave databaseUrl/databaseAppUrl/geminiApiKey/googleClientSecret/botToken/webhookSecret/googleRefreshToken redaction entries exactly as-is (botToken and webhookSecret are Telegram per-bot credentials, not WhatsApp).

In tests/jest.setup.ts, remove the four lines setting default test env vars for the fields being removed from EnvSchema: `process.env.APP_SECRET ??= 'test-app-secret';`, `process.env.WEBHOOK_VERIFY_TOKEN ??= 'test-verify-token';`, `process.env.WHATSAPP_ACCESS_TOKEN ??= 'test-whatsapp-token';`, `process.env.WHATSAPP_PHONE_NUMBER_ID ??= 'test-phone-number-id';`. Leave every other line (including the PLATFORM_BOT_TOKEN/PLATFORM_WEBHOOK_SECRET/WEBHOOK_BASE_URL lines) untouched.

In tests/config.test.ts, remove every line that sets or asserts the four removed fields: in the first test (`parses a valid full env...`), delete the four `process.env.APP_SECRET = ...` / `WEBHOOK_VERIFY_TOKEN = ...` / `WHATSAPP_ACCESS_TOKEN = ...` / `WHATSAPP_PHONE_NUMBER_ID = ...` assignment lines and the four matching `expect(config.appSecret)...` / `expect(config.webhookVerifyToken)...` / `expect(config.whatsappAccessToken)...` / `expect(config.whatsappPhoneNumberId)...` assertion lines; in the `throws...when a required var is absent` test, delete the `WEBHOOK_VERIFY_TOKEN`/`WHATSAPP_ACCESS_TOKEN`/`WHATSAPP_PHONE_NUMBER_ID` setup lines (keep `delete process.env.APP_SECRET;` removed too since APP_SECRET is no longer a required var to test for — that whole test case's premise disappears; delete the entire `it('throws synchronously with a message naming the missing key when a required var is absent', ...)` block since APP_SECRET was the var it tested for absence); in the two GEMINI_API_KEY/GOOGLE_CLIENT_ID `throws` tests, delete their `APP_SECRET`/`WEBHOOK_VERIFY_TOKEN`/`WHATSAPP_ACCESS_TOKEN`/`WHATSAPP_PHONE_NUMBER_ID` setup lines only (these tests still need their own remaining required vars set to isolate the one var under test).

Note: tests/config.test.ts already references config.platformBotToken/config.platformWebhookSecret, fields that do not exist on the current Config interface (STATE.md documents this as pre-existing test-suite debt unrelated to WhatsApp, part of the known 94 pre-existing failing tests). Do NOT fix that unrelated issue — only remove the WhatsApp/appSecret/webhookVerifyToken lines specified above; leave every other pre-existing line and assertion in the file exactly as it is, broken or not.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>src/config.ts's EnvSchema and Config interface/object no longer reference APP_SECRET/WEBHOOK_VERIFY_TOKEN/WHATSAPP_ACCESS_TOKEN/WHATSAPP_PHONE_NUMBER_ID; src/utils/logger.ts's redact paths no longer list appSecret/whatsappAccessToken/webhookVerifyToken in any form; tests/jest.setup.ts no longer sets defaults for those four vars; tests/config.test.ts no longer references them; `npx tsc --noEmit` compiles with zero errors; a repo-wide search for `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID`, `APP_SECRET`, or `WEBHOOK_VERIFY_TOKEN` across src/ returns nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Express app | Public HTTP surface; this task only *removes* an endpoint (`/webhooks/whatsapp`), it adds no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0x3-01 | Tampering | Removal of `/webhooks/whatsapp` route (was HMAC-verified via verifyWhatsAppSignature) | low | accept | The route is deleted entirely, not left unauthenticated — its attack surface (forged Meta webhook payloads) is eliminated, not weakened. No live WhatsApp number is configured for this route today (PROJECT.md: WhatsApp shelved), so there is no regression in currently-active protection. |
| T-0x3-02 | Information Disclosure | src/utils/logger.ts redact-path edits | low | mitigate | Removing whatsappAccessToken/appSecret/webhookVerifyToken redact entries is safe only because the corresponding config fields no longer exist (Task 3) — verified no other code path re-introduces a field with those names before this task's verify step passes. |
| T-0x3-03 | Tampering | queries.ts DB-helper removal (Task 2) | low | accept | insertOrIgnoreMessage/markMessageProcessed/findMessageByWhatsappId are removed together with their only caller; the underlying `messages` table and its RLS policies are left fully intact for tests/rls-enforcement.test.ts, so no security-relevant DB surface changes. |

No new inbound trust boundary is introduced by this task — it is a pure code-removal task that shrinks attack surface (fewer endpoints, fewer secrets in config) and touches zero lines of the active Telegram path (src/webhooks/telegram.ts, src/telegram/, function-executor.ts, src/consent/checker.ts).
</threat_model>

<verification>
Run each task's own `npx tsc --noEmit` gate as work proceeds (fast, no DB required — catches every dangling import/reference across the whole change set since Config/queries/schema types are shared across files).

After all three tasks, confirm no WhatsApp code remains outside of the two accepted survivors (the `messages.whatsapp_message_id` column, and the pre-existing out-of-scope todo file) with a repo-wide search:
`git grep -il whatsapp -- src tests | grep -v '.planning/'`
Expected result: no output (or only comment-only historical references inside src/database/schema.ts's `messages` table comment and src/conversation/router.ts's channel-agnostic doc comment, both explicitly out of scope per this plan and left untouched).

Run the targeted (not full-suite, per project convention) test files touched by this plan:
`npx jest --testPathPattern "consent.test.ts|config.test.ts"`
Note: config.test.ts is documented pre-existing-broken (STATE.md, platformBotToken debt) independent of this task — a failure there that is NOT related to WhatsApp/appSecret/webhookVerifyToken is expected and out of scope; only confirm no *new* failures reference removed WhatsApp fields.
</verification>

<success_criteria>
- src/whatsapp/, src/webhooks/whatsapp.ts, src/utils/validation.ts, src/business/resolver.ts deleted
- src/server.ts wires only the Telegram webhook router
- src/database/queries.ts no longer exports insertOrIgnoreMessage/markMessageProcessed/findMessageByWhatsappId; messages table schema and RLS test untouched
- src/config.ts, src/utils/logger.ts, tests/jest.setup.ts, tests/config.test.ts no longer reference APP_SECRET/WEBHOOK_VERIFY_TOKEN/WHATSAPP_ACCESS_TOKEN/WHATSAPP_PHONE_NUMBER_ID
- tests/whatsapp-client.test.ts, tests/webhook.test.ts, tests/idempotency.test.ts, tests/business-resolver.test.ts deleted; tests/consent.test.ts retains only its channel-agnostic unit tests
- `npx tsc --noEmit` passes with zero errors
- Zero changes to src/telegram/, src/webhooks/telegram.ts, src/onboarding/*, function-executor.ts, or src/consent/checker.ts
- package.json/package-lock.json: confirmed during planning that no WhatsApp-specific npm package is installed (the integration used raw `fetch()` against the Graph API, not a dedicated SDK) — no dependency removal needed, nothing to do here
</success_criteria>

<output>
Create `.planning/quick/260726-0x3-remove-whatsapp-integration-from-the-cod/260726-0x3-SUMMARY.md` when done.
</output>
