---
phase: 05-owner-self-serve-onboarding
plan: 07
type: execute
wave: 5
depends_on:
  - 05-04
  - 05-05
files_modified:
  - src/database/seed.ts
  - tests/fixtures.test.ts
  - tests/booking-queries.test.ts
  - src/config.ts
  - tests/jest.setup.ts
autonomous: false
requirements:
  - ONB-04
user_setup:
  - service: fly.io
    why: "Platform bot credentials and WEBHOOK_BASE_URL must be set as fly.io secrets before deploying"
    env_vars:
      - name: PLATFORM_BOT_TOKEN
        source: "BotFather on Telegram — create a new bot for the platform onboarding flow"
      - name: PLATFORM_WEBHOOK_SECRET
        source: "Generate via: node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""
      - name: WEBHOOK_BASE_URL
        source: "Your fly.io app URL, e.g. https://randevuclaw.fly.dev"
    dashboard_config:
      - task: "Remove old TEST_BOT_* secrets after this plan completes"
        location: "fly.io dashboard or: fly secrets unset TEST_BOT_1_TOKEN TEST_BOT_1_WEBHOOK_SECRET TEST_BOT_1_WEBHOOK_ID TEST_BOT_2_TOKEN TEST_BOT_2_WEBHOOK_SECRET TEST_BOT_2_WEBHOOK_ID"
      - task: "Apply migration 0004 to live Neon DB"
        location: "Run: psql $DATABASE_URL -f migrations/0004_phase5_onboarding.sql"
      - task: "Delete fixture business rows from live Neon DB"
        location: "Run: psql $DATABASE_URL -c \"DELETE FROM businesses WHERE slug IN ('pilates-athens', 'hair-salon-athens') CASCADE\""
must_haves:
  truths:
    - "src/database/seed.ts no longer defines or exports FIXTURES, SERVICE_FIXTURES, HOURS_FIXTURES, or seed()"
    - "generateSlug is still exported from seed.ts and passes its existing tests"
    - "tests/fixtures.test.ts tests generateSlug only; no seed() tests exist"
    - "tests/booking-queries.test.ts no longer calls seed() or references 'pilates-athens' / 'hair-salon-athens' DB rows"
    - "npm test exits 0 after fixture removal — no tests break from the seed cleanup"
  artifacts:
    - src/database/seed.ts with only generateSlug export
    - tests/fixtures.test.ts with generateSlug-only tests
    - tests/booking-queries.test.ts with seed() reference removed
  key_links:
    - "generateSlug retained and tested — used by platform.ts handleNameStep in Plan 03"
    - "TEST_BOT_* vars removed from config.ts and jest.setup.ts — seed.ts no longer reads them from process.env"
---

<objective>
Remove all hardcoded fixture businesses from seed.ts (ONB-04), rewrite the fixture tests to cover only the retained generateSlug function, and clean up the TEST_BOT_* env vars that existed to populate fixture bot credentials. Also includes a blocking human checkpoint for DB migration application and live fixture deletion.

Purpose: Ensures the codebase has no phantom test businesses and that every business in the system results from real owner onboarding.
Output: Slimmed seed.ts; rewritten fixtures.test.ts; booking-queries.test.ts seed() removal; config.ts and jest.setup.ts TEST_BOT_* cleanup; human checkpoint for live DB ops.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-owner-self-serve-onboarding/05-CONTEXT.md
@.planning/phases/05-owner-self-serve-onboarding/05-RESEARCH.md
@.planning/phases/05-owner-self-serve-onboarding/05-PATTERNS.md
@.planning/phases/05-owner-self-serve-onboarding/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove fixtures from seed.ts and rewrite fixtures.test.ts</name>
  <files>src/database/seed.ts, tests/fixtures.test.ts</files>
  <read_first>
    src/database/seed.ts — full file: generateSlug function (lines 12-30), FIXTURES/SERVICE_FIXTURES/HOURS_FIXTURES constants (lines 32-92), seed() function (lines 100-198); the if require.main === module guard at bottom
    tests/fixtures.test.ts — full file: understand which tests cover generateSlug vs seed()
    05-CONTEXT.md §D-10 — remove FIXTURES, SERVICE_FIXTURES, HOURS_FIXTURES, seed(); generateSlug retained
    05-CONTEXT.md §D-11 — tests use insertTestBusiness() now, not seed()
    tests/helpers/test-business.ts — insertTestBusiness() (created in Plan 01) as the replacement for fixture-based setup
  </read_first>
  <action>
    In src/database/seed.ts:
    - Remove the Fixture interface, ServiceFixture interface, HoursFixture interface
    - Remove FIXTURES, SERVICE_FIXTURES, HOURS_FIXTURES constants
    - Remove the seed() function entirely (D-10)
    - Remove the if require.main === module block at the bottom (no longer an entry point)
    - Remove imports that are no longer needed: businesses, services, businessHours table imports (if only used by seed()); findBusinessBySlug import; logger import (if only used by seed()); config import (if only used by seed())
    - KEEP: generateSlug function and its export; keep the imports generateSlug needs (none — it's a pure string function)
    - Add a comment at the top: "Phase 5 (ONB-04): FIXTURES and seed() removed. Every business is created via the onboarding flow (src/webhooks/platform.ts). Tests use tests/helpers/test-business.ts insertTestBusiness() for DB setup."

    Rewrite tests/fixtures.test.ts completely. The new file should:
    - Import only generateSlug from src/database/seed
    - Not import seed, db, or any database modules
    - Not call jest.mock for any DB module
    - Have one describe block: "generateSlug()"
    - Tests covering: basic slugification (spaces → hyphens, lowercase), non-ASCII chars (Greek business name), collision suffix (-2, -3), empty slug edge cases
    - Extract the generateSlug tests from the current fixtures.test.ts (lines ~88-96 cover generateSlug; expand them)
    - Remove ALL tests that reference seed(), db, or fixture business slugs in the context of DB inserts
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=fixtures --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FIXTURES\|SERVICE_FIXTURES\|HOURS_FIXTURES\|seed()" src/database/seed.ts` returns 0
    - `grep -c "export function generateSlug" src/database/seed.ts` returns 1
    - `npm test -- --testPathPattern=fixtures --no-coverage` exits 0
    - `grep -c "seed()\|pilates-athens\|hair-salon-athens" tests/fixtures.test.ts` returns 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>seed.ts only exports generateSlug; fixtures.test.ts only tests generateSlug; no fixture DB inserts in test file; TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Clean up test DB usage + env var cleanup</name>
  <files>tests/booking-queries.test.ts, src/config.ts, tests/jest.setup.ts</files>
  <read_first>
    tests/booking-queries.test.ts — full file: find the test that calls seed() (line ~309: "Test 3: returns an array containing at least the two seeded fixture business ids after seed() has run"); understand its purpose and how to replace it
    src/config.ts — full file: TEST_BOT_* optional fields in EnvSchema (lines 44-50) and the comment explaining they were for seed.ts; remove them
    tests/jest.setup.ts — full file: TEST_BOT_* and TELEGRAM_WEBHOOK_SECRET placeholder lines to remove
    tests/helpers/test-business.ts — insertTestBusiness() signature (Plan 01) as replacement for seed() in booking-queries
    05-CONTEXT.md §D-10/D-12 — per-test-file setup; each test inserts its own business
  </read_first>
  <action>
    In tests/booking-queries.test.ts:
    Find "Test 3: returns an array containing at least the two seeded fixture business ids after seed() has run" and replace it with a test that uses insertTestBusiness() instead:
    - New test name: "Test 3: listAllBusinessIds includes a business created via insertTestBusiness()"
    - In the test: call insertTestBusiness() (import from ./helpers/test-business) to create a test business. Call listAllBusinessIds(). Assert the result array includes the new business's id. Clean up: delete the test business in afterEach or after the assertion.
    - Remove any import of seed from the test file (only the one test reference).

    In src/config.ts:
    - Remove TEST_BOT_1_TOKEN, TEST_BOT_1_WEBHOOK_SECRET, TEST_BOT_1_WEBHOOK_ID, TEST_BOT_2_TOKEN, TEST_BOT_2_WEBHOOK_SECRET, TEST_BOT_2_WEBHOOK_ID from EnvSchema (the six z.string().optional() lines at lines 44-50).
    - Remove the Phase 04 D-09 comment that describes these vars.

    In tests/jest.setup.ts:
    - Remove the six TEST_BOT_* lines (process.env.TEST_BOT_1_TOKEN, etc.)
    - Remove the TELEGRAM_WEBHOOK_SECRET line (has been dead code since Phase 4 — config.ts no longer parses it; comment in jest.setup.ts acknowledges it was a bridge)
    - The resulting file should have only the core required env vars plus the Phase 5 PLATFORM_* vars added in Plan 01.

    Note: telegram-webhook.test.ts has hardcoded constants that happen to match the old TEST_BOT_* values but do NOT read from process.env. Those constants remain valid and do NOT need to change. The comments in telegram-webhook.test.ts (lines 18-19, 29, 36) referencing "jest.setup.ts" will become inaccurate — update those comments to say the values are hardcoded test constants.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep "TEST_BOT_\|TELEGRAM_WEBHOOK_SECRET" tests/jest.setup.ts` returns nothing
    - `grep "TEST_BOT_" src/config.ts` returns nothing
    - `npm test` exits 0 (full suite, all existing tests pass)
    - `grep -c "seed()" tests/booking-queries.test.ts` returns 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TEST_BOT_* vars removed from config.ts and jest.setup.ts; TELEGRAM_WEBHOOK_SECRET dead code removed; booking-queries.test.ts uses insertTestBusiness(); full npm test suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Apply migration + delete fixtures + update fly.io secrets</name>
  <files>migrations/0004_phase5_onboarding.sql (apply to DB), fly.io secrets (update)</files>
  <action>Human action required: apply SQL migration to both Neon DB and local test DB; delete fixture business rows from live Neon DB; set PLATFORM_BOT_TOKEN/PLATFORM_WEBHOOK_SECRET/WEBHOOK_BASE_URL in fly.io secrets; unset old TEST_BOT_* secrets. See how-to-verify steps below.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Migration applied; fixture rows deleted from live DB; fly.io secrets updated; npm test passes</done>
  <what-built>
    Plans 01-07 have implemented the complete owner self-serve onboarding system:
    - Platform bot handler at /webhooks/telegram/platform
    - DB-backed onboarding state machine (onboarding_sessions table)
    - Guided setup flow: name → hours (7 days) → services → setWebhook activation
    - ONB-03 edit keywords in per-business bot handler
    - All fixture businesses removed from seed.ts and tests
    Migration 0004_phase5_onboarding.sql creates the onboarding_sessions table.
  </what-built>
  <how-to-verify>
    1. Apply migration 0004 to the local randevuclaw_test DB:
       psql randevuclaw_test -f migrations/0004_phase5_onboarding.sql
       Verify: psql randevuclaw_test -c "\d onboarding_sessions" should show the table.

    2. Apply migration 0004 to the live Neon DB:
       psql $DATABASE_URL -f migrations/0004_phase5_onboarding.sql
       Verify: psql $DATABASE_URL -c "SELECT COUNT(*) FROM onboarding_sessions" should return 0 (empty table, no error).

    3. Delete fixture rows from live Neon DB (pilates-athens and hair-salon-athens):
       psql $DATABASE_URL -c "DELETE FROM businesses WHERE slug IN ('pilates-athens', 'hair-salon-athens')"
       Note: if the CASCADE keyword is needed due to FK constraints, use: DELETE FROM businesses WHERE slug IN ('pilates-athens', 'hair-salon-athens') — Postgres will error on FK violations; if so, delete dependent rows first (bookings, services, business_hours, etc.) or use TRUNCATE with CASCADE on a test environment only.
       Verify: psql $DATABASE_URL -c "SELECT slug FROM businesses" should return no rows (or only real onboarded businesses).

    4. Set fly.io secrets for production:
       fly secrets set PLATFORM_BOT_TOKEN=<your-platform-bot-token>
       fly secrets set PLATFORM_WEBHOOK_SECRET=$(node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
       fly secrets set WEBHOOK_BASE_URL=https://randevuclaw.fly.dev

    5. Remove old TEST_BOT_* secrets from fly.io:
       fly secrets unset TEST_BOT_1_TOKEN TEST_BOT_1_WEBHOOK_SECRET TEST_BOT_1_WEBHOOK_ID TEST_BOT_2_TOKEN TEST_BOT_2_WEBHOOK_SECRET TEST_BOT_2_WEBHOOK_ID

    6. Register the platform bot webhook with Telegram (one-time setup):
       After deploying, call: curl -X POST https://api.telegram.org/bot<PLATFORM_BOT_TOKEN>/setWebhook -d '{"url":"https://randevuclaw.fly.dev/webhooks/telegram/platform","secret_token":"<PLATFORM_WEBHOOK_SECRET>"}'
       Expected response: {"ok":true,"result":true}

    7. Run full test suite: npm test — should exit 0.
  </how-to-verify>
  <resume-signal>Type "approved" once all DB migrations applied, fly.io secrets updated, and npm test passes</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test environment → real DB | booking-queries.test.ts uses real Postgres; insertTestBusiness() must clean up after itself |
| developer machine → live Neon DB | Migration and fixture deletion run as admin; irreversible on the live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-18 | Tampering | DELETE fixtures from live DB is irreversible | medium | mitigate | Checkpoint requires human confirmation; migration is idempotent (IF NOT EXISTS); DELETE can be dry-run as SELECT first |
| T-05-19 | Information Disclosure | PLATFORM_BOT_TOKEN in fly.io secrets must not be committed to repo | high | mitigate | Values set via fly secrets command, never in .env or committed files; user_setup frontmatter guides correct flow |
| T-05-SC | Tampering | npm installs | low | accept | No new packages in this plan |
</threat_model>

<verification>
- `npm test` exits 0 (full suite)
- `grep "FIXTURES\|seed()" src/database/seed.ts` returns nothing
- `grep "TEST_BOT_" tests/jest.setup.ts` returns nothing
- `grep "TEST_BOT_" src/config.ts` returns nothing
- Migration applied to both local and live DB (confirmed in checkpoint)
</verification>

<success_criteria>
- src/database/seed.ts exports only generateSlug; no fixture-related code remains
- tests/fixtures.test.ts tests only generateSlug behavior
- tests/booking-queries.test.ts uses insertTestBusiness() instead of seed()
- TEST_BOT_* and TELEGRAM_WEBHOOK_SECRET removed from both config.ts and jest.setup.ts
- Full npm test suite passes after all removals
- Migration 0004 applied to live Neon DB and local test DB (confirmed via checkpoint)
- Fixture business rows deleted from live Neon DB
- fly.io secrets updated with real PLATFORM_BOT_TOKEN/PLATFORM_WEBHOOK_SECRET/WEBHOOK_BASE_URL
</success_criteria>

<output>
Create .planning/phases/05-owner-self-serve-onboarding/05-07-SUMMARY.md when done
</output>

## Artifacts this phase produces

Modified symbols in this plan:

| Symbol | File | Change |
|--------|------|--------|
| `generateSlug` | src/database/seed.ts | Retained; all fixture code removed |
| `seed` (previously exported) | src/database/seed.ts | Removed (ONB-04) |
| `FIXTURES` constant | src/database/seed.ts | Removed (ONB-04) |
| `SERVICE_FIXTURES` constant | src/database/seed.ts | Removed (ONB-04) |
| `HOURS_FIXTURES` constant | src/database/seed.ts | Removed (ONB-04) |
