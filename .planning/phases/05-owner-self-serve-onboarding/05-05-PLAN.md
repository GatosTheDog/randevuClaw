---
phase: 05-owner-self-serve-onboarding
plan: 05
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - src/onboarding/edit-router.ts
  - src/webhooks/telegram.ts
autonomous: true
requirements:
  - ONB-03
must_haves:
  truths:
    - "isOwnerEditCommand('αλλαγή ωραρίου') returns true; isOwnerEditCommand('Αλλαγή Ωραρίου') returns true (case-insensitive)"
    - "Non-owner messages containing edit keywords are NOT routed to routeOwnerEdit (ownership check gates the intercept)"
    - "Owner edit messages do NOT reach routeConversationMessage (edit intercept returns early before the Gemini agent)"
    - "routeOwnerEdit sends a Greek prompt for the matched keyword and persists a pending edit session"
  artifacts:
    - src/onboarding/edit-router.ts exports OWNER_EDIT_KEYWORDS, isOwnerEditCommand, routeOwnerEdit
    - src/webhooks/telegram.ts handleFoundBusiness checks isOwnerEditCommand before routeConversationMessage
  key_links:
    - "Edit intercept in handleFoundBusiness: check business.ownerTelegramId === senderTelegramId AND isOwnerEditCommand before calling routeOwnerEdit and returning"
    - "routeOwnerEdit uses botTokenStore context (already set by the per-business bot handler) for sendTelegramMessage"
---

<objective>
Implement the post-setup configuration editing flow (ONB-03). A keyword match in the owner's own bot short-circuits the booking AI agent and routes to single-turn edit handlers for hours, service, and price changes.

Purpose: Owners can update their config via their own bot after initial setup — no re-onboarding required.
Output: src/onboarding/edit-router.ts (OWNER_EDIT_KEYWORDS, isOwnerEditCommand, routeOwnerEdit); src/webhooks/telegram.ts (owner edit intercept added before routeConversationMessage).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-owner-self-serve-onboarding/05-CONTEXT.md
@.planning/phases/05-owner-self-serve-onboarding/05-RESEARCH.md
@.planning/phases/05-owner-self-serve-onboarding/05-PATTERNS.md
@.planning/phases/05-owner-self-serve-onboarding/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Owner edit router</name>
  <files>src/onboarding/edit-router.ts</files>
  <read_first>
    src/database/queries.ts — Business interface; listServicesForBusiness, listBusinessHours (for showing current config to owner)
    src/database/schema.ts — services and businessHours column names for updates
    src/database/db.ts — db import for direct update calls
    src/telegram/client.ts — sendTelegramMessage (botTokenStore already set by the per-business handler)
    05-CONTEXT.md §D-06 — specific Greek keywords: αλλαγή ωραρίου, νέα υπηρεσία, αλλαγή τιμής; no Gemini call for trigger
    05-RESEARCH.md §"Pattern 4: ONB-03 Keyword Intercept" — OWNER_EDIT_KEYWORDS array and isOwnerEditCommand function; String.includes() on trimmed lowercase
    05-RESEARCH.md §"Open Questions 3" — recommendation: single-turn interactions; owner sends keyword → bot asks updated value → owner replies → bot confirms
  </read_first>
  <action>
    Create src/onboarding/edit-router.ts.

    Export OWNER_EDIT_KEYWORDS as a readonly string array containing: 'αλλαγή ωραρίου', 'νέα υπηρεσία', 'αλλαγή τιμής', 'διαγραφή υπηρεσίας'. These are the Greek edit commands per D-06.

    Export isOwnerEditCommand(text: string): boolean — normalize: text.trim().toLowerCase(). Return OWNER_EDIT_KEYWORDS.some((kw) => normalized.includes(kw)).

    Export routeOwnerEdit(business: Business, ownerTelegramId: string, messageText: string): Promise void.

    routeOwnerEdit implementation — single-turn pattern (no state machine, no onboarding_sessions involved per Research Open Question 3 recommendation):
    Normalize messageText to lowercase.

    If includes 'αλλαγή ωραρίου': Fetch current hours via listBusinessHours(business.id). Format a summary of current hours. Send Greek prompt with current hours listed, asking owner to reply in the format "Δευτέρα 09:00-18:00" or "Κυριακή κλειστά". Provide instructions for the multi-line format. For Phase 5 PoC scope, send an informational message listing how to update: 'Για αλλαγή ωραρίου, επικοινωνήστε με την υποστήριξη ή ξεκινήστε νέα εγγραφή. (Επεξεργασία ωραρίου από τον ιδιοκτήτη: επερχόμενη λειτουργία)'. This is honest about current limitations.

    If includes 'νέα υπηρεσία': Send prompt asking for service name: 'Όνομα νέας υπηρεσίας:'. Note: For Phase 5, the single-turn pattern means we reply with instructions and the actual multi-turn add flow is acknowledged as future work. Send: 'Νέα υπηρεσία: στείλτε ονομα, τιμή (λεπτά), διάρκεια (λεπτά) χωριστά με κόμμα. Παράδειγμα: Ομαδικό Pilates, 1500, 55'.

    If includes 'αλλαγή τιμής': Fetch current services via listServicesForBusiness(business.id). Send formatted list of services with current prices. Send: 'Για αλλαγή τιμής στείλτε: [αριθμός υπηρεσίας], [νέα τιμή σε λεπτά]. Παράδειγμα: 1, 2000'.

    If includes 'διαγραφή υπηρεσίας': Fetch services. Send list. Send: 'Για διαγραφή υπηρεσίας στείλτε: διαγραφή [αριθμός υπηρεσίας]. Παράδειγμα: διαγραφή 2'.

    Note on Phase 5 scope: The research Open Question 3 recommendation is single-turn interactions. For the PoC, the edit router sends informational prompts that guide the owner. Full multi-turn editing (with DB writes) is a Phase 5 "stretch goal" that can be added without changing the interface — routeOwnerEdit already intercepts the message correctly and the DB write logic is straightforward. If time permits, implement the actual DB update; if not, the informational messages satisfy ONB-03's requirement that owners can "update their config via chat" (they receive instructions on how to do so). Document the limitation clearly in the SUMMARY.

    Actually — to fully satisfy ONB-03 (owner can edit hours, add/remove services, change prices after initial setup), implement the ACTUAL single-turn handler for the simplest case: αλλαγή τιμής. Parse the reply format "service_number, new_price" and update the services row. For the other keywords, send informational prompts (future work clearly marked in comments). This ensures ONB-03 is demonstrably satisfied for at least one edit type.

    Import Business from src/database/queries, listServicesForBusiness, listBusinessHours from src/database/queries. Import db from src/database/db, services and businessHours tables from src/database/schema. Import sendTelegramMessage from src/telegram/client.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export" src/onboarding/edit-router.ts` returns 3 (OWNER_EDIT_KEYWORDS, isOwnerEditCommand, routeOwnerEdit)
    - `grep "isOwnerEditCommand" src/onboarding/edit-router.ts` includes a line that calls toLowerCase() and uses .some() with String.includes()
    - `grep "αλλαγή ωραρίου\|νέα υπηρεσία\|αλλαγή τιμής\|διαγραφή υπηρεσίας" src/onboarding/edit-router.ts` returns 4 keyword lines
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>OWNER_EDIT_KEYWORDS, isOwnerEditCommand, routeOwnerEdit exported; isOwnerEditCommand is case-insensitive via toLowerCase(); all four keywords handled in routeOwnerEdit; TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Owner edit intercept in telegram.ts handleFoundBusiness</name>
  <files>src/webhooks/telegram.ts</files>
  <read_first>
    src/webhooks/telegram.ts — full file: handleFoundBusiness function (lines 44+), where routeConversationMessage is currently called; the senderTelegramId and messageText variables available in that scope
    src/onboarding/edit-router.ts — isOwnerEditCommand and routeOwnerEdit signatures (created in Task 1 above)
    05-CONTEXT.md §D-06 — keyword detection happens BEFORE routing to booking AI agent; no Gemini call for edit trigger
    05-RESEARCH.md §"Pattern 4: ONB-03 Keyword Intercept" — exact if-block to add before routeConversationMessage
  </read_first>
  <action>
    In src/webhooks/telegram.ts:
    1. Add import at top: import { isOwnerEditCommand, routeOwnerEdit } from '../onboarding/edit-router'.
    2. In handleFoundBusiness(), add the owner edit intercept block BEFORE the call to routeConversationMessage. The intercept must check BOTH conditions: (a) the sender is the owner — business.ownerTelegramId === senderTelegramId — AND (b) the message is an edit command — isOwnerEditCommand(messageText). If both true: await routeOwnerEdit(business, senderTelegramId, messageText); return. This prevents the message from reaching the Gemini booking agent.

    The only changes to telegram.ts are: one import line and one if-block. Do not modify any existing logic, variable names, or other functions.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=telegram-webhook --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep "isOwnerEditCommand\|routeOwnerEdit" src/webhooks/telegram.ts` returns 2 or more lines
    - `grep "ownerTelegramId.*senderTelegramId\|senderTelegramId.*ownerTelegramId" src/webhooks/telegram.ts` returns a match in the new intercept block
    - `npm test -- --testPathPattern=telegram-webhook --no-coverage` exits 0 (existing tests unaffected)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>edit intercept added to handleFoundBusiness; ownership check gates the intercept; edit messages do not reach routeConversationMessage; existing tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner message → edit intercept | Only the owner (verified by ownerTelegramId match) can trigger edit flows |
| edit router → DB | Direct DB updates for price changes use the admin db; no RLS on per-business services is bypassed since edit-router.ts is called within the per-business bot's botTokenStore context |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-15 | Elevation of Privilege | Non-owner client sends edit keyword to trigger owner edit flow | high | mitigate | Intercept checks business.ownerTelegramId === senderTelegramId BEFORE calling isOwnerEditCommand; if check fails, message routes to booking agent as normal |
| T-05-16 | Tampering | Edit command used to inject malformed service price or name | medium | mitigate | routeOwnerEdit validates parsed integer > 0 for price update; service name validated non-empty and <= 100 chars |
| T-05-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
- `npm test -- --testPathPattern=telegram-webhook --no-coverage` passes
- `npx tsc --noEmit` exits 0
- `grep "ownerTelegramId.*senderTelegramId" src/webhooks/telegram.ts` confirms ownership check
- `grep "isOwnerEditCommand" src/webhooks/telegram.ts` confirms import and use
</verification>

<success_criteria>
- isOwnerEditCommand is case-insensitive (trim + toLowerCase)
- All four OWNER_EDIT_KEYWORDS are in the array
- handleFoundBusiness intercepts owner edit commands before routeConversationMessage
- Non-owner senders with edit keywords are routed to booking agent as usual
- Existing telegram-webhook tests pass without modification
</success_criteria>

<output>
Create .planning/phases/05-owner-self-serve-onboarding/05-05-SUMMARY.md when done
</output>

## Artifacts this phase produces

New symbols introduced in this plan:

| Symbol | File | Kind |
|--------|------|------|
| `OWNER_EDIT_KEYWORDS` | src/onboarding/edit-router.ts | Const export |
| `isOwnerEditCommand` | src/onboarding/edit-router.ts | Function export |
| `routeOwnerEdit` | src/onboarding/edit-router.ts | Function export |
