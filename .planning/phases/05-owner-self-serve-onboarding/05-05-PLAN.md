---
phase: 05-owner-self-serve-onboarding
plan: 05
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - src/onboarding/edit-router.ts
  - src/webhooks/telegram.ts
autonomous: true
requirements:
  - ONB-03
must_haves:
  truths:
    - "isOwnerEditCommand('αλλαγή ωραρίου') returns true; isOwnerEditCommand('Αλλαγή Ωραρίου') returns true (case-insensitive)"
    - "Non-owner messages containing edit keywords are NOT routed to routeOwnerEdit (ownership check gates the intercept)"
    - "Owner edit messages do NOT reach routeConversationMessage (edit intercept returns early before the Gemini agent)"
    - "routeOwnerEdit for 'αλλαγή ωραρίου' with inline data (e.g. keyword + 'Δευτέρα,09:00,17:00') upserts the matching business_hours row; invalid HH:MM is rejected with a Greek error; keyword-only sends format instructions (D-06)"
    - "routeOwnerEdit for 'νέα υπηρεσία' with inline CSV (e.g. keyword + 'Reformer,2000,60') inserts a services row; keyword-only sends format instructions (D-06)"
    - "routeOwnerEdit for 'αλλαγή τιμής' with inline data (e.g. keyword + '1,2000') updates services.price for the indexed service; keyword-only lists services with format instructions (D-06)"
    - "routeOwnerEdit for 'διαγραφή υπηρεσίας': first call lists services + sets a module-level pendingDeleteByBusiness Map keyed by business.id; owner's subsequent number reply triggers the services row deletion (two-turn per D-06)"
    - "The telegram.ts intercept fires when isOwnerEditCommand is true OR hasPendingEditState(business.id) is true — second condition handles the confirmation turn of διαγραφή υπηρεσίας (D-06)"
  artifacts:
    - src/onboarding/edit-router.ts exports OWNER_EDIT_KEYWORDS, isOwnerEditCommand, routeOwnerEdit, hasPendingEditState
    - src/webhooks/telegram.ts handleFoundBusiness checks (isOwnerEditCommand OR hasPendingEditState) before routeConversationMessage
  key_links:
    - "Edit intercept in handleFoundBusiness: check business.ownerTelegramId === senderTelegramId AND (isOwnerEditCommand(messageText) OR hasPendingEditState(business.id)) before calling routeOwnerEdit and returning"
    - "routeOwnerEdit uses botTokenStore context (already set by the per-business bot handler) for sendTelegramMessage"
    - "pendingDeleteByBusiness Map in edit-router.ts holds the service list for the second turn of διαγραφή υπηρεσίας — keyed by business.id, cleared on reply (valid or invalid)"
---

<objective>
Implement the post-setup configuration editing flow (ONB-03). A keyword match in the owner's own bot short-circuits the booking AI agent and routes to single-turn edit handlers for hours, service, and price changes.

Purpose: Owners can update their config via their own bot after initial setup — no re-onboarding required.
Output: src/onboarding/edit-router.ts (OWNER_EDIT_KEYWORDS, isOwnerEditCommand, routeOwnerEdit); src/webhooks/telegram.ts (owner edit intercept added before routeConversationMessage).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-owner-self-serve-onboarding/05-CONTEXT.md
@.planning/phases/05-owner-self-serve-onboarding/05-RESEARCH.md
@.planning/phases/05-owner-self-serve-onboarding/05-PATTERNS.md
@.planning/phases/05-owner-self-serve-onboarding/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Owner edit router</name>
  <files>src/onboarding/edit-router.ts</files>
  <read_first>
    src/database/queries.ts — Business interface; listServicesForBusiness, listBusinessHours (for showing current config to owner)
    src/database/schema.ts — services and businessHours column names for updates
    src/database/db.ts — db import for direct update calls
    src/telegram/client.ts — sendTelegramMessage (botTokenStore already set by the per-business handler)
    05-CONTEXT.md §D-06 — specific Greek keywords: αλλαγή ωραρίου, νέα υπηρεσία, αλλαγή τιμής; no Gemini call for trigger
    05-RESEARCH.md §"Pattern 4: ONB-03 Keyword Intercept" — OWNER_EDIT_KEYWORDS array and isOwnerEditCommand function; String.includes() on trimmed lowercase
    05-RESEARCH.md §"Open Questions 3" — recommendation: single-turn interactions; owner sends keyword → bot asks updated value → owner replies → bot confirms
  </read_first>
  <action>
    Create src/onboarding/edit-router.ts. All four keywords implement real DB writes; no informational-only stubs (per D-06 and ONB-03 success criterion 4).

    Declare a module-level constant (not exported): pendingDeleteByBusiness as a new Map keyed by business.id (number) to an array of service objects: Array of { id: number; name: string; price: number; durationMin: number }. This holds the pending deletion state for the two-turn διαγραφή υπηρεσίας flow.

    Export OWNER_EDIT_KEYWORDS as a readonly string array containing: 'αλλαγή ωραρίου', 'νέα υπηρεσία', 'αλλαγή τιμής', 'διαγραφή υπηρεσίας'. Greek edit commands per D-06.

    Export isOwnerEditCommand(text: string): boolean — normalize via text.trim().toLowerCase(). Return OWNER_EDIT_KEYWORDS.some((kw) => normalized.includes(kw)).

    Export hasPendingEditState(businessId: number): boolean — return pendingDeleteByBusiness.has(businessId). Called from the telegram.ts intercept so that the owner's second-turn number reply for διαγραφή υπηρεσίας is routed back to routeOwnerEdit.

    Declare TIME_REGEX (not exported): a regex matching 24-hour HH:MM format — groups [01]\d and 2[0-3] for hour, [0-5]\d for minutes, anchored ^ and $.

    Declare GREEK_DAY_NAME_TO_INDEX (not exported) as Record<string, number>: 'κυριακή' maps to 0, 'δευτέρα' to 1, 'τρίτη' to 2, 'τετάρτη' to 3, 'πέμπτη' to 4, 'παρασκευή' to 5, 'σάββατο' to 6. Matches JS Date.getDay() / business_hours.dayOfWeek convention (0=Sunday).

    Export routeOwnerEdit(business: Business, ownerTelegramId: string, messageText: string): Promise<void>.

    routeOwnerEdit implementation:

    STEP 0 — Pending delete check (must execute BEFORE keyword matching):
    If pendingDeleteByBusiness.has(business.id): retrieve serviceList from the Map, immediately call pendingDeleteByBusiness.delete(business.id) to clear the state, parse messageText.trim() as an integer via parseInt with radix 10. If the parsed index is NaN or outside the range 1..serviceList.length: await sendTelegramMessage(ownerTelegramId, 'Μη έγκυρος αριθμός. Η διαγραφή ακυρώθηκε.') and return. Otherwise: svc = serviceList[parsedIndex - 1]; await db.delete(services).where(eq(services.id, svc.id)); await sendTelegramMessage(ownerTelegramId, 'Η υπηρεσία "' + svc.name + '" διαγράφηκε.') and return.

    STEP 1 — Normalize: const normalized = messageText.trim().toLowerCase().

    STEP 2 — αλλαγή ωραρίου branch:
    If normalized.includes('αλλαγή ωραρίου'): extract dataStr by finding the first occurrence of 'αλλαγή ωραρίου' in normalized and taking everything after it, trimmed. If dataStr is empty: await sendTelegramMessage(ownerTelegramId, 'Αλλαγή ωραρίου: στείλτε "αλλαγή ωραρίου [ημέρα],[ώρα έναρξης],[ώρα λήξης]" — π.χ.: αλλαγή ωραρίου Δευτέρα,09:00,17:00') and return. If non-empty: split dataStr by ',' to get parts[0], parts[1], parts[2]. dayNameKey = parts[0]?.trim().toLowerCase(); openTime = parts[1]?.trim(); closeTime = parts[2]?.trim(); dayOfWeek = GREEK_DAY_NAME_TO_INDEX[dayNameKey]. Validate: dayOfWeek is not undefined AND TIME_REGEX.test(openTime) AND TIME_REGEX.test(closeTime). On failure: await sendTelegramMessage(ownerTelegramId, 'Μη έγκυρα στοιχεία. Στείλτε π.χ.: αλλαγή ωραρίου Δευτέρα,09:00,17:00') and return. On success: await db.insert(businessHours).values({ businessId: business.id, dayOfWeek, openTime, closeTime, isClosed: false }).onConflictDoUpdate({ target: [businessHours.businessId, businessHours.dayOfWeek], set: { openTime, closeTime, isClosed: false } }). Read the column names for onConflictDoUpdate target from src/database/schema.ts businessHours table definition. Await sendTelegramMessage(ownerTelegramId, 'Ωράριο ' + parts[0].trim() + ' ενημερώθηκε: ' + openTime + '-' + closeTime + '.') and return.

    STEP 3 — νέα υπηρεσία branch:
    If normalized.includes('νέα υπηρεσία'): extract dataStr after the keyword (same slice-and-trim pattern). If empty: await sendTelegramMessage(ownerTelegramId, 'Νέα υπηρεσία: στείλτε "νέα υπηρεσία [όνομα],[τιμή σε λεπτά],[διάρκεια σε λεπτά]" — π.χ.: νέα υπηρεσία Reformer,2000,60') and return. If non-empty: split by ',' for [nameRaw, priceRaw, durationRaw]. name = nameRaw?.trim(); price = parseInt(priceRaw?.trim() ?? '', 10); durationMin = parseInt(durationRaw?.trim() ?? '', 10). Validate: name is non-empty AND name.length <= 100 AND price > 0 (not NaN) AND durationMin > 0 (not NaN). On failure: await sendTelegramMessage(ownerTelegramId, 'Μη έγκυρα στοιχεία. Στείλτε π.χ.: νέα υπηρεσία Reformer,2000,60') and return. On success: await db.insert(services).values({ businessId: business.id, name, price, durationMin }). Await sendTelegramMessage(ownerTelegramId, 'Η υπηρεσία "' + name + '" προστέθηκε.') and return.

    STEP 4 — αλλαγή τιμής branch:
    If normalized.includes('αλλαγή τιμής'): fetch serviceList = await listServicesForBusiness(business.id). Build listText as a numbered list joined by newline (e.g. '1. Reformer — 2000 λεπτά\n2. Pilates — 1500 λεπτά'). Extract dataStr after the keyword. If empty: await sendTelegramMessage(ownerTelegramId, listText + '\nΑλλαγή τιμής: στείλτε "αλλαγή τιμής [αριθμός],[νέα τιμή σε λεπτά]" — π.χ.: αλλαγή τιμής 1,2000') and return. If non-empty: split by ',' for [indexStr, priceStr]. idx = parseInt(indexStr?.trim(), 10) - 1; newPrice = parseInt(priceStr?.trim(), 10). Validate: idx >= 0 AND idx < serviceList.length AND newPrice > 0 (not NaN). On failure: await sendTelegramMessage(ownerTelegramId, listText + '\nΜη έγκυρα στοιχεία.') and return. On success: svc = serviceList[idx]; await db.update(services).set({ price: newPrice }).where(eq(services.id, svc.id)). Await sendTelegramMessage(ownerTelegramId, 'Η τιμή της υπηρεσίας "' + svc.name + '" ενημερώθηκε σε ' + newPrice + ' λεπτά.') and return.

    STEP 5 — διαγραφή υπηρεσίας branch:
    If normalized.includes('διαγραφή υπηρεσίας'): fetch serviceList = await listServicesForBusiness(business.id). If serviceList.length === 0: await sendTelegramMessage(ownerTelegramId, 'Δεν υπάρχουν καταχωρημένες υπηρεσίες.') and return. Build listText as a numbered list. Call pendingDeleteByBusiness.set(business.id, serviceList). Await sendTelegramMessage(ownerTelegramId, listText + '\nΣτείλτε τον αριθμό της υπηρεσίας για διαγραφή:') and return.

    Imports: Business from src/database/queries; listServicesForBusiness from src/database/queries; db from src/database/db; services, businessHours from src/database/schema; sendTelegramMessage from src/telegram/client; eq from drizzle-orm.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export" src/onboarding/edit-router.ts` returns 4 or more (OWNER_EDIT_KEYWORDS, isOwnerEditCommand, routeOwnerEdit, hasPendingEditState)
    - `grep "hasPendingEditState" src/onboarding/edit-router.ts` returns a match (export declaration)
    - `grep "isOwnerEditCommand" src/onboarding/edit-router.ts` includes a line that calls toLowerCase() and uses .some() with String.includes()
    - `grep "αλλαγή ωραρίου\|νέα υπηρεσία\|αλλαγή τιμής\|διαγραφή υπηρεσίας" src/onboarding/edit-router.ts` returns 4 keyword lines
    - `grep -c "db.insert\|db.update\|db.delete" src/onboarding/edit-router.ts` returns 4 or more (hours upsert, services insert, price update, services delete)
    - `grep "onConflictDoUpdate" src/onboarding/edit-router.ts` returns 1 match (business_hours upsert for αλλαγή ωραρίου)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>OWNER_EDIT_KEYWORDS, isOwnerEditCommand, routeOwnerEdit, hasPendingEditState exported; isOwnerEditCommand is case-insensitive; all four keywords write to DB when inline data is provided; αλλαγή ωραρίου upserts business_hours; νέα υπηρεσία inserts services; αλλαγή τιμής updates services.price; διαγραφή υπηρεσίας is two-turn via pendingDeleteByBusiness Map; TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Owner edit intercept in telegram.ts handleFoundBusiness</name>
  <files>src/webhooks/telegram.ts</files>
  <read_first>
    src/webhooks/telegram.ts — full file: handleFoundBusiness function (lines 44+), where routeConversationMessage is currently called; the senderTelegramId and messageText variables available in that scope
    src/onboarding/edit-router.ts — isOwnerEditCommand and routeOwnerEdit signatures (created in Task 1 above)
    05-CONTEXT.md §D-06 — keyword detection happens BEFORE routing to booking AI agent; no Gemini call for edit trigger
    05-RESEARCH.md §"Pattern 4: ONB-03 Keyword Intercept" — exact if-block to add before routeConversationMessage
  </read_first>
  <action>
    In src/webhooks/telegram.ts:
    1. Add import at top: import { isOwnerEditCommand, routeOwnerEdit, hasPendingEditState } from '../onboarding/edit-router'.
    2. In handleFoundBusiness(), add the owner edit intercept block BEFORE the call to routeConversationMessage. The intercept must check: (a) the sender is the owner — business.ownerTelegramId === senderTelegramId — AND (b) either the message is an edit keyword command (isOwnerEditCommand(messageText)) OR there is a pending deletion state for this business (hasPendingEditState(business.id)). If both conditions are met: await routeOwnerEdit(business, senderTelegramId, messageText); return. The hasPendingEditState check handles the second turn of διαγραφή υπηρεσίας: when the owner replies with just a number after seeing the service list, isOwnerEditCommand returns false but hasPendingEditState returns true, routing the reply to routeOwnerEdit which clears the pending state and executes the delete.

    The only changes to telegram.ts are: one import line (adding hasPendingEditState to the existing import) and one if-block with the updated compound condition. Do not modify any existing logic, variable names, or other functions.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=telegram-webhook --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep "isOwnerEditCommand\|routeOwnerEdit\|hasPendingEditState" src/webhooks/telegram.ts` returns 3 or more lines (import + use of each)
    - `grep "hasPendingEditState" src/webhooks/telegram.ts` returns a match in the new intercept block
    - `grep "ownerTelegramId.*senderTelegramId\|senderTelegramId.*ownerTelegramId" src/webhooks/telegram.ts` returns a match in the new intercept block
    - `npm test -- --testPathPattern=telegram-webhook --no-coverage` exits 0 (existing tests unaffected)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>edit intercept added to handleFoundBusiness; ownership check AND (isOwnerEditCommand OR hasPendingEditState) gate the intercept; both edit keywords and pending-delete replies are routed to routeOwnerEdit; edit messages do not reach routeConversationMessage; existing tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner message → edit intercept | Only the owner (verified by ownerTelegramId match) can trigger edit flows |
| edit router → DB | Direct DB updates for price changes use the admin db; no RLS on per-business services is bypassed since edit-router.ts is called within the per-business bot's botTokenStore context |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-15 | Elevation of Privilege | Non-owner client sends edit keyword to trigger owner edit flow | high | mitigate | Intercept checks business.ownerTelegramId === senderTelegramId BEFORE calling isOwnerEditCommand; if check fails, message routes to booking agent as normal |
| T-05-16 | Tampering | Edit command used to inject malformed service price or name | medium | mitigate | routeOwnerEdit validates parsed integer > 0 for price update; service name validated non-empty and <= 100 chars |
| T-05-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
- `npm test -- --testPathPattern=telegram-webhook --no-coverage` passes
- `npx tsc --noEmit` exits 0
- `grep "ownerTelegramId.*senderTelegramId" src/webhooks/telegram.ts` confirms ownership check
- `grep "isOwnerEditCommand\|hasPendingEditState" src/webhooks/telegram.ts` confirms import and use of both guards
- `grep -c "db.insert\|db.update\|db.delete" src/onboarding/edit-router.ts` returns 4 or more (proves all four keywords write to DB)
- `grep "onConflictDoUpdate" src/onboarding/edit-router.ts` confirms business_hours upsert
</verification>

<success_criteria>
- isOwnerEditCommand is case-insensitive (trim + toLowerCase)
- hasPendingEditState exported; telegram.ts intercept checks it alongside isOwnerEditCommand
- All four OWNER_EDIT_KEYWORDS are in the array and trigger real DB writes when inline data is provided
- αλλαγή ωραρίου: upserts business_hours row via onConflictDoUpdate on (businessId, dayOfWeek)
- νέα υπηρεσία: inserts services row from "name,price,duration" CSV in the same message
- αλλαγή τιμής: updates services.price from "number,price" inline data
- διαγραφή υπηρεσίας: two-turn — first call lists services + sets pendingDeleteByBusiness Map; owner's number reply deletes the services row
- handleFoundBusiness intercepts owner edit keywords AND pending-delete replies before routeConversationMessage
- Non-owner senders with edit keywords are routed to booking agent as usual
- Existing telegram-webhook tests pass without modification
</success_criteria>

<output>
Create .planning/phases/05-owner-self-serve-onboarding/05-05-SUMMARY.md when done
</output>

## Artifacts this phase produces

New symbols introduced in this plan:

| Symbol | File | Kind |
|--------|------|------|
| `OWNER_EDIT_KEYWORDS` | src/onboarding/edit-router.ts | Const export |
| `isOwnerEditCommand` | src/onboarding/edit-router.ts | Function export |
| `routeOwnerEdit` | src/onboarding/edit-router.ts | Function export |
| `hasPendingEditState` | src/onboarding/edit-router.ts | Function export |
