---
phase: 05-owner-self-serve-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0004_phase5_onboarding.sql
  - src/database/schema.ts
  - src/config.ts
  - tests/jest.setup.ts
  - tests/config.test.ts
  - tests/helpers/test-business.ts
autonomous: true
requirements:
  - ONB-02
  - ONB-04
must_haves:
  truths:
    - "onboarding_sessions table is declared in schema.ts with id, businessId (NOT NULL FK), currentStep, collectedData (nullable), createdAt, updatedAt"
    - "config.platformBotToken, config.platformWebhookSecret, config.webhookBaseUrl are required fields on the Config interface"
    - "jest.setup.ts provides test placeholders for PLATFORM_BOT_TOKEN, PLATFORM_WEBHOOK_SECRET, WEBHOOK_BASE_URL via ??= assignment"
    - "insertTestBusiness() inserts a business + services + business_hours row and returns the Business object; closed days write isClosed: true rows"
  artifacts:
    - migrations/0004_phase5_onboarding.sql
    - src/database/schema.ts exports onboardingSessions
    - src/config.ts has platformBotToken, platformWebhookSecret, webhookBaseUrl on Config interface
    - tests/helpers/test-business.ts exports insertTestBusiness
  key_links:
    - "onboarding_sessions.business_id NOT NULL FK to businesses.id — business row created before session row"
    - "unique_onboarding_session_per_business index on business_id — one active session per business"
    - "config.platformBotToken consumed by Plan 04 src/webhooks/platform.ts handler"
    - "insertTestBusiness() consumed by Plan 06 integration tests"
---

<objective>
Establish the database schema, environment configuration, and test infrastructure that every downstream plan depends on. Adds the `onboarding_sessions` table (D-04/D-05), adds three required env vars to config (D-01 per-platform-bot credentials), and creates the `insertTestBusiness()` test helper (D-11) that replaces fixture-based test setup.

Purpose: Zero-dependency foundation; nothing else in Phase 5 can proceed until the schema and config contracts are in place.
Output: SQL migration file, updated schema.ts, updated config.ts + tests/jest.setup.ts, new tests/helpers/test-business.ts
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-owner-self-serve-onboarding/05-CONTEXT.md
@.planning/phases/05-owner-self-serve-onboarding/05-RESEARCH.md
@.planning/phases/05-owner-self-serve-onboarding/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0004 + schema.ts onboarding_sessions table</name>
  <files>migrations/0004_phase5_onboarding.sql, src/database/schema.ts</files>
  <read_first>
    src/database/schema.ts — current imports (pgTable, serial, text, integer, boolean, timestamp, uniqueIndex already imported) and existing table definitions to match style
    migrations/0003_phase4_per_bot.sql — migration file format: header comment, how-to-apply, idempotency notes
    05-RESEARCH.md §"Pattern 6: onboarding_sessions Schema" — exact column spec
    05-RESEARCH.md §"Migration 0004 SQL Skeleton" — exact CREATE TABLE SQL
    05-PATTERNS.md §"src/database/schema.ts" — uniqueIndex + nullable column comment pattern
  </read_first>
  <action>
    Create migrations/0004_phase5_onboarding.sql with the following sections:
    Header comment matching 0003's format (purpose, how to apply via `psql $DATABASE_URL -f ...`, idempotency note: CREATE TABLE wrapped in DO block).
    Section 1: CREATE TABLE onboarding_sessions with columns: id SERIAL PRIMARY KEY, business_id INTEGER NOT NULL REFERENCES businesses(id), current_step TEXT NOT NULL, collected_data TEXT (nullable — partial service state during svc_name/svc_price steps), created_at TIMESTAMP NOT NULL DEFAULT NOW(), updated_at TIMESTAMP NOT NULL DEFAULT NOW().
    Section 2: CREATE UNIQUE INDEX unique_onboarding_session_per_business ON onboarding_sessions (business_id). Wrap in IF NOT EXISTS check.
    Section 3: GRANT SELECT, INSERT, UPDATE ON onboarding_sessions TO randevuclaw_app. (No RLS on this table — platform bot uses admin db for cross-tenant onboarding operations.)
    Wrap CREATE TABLE in a DO block with IF NOT EXISTS check (same idempotency pattern as 0003).

    In src/database/schema.ts, append the onboardingSessions table definition after the existing telegramUpdates table. No new imports needed (pgTable, serial, integer, text, timestamp, uniqueIndex are already imported). Follow the nullable column comment style from businesses table (lines 17-43). Use `$onUpdate(() => new Date())` on updatedAt — per D-05 and RESEARCH.md Pattern 6, this is the Drizzle application-level trigger pattern for drizzle-orm 0.45.2+. Export as `onboardingSessions`.
  </action>
  <verify>
    <automated>grep -c "onboardingSessions" src/database/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onboardingSessions" src/database/schema.ts` returns 1 or more
    - `grep "onboarding_sessions" migrations/0004_phase5_onboarding.sql` matches the CREATE TABLE statement
    - `grep "unique_onboarding_session_per_business" migrations/0004_phase5_onboarding.sql` returns a result
    - `grep "GRANT SELECT, INSERT, UPDATE ON onboarding_sessions TO randevuclaw_app" migrations/0004_phase5_onboarding.sql` returns a result
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>migration SQL file exists; schema.ts exports onboardingSessions with correct column types; TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Config additions + test env placeholders</name>
  <files>src/config.ts, tests/jest.setup.ts, tests/config.test.ts</files>
  <read_first>
    src/config.ts — full file: EnvSchema, Config interface, and config object mapping; see existing optional DATABASE_APP_URL for the addition pattern
    tests/jest.setup.ts — full file: ??= placeholder pattern for all existing env vars
    tests/config.test.ts — full file: test 1 sets full valid env and checks config fields; tests 2-4 partially set env
    05-CONTEXT.md §D-01 — PLATFORM_BOT_TOKEN, PLATFORM_WEBHOOK_SECRET as required; per D-01 these are the only remaining global bot env vars
    05-PATTERNS.md §"src/config.ts" — exact field names: platformBotToken, platformWebhookSecret, webhookBaseUrl; add to EnvSchema and Config interface
  </read_first>
  <action>
    In src/config.ts EnvSchema: add three required fields after the existing LOG_LEVEL/PORT/NODE_ENV block:
    PLATFORM_BOT_TOKEN: z.string().min(1) — comment: "Phase 5 (D-01): platform onboarding bot token. Only global bot token remaining; all per-business tokens are DB-driven."
    PLATFORM_WEBHOOK_SECRET: z.string().min(1) — comment: "Phase 5 (D-01): HMAC secret for platform bot webhook. Separate from per-business webhookSecret values stored in businesses table."
    WEBHOOK_BASE_URL: z.string().min(1) — comment: "Phase 5: base URL for constructing setWebhook URLs (e.g. https://randevuclaw.fly.dev)."
    Add to Config interface: platformBotToken: string; platformWebhookSecret: string; webhookBaseUrl: string.
    Add to config object mapping: platformBotToken: env.PLATFORM_BOT_TOKEN, platformWebhookSecret: env.PLATFORM_WEBHOOK_SECRET, webhookBaseUrl: env.WEBHOOK_BASE_URL.

    In tests/jest.setup.ts, append after the existing TEST_BOT_* lines:
    process.env.PLATFORM_BOT_TOKEN ??= 'test-platform-bot-token';
    process.env.PLATFORM_WEBHOOK_SECRET ??= 'test-platform-webhook-secret';
    process.env.WEBHOOK_BASE_URL ??= 'https://test.example.com';

    In tests/config.test.ts test 1 ("parses a valid full env"), add three assertions after the existing `config.telegramBotToken` undefined check:
    expect(config.platformBotToken).toBe('test-platform-bot-token');
    expect(config.platformWebhookSecret).toBe('test-platform-webhook-secret');
    expect(config.webhookBaseUrl).toBe('https://test.example.com');
    Note: test 1's `process.env = { ...ORIGINAL_ENV }` copies PLATFORM_BOT_TOKEN from jest.setup.ts via ORIGINAL_ENV, so no additional env.set is needed in test 1.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=config --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "platformBotToken" src/config.ts` returns 3 (EnvSchema field, Config interface field, config mapping)
    - `grep "PLATFORM_BOT_TOKEN ??=" tests/jest.setup.ts` returns a result
    - `npm test -- --testPathPattern=config --no-coverage` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>config.ts requires PLATFORM_BOT_TOKEN/PLATFORM_WEBHOOK_SECRET/WEBHOOK_BASE_URL; jest.setup.ts provides test defaults; config.test.ts verifies new fields</done>
</task>

<task type="auto">
  <name>Task 3: insertTestBusiness() helper</name>
  <files>tests/helpers/test-business.ts</files>
  <read_first>
    src/database/queries.ts — Business interface (lines 25-37): exact field names for the returned object
    src/database/schema.ts — businesses, services, businessHours table column names for the insert calls
    src/database/seed.ts — HOURS_FIXTURES pattern: closed days write isClosed: true, openTime: '00:00', closeTime: '00:00' rows so findBusinessHoursForDay always finds a row for every dayOfWeek 0-6
    05-RESEARCH.md §"Pattern 3: insertTestBusiness() Helper" — complete example with TestBusinessOptions and insert pattern
    05-PATTERNS.md §"tests/helpers/test-business.ts" — imports from src/database/db.ts (not appDb) and schema
  </read_first>
  <action>
    Create tests/helpers/test-business.ts. Import db from src/database/db, and businesses, services, businessHours from src/database/schema. Import crypto from 'crypto'. Export the TestBusinessOptions interface and insertTestBusiness(options: TestBusinessOptions) function.

    TestBusinessOptions fields (all optional): name, slug, ownerTelegramId, botToken, webhookId, webhookSecret, withDefaultHours (default true), withDefaultServices (default true).

    insertTestBusiness logic:
    1. Generate a random webhookId via crypto.randomUUID() if not provided.
    2. Insert into businesses using db.insert(businesses).values({...}).returning(). Use options values if provided, else sensible test defaults (name: 'Test Business', slug: 'test-' + webhookId.slice(0, 8), ownerTelegramId: '999999999', botToken: 'test-token-' + webhookId.slice(0, 8), webhookId, webhookSecret: crypto.randomBytes(32).toString('hex')).
    3. If withDefaultServices !== false: insert one service row (name: 'Test Service', durationMin: 60, price: 2000).
    4. If withDefaultHours !== false: insert 7 business_hours rows for days 0-6. Day 0 (Sunday): isClosed: true, openTime: '00:00', closeTime: '00:00'. Days 1-6: isClosed: false, openTime: '09:00', closeTime: '18:00'. This matches the HOURS_FIXTURES pattern so findBusinessHoursForDay always finds a row for every day.
    5. Return the business row as type Business (imported from src/database/queries).

    Use admin db (not appDb) — test helper writes directly without RLS context.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "insertTestBusiness" tests/helpers/test-business.ts` returns 1 (export) or more
    - `grep "isClosed: day === 0" tests/helpers/test-business.ts` OR equivalent closed-Sunday logic is present
    - `npx tsc --noEmit` exits 0 with no errors related to test-business.ts
  </acceptance_criteria>
  <done>tests/helpers/test-business.ts exports insertTestBusiness(); closed days write isClosed: true rows; TypeScript types are correct</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer env → config.ts | PLATFORM_BOT_TOKEN / PLATFORM_WEBHOOK_SECRET must never be hardcoded in source files |
| migration SQL → Postgres | Migration runs as admin user; no user input in SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | PLATFORM_BOT_TOKEN / PLATFORM_WEBHOOK_SECRET in source | high | mitigate | Values live only in env vars (.env.local / fly secrets); config.ts parses them at runtime; never written to migration SQL or schema files; jest.setup.ts uses fake test values |
| T-05-02 | Tampering | migration SQL idempotency (re-run creates duplicate rows) | low | mitigate | CREATE TABLE wrapped in DO/IF NOT EXISTS block; CREATE UNIQUE INDEX uses IF NOT EXISTS; GRANT is natively idempotent |
| T-05-SC | Tampering | npm installs | low | accept | No new packages in this plan; all dependencies (drizzle-orm, pg, zod, crypto) are existing project deps |
</threat_model>

<verification>
After all three tasks complete:
- `npx tsc --noEmit` exits 0
- `npm test -- --testPathPattern=config --no-coverage` passes
- `grep "onboardingSessions" src/database/schema.ts` returns a match
- `grep "platformBotToken" src/config.ts` returns a match
- `grep "PLATFORM_BOT_TOKEN" tests/jest.setup.ts` returns a match
</verification>

<success_criteria>
- migrations/0004_phase5_onboarding.sql creates the onboarding_sessions table idempotently
- src/database/schema.ts exports onboardingSessions with all five columns (id, businessId, currentStep, collectedData, createdAt, updatedAt) and unique index on businessId
- src/config.ts exposes platformBotToken, platformWebhookSecret, webhookBaseUrl on the Config interface as required string fields
- tests/jest.setup.ts provides test placeholder values for the three new env vars
- tests/helpers/test-business.ts exports insertTestBusiness() that creates a full business with 7 hours rows (closed Sunday) and 1 service
</success_criteria>

<output>
Create .planning/phases/05-owner-self-serve-onboarding/05-01-SUMMARY.md when done
</output>

## Artifacts this phase produces

New symbols introduced in this plan:

| Symbol | File | Kind |
|--------|------|------|
| `onboardingSessions` | src/database/schema.ts | Drizzle table export |
| `config.platformBotToken` | src/config.ts | Config field |
| `config.platformWebhookSecret` | src/config.ts | Config field |
| `config.webhookBaseUrl` | src/config.ts | Config field |
| `insertTestBusiness` | tests/helpers/test-business.ts | Function export |
| `TestBusinessOptions` | tests/helpers/test-business.ts | Interface export |
