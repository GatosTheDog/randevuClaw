---
phase: 05-owner-self-serve-onboarding
plan: 06
type: execute
wave: 5
depends_on:
  - 05-04
  - 05-05
files_modified:
  - tests/onboarding-platform.test.ts
  - tests/onboarding-flow.test.ts
autonomous: true
requirements:
  - BOT-01
  - ONB-01
  - ONB-02
  - ONB-03
must_haves:
  truths:
    - "onboarding-platform.test.ts covers: HMAC rejection (401), valid getMe validation, new-owner creates business + session, re-registration resets session"
    - "onboarding-flow.test.ts covers: full name→hours→services flow completes with currentStep='done', resume mid-flow picks up at correct step"
    - "onboarding-flow.test.ts covers: closed day writes isClosed: true row, invalid HH:MM does not advance step"
    - "All mocks follow jest.spyOn pattern (D-13) — no real PLATFORM_BOT_TOKEN needed for CI"
    - "clearBotRegistry called in afterEach to reset Telegraf registry between tests"
  artifacts:
    - tests/onboarding-platform.test.ts tests BOT-01 behaviors
    - tests/onboarding-flow.test.ts tests ONB-01/ONB-02 state machine behaviors
  key_links:
    - "getMeBotInfo mocked via jest.spyOn(telegramClient, 'getMeBotInfo') — resolves success or rejects for invalid token tests"
    - "sendTelegramMessage mocked to capture prompt text for assertions"
    - "onboarding/queries.ts mocked for DB isolation in platform tests; real DB used selectively in flow tests for step advancement"
---

<objective>
Write integration tests proving BOT-01 (platform bot registration), ONB-01 (full guided flow), ONB-02 (mid-flow resume), and ONB-03 (edit keyword intercept) work end-to-end. Tests follow the D-13 mock pattern — no real platform bot token needed for CI.

Purpose: Automated proof that the phase goal is achieved; these tests become the regression barrier for future changes to the onboarding subsystem.
Output: tests/onboarding-platform.test.ts; tests/onboarding-flow.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-owner-self-serve-onboarding/05-CONTEXT.md
@.planning/phases/05-owner-self-serve-onboarding/05-RESEARCH.md
@.planning/phases/05-owner-self-serve-onboarding/05-PATTERNS.md
@.planning/phases/05-owner-self-serve-onboarding/05-04-SUMMARY.md
@.planning/phases/05-owner-self-serve-onboarding/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Platform handler integration tests (BOT-01)</name>
  <files>tests/onboarding-platform.test.ts</files>
  <read_first>
    tests/telegram-webhook.test.ts — full file: import pattern, jest.mock calls, supertest request helper, typed mock pattern (jest.MockedFunction), clearBotRegistry in afterEach
    src/webhooks/platform.ts — handlePlatformBotWebhook (Plan 04) — what the handler reads from request
    src/onboarding/queries.ts — function signatures to mock
    src/telegram/client.ts — getMeBotInfo, sendTelegramMessage, botTokenStore exports to mock
    tests/jest.setup.ts — PLATFORM_WEBHOOK_SECRET = 'test-platform-webhook-secret' (mock secret used in test requests)
    05-PATTERNS.md §"tests/onboarding-platform.test.ts" — supertest request helper with x-telegram-bot-api-secret-token header; mock getMe pattern
    05-RESEARCH.md §"Phase Requirements → Test Map" for BOT-01 — test scenarios list
    05-CONTEXT.md §D-13 — mock callTelegramApi via jest.spyOn; no real platform bot token
  </read_first>
  <action>
    Create tests/onboarding-platform.test.ts. Use supertest against the imported app (import app from '../src/server').

    Mocks: jest.mock('../src/database/queries'), jest.mock('../src/onboarding/queries'), jest.mock('../src/telegram/client'), jest.mock('../src/telegram/registry').
    Spy on getMeBotInfo from src/telegram/client using jest.spyOn for per-test control (D-13 pattern). Also spy on registerBotWebhook, unregisterBotWebhook.

    Constants:
    PLATFORM_SECRET = 'test-platform-webhook-secret' (must match PLATFORM_WEBHOOK_SECRET placeholder in jest.setup.ts).
    NEW_OWNER_ID = 12345678 (numeric).
    VALID_BOT_TOKEN = '1234567890:ABCdefGHIjklMNOpqrSTUvwxYZ'.

    Helper: postPlatformWebhook(body, secret = PLATFORM_SECRET) — supertest POST to '/webhooks/telegram/platform' with the secret header and JSON body.

    Helper: makeMessageUpdate(updateId, fromId, text) — returns TelegramUpdate shape: { update_id: updateId, message: { message_id: updateId, from: { id: fromId }, chat: { id: fromId }, text } }.

    beforeEach: clearBotRegistry from src/telegram/registry; jest.clearAllMocks().

    Test suite "HMAC verification":
    - "rejects request with missing secret token" — POST without header; expect 401
    - "rejects request with wrong secret token" — POST with 'wrong-secret'; expect 401
    - "accepts request with correct secret token" — POST with PLATFORM_SECRET; mock findActiveSessionByOwnerTelegramId to return null, mock findBusinessByOwnerTelegramId to return null, mock getMeBotInfo to resolve { id: 1, username: 'testbot', firstName: 'Test' }, mock createBusinessForOnboarding to resolve a business object, mock createOrResetOnboardingSession to resolve; expect 200

    Test suite "New owner registration (BOT-01)":
    - "valid bot token: creates business, creates session, sends first Greek prompt" — mock findActive=null, findExisting=null, getMeBotInfo resolves valid. Assert createBusinessForOnboarding called once. Assert createOrResetOnboardingSession called with 'name'. Assert sendTelegramMessage called with ownerTelegramId string and message containing 'Καλωσήρθατε' or 'ονομάζεται'.
    - "invalid bot token: getMeBotInfo throws, sends Greek error, does not create business" — mock getMeBotInfo to throw. Assert createBusinessForOnboarding NOT called. Assert sendTelegramMessage called with a message containing 'Μη έγκυρο' or similar.

    Test suite "Resume mid-flow (ONB-02)":
    - "active session found: dispatches to dispatchOnboardingStep without re-creating business" — mock findActive to return a session with currentStep='hours_2_query'. Mock dispatchOnboardingStep (import from ../src/onboarding/router and spy or mock the module). Assert dispatchOnboardingStep called once. Assert createBusinessForOnboarding NOT called.

    Test suite "Re-registration":
    - "owner with done session: deleteWebhook called, session reset to name" — mock findActive=null (no active session), mock findBusinessByOwnerTelegramId to return a business with botToken set and currentStep would be 'done'. Mock getMeBotInfo resolves. Mock unregisterBotWebhook resolves. Mock activateBusiness resolves. Assert unregisterBotWebhook called with the old botToken. Assert createOrResetOnboardingSession called with 'name'.

    Test suite "Deduplication":
    - "duplicate update_id is ignored without creating session" — mock insertOrIgnoreTelegramUpdate to return 'ignored'. Assert createOrResetOnboardingSession NOT called.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=onboarding-platform --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=onboarding-platform --no-coverage` exits 0 with all tests passing
    - `grep -c "it(" tests/onboarding-platform.test.ts` returns 8 or more (one test per scenario above)
    - `grep "timingSafeEqual\|401\|Unauthorized" tests/onboarding-platform.test.ts` returns matches for HMAC test assertions
    - `grep "createBusinessForOnboarding\|createOrResetOnboardingSession" tests/onboarding-platform.test.ts` returns assertions
  </acceptance_criteria>
  <done>All platform handler tests pass; HMAC rejection, valid registration, resume, re-registration, and dedup scenarios covered</done>
</task>

<task type="auto">
  <name>Task 2: State machine and edit flow tests (ONB-01/ONB-02/ONB-03)</name>
  <files>tests/onboarding-flow.test.ts</files>
  <read_first>
    tests/conversation-router.test.ts — dispatch-on-state-field pattern; typed mock setup for DB query functions; mock sendTelegramMessage to capture sent text
    src/onboarding/steps.ts — all handler function signatures (Plan 03); OnboardingStep type
    src/onboarding/router.ts — dispatchOnboardingStep signature (Plan 03)
    src/onboarding/queries.ts — updateOnboardingStep, findActiveSessionByOwnerTelegramId (mocked in unit tests)
    src/onboarding/edit-router.ts — isOwnerEditCommand, OWNER_EDIT_KEYWORDS (Plan 05)
    tests/jest.setup.ts — env vars available in test environment
    05-PATTERNS.md §"tests/onboarding-flow.test.ts" — mock setup pattern, beforeEach clearBotRegistry
    05-RESEARCH.md §"Phase Requirements → Test Map" for ONB-01/ONB-02 — specific test scenarios
  </read_first>
  <action>
    Create tests/onboarding-flow.test.ts. Use unit-testing style (mock DB queries and Telegram client).

    Mocks: jest.mock('../src/onboarding/queries'), jest.mock('../src/database/queries'), jest.mock('../src/telegram/client'), jest.mock('../src/telegram/registry'), jest.mock('../src/telegram/client').

    Helper: makeSession(currentStep, collectedData = null) — returns OnboardingSession shape with id: 1, businessId: 1, currentStep, collectedData.
    Helper: makeBusiness(overrides = {}) — returns Business shape with id: 1, name: 'Test', slug: 'test', botToken: 'bot-token', webhookId: 'wh-id', webhookSecret: 'secret', ownerTelegramId: '999'.

    beforeEach: jest.clearAllMocks(). Mock updateOnboardingStep to resolve undefined. Mock sendTelegramMessage to resolve { messageId: 1 }.

    Test suite "dispatchOnboardingStep — name step" (unit tests on the router):
    - "valid name: updates businesses, advances to hours_0_query, sends Greek prompt" — call dispatchOnboardingStep(makeSession('name'), makeBusiness(), '999', 'Pilates Studio Athens'). Assert updateOnboardingStep called with (1, 'hours_0_query', null). Assert sendTelegramMessage called with text containing 'Κυριακή' (the first day prompt).
    - "empty name: does not advance step, sends error prompt" — dispatch with '   '. Assert updateOnboardingStep NOT called.

    Test suite "Hours steps":
    - "hours_0_query 'ναι': advances to hours_0_open, asks for start time" — dispatch with 'Ναι'. Assert updateOnboardingStep called with (1, 'hours_0_open', anything). Assert sendTelegramMessage text contains 'Κυριακή' and start-time prompt.
    - "hours_0_query 'όχι': inserts isClosed row, advances to hours_1_query" — mock db.insert to resolve. Assert sendTelegramMessage text contains 'Δευτέρα'.
    - "hours_0_open valid HH:MM: saves to collectedData, advances to hours_0_close" — dispatch with '09:00'. Assert updateOnboardingStep called with (1, 'hours_0_close', JSON.stringify({ currentDayOpenTime: '09:00' })).
    - "hours_0_open invalid format: does not advance step, sends error message" — dispatch with '9am'. Assert updateOnboardingStep NOT called. Assert sendTelegramMessage text contains 'ΩΩ:ΛΛ' or 'έγκυρη ώρα'.
    - "hours_0_close valid HH:MM: inserts business_hours row, advances to hours_1_query" — dispatch with '18:00'. Assert db.insert called. Assert updateOnboardingStep called with (1, 'hours_1_query', anything).

    Test suite "Resume mid-flow (ONB-02)":
    - "session at hours_3_query resumes at hours_3_query without re-asking earlier days" — call dispatchOnboardingStep with step='hours_3_query'. Assert sendTelegramMessage called with text containing 'Τετάρτη'. Assert updateOnboardingStep NOT called in this single dispatch (awaiting owner's yes/no response — the step hasn't advanced yet).

    Test suite "Service steps":
    - "svc_name valid: advances to svc_price" — dispatch with 'Reformer Pilates'. Assert updateOnboardingStep called with (1, 'svc_price', JSON.stringify with name set).
    - "svc_price valid cents: advances to svc_duration" — dispatch with '2500'. Assert updateOnboardingStep called with 'svc_duration'.
    - "svc_price invalid: does not advance" — dispatch with 'twenty euros'. Assert updateOnboardingStep NOT called.
    - "svc_duration valid minutes: inserts service row, advances to svc_more" — Assert db.insert called for services table. Assert updateOnboardingStep called with (1, 'svc_more', anything).
    - "svc_more 'όχι': calls handleActivate path" — mock registerBotWebhook, unregisterBotWebhook, activateBusiness. Dispatch 'όχι'. Assert updateOnboardingStep called with 'done'. Assert sendTelegramMessage text contains 'ενεργή'.
    - "svc_more 'ναι': clears currentService, advances to svc_name" — mock collectedData with { currentService: { name: 'Old', price: 1000 } }. Dispatch 'ναι'. Assert updateOnboardingStep called with step='svc_name' and collectedData containing cleared currentService.

    Test suite "isOwnerEditCommand (ONB-03)":
    - "returns true for 'αλλαγή ωραρίου'" — call isOwnerEditCommand directly. Assert returns true.
    - "returns true for 'ΑΛΛΑΓΉ ΤΙΜΉΣ' (upper case)" — Assert returns true.
    - "returns false for 'θέλω ραντεβού'" — Assert returns false.
    - "non-owner message with edit keyword is not intercepted" — this is tested in telegram-webhook test suite (Plan 05 tests); add a note comment.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=onboarding-flow --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=onboarding-flow --no-coverage` exits 0 with all tests passing
    - `grep -c "it(" tests/onboarding-flow.test.ts` returns 14 or more
    - `grep "isClosed.*true\|isOwnerEditCommand" tests/onboarding-flow.test.ts` returns matches
    - Full suite `npm test` still exits 0 (no regressions)
  </acceptance_criteria>
  <done>All ONB-01/ONB-02/ONB-03 unit tests pass; step advancement, validation, closed-day handling, resume, and edit keyword detection covered; full npm test green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test environment → real Telegram API | All Telegram API calls are mocked via jest.spyOn; no real tokens used in CI |
| test environment → real DB | Unit tests mock DB functions; no real DB mutations in these tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-17 | Spoofing | Test secret value leaked from jest.setup.ts to test assertions | low | accept | Test secrets are throwaway values (test-platform-webhook-secret); not production values; no risk |
| T-05-SC | Tampering | npm installs | low | accept | No new packages; supertest already in devDependencies |
</threat_model>

<verification>
- `npm test -- --testPathPattern=onboarding-platform --no-coverage` exits 0
- `npm test -- --testPathPattern=onboarding-flow --no-coverage` exits 0
- `npm test` (full suite) exits 0 — no regressions in existing tests
</verification>

<success_criteria>
- BOT-01: HMAC rejection, valid registration, invalid token rejection, re-registration all tested
- ONB-01: Full step sequence (name → hours_0 → ... → svc_more → done) covered
- ONB-02: Resume mid-flow test confirms correct step prompt without re-asking earlier steps
- ONB-03: isOwnerEditCommand is case-insensitive; all four keywords detected
- All mocks follow D-13 pattern (jest.spyOn); no real Telegram API calls in CI
</success_criteria>

<output>
Create .planning/phases/05-owner-self-serve-onboarding/05-06-SUMMARY.md when done
</output>

## Artifacts this phase produces

New symbols introduced in this plan:

| Symbol | File | Kind |
|--------|------|------|
| `onboarding-platform.test.ts` | tests/onboarding-platform.test.ts | Test file |
| `onboarding-flow.test.ts` | tests/onboarding-flow.test.ts | Test file |
