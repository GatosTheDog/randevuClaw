---
phase: 01-foundation-webhook-business-resolution
plan: 3
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/webhooks/whatsapp.ts
  - src/consent/checker.ts
  - tests/idempotency.test.ts
  - tests/consent.test.ts
  - tests/consent-schema.test.ts
autonomous: true
requirements: [PLAT-01, COMP-01]
must_haves:
  truths:
    - "Sending the identical WhatsApp message ID twice for a message that resolves to a known business produces exactly one reply and one row in the messages table, not two (idempotent, deduplicated webhook handling); messages.businessId stays a non-null FK (per Plan 01-01's schema, unmodified) — no sentinel/placeholder business row is ever inserted for an unresolved business code"
    - "A client's very first message ever to a given business receives a Greek data-consent notice explaining what is stored (phone number, booking history) before any other conversation happens"
    - "A second message from the same (phone, business) pair does not repeat the consent notice"
    - "The consent flag and timestamp are recorded on the client_business_relationships row, not a separate consent-events log table"
  artifacts:
    - path: "src/consent/checker.ts"
      provides: "first-contact detection and implied-consent tracking per (business, phone) pair"
      exports: ["getOrCreateClientRelationship", "CONSENT_NOTICE_GREEK_TEMPLATE"]
  key_links:
    - from: "src/webhooks/whatsapp.ts"
      to: "src/database/queries.ts"
      via: "insertOrIgnoreMessage(...) called only after business resolution succeeds (valid non-null businessId), before any reply is generated, to detect duplicates for the found-business path"
      pattern: "insertOrIgnoreMessage\\("
    - from: "src/webhooks/whatsapp.ts"
      to: "src/consent/checker.ts"
      via: "getOrCreateClientRelationship(businessId, senderPhone) called to decide whether to prepend the consent notice"
      pattern: "getOrCreateClientRelationship\\("
    - from: "src/webhooks/whatsapp.ts"
      to: "src/database/queries.ts"
      via: "markMessageProcessed(...) called only after sendWhatsAppMessage succeeds"
      pattern: "markMessageProcessed\\("
---

## Phase Goal

**As a** client of a Greek service business, **I want to** text a business-specific WhatsApp deep link and be reliably routed to the right business, with a data-consent notice on my first contact, **so that** I can start a booking conversation with zero confusion about who I'm talking to, even if my message gets sent or delivered twice.

> Note: ROADMAP.md's Phase 1 `**Goal**:` line is descriptive prose, not the strict "As a / I want to / so that" format. This story is derived directly from that Goal plus Success Criteria 1-4 (business resolution, consent notice, dedup, not-found handling) without adding new scope. Success Criterion 5 (Meta Business Verification) is an operational prerequisite, not part of the client-facing story — it is covered by Plan 01-04.

<objective>
Layer the remaining two Phase 1 vertical slices onto the working webhook from Plan 01-02: message deduplication (Roadmap SC3) and first-contact consent (Roadmap SC2 / COMP-01). Both modify the same handler (src/webhooks/whatsapp.ts), which is why this plan depends on and runs after Plan 01-02 rather than in parallel.

Purpose: Close the two remaining Phase 1 success criteria that the walking-skeleton slice (01-02) deliberately deferred, without touching business resolution or reply-sending logic that 01-02 already proved works.
Output: Duplicate-safe, consent-compliant webhook handler.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-webhook-business-resolution/01-CONTEXT.md
@.planning/phases/01-foundation-webhook-business-resolution/01-RESEARCH.md
@.planning/phases/01-foundation-webhook-business-resolution/01-01-SUMMARY.md
@.planning/phases/01-foundation-webhook-business-resolution/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01-01 (unchanged, consumed only) -->
```typescript
export async function insertOrIgnoreMessage(whatsappMessageId: string, businessId: number, senderPhone: string, messageBody: string): Promise<'inserted' | 'ignored'>;
export async function markMessageProcessed(whatsappMessageId: string): Promise<void>;
export async function findClientBusinessRelationship(businessId: number, senderPhone: string): Promise<ClientBusinessRelationship | null>;
export async function insertClientBusinessRelationship(businessId: number, senderPhone: string): Promise<ClientBusinessRelationship>;
```

<!-- From Plan 01-02 (this plan modifies handleWebhookPost's body, not its signature) -->
```typescript
export async function handleWebhookPost(req: Request, res: Response): Promise<void>;
export function buildBusinessFoundReplyGreek(businessName: string): string;
export const BUSINESS_NOT_FOUND_REPLY_GREEK: string;
```
Current handleWebhookPost flow (Plan 01-02, being extended not replaced): verify signature -> parse+validate payload -> extract message -> if non-text, 200 and return -> extractAndNormalizeBusinessCode -> findBusinessBySlug -> build reply (found/not-found) -> sendWhatsAppMessage -> respond 200.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire idempotent message dedup into the webhook handler</name>
  <files>src/webhooks/whatsapp.ts, tests/idempotency.test.ts</files>
  <read_first>
    .planning/phases/01-foundation-webhook-business-resolution/01-CONTEXT.md decisions D-05, D-06, D-07, D-08
    .planning/phases/01-foundation-webhook-business-resolution/01-RESEARCH.md ("Pitfall 2: Duplicate Webhook Processing Leads to Silent Data Corruption", "Pattern 3: Idempotent Message Insertion")
  </read_first>
  <behavior>
    - Test 1: two POSTs with the same `messages[0].id`, both resolving to a KNOWN business (`findBusinessBySlug` mocked to return a business both times) — the first calls `insertOrIgnoreMessage` (mocked `'inserted'`) then `sendWhatsAppMessage` exactly once and responds 200; the second (mocked `insertOrIgnoreMessage` returning `'ignored'`) does NOT call `sendWhatsAppMessage` again, logs the duplicate, and still responds 200 (D-07 silent no-op — Meta must always get a 200 ack or it will keep retrying).
    - Test 2: on first receipt of a resolved-business message (`insertOrIgnoreMessage` returns `'inserted'`), `markMessageProcessed` is called only AFTER `sendWhatsAppMessage` resolves successfully — if `sendWhatsAppMessage` throws, `markMessageProcessed` is never called for that message ID (D-08: accepts a rare duplicate-reply risk on a crash between send and mark, in exchange for never silently dropping a message crashed on mid-processing).
    - Test 3: business-not-found path — `findBusinessBySlug` mocked to return `null`; the handler performs a read-only existence check (`SELECT 1 FROM messages WHERE whatsapp_message_id = ?`, mocked at the db-query level) before replying. When the mock indicates NO existing row, `BUSINESS_NOT_FOUND_REPLY_GREEK` is sent and `insertOrIgnoreMessage` is NEVER called (no insert against the FK-constrained `businessId` column — proves no sentinel/placeholder business ID is ever used). When the mock indicates a row for that `whatsappMessageId` ALREADY exists (simulating an earlier delivery of the identical message ID that had resolved to a valid business and been recorded), the not-found reply is suppressed and `sendWhatsAppMessage` is not called — this guards the specific race where a duplicate delivery flips from found to not-found between attempts.
  </behavior>
  <action>
    In src/webhooks/whatsapp.ts, restructure `handleWebhookPost` so business resolution (`extractAndNormalizeBusinessCode` + `findBusinessBySlug`, both already implemented in Plan 01-02) happens BEFORE the dedup decision — dedup is keyed on `messages.businessId`, which stays the `notNull references(() => businesses.id)` FK exactly as defined in Plan 01-01's schema.ts; this plan does not modify that schema and never inserts a sentinel/placeholder businessId. After the existing type==='text' guard, branch on whether a business was resolved:
    - Business FOUND: call `insertOrIgnoreMessage(message.id, business.id, message.from, message.text.body)`. If `'ignored'`, log at info level ("duplicate message ignored", messageId) and respond 200 immediately — no reply send, no further processing (D-07). If `'inserted'`, continue with the reply-building flow (Task 2 of this plan layers the consent check onto this branch), then after `sendWhatsAppMessage` resolves successfully, call `await markMessageProcessed(message.id)` (D-08 — only after success; if sendWhatsAppMessage throws, the catch block logs the error and responds 200 without calling markMessageProcessed, leaving no row change for this rare crash-window case).
    - Business NOT FOUND: do NOT call `insertOrIgnoreMessage` — there is no valid businessId to satisfy the FK, and no synthetic/sentinel value is used. Instead, before sending the not-found reply, run a direct read-only existence check using the already-imported `db`/`schema` — a local `db.select().from(messages).where(eq(messages.whatsappMessageId, message.id)).limit(1)` (queries.ts does not need a new exported function for this single internal check). If a row already exists (an earlier delivery of this exact message ID was already resolved to a valid business and recorded), skip sending `BUSINESS_NOT_FOUND_REPLY_GREEK` and respond 200 (guards the found/not-found race). If no row exists, send `BUSINESS_NOT_FOUND_REPLY_GREEK` and respond 200 — still no insert into `messages` for this case.
    Document as a code comment the accepted, narrower guarantee this implies: a message whose business code genuinely never resolves (permanently not-found) has no FK-safe row to persist a "processed" marker against, so a Meta-retried delivery of that same message ID may send the not-found reply more than once; this is intentionally a weaker guarantee than the found-business path's full DB-backed dedup, and is consistent with D-05's Postgres-only (no sentinel row, no Redis, no new dedup table) approach — full not-found dedup would require either a nullable `messages.businessId` or a separate dedup table, both out of scope for this surgical fix.
    Note: messages are retained forever with no TTL/cleanup job per D-06 — do not add one.
  </action>
  <acceptance_criteria>
    - All 3 behavior cases pass
    - No TTL/cleanup logic exists anywhere in the diff (D-06)
    - `insertOrIgnoreMessage` is called ONLY on the business-found branch, always with a valid non-null `business.id` — grep confirms no `?? 0`, no hardcoded businessId literal, and no other sentinel/placeholder value passed as the businessId argument anywhere in src/webhooks/whatsapp.ts
    - `sendWhatsAppMessage` is called at most once per unique whatsappMessageId for the found-business path (fully DB-backed via the messages UNIQUE constraint); for the not-found path, a duplicate reply is suppressed only when a prior row for that whatsappMessageId already exists (the found/not-found race case) — the accepted limitation for a permanently-unresolved code is documented in-code, not silently hidden
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --testPathPattern=idempotency.test.ts</automated>
  </verify>
  <done>Duplicate WhatsApp webhook deliveries for a resolved business produce exactly one reply and one messages-table row; the not-found path is protected via a read-only existence check (never an insert) with the documented, accepted limitation for messages whose business code never resolves — matching Roadmap SC3 and D-05/D-07/D-08 without introducing a sentinel businessId or violating the FK from Plan 01-01's schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: First-contact consent notice</name>
  <files>src/consent/checker.ts, src/webhooks/whatsapp.ts, tests/consent.test.ts, tests/consent-schema.test.ts</files>
  <read_first>
    .planning/phases/01-foundation-webhook-business-resolution/01-CONTEXT.md decisions D-09, D-10, D-11, D-12
    .planning/phases/01-foundation-webhook-business-resolution/01-RESEARCH.md ("Pattern 4: First-Contact Consent Tracking", "Pitfall 4: First-Contact Consent Notice Is Shown On Every Message", "Open Question 3")
  </read_first>
  <behavior>
    - Test 1: `getOrCreateClientRelationship(businessId, senderPhone)` on a phone/business pair with no existing row returns `{ isFirstContact: true, consentGiven: true }` and calls `insertClientBusinessRelationship(businessId, senderPhone)` exactly once (D-10 implied consent, no blocking confirmation step).
    - Test 2: the same call repeated for an existing pair returns `{ isFirstContact: false, consentGiven: true }` and does NOT call `insertClientBusinessRelationship` again (Pitfall 4 — must not repeat consent on every message).
    - Test 3 (consent.test.ts, webhook-level): a first-contact reply is `${CONSENT_NOTICE_GREEK_TEMPLATE(businessName)}\n\n${buildBusinessFoundReplyGreek(businessName)}` (notice prepended to the same message, not a separate send); a second-contact reply from the same sender is just `buildBusinessFoundReplyGreek(businessName)` with no consent text.
    - Test 4 (consent-schema.test.ts, integration-style against a mocked db): after first contact, the row inserted into client_business_relationships has `consentGiven: true` and a `consentTimestamp` set to the insertion time — verified by asserting the exact values passed to `db.insert(clientBusinessRelationships).values(...)`.
  </behavior>
  <action>
    In src/consent/checker.ts, implement `CONSENT_NOTICE_GREEK_TEMPLATE = (businessName: string) => \`Για να διαχειριστούμε το ραντεβού σας με την επιχείρηση ${businessName}, αποθηκεύουμε τον αριθμό τηλεφώνου σας και το ιστορικό ραντεβού σας.\`` — contract-necessity framing per D-11, deliberately no "reply STOP" opt-out line. Implement `getOrCreateClientRelationship(businessId: number, senderPhone: string): Promise<{ isFirstContact: boolean; consentGiven: boolean }>`: call `findClientBusinessRelationship(businessId, senderPhone)` (from Plan 01-01's queries.ts, already filters by both businessId AND senderPhone per D-13); if found, return `{ isFirstContact: false, consentGiven: existing.consentGiven }`; if not found, call `insertClientBusinessRelationship(businessId, senderPhone)` (which sets consentGiven true and consentTimestamp to now per D-10/D-12) and return `{ isFirstContact: true, consentGiven: true }`.
    In src/webhooks/whatsapp.ts, in the business-found branch of `handleWebhookPost` (after dedup, after `findBusinessBySlug` succeeds), call `const { isFirstContact } = await getOrCreateClientRelationship(business.id, message.from)` and build the reply as `isFirstContact ? \`${CONSENT_NOTICE_GREEK_TEMPLATE(business.name)}\n\n${buildBusinessFoundReplyGreek(business.name)}\` : buildBusinessFoundReplyGreek(business.name)` — one WhatsApp message, notice prepended, never a separate send (Open Question 3's recommended resolution). The business-not-found branch does NOT perform a consent check (there is no business to attach a relationship to).
  </action>
  <acceptance_criteria>
    - All 4 behavior cases pass
    - No new table or "consent_events" log is introduced — consent lives only on client_business_relationships (D-12)
    - Consent notice text contains no "reply STOP" or opt-out phrasing (D-11)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --testPathPattern="consent.test.ts|consent-schema.test.ts"</automated>
  </verify>
  <done>First-ever contact for a (phone, business) pair receives the Greek consent notice prepended to the business-found reply; subsequent contacts do not repeat it; consent is recorded per D-12's exact shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webhook -> messages/client_business_relationships tables | Write path exercised on every inbound message; must not leak one business's client history into another's |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Spoofing / Tampering (replay) | src/webhooks/whatsapp.ts, src/database/queries.ts insertOrIgnoreMessage | high | mitigate | Postgres UNIQUE constraint on messages.whatsapp_message_id + onConflictDoNothing(); Meta's retried webhook deliveries for a resolved business are silently ignored — exactly one reply per unique message ID (D-05/D-07/D-08, Roadmap SC3). The not-found path cannot use this same insert (messages.businessId is a non-null FK, no sentinel value used) so it relies on a read-only existence check instead — accepted narrower guarantee, documented in-code, for messages whose business code never resolves |
| T-01-11 | Information Disclosure | src/consent/checker.ts, client_business_relationships table | medium | mitigate | Data minimization — only phone number + consent flag + timestamp stored per relationship, no extra PII (COMP-01); TLS in transit + Neon at-rest encryption |
| T-01-12 | Information Disclosure / Access Control | src/consent/checker.ts, src/database/queries.ts | high | mitigate | Every client-business relationship query filters by BOTH business_id AND sender_phone (composite WHERE matching the unique_client_business index) — prevents one business's client history from leaking into another business's lookup (D-13 app-level tenant isolation) |

</threat_model>

<verification>
`npm test -- --testPathPattern="idempotency|consent"` — full suite remains network-free per 01-VALIDATION.md's ~90s runtime budget (db calls mocked). Manual replay verification (send the identical WhatsApp message twice) is covered by 01-VALIDATION.md's phase-gate sampling before `/gsd-verify-work`.
</verification>

<success_criteria>
- Duplicate WhatsApp message IDs for a resolved business never produce a second reply or a second processed row; the not-found path never crashes on the businessId FK and never inserts a sentinel value, with the documented, accepted limitation that a permanently-unresolved business code's not-found reply may repeat on a Meta retry
- First-ever (phone, business) contact shows the Greek consent notice; every later contact does not
- Consent is recorded exactly as D-12 specifies (flag + timestamp on the relationship row)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-webhook-business-resolution/01-03-SUMMARY.md` when done
</output>
