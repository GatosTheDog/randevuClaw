---
phase: 13-slotless-booking
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - src/webhooks/telegram.ts
  - tests/slotless-requests.test.ts
autonomous: true
requirements:
  - SLOT-02
  - SLOT-03
  - SLOT-04
  - SLOT-05
  - SLOT-06

must_haves:
  truths:
    - Owner taps Ναι on a slotless request keyboard → booking is created, 1 credit deducted, client receives a Greek confirmation message, owner's keyboard is cleared
    - Owner taps Όχι on a slotless request keyboard → request is set to rejected, client receives a Greek rejection message, owner's keyboard is cleared
    - When membership lapsed between request and Ναι tap, approveSlotlessRequest returns null and the owner receives a Greek error message; no booking is created
    - parseCallbackData correctly parses "slotless:req_approve:<id>" and "slotless:req_reject:<id>" patterns and returns a SlotlessCallbackResult discriminant
    - Integration tests cover SLOT-01 (insert returns pending), SLOT-03 (approve deducts credit), SLOT-03 (approve with lapsed membership returns null), SLOT-04 (all outcomes persisted), SLOT-05 (list returns history), SLOT-06 (count returns correct number)
  artifacts:
    - src/webhooks/telegram.ts — parseCallbackData extended; handleCallbackQuery extended with slotless branch
    - tests/slotless-requests.test.ts — integration tests covering SLOT-01 through SLOT-06
  key_links:
    - The owner notification for a pending slotless request (SLOT-02) is sent at request-creation time by bookAppointmentTool (Plan 02), NOT by the callback handler. The callback handler only fires when the owner taps Ναι/Όχι.
    - The keyboard sent with the slotless notification must use callback_data "slotless:req_approve:<slotlessRequestId>" and "slotless:req_reject:<slotlessRequestId>" — callback parser must know these patterns
    - Plan 02's bookAppointmentTool must send the owner keyboard when it inserts a slotless request; this plan adds the callback handler that processes the keyboard tap
---

<objective>
Complete the slotless booking flow by:
1. Extending bookAppointmentTool (Plan 02) to send the owner a Ναι/Όχι keyboard alongside the slotless notification (SLOT-02).
2. Extending parseCallbackData and handleCallbackQuery in telegram.ts to handle slotless approval/rejection (SLOT-02, SLOT-03).
3. Writing integration tests that cover all six SLOT requirements.

Purpose: Close the owner-side keyboard loop so approved requests become bookings and rejected requests notify the client.
Output: Modified telegram.ts, new tests/slotless-requests.test.ts.
</objective>

<execution_context>
@/root/.claude/gsd-core/workflows/execute-plan.md
@/root/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/webhooks/telegram.ts
@src/conversation/function-executor.ts
@src/session/slotless-requests.ts
@tests/helpers/test-business.ts
@tests/helpers/billing-fixtures.ts
@tests/helpers/session-fixtures.ts
@tests/cancellation-cutoff.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Owner keyboard for slotless notification + callback handler in telegram.ts</name>
  <files>src/conversation/function-executor.ts, src/webhooks/telegram.ts</files>
  <action>
--- src/conversation/function-executor.ts (SLOT-02 keyboard) ---

In the slotless fork added in Plan 02 (inside bookAppointmentTool, after the insertSlotlessRequest call), add:

After insertSlotlessRequest returns a non-null request row, send the owner a notification with a Ναι/Όχι keyboard:

  if (request && context.business.ownerTelegramId) {
    const clientName = await getClientName(context.business.id, context.clientPhone);
    const membership = await getClientActiveMembership(context.business.id, context.clientPhone);
    const membershipStatus = membership
      ? `Ενεργή συνδρομή (${membership.sessionsRemaining ?? 'απεριόριστες'} sessions)`
      : 'Χωρίς ενεργή συνδρομή';
    const notifText =
      `Νέο αίτημα χωρίς διαθέσιμη θέση:\n` +
      `Πελάτης: ${clientName ?? context.clientPhone}\n` +
      `Ημερομηνία: ${parsed.calendar_date} ${parsed.calendar_time}\n` +
      `Υπηρεσία: ${service.name}\n` +
      `Συνδρομή: ${membershipStatus}`;
    try {
      await sendTelegramMessageWithKeyboard(context.business.ownerTelegramId, notifText, [
        [
          { text: '✅ Ναι', callback_data: `slotless:req_approve:${request.id}` },
          { text: '❌ Όχι', callback_data: `slotless:req_reject:${request.id}` },
        ],
      ]);
    } catch (err) {
      logger.error({ err }, 'Slotless request owner notification failed (best-effort)');
    }
  }

Import getClientActiveMembership at the top of function-executor.ts (it is already imported via billing/queries — verify and add if missing).

--- src/webhooks/telegram.ts (callback handler) ---

Add a new result type alongside BookingCallbackResult and BillingCallbackResult:

  export type SlotlessCallbackResult = {
    action: 'slotless:req_approve' | 'slotless:req_reject';
    slotlessRequestId: number;
  };

Extend parseCallbackData to also parse the pattern "slotless:req_approve:<id>" and "slotless:req_reject:<id>":

Add inside the function, before the `return null` line:
  const slotlessMatch = data?.match(/^slotless:(req_approve|req_reject):(\d+)$/);
  if (slotlessMatch) {
    return {
      action: `slotless:${slotlessMatch[1]}` as SlotlessCallbackResult['action'],
      slotlessRequestId: Number(slotlessMatch[2]),
    };
  }

Update the parseCallbackData return type to include SlotlessCallbackResult in the union.

In handleCallbackQuery, after the billingCallback ('firstId' in parsed) branch, add a slotless callback branch:

  if ('slotlessRequestId' in parsed) {
    const slotlessResult = parsed as SlotlessCallbackResult;
    // T-13-07: resolve business from owner's Telegram ID (same pattern as billing branch)
    const ownerBusiness = await findBusinessByOwnerTelegramId(senderTelegramId);
    if (!ownerBusiness) {
      logger.warn({ senderTelegramId, action: slotlessResult.action }, 'slotless callback from unregistered owner, ignoring');
      return;
    }

    if (slotlessResult.action === 'slotless:req_approve') {
      const result = await approveSlotlessRequest(slotlessResult.slotlessRequestId, ownerBusiness.id);
      if (result === null) {
        // Membership lapsed or request already resolved
        await sendTelegramMessage(senderTelegramId, 'Δεν ήταν δυνατή η έγκριση: η συνδρομή του πελάτη έχει λήξει ή το αίτημα δεν ισχύει πλέον.');
      } else {
        const { booking, request } = result;
        // Notify client
        try {
          await sendTelegramMessage(
            booking.clientPhone,
            `Το αίτημα σας εγκρίθηκε! Η κράτησή σας επιβεβαιώθηκε για ${booking.calendarDate} στις ${booking.calendarTime}.`
          );
        } catch (err) {
          logger.error({ err, bookingId: booking.id }, 'Slotless approval client notification failed (best-effort)');
        }
        await sendTelegramMessage(senderTelegramId, `✅ Εγκρίθηκε. Κράτηση #${booking.id} δημιουργήθηκε.`);
      }
    } else {
      // slotless:req_reject
      const request = await rejectSlotlessRequest(slotlessResult.slotlessRequestId);
      if (request === null) {
        await sendTelegramMessage(senderTelegramId, 'Το αίτημα δεν βρέθηκε ή έχει ήδη επιλυθεί.');
      } else {
        // Notify client
        try {
          await sendTelegramMessage(
            request.clientPhone,
            'Το αίτημα κράτησής σας δεν εγκρίθηκε. Παρακαλώ επικοινωνήστε με την επιχείρηση για εναλλακτικές ώρες.'
          );
        } catch (err) {
          logger.error({ err, requestId: request.id }, 'Slotless rejection client notification failed (best-effort)');
        }
        await sendTelegramMessage(senderTelegramId, '❌ Απορρίφθηκε.');
      }
    }

    // Clear keyboard regardless of outcome
    if (callbackQuery.message?.message_id) {
      await editTelegramMessageReplyMarkup(senderTelegramId, callbackQuery.message.message_id, []);
    }
    return;
  }

Add imports at the top of telegram.ts:
  import { approveSlotlessRequest, rejectSlotlessRequest } from '../session/slotless-requests';
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>TypeScript compiles without errors. parseCallbackData handles slotless patterns. handleCallbackQuery approves or rejects requests, clears keyboard, notifies client, and notifies owner. All notifications are best-effort wrapped in try/catch so a Telegram failure cannot abort a committed DB transaction.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests covering SLOT-01 through SLOT-06</name>
  <files>tests/slotless-requests.test.ts</files>
  <action>
Create tests/slotless-requests.test.ts following the exact pattern of tests/cancellation-cutoff.test.ts and tests/session-booking-flow.test.ts.

Header (copy the exact pattern):
  // covers SLOT-01, SLOT-02, SLOT-03, SLOT-04, SLOT-05, SLOT-06
  // Integration tests against a REAL local Postgres connection.
  // Requires migrations 0010 (session catalog + slotlessRequests table) to be applied.

  const TEST_DATABASE_URL =
    process.env.SESSION_TEST_DATABASE_URL ??
    process.env.BILLING_TEST_DATABASE_URL ??
    'postgresql://manolis@localhost:5432/randevuclaw_test';

  const ORIGINAL_DATABASE_URL = process.env.DATABASE_URL;
  process.env.DATABASE_URL = TEST_DATABASE_URL;
  jest.resetModules();

Use require() for all imports (eslint-disable as in sister tests). Import:
  - db from ../src/database/db
  - eq, and from drizzle-orm
  - slotlessRequests, businesses, memberships, membershipLedger, bookings, services from ../src/database/schema
  - insertSlotlessRequest, approveSlotlessRequest, rejectSlotlessRequest, listSlotlessRequestsForClient, countSlotlessRequestsSinceCheckin from ../src/session/slotless-requests
  - insertTestBusiness from ./helpers/test-business
  - insertTestPackage, insertTestMembership from ./helpers/billing-fixtures
  - withBusinessContext from ../src/database/queries

afterAll: restore DATABASE_URL.

Helper function uniquePhone(): returns `slotless-${Date.now()}-${Math.random().toString(36).slice(2)}`.

Write the following test cases inside a top-level describe('Slotless Booking Requests', ...) block:

--- Test 1 (SLOT-01): insertSlotlessRequest inserts a pending row ---
  beforeAll: insertTestBusiness with slotlessRequestsEnabled=true (set directly via db.update after insert).
  Call insertSlotlessRequest with valid params.
  Expect returned row to have status='pending', bookingId=null, correct businessId/clientPhone/serviceId/date/time.

--- Test 2 (SLOT-01): insertSlotlessRequest is idempotent (same idempotencyKey) ---
  Insert twice with the same idempotencyKey.
  Expect both calls to succeed without throwing; second call returns null (onConflictDoNothing).
  Query DB directly: only one row exists for that key.

--- Test 3 (SLOT-03): approveSlotlessRequest atomically creates booking + deducts credit ---
  Setup: business, service, membership with sessionsRemaining=5.
  Insert a slotless request (status='pending').
  Call approveSlotlessRequest(request.id, business.id) wrapped in withBusinessContext.
  Expect returned { booking, request } to be non-null.
  Expect booking.bookingStatus='confirmed', booking.calendarDate=requestedSessionDate.
  Expect DB: slotlessRequests row has status='approved', bookingId=booking.id.
  Expect DB: memberships.sessionsRemaining decreased by 1 (now 4).
  Expect DB: membershipLedger has a row with operationType='session_deducted', bookingId=booking.id.

--- Test 4 (SLOT-03): approveSlotlessRequest returns null when membership lapsed ---
  Setup: business, service, membership with expiresAt=one day ago (isActive=true but expired).
  Insert a slotless request.
  Call approveSlotlessRequest within withBusinessContext.
  Expect null returned (getActiveMembershipForDeduction returns null for expired membership).
  Expect DB: slotlessRequests row still has status='pending' (no mutation on null return).

--- Test 5 (SLOT-04): rejectSlotlessRequest sets status='rejected' ---
  Setup: insert a slotless request.
  Call rejectSlotlessRequest(request.id).
  Expect returned row to have status='rejected'.
  Expect DB: row has status='rejected'.

--- Test 6 (SLOT-04): rejectSlotlessRequest on already-rejected row returns null ---
  Insert a request, reject it, reject again.
  Second rejectSlotlessRequest returns null (WHERE status='pending' not matched).

--- Test 7 (SLOT-05): listSlotlessRequestsForClient returns all rows in DESC order ---
  Insert 3 requests for the same client with different dates.
  Call listSlotlessRequestsForClient.
  Expect 3 rows returned, sorted by createdAt DESC (most recent first).

--- Test 8 (SLOT-06): countSlotlessRequestsSinceCheckin returns correct count ---
  Insert 3 requests for client, one with createdAt backdated to 5 days ago.
  Call countSlotlessRequestsSinceCheckin with sinceDate = 2 days ago (ISO string).
  Expect count = 2 (only the 2 recent ones).
  Note: since test DB inserts are real-time, use the 3-request setup from test 7 and query with sinceDate=yesterday to get 3 (all inserted today).

Each test uses its own uniquePhone() to avoid cross-test contamination. Use beforeAll per describe block to set up shared business/service rows; clean up slotlessRequests rows in afterEach using db.delete(slotlessRequests).where(eq(slotlessRequests.clientPhone, phone)).

Keep tests self-contained and fast. No mocking — all tests hit the real test DB.
  </action>
  <verify>
    <automated>npx jest tests/slotless-requests.test.ts --testNamePattern="inserts a pending row|is idempotent" --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>tests/slotless-requests.test.ts exists and TypeScript compiles it without errors. The two idempotency tests (SLOT-01) pass against the local test DB. All eight test cases are present in the file with correct describe/it structure matching the project test pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram callback_data → parseCallbackData | slotlessRequestId extracted from callback_data must be a digit-only integer; regex enforces this |
| handleCallbackQuery → approveSlotlessRequest | ownerBusiness is resolved from authenticated senderTelegramId, never from untrusted callback_data, preventing cross-tenant approval |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-13-07 | Spoofing | slotless callback — cross-owner | high | mitigate | ownerBusiness resolved from senderTelegramId (same pattern as billing:pkg_confirm branch); slotlessRequestId alone cannot grant approval |
| T-13-08 | Tampering | double-tap on Ναι keyboard | medium | mitigate | approveSlotlessRequest checks status='pending' inside transaction; second tap returns null and no second booking is created |
| T-13-09 | Repudiation | slotless request not persisted | low | mitigate | SLOT-04 enforced at insert level (insertSlotlessRequest writes before sending the owner notification); even if Telegram send fails, the row exists |
| T-13-SC | Tampering | npm/pip installs | low | accept | No new packages installed in this plan; all imports are from files already in the project |
</threat_model>

<verification>
npx tsc --noEmit 2>&1 | tail -5
npx jest tests/slotless-requests.test.ts --passWithNoTests 2>&1 | tail -15
</verification>

<success_criteria>
- TypeScript compiles all modified and new files without errors
- parseCallbackData returns SlotlessCallbackResult for slotless:req_approve and slotless:req_reject patterns
- handleCallbackQuery processes slotless taps: approve creates booking + notifies client, reject notifies client, keyboard cleared in both cases
- lapsed membership on approval → null returned, owner receives Greek error, no booking created
- tests/slotless-requests.test.ts has 8 test cases covering SLOT-01 through SLOT-06
- The first two test cases pass on the local test DB (idempotency gate)
</success_criteria>

<output>
Create .planning/phases/13-slotless-booking/13-03-SUMMARY.md when done
</output>
