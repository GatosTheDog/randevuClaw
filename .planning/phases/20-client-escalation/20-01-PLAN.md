---
phase: 20-client-escalation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/telegram/escalation.ts
  - src/telegram/handlers/client-menu.ts
autonomous: true
requirements:
  - ESCL-01
  - ESCL-02

must_haves:
  truths:
    - When checkEnforcementAndGetMembership returns allowed=false, the client receives a Greek apology message and admin receives an escalation notification
    - When bookSessionInstance returns status=full, the client receives a Greek apology message and admin receives an escalation notification
    - The escalation notification sent to the admin contains the client name (or Telegram ID fallback), the action attempted, and the specific failure reason
    - Admin escalation message is sent via botTokenStore.run(business.botToken, ...) to the correct per-business bot
    - The client apology message is always "Δυστυχώς δεν ήταν δυνατή η κράτησή σας. Ο διαχειριστής ειδοποιήθηκε."
  artifacts:
    - src/telegram/escalation.ts — exports sendEscalationToAdmin and buildEscalationKeyboard
    - src/telegram/handlers/client-menu.ts — handleBookSessionExecute updated with escalation hooks at both enforcement and full-capacity branches
  key_links:
    - sendEscalationToAdmin called from handleBookSessionExecute after enforcement block (!enforcementResult.allowed)
    - sendEscalationToAdmin called from handleBookSessionExecute after full-capacity block (bookResult.status === 'full')
    - botTokenStore.run(business.botToken, ...) wraps all admin sends in sendEscalationToAdmin
    - Escalation keyboard callback_data uses escl: prefix and stays under 64 bytes via minimal IDs
---

<objective>
Create the escalation engine: a dedicated module that composes and delivers the admin
escalation notification, then wire it into the two blocking branches in
handleBookSessionExecute (enforcement gate and full-capacity gate).

Purpose: Blocked clients must never see raw errors or silence. Every block triggers a
Greek apology to the client plus a rich admin notification with context and action
buttons — satisfying ESCL-01 and ESCL-02.

Output: src/telegram/escalation.ts (engine) + updated client-menu.ts with escalation
hooks at both failure branches.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/telegram/handlers/client-menu.ts
@src/billing/enforcement.ts
@src/session/manager.ts
@src/telegram/handlers/admin-menu.ts
@src/database/queries.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/telegram/escalation.ts — escalation engine</name>
  <files>src/telegram/escalation.ts</files>
  <behavior>
    - sendEscalationToAdmin(business, clientTelegramId, action, reason) sends keyboard message to admin via botTokenStore.run
    - The admin message format: "Πελάτης [name or clientTelegramId] προσπάθησε [action] και μπλοκαρίστηκε: [reason]"
    - buildEscalationKeyboard(clientTelegramId, instanceId | undefined) returns InlineKeyboard with two buttons:
        button 1: text="Εγκρίνω εξαίρεση", callback_data="escl:approve:[instanceId]:[clientTelegramId]" (only if instanceId provided)
        button 2: text="Απάντηση πελάτη", callback_data="escl:reply:[clientTelegramId]"
    - When no instanceId is provided (enforcement block with no specific instance yet), only the reply button is emitted
    - assertCallbackDataSize guard on every callback_data string (mirrors admin-menu.ts pattern)
    - clientName is resolved via findClientBusinessRelationship(business.id, clientTelegramId) — use clientName ?? clientTelegramId as display name
    - If botToken is missing on business, log a warning and return without throwing
    - All sends inside botTokenStore.run(business.botToken, async () => { ... })
    - Best-effort: sendEscalationToAdmin itself does not throw; catch + log errors internally
    - Do NOT inline the Greek apology to client here — callers send that before calling sendEscalationToAdmin
  </behavior>
  <action>
    Create src/telegram/escalation.ts as a new module.

    Imports needed: Business from '../../database/queries' (or '../database/queries' — adjust to relative path from src/telegram/), findClientBusinessRelationship from '../database/queries', sendTelegramMessage and sendTelegramMessageWithKeyboard and InlineKeyboard and botTokenStore from './client', logger from '../utils/logger'.

    Export type EscalationReason = 'membership_expired' | 'class_full' | 'slotless_disabled'. Callers pass one of these; sendEscalationToAdmin maps to a human-readable Greek phrase:
      - 'membership_expired' → "η συνδρομή έχει λήξει ή εξαντληθεί"
      - 'class_full' → "το μάθημα είναι πλήρες"
      - 'slotless_disabled' → "οι αιτήσεις χωρίς slot δεν είναι ενεργές"

    Export async function sendEscalationToAdmin(
      business: Business,
      clientTelegramId: string,
      action: string,
      reason: EscalationReason,
      instanceId?: number
    ): Promise<void>

    Inside sendEscalationToAdmin:
    1. Guard: if !business.botToken or !business.ownerTelegramId, log warning and return.
    2. Resolve clientName: call findClientBusinessRelationship(business.id, clientTelegramId) — use result.clientName ?? clientTelegramId as displayName.
    3. Map reason to Greek phrase (per EscalationReason map above).
    4. Build message: `Πελάτης ${displayName} προσπάθησε ${action} και μπλοκαρίστηκε: ${greekReason}.`
    5. Build keyboard via buildEscalationKeyboard(clientTelegramId, instanceId).
    6. Wrap the send in try/catch; on error log and return (best-effort).
    7. Inside botTokenStore.run(business.botToken, async () => { await sendTelegramMessageWithKeyboard(business.ownerTelegramId!, message, keyboard); }).

    Export function buildEscalationKeyboard(clientTelegramId: string, instanceId?: number): InlineKeyboard

    Inside buildEscalationKeyboard:
    - The clientTelegramId is a numeric string (Telegram user ID). Keep callback_data compact:
        approve data: `escl:approve:${instanceId}:${clientTelegramId}` — only emit approve button when instanceId is defined
        reply data:   `escl:reply:${clientTelegramId}`
    - Call assertCallbackDataSize on each data string (copy the guard pattern from admin-menu.ts).
    - Return InlineKeyboard: if instanceId defined → [[approveButton, replyButton]]; else → [[replyButton]].

    The assertCallbackDataSize helper should be a module-local function (same pattern as admin-menu.ts and client-menu.ts — not imported from either).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="escalation" --testTimeout=20000</automated>
  </verify>
  <done>
    escalation.ts exports sendEscalationToAdmin and buildEscalationKeyboard; buildEscalationKeyboard emits approve + reply buttons when instanceId is present, only reply when absent; all callback_data strings pass the 64-byte guard; sendEscalationToAdmin is best-effort (never throws).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire escalation into handleBookSessionExecute (ESCL-01, ESCL-02)</name>
  <files>src/telegram/handlers/client-menu.ts</files>
  <action>
    Edit handleBookSessionExecute in src/telegram/handlers/client-menu.ts to hook
    sendEscalationToAdmin at both blocking branches. Add the import at the top of
    the file:

      import { sendEscalationToAdmin, EscalationReason } from '../escalation';

    Branch 1 — enforcement block (!enforcementResult.allowed):
    Replace the current body:
      await sendTelegramMessage(chatId, enforcementResult.message ?? 'Δεν επιτρέπεται η κράτηση.');
      return;
    With:
      await sendTelegramMessage(chatId, 'Δυστυχώς δεν ήταν δυνατή η κράτησή σας. Ο διαχειριστής ειδοποιήθηκε.');
      await sendEscalationToAdmin(business, senderTelegramId, 'κράτηση μαθήματος', 'membership_expired');
      return;

    Note: at the enforcement block, instanceId is not yet known (the client has confirmed
    but we have not yet resolved the serviceId or called bookSessionInstance), so pass
    no instanceId to sendEscalationToAdmin — this correctly produces a reply-only keyboard.

    Branch 2 — full-capacity block (bookResult.status === 'full'):
    Replace the current body:
      await sendTelegramMessage(chatId, 'Δυστυχώς αυτό το μάθημα έχει γεμίσει. Δοκιμάστε άλλη ώρα.');
      return;
    With:
      await sendTelegramMessage(chatId, 'Δυστυχώς δεν ήταν δυνατή η κράτησή σας. Ο διαχειριστής ειδοποιήθηκε.');
      await sendEscalationToAdmin(business, senderTelegramId, 'κράτηση μαθήματος', 'class_full', instanceId);
      return;

    Here instanceId IS available (it was passed into the function), so the escalation
    keyboard will include the "Εγκρίνω εξαίρεση" approve button.

    The client-facing apology text is identical in both branches per the planning constraint.

    Do not alter any other logic in the function. The success path ("Η κράτησή σας
    επιβεβαιώθηκε!") and all other branches remain unchanged.

    Log the escalation trigger at info level after sendEscalationToAdmin returns:
      logger.info({ businessId: business.id, senderTelegramId, instanceId, reason }, 'escalation triggered');
    (reason is the EscalationReason string, not the Greek phrase.)
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="client-menu|escalation" --testTimeout=20000</automated>
  </verify>
  <done>
    handleBookSessionExecute sends the Greek apology and calls sendEscalationToAdmin on both enforcement-block and full-capacity-block paths; the success path is unchanged; TypeScript compiles without errors (npx tsc --noEmit).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram callback → escalation engine | clientTelegramId arrives from HMAC-verified webhook; never sourced from callback_data |
| Admin escalation send | botTokenStore.run scopes the send to the correct per-business bot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Spoofing | sendEscalationToAdmin clientTelegramId | low | mitigate | clientTelegramId is derived from senderTelegramId (HMAC-verified upstream in handleTelegramWebhookPost); never from callback_data |
| T-20-02 | Information Disclosure | Escalation message text | low | accept | Message reveals client display name to admin — this is the intended UX; admin owns the business |
| T-20-03 | Denial of Service | sendEscalationToAdmin infinite retry | low | mitigate | Function is best-effort (catch + log, no throw); Telegram API errors do not block client response |
</threat_model>

<verification>
- npx tsc --noEmit passes (no TypeScript errors)
- npm test -- --testPathPattern="escalation" --testTimeout=20000 passes
- npm test -- --testPathPattern="client-menu" --testTimeout=20000 passes (existing tests unbroken)
</verification>

<success_criteria>
1. src/telegram/escalation.ts compiles and exports sendEscalationToAdmin + buildEscalationKeyboard + EscalationReason
2. handleBookSessionExecute's enforcement-block branch sends the apology and escalation (no instanceId → reply-only keyboard)
3. handleBookSessionExecute's full-capacity-block branch sends the apology and escalation (instanceId present → approve + reply keyboard)
4. All existing client-menu tests remain green
</success_criteria>

<output>
Create `.planning/phases/20-client-escalation/20-01-SUMMARY.md` when done
</output>
