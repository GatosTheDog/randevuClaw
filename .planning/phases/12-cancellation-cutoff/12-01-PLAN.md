---
phase: 12-cancellation-cutoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/database/queries.ts
  - src/billing/tools.ts
  - src/onboarding/ai-owner-agent.ts
autonomous: true
requirements:
  - CANC-01
  - CANC-02
must_haves:
  truths:
    - Owner sends a chat command to enable/disable or set cutoff hours and the businesses row is updated atomically
    - Cancellation cutoff can be set to enabled with any hours value >= 1
    - Cancellation cutoff can be turned off; subsequent behaviour reverts to always restoring credits
    - Business interface exposes cancellationCutoffEnabled and cancellationCutoffHours to downstream code
  artifacts:
    - src/billing/tools.ts — handleSetCancellationCutoff function
    - src/onboarding/ai-owner-agent.ts — set_cancellation_cutoff entry in OWNER_TOOLS array and executeOwnerTool switch
    - src/database/queries.ts — Business interface includes cancellationCutoffEnabled and cancellationCutoffHours
  key_links:
    - executeOwnerTool case 'set_cancellation_cutoff' calls handleSetCancellationCutoff inside withBusinessContext
    - Business interface fields flow into ToolContext so cancelAppointmentTool (Plan 02) can read them
---

<objective>
Add the set_cancellation_cutoff owner tool so business owners can enable, disable, and configure the cancellation cutoff window via Telegram chat. This plan wires the full chain: Gemini tool declaration → executeOwnerTool dispatch → DB update helper → Business interface fields exposed for downstream cancellation logic.

Purpose: CANC-01 and CANC-02 require the owner to control the cutoff without touching code. This plan delivers the full owner-facing configuration surface.

Output: set_cancellation_cutoff in OWNER_TOOLS, handleSetCancellationCutoff in billing/tools.ts, Business interface updated, ToolContext updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/database/schema.ts
@src/database/queries.ts
@src/billing/tools.ts
@src/onboarding/ai-owner-agent.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Business interface + add setCancellationCutoff DB helper</name>
  <files>src/database/queries.ts</files>
  <action>
    Two changes to src/database/queries.ts:

    1. Extend the Business interface (around line 25) to add two fields after allowMultiBooking:
       - cancellationCutoffEnabled: boolean
       - cancellationCutoffHours: number
       Add JSDoc: "Phase 12 (CANC-01): whether the cancellation cutoff window is active." and "Phase 12 (CANC-01): hours before session at which credit forfeiture kicks in."

    2. Add a new exported async function setCancellationCutoff after findBusinessById:

       export async function setCancellationCutoff(
         businessId: number,
         enabled: boolean,
         hours: number
       ): Promise<void>

       Body: use getConn().update(businesses).set({ cancellationCutoffEnabled: enabled, cancellationCutoffHours: hours }).where(eq(businesses.id, businessId)).
       Import businesses from '../database/schema' (already imported).
       This function is called from handleSetCancellationCutoff inside withBusinessContext, so RLS is already active at call time — getConn() is correct here.

    No other changes to queries.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compiles clean; Business interface has cancellationCutoffEnabled (boolean) and cancellationCutoffHours (number); setCancellationCutoff is exported from src/database/queries.ts</done>
</task>

<task type="auto">
  <name>Task 2: Add handleSetCancellationCutoff in billing/tools.ts</name>
  <files>src/billing/tools.ts</files>
  <action>
    Add to src/billing/tools.ts after the handleSetEnforcementPolicy function:

    Import setCancellationCutoff from '../database/queries' (add to existing import from that module).

    Define a Zod schema at module level:
      const SetCancellationCutoffSchema = z.object({
        enabled: z.boolean(),
        hours: z.number().int().min(1).max(168),
      });

    Add the exported handler:
      export async function handleSetCancellationCutoff(
        businessId: number,
        args: Record<string, unknown>
      ): Promise<string>

    Implementation:
    - Use SetCancellationCutoffSchema.safeParse(args). On failure, return the Greek message: 'Μη έγκυρα δεδομένα. Απαιτείται enabled (true/false) και hours (1-168).'
    - On success: call await setCancellationCutoff(businessId, parsed.data.enabled, parsed.data.hours).
    - Log with logger.info({ businessId, enabled: parsed.data.enabled, hours: parsed.data.hours }, 'Cancellation cutoff set via NLU').
    - Return Greek confirmation: if enabled is true, return 'Το παράθυρο ακύρωσης ορίστηκε σε ' + parsed.data.hours + ' ώρες. Ακυρώσεις εντός αυτού του διαστήματος θα χάνουν session.' else return 'Το παράθυρο ακύρωσης απενεργοποιήθηκε. Όλες οι ακυρώσεις επιστρέφουν session κανονικά.'
    - Wrap in try/catch; on error: log logger.error and return 'Σφάλμα κατά την ενημέρωση ρύθμισης. Δοκιμάστε ξανά.'
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compiles clean; handleSetCancellationCutoff is exported from src/billing/tools.ts</done>
</task>

<task type="auto">
  <name>Task 3: Register set_cancellation_cutoff in OWNER_TOOLS + executeOwnerTool</name>
  <files>src/onboarding/ai-owner-agent.ts</files>
  <action>
    Two changes to src/onboarding/ai-owner-agent.ts:

    1. Import handleSetCancellationCutoff from '../billing/tools' (add to existing import from that module).

    2. Add new tool declaration to the OWNER_TOOLS array after the set_enforcement_policy entry (before the Phase 10 session tools section comment):

       {
         type: 'function' as const,
         name: 'set_cancellation_cutoff',
         description: 'Ορίζει το παράθυρο ακύρωσης: αν ένας πελάτης ακυρώσει εντός X ωρών πριν τη σεζόν, χάνει το session. Χρησιμοποίησε enabled=true για ενεργοποίηση, enabled=false για απενεργοποίηση.',
         parameters: {
           type: 'object',
           properties: {
             enabled: {
               type: 'boolean',
               description: 'true = ενεργοποίηση παραθύρου ακύρωσης, false = απενεργοποίηση',
             },
             hours: {
               type: 'integer',
               description: 'Ώρες πριν τη σεζόν κάτω από τις οποίες ακυρώσεις χάνουν session (π.χ. 8). Απαιτείται όταν enabled=true.',
             },
           },
           required: ['enabled', 'hours'],
         },
       }

    3. Add to the ToolArgs interface (after the Phase 10 session catalog fields block):
       // Phase 12: cancellation cutoff fields (CANC-01, CANC-02)
       enabled?: boolean;
       hours?: number;

    4. Add a new case to the executeOwnerTool switch after the set_enforcement_policy case:

       case 'set_cancellation_cutoff': {
         return withBusinessContext(business.id, () =>
           handleSetCancellationCutoff(business.id, args as Record<string, unknown>)
         );
       }

    No other changes to ai-owner-agent.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compiles clean; OWNER_TOOLS has set_cancellation_cutoff entry; executeOwnerTool dispatches it via withBusinessContext</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner chat → executeOwnerTool | Gemini-parsed args are Zod-validated before any DB write |
| executeOwnerTool → DB | withBusinessContext enforces RLS; setCancellationCutoff only updates the business row belonging to the authenticated tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-12-01-01 | Tampering | set_cancellation_cutoff args | medium | mitigate | SetCancellationCutoffSchema Zod enum enforces hours 1-168 and boolean enabled before DB write |
| T-12-01-02 | Tampering | setCancellationCutoff UPDATE | medium | mitigate | withBusinessContext sets RLS app.current_business_id; WHERE eq(businesses.id, businessId) is a second ownership guard |
| T-12-01-03 | Elevation | cross-tenant cutoff change | high | mitigate | withBusinessContext + businessId guard in WHERE clause — foreign businessId results in zero rows updated |
</threat_model>

<verification>
TypeScript compiles clean across all three modified files. set_cancellation_cutoff appears in the OWNER_TOOLS array. handleSetCancellationCutoff is exported from billing/tools.ts. Business interface has both cutoff fields.
</verification>

<success_criteria>
- Owner messages "ενεργοποίησε το παράθυρο ακύρωσης 24 ώρες" → Gemini calls set_cancellation_cutoff({enabled:true, hours:24}) → DB row updated → Greek confirmation sent
- Owner messages "απενεργοποίησε το παράθυρο ακύρωσης" → DB updated → confirmation sent
- Business interface fields cancellationCutoffEnabled and cancellationCutoffHours are available to Plan 02's cutoff check
- npx tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/12-cancellation-cutoff/12-01-SUMMARY.md` when done
</output>
