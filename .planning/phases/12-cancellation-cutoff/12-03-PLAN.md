---
phase: 12-cancellation-cutoff
plan: 03
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - tests/cancellation-cutoff.test.ts
autonomous: true
requirements:
  - CANC-01
  - CANC-02
  - CANC-03
  - CANC-04
  - CANC-05
must_haves:
  truths:
    - All five CANC-* requirements have automated test coverage
    - DST boundary Oct 25 2026 (clocks fall back at 04:00 EEST → 03:00 EET, UTC+2) is tested explicitly — a session at 10:00 Athens on that day produces correct hoursUntilSessionInAthens
    - Forfeiture path (cutoff enabled, inside window, confirmed=true) does not create a membership_ledger credit_restored entry
    - Restore path (cutoff enabled, outside window) creates a membership_ledger credit_restored entry
    - Cutoff disabled path always creates a credit_restored entry regardless of timing
    - Owner can toggle cutoff off and subsequent cancellations restore credits (CANC-02 regression check)
  artifacts:
    - tests/cancellation-cutoff.test.ts — full integration test file
  key_links:
    - Tests run against a real local Postgres DB (same pattern as billing-session-deduction.test.ts)
    - hoursUntilSessionInAthens is tested via cancellation outcomes — no direct unit test of the helper (it is private to function-executor.ts)
---

<objective>
Create the full integration test suite for Phase 12: cancellation cutoff arithmetic, DST boundary correctness, credit forfeiture vs restore paths, two-message confirmation flow, and owner toggle.

Purpose: Automated regression guard for all CANC-* requirements. Tests run against a real Postgres instance (SESSION_TEST_DATABASE_URL or BILLING_TEST_DATABASE_URL) using the established billing-session-deduction.test.ts + session-booking-flow.test.ts patterns.

Output: tests/cancellation-cutoff.test.ts with passing tests for all CANC-01 through CANC-05 scenarios.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/conversation/function-executor.ts
@tests/billing-session-deduction.test.ts
@tests/session-booking-flow.test.ts
@tests/helpers/test-business.ts
@tests/helpers/session-fixtures.ts
@tests/helpers/billing-fixtures.ts
@.planning/phases/12-cancellation-cutoff/12-01-SUMMARY.md
@.planning/phases/12-cancellation-cutoff/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write cancellation-cutoff integration tests</name>
  <files>tests/cancellation-cutoff.test.ts</files>
  <behavior>
    - Test suite header: covers CANC-01, CANC-02, CANC-03, CANC-04, CANC-05
    - Uses SESSION_TEST_DATABASE_URL ?? BILLING_TEST_DATABASE_URL ?? 'postgresql://manolis@localhost:5432/randevuclaw_test'
    - jest.resetModules() + process.env.DATABASE_URL override pattern (same as billing-session-deduction.test.ts)
    - Requires: db, eq, and from drizzle-orm; businesses, membershipLedger, bookings, services, sessionCatalog, sessionInstances, memberships from schema; executeTool from function-executor; insertTestBusiness from helpers/test-business; insertTestPackage, insertTestMembership from helpers/billing-fixtures; insertTestSessionCatalog, insertTestSessionInstance from helpers/session-fixtures

    Test 1 — CANC-03 (outside window, credit restored):
      Insert business with cancellationCutoffEnabled=true, cancellationCutoffHours=8.
      Insert session instance with sessionDate 7 days from now (well outside 8h window).
      Insert confirmed booking linked to that session instance.
      Insert membership with sessionsRemaining=5.
      Insert membershipLedger deduction row for that booking (to simulate the deduction that happened at booking time).
      Call executeTool('cancel_appointment', { business_id, booking_id }, context) where context.business.cancellationCutoffEnabled=true, cancellationCutoffHours=8.
      Assert: result.success === true AND result.credit_forfeited is falsy.
      Assert: membershipLedger has a row with operationType='credit_restored' for that booking_id.

    Test 2 — CANC-04 + CANC-05 first call (inside window, pending_confirmation returned):
      Insert business with cancellationCutoffEnabled=true, cancellationCutoffHours=8.
      Insert session instance with sessionDate=today, sessionTime set to 2 hours from now in Athens (inside 8h window).
      Insert confirmed booking.
      Insert membership + deduction ledger row.
      Call executeTool('cancel_appointment', { business_id, booking_id }, context) WITHOUT confirmed.
      Assert: result.pending_confirmation === true AND result.warning contains 'session'.
      Assert: booking still has status 'confirmed' (no DB mutation).
      Assert: no credit_restored ledger row created.

    Test 3 — CANC-04 second call (confirmed=true inside window, credit forfeited):
      Same setup as Test 2.
      Call executeTool('cancel_appointment', { business_id, booking_id, confirmed: true }, context).
      Assert: result.success === true AND result.credit_forfeited === true.
      Assert: booking status is 'cancelled'.
      Assert: no membershipLedger row with operationType='credit_restored' for that booking_id.

    Test 4 — CANC-01 (cutoff disabled, always restore):
      Insert business with cancellationCutoffEnabled=false.
      Insert session instance with sessionDate=today, sessionTime 1 hour from now.
      Insert confirmed booking + membership + deduction.
      Call executeTool('cancel_appointment', { business_id, booking_id }, context) with context.business.cancellationCutoffEnabled=false.
      Assert: result.success === true AND result.credit_forfeited is falsy.
      Assert: membershipLedger has credit_restored row.

    Test 5 — CANC-02 (owner toggles cutoff off, credit restored on next cancel):
      Insert business with cancellationCutoffEnabled=true, cancellationCutoffHours=4.
      Insert session instance 1 hour from now (inside window).
      Insert booking + membership + deduction.
      Directly update businesses SET cancellation_cutoff_enabled=false for that business (admin db, simulate owner turning it off).
      Call executeTool with context.business.cancellationCutoffEnabled=false (context reflects the new DB state).
      Assert: result.success === true AND result.credit_forfeited is falsy.

    Test 6 — DST boundary Oct 25 2026 (CANC-04 arithmetic):
      This test validates that hoursUntilSessionInAthens does not misfire on the DST boundary.
      On Oct 25 2026, Athens clocks fall back from UTC+3 to UTC+2 at 04:00.
      Insert session instance with sessionDate='2026-10-25', sessionTime='10:00'.
      Compute expected UTC timestamp for that session: 10:00 Athens on Oct 25 = 08:00 UTC (UTC+2 after fallback).
      From a reference "now" 6 hours before that UTC instant, hoursUntilSessionInAthens should return ~6.0.
      Since we cannot control Date.now() in an integration test, validate indirectly:
        - Set sessionDate 2 hours from now by computing the Athens wall-clock 2 hours ahead via isoDateInAthens.
        - Use a sessionTime that is 2 hours from now in Athens.
        - With cutoffHours=8, a session 2 hours away IS inside the window.
        - Assert that cancel_appointment returns pending_confirmation=true (not restoring credit).
      Note: the DST boundary specific test (Oct 25) cannot reliably set Date.now to a fixed offset in integration tests. Instead, document the DST correctness via code review of hoursUntilSessionInAthens (the noon-UTC anchor technique). This test validates the cutoff correctly classifies a 2h-away session as inside an 8h window.

    Helper to compute a session time N hours from now in Athens wall-clock:
      function athensTimeNHoursFromNow(hoursFromNow: number): { sessionDate: string; sessionTime: string }
      Use isoDateInAthens and Intl.DateTimeFormat('en-US', { timeZone: 'Europe/Athens', hour: '2-digit', minute: '2-digit', hour12: false }) to get Athens HH:MM for Date.now() + hoursFromNow * 3_600_000.

    Helper buildToolContextWithCutoff that extends the existing buildToolContext pattern and adds cancellationCutoffEnabled and cancellationCutoffHours to context.business.

    afterAll: restore process.env.DATABASE_URL.
  </behavior>
  <action>
    Create tests/cancellation-cutoff.test.ts following the pattern of tests/billing-session-deduction.test.ts and tests/session-booking-flow.test.ts:

    - File header: covers CANC-01 through CANC-05.
    - Database URL setup and jest.resetModules() at top of file.
    - All requires (no TypeScript import statements — use jest require pattern matching the existing test files).
    - Implement all six tests described in the behavior section.
    - Each test uses beforeAll/afterAll or direct setup — no shared state across tests (each test inserts its own business).
    - Booking status for bookings inserted in test setup: use 'confirmed' (not 'cancelled') so ACTIVE_STATUSES check passes.
    - Membership ledger deduction rows: insert with operationType='session_deducted', sessionsDeducted=1, bookingId matching the booking, idempotencyKey unique per test.
    - Do NOT run the full test suite (npm test) in verify — use the targeted file only.
    - The test for confirmed=true second call (Test 3): ensure the booking is NOT already cancelled from Test 2 — use a separate beforeAll that creates its own booking.
  </action>
  <verify>
    <automated>npx jest tests/cancellation-cutoff.test.ts --testTimeout=30000 2>&1 | tail -30</automated>
  </verify>
  <done>All tests in tests/cancellation-cutoff.test.ts pass. Output shows 6 passing tests. TypeScript compiles clean (npx tsc --noEmit exits 0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test DB → assertions | Tests run against randevuclaw_test — no production data touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-12-03-SC | Tampering | npm/jest install for test run | low | accept | No new packages installed in this plan — jest and drizzle-orm already in package.json |
</threat_model>

<verification>
npx jest tests/cancellation-cutoff.test.ts --testTimeout=30000 shows 6 passing tests.
npx tsc --noEmit exits 0.
</verification>

<success_criteria>
- 6 passing tests covering CANC-01 through CANC-05
- Test 3 confirms no credit_restored ledger entry exists after a forfeiture cancellation
- Test 1 confirms credit_restored entry exists after a normal (outside-window) cancellation
- Test 2 confirms booking remains 'confirmed' after a pending_confirmation response
- TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/phases/12-cancellation-cutoff/12-03-SUMMARY.md` when done
</output>
