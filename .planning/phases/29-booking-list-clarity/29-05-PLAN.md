---
phase: 29-booking-list-clarity
plan: 05
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/telegram/handlers/client-menu.ts
  - tests/webhooks/client-menu.test.ts
autonomous: true
requirements: [UX-01, UX-04, UX-05]

must_haves:
  truths:
    - "A client browsing bookable classes via /menu never sees a same-day class whose start time has already passed."
    - "A client at a business using open-slot (non-fixed-class) booking mode sees a root-menu button whose label reflects that reality upfront, and gets a working back button if they still tap it."
    - "The book-a-class list shows the service name next to each date/time."
  artifacts:
    - showClientRootMenu's booking button label is conditional on business.bookingMode
    - showBookSessionList calls listSessions(business.id, 14, true) and renders service names via a batched Map<number,string> lookup
    - showBookSessionList's open-slot redirect branch sends a back-button keyboard, matching its sibling no-availability branch
    - handleBookSessionExecute resolves serviceId/sessionDate/sessionTime via findSessionInstanceById instead of its own inline Drizzle join
  key_links:
    - "showBookSessionList's excludePastToday=true call and its D-10 service-name enrichment both live in the same function edit — a session that survives the past-time filter still needs its service name resolved before rendering."
    - "handleBookSessionExecute's refactor removes this file's last remaining direct sessionInstances/sessionCatalog Drizzle usage — the db/schema/eq imports become dead and must be removed, or tsc --noEmit will not catch it (unused imports do not fail compilation here, but leaving them is a code-quality regression the acceptance criteria explicitly checks for)."
---

<objective>
Fix the client-side half of UX-01 (same-day past-time exclusion in the `/menu` booking flow, D-01), UX-05 in full (relabel the root-menu booking button for open-slot businesses and add a back-button to its redirect message, D-03 + D-04), and the first of client-menu.ts's three UX-04 list fixes (showBookSessionList shows service names, D-10). Also completes the D-06 consolidation for this file's `handleBookSessionExecute` (replacing its inline, businessId-unscoped Drizzle join with the shared `findSessionInstanceById` helper).

Purpose: This is the client's primary `/menu` → "Κράτηση μαθήματος" flow. Today it can show an already-started class as bookable, always says "μαθήματος" even for businesses that don't run fixed classes at all, dead-ends with no back button when redirecting an open-slot client to chat, and shows a bare date/time with no indication of which service/class it is.
Output: The book-a-class flow is time-accurate, correctly labeled per business type, always recoverable, and contextual.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-booking-list-clarity/29-CONTEXT.md
@.planning/phases/29-booking-list-clarity/29-RESEARCH.md
@.planning/phases/29-booking-list-clarity/29-PATTERNS.md
@.planning/phases/29-booking-list-clarity/29-01-SUMMARY.md

@src/telegram/handlers/client-menu.ts
@tests/webhooks/client-menu.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relabel the root-menu booking button for non-fixed_sessions businesses (D-03)</name>
  <files>src/telegram/handlers/client-menu.ts, tests/webhooks/client-menu.test.ts</files>
  <read_first>
    src/telegram/handlers/client-menu.ts (lines 61-95 — showClientRootMenu's full current body, including the `void business;` placeholder comment at the end)
    tests/webhooks/client-menu.test.ts (lines 1-115 — mock/fixture conventions; BASE_BUSINESS's default bookingMode is 'fixed_sessions')
  </read_first>
  <action>
    In `showClientRootMenu`, add `const bookingButtonText = business.bookingMode === 'fixed_sessions' ? 'Κράτηση μαθήματος' : 'Κράτηση ραντεβού';` before the `keyboard` declaration (`'ραντεβού'` — appointment — is this codebase's already-established Greek term for open-slot bookings, used throughout cancelAppointmentTool/bookAppointmentTool's existing messages; this stays consistent with existing vocabulary rather than inventing new wording). Change the keyboard's first button from the literal `{ text: 'Κράτηση μαθήματος', callback_data: callbackDataBook }` to `{ text: bookingButtonText, callback_data: callbackDataBook }`. Remove the trailing `// Suppress unused variable warning — business will be used in later plans` comment and its `void business;` line — `business` is now genuinely used.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="client-menu" --testTimeout=20000</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts showClientRootMenu renders 'Κράτηση μαθήματος' when business.bookingMode === 'fixed_sessions' (existing default fixture behavior, unchanged).
    - A new test asserts showClientRootMenu renders 'Κράτηση ραντεβού' when business.bookingMode is 'open_slots'.
    - No `void business;` or its suppress-comment remains in this function.
  </acceptance_criteria>
  <done>The root-menu booking button label reflects business.bookingMode; both branches are tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: showBookSessionList — exclude past-time sessions, add back-button to the redirect, show service names (D-01, D-04, D-10)</name>
  <files>src/telegram/handlers/client-menu.ts, tests/webhooks/client-menu.test.ts</files>
  <read_first>
    src/telegram/handlers/client-menu.ts (lines 1-40 imports; lines 125-161 showBookSessionList's full current body)
    src/utils/greek-messages.ts (BACK_MENU_LABELS, added in Plan 29-01)
    src/scheduler/agenda.ts (formatAgendaMessage's Map&lt;number,string&gt; batching pattern, lines 86-92, to copy exactly)
  </read_first>
  <behavior>
    - showBookSessionList calls listSessions(business.id, 14, true) — never the bare 2-argument form.
    - When business.bookingMode !== 'fixed_sessions', the redirect message is sent WITH a keyboard containing `{ text: '« Πίσω', callback_data: 'cmenu:root' }` (currently sent with none).
    - The redirect message text reads 'Για κράτηση ραντεβού, γράψε μου στο chat τι θέλεις να κλείσεις.' (updated from 'μαθήματος' to 'ραντεβού' to stay consistent with Task 1's new button label within this same flow — not a broader terminology unification across other files, which stays out of scope).
    - Each row in the resulting keyboard reads `${serviceName} - ${sessionDate} ${sessionTime}`, with at most one findServiceById call per distinct serviceId among the (already past-time-filtered, capacity-filtered, 10-capped) available sessions.
    - A serviceId whose findServiceById lookup returns null falls back to '(άγνωστη υπηρεσία)'.
  </behavior>
  <action>
    Add `findServiceById` to the existing `import { Business, listClientBookings, findBookingByIdUnscoped, updateBookingStatus, updateBookingOwnerMessageId } from '../../database/queries';` line. Add a new import `import { BACK_MENU_LABELS } from '../../utils/greek-messages';`.

    Change the `business.bookingMode !== 'fixed_sessions'` guard branch from a bare `sendTelegramMessage` to: build `const keyboard: InlineKeyboard = [[{ text: BACK_MENU_LABELS.CLIENT, callback_data: 'cmenu:root' }]];` and call `sendTelegramMessageWithKeyboard(chatId, 'Για κράτηση ραντεβού, γράψε μου στο chat τι θέλεις να κλείσεις.', keyboard);` before the `return;` (matches the sibling `available.length === 0` branch's existing pattern 6 lines below it, per D-04).

    Change `const sessions = await listSessions(business.id, 14);` to `const sessions = await listSessions(business.id, 14, true);` (D-01).

    In the `available.length === 0` branch, replace the inline `[{ text: '« Πίσω', callback_data: 'cmenu:root' }]` literal with `[{ text: BACK_MENU_LABELS.CLIENT, callback_data: 'cmenu:root' }]` (D-07 consolidation, since this line is already being touched this task).

    Before building `rows`, add the batching block: `const serviceIds = [...new Set(available.map((s) => s.serviceId))]; const serviceNamesById = new Map<number, string>(); for (const serviceId of serviceIds) { const service = await findServiceById(business.id, serviceId); serviceNamesById.set(serviceId, service?.name ?? '(άγνωστη υπηρεσία)'); }`. Change the `rows` map's button text from `` `${s.sessionDate} ${s.sessionTime}` `` to `` `${serviceNamesById.get(s.serviceId)} - ${s.sessionDate} ${s.sessionTime}` ``. Replace the trailing `[{ text: '« Πίσω', callback_data: 'cmenu:root' }]` literal with `[{ text: BACK_MENU_LABELS.CLIENT, callback_data: 'cmenu:root' }]`.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="client-menu" --testTimeout=20000</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts listSessions was called with (business.id, 14, true).
    - A test asserts the open-slot redirect path now includes a keyboard call (currently asserted via `expect(mockedSendTelegramMessage).toHaveBeenCalledWith(senderTelegramId, expect.stringContaining('γράψε μου'))` in the existing "book — business.bookingMode === open_slots" test — this must be updated to assert `sendTelegramMessageWithKeyboard` was called instead, with the back-button keyboard).
    - A test asserts a rendered button/row includes the resolved service name alongside date/time.
    - A test proves batching: 2 available sessions sharing one serviceId result in exactly 1 findServiceById call.
  </acceptance_criteria>
  <done>showBookSessionList excludes past-time sessions, always offers a way back, and shows service names via a batched lookup; all new and existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: handleBookSessionExecute uses findSessionInstanceById instead of its own inline join (D-06)</name>
  <files>src/telegram/handlers/client-menu.ts, tests/webhooks/client-menu.test.ts</files>
  <read_first>
    src/telegram/handlers/client-menu.ts (lines 1-40 imports; lines 187-290 handleBookSessionExecute's full current body, specifically the `instanceRow` inline join at lines 207-217 and its 2 downstream usages of `instanceRow[0]?.sessionDate`/`instanceRow[0]?.sessionTime`)
    src/session/manager.ts (findSessionInstanceById, added in Plan 29-01)
  </read_first>
  <behavior>
    - handleBookSessionExecute resolves serviceId, sessionDate, and sessionTime via a single findSessionInstanceById(business.id, instanceId) call.
    - When findSessionInstanceById returns null (instance not found, wrong business, or already cancelled), the client is sent 'Το μάθημα δεν βρέθηκε.' and bookSessionInstance is never called.
    - The owner-alert text still correctly includes the real sessionDate/sessionTime.
  </behavior>
  <action>
    Add `findSessionInstanceById` to the existing `import { listSessions, bookSessionInstance } from '../../session/manager';` line. Remove the now-unused `import { db } from '../../database/db';`, `import { sessionInstances, sessionCatalog } from '../../database/schema';`, and `import { eq } from 'drizzle-orm';` lines — grep this file first to confirm these 3 imports have no other usage before removing them (they are used ONLY inside the block this task replaces).

    Replace the block:
    ```
    const instanceRow = await db
      .select({
        serviceId: sessionCatalog.serviceId,
        sessionDate: sessionInstances.sessionDate,
        sessionTime: sessionInstances.sessionTime,
      })
      .from(sessionInstances)
      .innerJoin(sessionCatalog, eq(sessionInstances.catalogId, sessionCatalog.id))
      .where(eq(sessionInstances.id, instanceId))
      .limit(1);
    const serviceId = instanceRow[0]?.serviceId;

    if (serviceId === undefined) {
      await sendTelegramMessage(chatId, 'Το μάθημα δεν βρέθηκε.');
      return;
    }
    ```
    with:
    ```
    const session = await findSessionInstanceById(business.id, instanceId);
    if (!session) {
      await sendTelegramMessage(chatId, 'Το μάθημα δεν βρέθηκε.');
      return;
    }
    const serviceId = session.serviceId;
    ```
    Then update the owner-alert text further down from `(instanceRow[0]?.sessionDate ?? '?')` / `(instanceRow[0]?.sessionTime ?? '?')` to `session.sessionDate` / `session.sessionTime` (the `?? '?'` fallback is no longer needed — `session` is guaranteed non-null past the guard above, and `sessionDate`/`sessionTime` are always-present string fields on the SessionInstance type).

    Also replace the trailing `[{ text: '« Αρχικό μενού', callback_data: 'cmenu:root' }]` literal (the backKeyboard near the end of this function) with `[{ text: BACK_MENU_LABELS.CLIENT, callback_data: 'cmenu:root' }]` — this is one of the two `'« Αρχικό μενού'` occurrences D-07 reconciles into the single `'« Πίσω'` client label; `BACK_MENU_LABELS` is already imported from Task 2.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="client-menu" --testTimeout=20000</automated>
  </verify>
  <acceptance_criteria>
    - No `db.select({ serviceId: sessionCatalog.serviceId, ... })` inline join remains in this file.
    - The `db`, `sessionInstances`, `sessionCatalog`, `eq` imports are removed (confirmed unused elsewhere in this file via grep).
    - A test mocks findSessionInstanceById to return null and asserts 'Το μάθημα δεν βρέθηκε.' is sent and bookSessionInstance is never called.
    - The existing "book:yes — success → owner IS notified with an Έγκριση/Απόρριψη keyboard" test (which currently mocks the raw db.select chain) is updated to mock findSessionInstanceById instead, and still passes with the same assertions on the owner alert's date/time content.
    - Zero remaining occurrences of the literal '« Αρχικό μενού' anywhere in src/telegram/handlers/client-menu.ts.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>handleBookSessionExecute uses the shared, businessId-scoped findSessionInstanceById; no dead Drizzle imports remain; the client-side back-button label is now fully consolidated onto BACK_MENU_LABELS.CLIENT in every location this plan touches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cmenu:book:yes:<instanceId> callback_data → handleBookSessionExecute → findSessionInstanceById(business.id, instanceId) | instanceId is attacker-controllable input; business is the webhook-scoped, HMAC-verified business — findSessionInstanceById's businessId scoping (from Plan 29-01) means a crafted instanceId belonging to another business always resolves to null, never leaking that business's session/service data into this client's booking flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Information Disclosure | handleBookSessionExecute → findSessionInstanceById | high | mitigate (inherited) | Established in Plan 29-01, Task 2 (businessId-scoped WHERE clause). This plan's Task 3 additionally closes an incidental pre-existing gap: the OLD inline join here had no businessId filter at all — only the downstream bookSessionInstance() mutation call was actually protected (its own subquery guard, T-10-02, pre-existing and unchanged). This refactor scopes the read-side lookup too, not just the write-side mutation. |
| T-29-02 | Denial of Service (functional regression) | showBookSessionList | low | accept | excludePastToday=true is the intended, correct default for this client-facing function; the risk of "client can no longer book something they should be able to" is covered by Plan 29-01's boundary tests on the underlying listSessions filter, reused here unchanged. |

</threat_model>

<verification>
Run `npm test -- --testPathPattern="client-menu" --testTimeout=20000`. All existing tests plus all newly added/updated tests must pass. Run `npx tsc --noEmit` to confirm the whole project compiles after removing the dead db/schema/eq imports.
</verification>

<success_criteria>
- The root-menu button label reflects business.bookingMode.
- showBookSessionList excludes past-time sessions, offers a back button on the open-slot redirect, and shows service names.
- handleBookSessionExecute uses the shared findSessionInstanceById helper; no dead imports remain.
- tests/webhooks/client-menu.test.ts is fully green.
</success_criteria>

<output>
Create `.planning/phases/29-booking-list-clarity/29-05-SUMMARY.md` when done
</output>
