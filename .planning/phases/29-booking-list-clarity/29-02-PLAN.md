---
phase: 29-booking-list-clarity
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/conversation/function-executor.ts
  - tests/session-booking-flow.test.ts
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "A client asking the bot (via free-text Greek chat) to list, book, or reschedule sessions never sees or successfully targets a same-day session whose start time has already passed."
    - "cancelAppointmentTool's cutoff-hours check still computes hours-until-session correctly after switching to the shared helper — no behavior change to CANC-03/04/05."
  artifacts:
    - src/conversation/function-executor.ts's listSessionsForClientTool, bookSessionTool (both branches), and rescheduleSessionTool call listSessions(context.business.id, 90, true)
    - src/conversation/function-executor.ts's local hoursUntilSessionInAthens function is deleted; cancelAppointmentTool uses the shared src/utils/timezone.ts hoursUntilSession instead
  key_links:
    - "All 4 listSessions call sites in this file pass true as the 3rd argument — missing even one leaves that one AI tool showing past-time slots as bookable (Pitfall 3)."
---

<objective>
Wire the AI conversational tool layer into Wave 1's new `listSessions(businessId, limitDays, excludePastToday)` filter (D-01) and consolidated `hoursUntilSession` helper (D-02), completing UX-01 for every free-chat booking path (as opposed to the `/menu`-driven client-menu.ts path, handled by Plan 29-05).

Purpose: `function-executor.ts` is where Gemini's `list_sessions_for_client`, `book_session`, and `reschedule_session` tool calls are actually executed — if this file's 4 `listSessions()` calls don't pass `true`, a client chatting in free Greek text can still book (or be shown) a session that already started, even though the `/menu` UI-driven path (Plan 29-05) is fixed.
Output: All 4 client-facing `listSessions()` calls in this file pass `excludePastToday=true`; the file's local duplicate of the hours-until-session algorithm is removed in favor of the Wave 1 shared export.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-booking-list-clarity/29-CONTEXT.md
@.planning/phases/29-booking-list-clarity/29-RESEARCH.md
@.planning/phases/29-booking-list-clarity/29-01-SUMMARY.md

@src/conversation/function-executor.ts
@tests/session-booking-flow.test.ts
@tests/helpers/session-fixtures.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the 4 client-facing listSessions call sites to excludePastToday=true (D-01)</name>
  <files>src/conversation/function-executor.ts, tests/session-booking-flow.test.ts</files>
  <read_first>
    src/conversation/function-executor.ts (lines 529-780 — listSessionsForClientTool at ~535, bookSessionTool's multi-booking branch at ~564-649 and single-booking branch at ~651-718, rescheduleSessionTool at ~723-800; note the 4 exact `await listSessions(context.business.id)` call sites at lines 541, 591, 669, 745)
    tests/session-booking-flow.test.ts (lines 1-100 for the real-DB test setup convention, and lines 375-553 for the existing SBOK-03/SBOK-04 executeTool(...) test pattern to follow for new tests; note athensTimeNHoursFromNow-style helpers do not yet exist in this file — you will add a small local helper)
  </read_first>
  <behavior>
    - executeTool('list_sessions_for_client', ...) never includes a same-day session whose start time (Athens) is in the past.
    - executeTool('book_session', { session_instance_id }) [single-booking path] returns error 'session_not_found' when the target instance is a same-day session whose start time has passed.
    - executeTool('book_session', { session_instance_ids: [...] }) [multi-booking path] treats a same-day past-time instance as a conflict (not found in the excludePastToday-filtered list), same as any other unavailable instance.
    - executeTool('reschedule_session', { new_session_instance_id }) returns error 'session_not_found' when the target instance is a same-day session whose start time has passed.
  </behavior>
  <action>
    Change all 4 occurrences of `await listSessions(context.business.id)` in this file (listSessionsForClientTool at line 541, bookSessionTool's multi-booking branch at line 591, bookSessionTool's single-booking branch at line 669, rescheduleSessionTool at line 745) to `await listSessions(context.business.id, 90, true)` — explicit `90` preserves the existing default `limitDays` behavior exactly (these call sites never previously passed a 2nd argument either), and `true` is the new 3rd argument enabling the same-day past-time exclusion (D-01). Do not change any other logic in these 4 functions.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="session-booking-flow" --testTimeout=30000</automated>
  </verify>
  <acceptance_criteria>
    - All 4 listSessions call sites in this file now read `listSessions(context.business.id, 90, true)`.
    - A new test in tests/session-booking-flow.test.ts inserts a same-day session instance with a start time a few minutes in the past (Athens wall-clock, computed relative to the real clock — do not use jest fake timers in this real-DB test file, mirroring the existing `athensTimeNHoursFromNow`-style helper convention from tests/cancellation-cutoff.test.ts) and proves list_sessions_for_client excludes it, book_session (single) returns session_not_found for it, and reschedule_session returns session_not_found when targeting it.
    - Existing SBOK-01 through SBOK-04 tests in this file continue passing unmodified (regression-free).
  </acceptance_criteria>
  <done>All 4 client-facing tool call sites in function-executor.ts exclude same-day past-time sessions; new and existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the local hoursUntilSessionInAthens duplicate, use the shared timezone.ts export (D-02)</name>
  <files>src/conversation/function-executor.ts, tests/cancellation-cutoff.test.ts</files>
  <read_first>
    src/conversation/function-executor.ts (lines 1-22 imports, 107-123 the local hoursUntilSessionInAthens function, line 348 its one call site inside cancelAppointmentTool's cutoff check)
    src/utils/timezone.ts (the hoursUntilSession export added in Plan 29-01, Task 1)
    tests/cancellation-cutoff.test.ts (full file — this is the existing real-DB suite covering CANC-03/04/05, which exercises cancelAppointmentTool's cutoff branch; confirm it still passes unmodified since this task changes an internal implementation detail, not behavior)
  </read_first>
  <action>
    Add `hoursUntilSession` to this file's existing `import { formatExpiryDateGreek, isoDateInAthens } from '../utils/timezone';` line. Delete the local `function hoursUntilSessionInAthens(sessionDate: string, sessionTime: string): number { ... }` block (lines 107-123) entirely. Update its one call site inside `cancelAppointmentTool` (line 348, `const hoursLeft = hoursUntilSessionInAthens(booking.calendarDate, booking.calendarTime);`) to call `hoursUntilSession(booking.calendarDate, booking.calendarTime)` instead. This is a pure rename/relocation with zero behavior change — the algorithm is byte-for-byte identical to what Plan 29-01 already ported into timezone.ts.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="cancellation-cutoff" --testTimeout=30000</automated>
  </verify>
  <acceptance_criteria>
    - No occurrence of `hoursUntilSessionInAthens` remains anywhere in src/conversation/function-executor.ts.
    - `npx tsc --noEmit` passes (proves the import + call-site rename is correct).
    - The full existing tests/cancellation-cutoff.test.ts suite (CANC-01 through CANC-05, all 6+ tests) passes unmodified — this proves the cutoff-hours behavior is unchanged.
  </acceptance_criteria>
  <done>function-executor.ts has no local hours-until-session duplicate; it imports and uses the shared timezone.ts export; cancellation-cutoff tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini tool-call arguments → session_instance_id / new_session_instance_id | These IDs are supplied by Gemini's function-calling output, itself derived from free-text client chat; no new trust boundary is introduced by this plan since bookSessionInstance's own businessId-scoped subquery guard (T-10-02, pre-existing) is unchanged and is the real security boundary — this plan only changes which sessions are considered "listed/findable" before that guard runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-02 | Denial of Service (functional regression) | listSessionsForClientTool / bookSessionTool / rescheduleSessionTool | low | accept | These are the intended consumers of excludePastToday=true (client-facing by design); no owner-facing tool in this file is touched. Regression risk of "client can no longer book a session they should be able to" is covered by the boundary tests added in Task 1. |

</threat_model>

<verification>
Run `npm test -- --testPathPattern="session-booking-flow|cancellation-cutoff" --testTimeout=30000`. All tests in both files (existing + newly added) must pass. Run `npx tsc --noEmit` to confirm the whole project compiles.
</verification>

<success_criteria>
- All 4 client-facing listSessions calls in function-executor.ts pass excludePastToday=true.
- The file's local hoursUntilSessionInAthens duplicate is gone; the shared timezone.ts export is used instead.
- session-booking-flow.test.ts and cancellation-cutoff.test.ts are both green.
</success_criteria>

<output>
Create `.planning/phases/29-booking-list-clarity/29-02-SUMMARY.md` when done
</output>
