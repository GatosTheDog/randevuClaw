---
phase: "16-single-bot-architecture"
plan: 3
type: execute
wave: 3
depends_on:
  - "16-01"
  - "16-02"
files_modified:
  - tests/webhooks/telegram-webhook.onboarding.test.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "A new test file covers the four Phase 16 routing paths and all pass"
    - "Existing test suite (npm test) remains green after the new tests are added"
    - "Tests exercise real module code via mocked DB/Telegram dependencies, not implementation stubs"
  artifacts:
    - tests/webhooks/telegram-webhook.onboarding.test.ts
  key_links:
    - "test file → imports handleFoundBusiness indirectly via supertest on the app module"
    - "mock findActiveSessionByOwnerTelegramId → controls onboarding routing branch"
    - "mock dispatchOnboardingStep → verifies onboarding dispatch was called"
---

<objective>
Write integration tests for the Phase 16 routing changes. The test file targets the four
routing branches introduced in Plan 16-02 inside handleFoundBusiness: (1) owner with
incomplete onboarding and active session, (2) owner with incomplete onboarding and no
session, (3) owner with completed onboarding, (4) non-owner client. Also verifies that the
platform bot route (/webhooks/telegram/platform) no longer responds — it is gone (ARCH-01).

Purpose: Validate all seven Phase 16 requirements through automated tests that catch
regressions in the critical routing layer.

Output: tests/webhooks/telegram-webhook.onboarding.test.ts — all tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-single-bot-architecture/16-01-SUMMARY.md
@.planning/phases/16-single-bot-architecture/16-02-SUMMARY.md
@src/webhooks/telegram.ts
@src/onboarding/queries.ts
@src/onboarding/router.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write and pass onboarding routing tests for Phase 16 paths</name>
  <files>tests/webhooks/telegram-webhook.onboarding.test.ts</files>
  <action>
    Create tests/webhooks/telegram-webhook.onboarding.test.ts. Read the existing test file
    tests/webhooks/telegram-webhook.test.ts first to understand the established patterns:
    how business fixtures are constructed, how HMAC signatures are computed for the test
    webhook secret, how the app is imported via supertest, and how mocks are set up for
    database and Telegram client functions.

    Mirror those patterns exactly in the new test file. Do not invent new patterns.

    The test file must cover these four scenarios plus the ARCH-01 platform-route check:

    SCENARIO A — ARCH-03 / AUTH-01: Owner with onboarding incomplete, active session exists
      Setup: Business fixture with ownerTelegramId='owner-123', onboardingCompleted=false.
      Mock: findBusinessByWebhookId returns the fixture. findActiveSessionByOwnerTelegramId
      returns { session: { id: 1, currentStep: 'name', ... }, business: fixture }.
      Mock: dispatchOnboardingStep resolves without error.
      Mock: markTelegramUpdateProcessed resolves.
      Post a message update to /webhooks/telegram/:webhookId with from.id=owner-123.
      Assert: dispatchOnboardingStep was called with the session and 'name' step.
      Assert: aiOwnerAgent was NOT called.
      Assert: Response is 200.

    SCENARIO B — ARCH-03: Owner with onboarding incomplete, NO active session
      Setup: Business fixture with ownerTelegramId='owner-123', onboardingCompleted=false.
      Mock: findBusinessByWebhookId returns fixture. findActiveSessionByOwnerTelegramId
      returns null.
      Mock: createOrResetOnboardingSession resolves with a session object.
      Mock: sendTelegramMessage resolves.
      Mock: markTelegramUpdateProcessed resolves.
      Post a message from owner-123.
      Assert: createOrResetOnboardingSession was called with (business.id, 'name').
      Assert: sendTelegramMessage was called with 'owner-123' and a string containing
      'Καλωσήρθατε'.
      Assert: dispatchOnboardingStep was NOT called.
      Assert: aiOwnerAgent was NOT called.
      Assert: Response is 200.

    SCENARIO C — ARCH-02 / AUTH-01: Owner with onboarding complete → aiOwnerAgent
      Setup: Business fixture with ownerTelegramId='owner-123', onboardingCompleted=true.
      Mock: findBusinessByWebhookId returns fixture. aiOwnerAgent resolves with 'Γεια σου'.
      Mock: sendTelegramMessage resolves. markTelegramUpdateProcessed resolves.
      Post a message from owner-123.
      Assert: aiOwnerAgent was called.
      Assert: dispatchOnboardingStep was NOT called.
      Assert: sendTelegramMessage called with the aiOwnerAgent reply.
      Assert: Response is 200.

    SCENARIO D — ARCH-04 / AUTH-02: Client (non-owner) → client path
      Setup: Business fixture with ownerTelegramId='owner-123', onboardingCompleted=true.
      Mock: findBusinessByWebhookId returns fixture. routeConversationMessage resolves.
      insertClientBusinessRelationship resolves.
      Post a message from a different sender id, e.g. from.id=client-456.
      Assert: routeConversationMessage was called (not aiOwnerAgent, not dispatchOnboardingStep).
      Assert: insertClientBusinessRelationship was called with client-456.
      Assert: Response is 200.

    SCENARIO E — ARCH-01: Platform route no longer exists
      POST /webhooks/telegram/platform with any body.
      Assert: Response is 404 (Express has no handler for this path anymore;
      the :webhookId catch-all will try to look up a business with webhookId='platform'
      and return 404 from findBusinessByWebhookId returning null — or 400 if webhookId
      is missing). Either 404 or 400 is acceptable; the key assertion is that the request
      does NOT return 200 with onboarding behavior.
      (No HMAC header needed — the handler never reaches auth before returning 404/400.)

    Null ownerTelegramId guard — inline variant within SCENARIO D:
      Add a sub-case where business.ownerTelegramId is null. The sender id matches nothing.
      Assert: routeConversationMessage is called (client path), NOT the owner path.

    Use jest.mock() at module level for:
      '../src/onboarding/queries' — mock findActiveSessionByOwnerTelegramId,
        createOrResetOnboardingSession, findBusinessByOwnerTelegramId
      '../src/onboarding/router' — mock dispatchOnboardingStep
      '../src/onboarding/ai-owner-agent' — mock aiOwnerAgent
      '../src/conversation/router' — mock routeConversationMessage
      '../src/database/queries' — mock findBusinessByWebhookId, insertOrIgnoreTelegramUpdate,
        markTelegramUpdateProcessed, insertClientBusinessRelationship, withBusinessContext

    withBusinessContext mock must call its callback: jest.fn().mockImplementation(
      (_id: unknown, fn: () => Promise<void>) => fn()
    ).

    HMAC signature computation must match the existing test file's approach exactly
    (use crypto.createHmac with the test webhook secret and the raw JSON body).

    Each scenario runs in its own describe/it block with beforeEach resetting mocks.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="telegram-webhook.onboarding" 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    All five scenarios pass (A through E). The command exits 0 with no failing tests.
    npm test (full suite) also exits 0 — no regressions in existing tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full suite green check</name>
  <files></files>
  <action>
    Run the full Jest test suite and confirm it passes. This is a verification-only task
    with no code changes. If any tests fail that were passing before Phase 16, read the
    failing test output, identify the root cause, and fix it (in telegram.ts, steps.ts, or
    the new test file as appropriate). Common issues to watch for:
    - Existing tests that mock business fixtures without an onboardingCompleted field —
      those will need the field added with value true to preserve the old "complete owner"
      behavior and avoid routing them into the onboarding branch.
    - Import path issues in the new test file.
    - Missing mock setup for the new onboarding imports in telegram.ts.

    Do not modify any non-test source files to make tests pass unless the underlying
    production logic is genuinely wrong. If a production bug is found, fix it and note it
    in the summary.
  </action>
  <verify>
    <automated>npm test 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    npm test exits 0. All suites pass including the new telegram-webhook.onboarding test.
    Zero failing tests, zero errored suites.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks → production modules | Mocks must mirror real module signatures; a divergent mock masks real bugs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-08 | Tampering | Test mock divergence from production | low | mitigate | Task 1 requires reading the existing test file patterns before writing new tests; HMAC signing must match production exactly so the test exercises the real auth path, not a bypassed one. |
| T-16-09 | Spoofing | Scenario E platform route test | low | accept | Test confirms 404/400 response; a 200 would indicate the route was not removed — acceptable as a detection-only check rather than a prevention. |
</threat_model>

<verification>
After both tasks:
- `npm test -- --testPathPattern="telegram-webhook.onboarding" 2>&1 | tail -5` shows "Tests: N passed"
- `npm test 2>&1 | tail -5` shows all suites pass, no failures
- The five scenarios (A, B, C, D, E) each have a passing test assertion
</verification>

<success_criteria>
1. Scenario A: dispatchOnboardingStep called for owner with incomplete onboarding and active session
2. Scenario B: createOrResetOnboardingSession called and welcome message sent when no session exists
3. Scenario C: aiOwnerAgent called when owner has onboarding_completed=true
4. Scenario D: Client routed to routeConversationMessage; insertClientBusinessRelationship called
5. Scenario E: POST to /webhooks/telegram/platform returns 404 or 400, not 200 with onboarding behavior
6. Full npm test suite exits 0 with no regressions
</success_criteria>

<output>
Create `.planning/phases/16-single-bot-architecture/16-03-SUMMARY.md` when done
</output>
