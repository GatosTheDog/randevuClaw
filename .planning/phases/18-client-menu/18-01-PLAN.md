---
phase: 18-client-menu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/webhooks/telegram.ts
  - src/telegram/handlers/client-menu.ts
autonomous: true
requirements:
  - CMENU-01
  - CMENU-05
must_haves:
  truths:
    - Client sends /start and receives a 4-button 2x2 inline keyboard (Κράτηση μαθήματος / Οι κρατήσεις μου / Ακύρωση κράτησης / Υπόλοιπο μαθημάτων)
    - Client sends any other text and it reaches routeConversationMessage unchanged
    - cmenu: callback_data strings are parsed by parseCallbackData and return ClientMenuCallbackResult
    - handleCallbackQuery dispatches clientMenuAction callbacks to handleClientMenuCallback with business in scope
  artifacts:
    - src/telegram/handlers/client-menu.ts (new file with ClientMenuCallbackResult type + showClientRootMenu + handleClientMenuCallback dispatcher)
  key_links:
    - handleFoundBusiness client branch calls showClientRootMenu for /start then returns — never falls through to routeConversationMessage for /start
    - handleCallbackQuery receives business as third parameter and dispatches cmenu: callbacks before the existing client_cancel branch
---

<objective>
Infrastructure plan: add /start pre-emption in the client branch of handleFoundBusiness, create src/telegram/handlers/client-menu.ts with ClientMenuCallbackResult type and the root menu + dispatcher skeleton, register the cmenu: parse arm in parseCallbackData, and thread business into handleCallbackQuery so client menu callbacks can resolve the tenant without an extra DB lookup.

Purpose: All subsequent plans (18-02, 18-03) add handler functions to client-menu.ts and new dispatch cases to handleClientMenuCallback. Nothing can work until the routing scaffold and callback namespace are wired in telegram.ts.

Output: /start shows the 4-button root menu. cmenu:root callback re-shows the same menu. All other cmenu: callbacks fall through to a default branch that sends "Άγνωστη ενέργεια." until the subsequent plans fill them in. Free Greek text still reaches routeConversationMessage (CMENU-05).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@src/webhooks/telegram.ts
@src/telegram/handlers/admin-menu.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/telegram/handlers/client-menu.ts with ClientMenuCallbackResult, showClientRootMenu, and handleClientMenuCallback skeleton</name>
  <files>src/telegram/handlers/client-menu.ts</files>
  <action>
    Create a new file src/telegram/handlers/client-menu.ts. Mirror the structure of admin-menu.ts exactly.

    Export the type ClientMenuCallbackResult with discriminant field clientMenuAction (string) and optional id (number | undefined). The clientMenuAction field name must be unique — verify it does not appear in any existing exported type in telegram.ts before writing.

    Copy assertCallbackDataSize verbatim from admin-menu.ts as a module-private function (do NOT import it from admin-menu.ts — it is not exported there and adding an export creates unnecessary coupling per RESEARCH.md Pitfall 4).

    Implement showClientRootMenu(chatId: string, business: Business): Promise<void> per RESEARCH.md Research Area 8. Four buttons in 2x2 layout:
    - Row 1: "Κράτηση μαθήματος" → cmenu:book, "Οι κρατήσεις μου" → cmenu:bookings
    - Row 2: "Ακύρωση κράτησης" → cmenu:cancel, "Υπόλοιπο μαθημάτων" → cmenu:balance
    Welcome text: "Καλώς ήρθες! Τι θέλεις να κάνεις;"
    Call assertCallbackDataSize on every callback_data string before building the keyboard.

    Implement handleClientMenuCallback(result: ClientMenuCallbackResult, business: Business, chatId: string): Promise<void> as the central dispatcher using switch(true) pattern matching result.clientMenuAction, same style as handleMenuCallback in admin-menu.ts.

    For this plan (18-01), wire only these cases in the dispatcher:
    - 'root' → showClientRootMenu(chatId, business)
    - default → sendTelegramMessage(chatId, 'Άγνωστη ενέργεια.')

    Plans 18-02 and 18-03 will add the remaining cases ('book', 'bookings', 'cancel', 'balance', and their sub-actions). Do not add stub throws — the default branch handles gracefully.

    Imports needed: Business from ../../database/queries, sendTelegramMessage and sendTelegramMessageWithKeyboard and InlineKeyboard from ../client, logger from ../../utils/logger.

    Export: ClientMenuCallbackResult (type), showClientRootMenu, handleClientMenuCallback.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>File compiles with no TypeScript errors. showClientRootMenu and handleClientMenuCallback are exported. ClientMenuCallbackResult type has clientMenuAction discriminant.</done>
</task>

<task type="auto">
  <name>Task 2: Register cmenu: parse arm in parseCallbackData and add /start intercept + business threading in telegram.ts</name>
  <files>src/webhooks/telegram.ts</files>
  <action>
    Three distinct edits to src/webhooks/telegram.ts. Read the full file before editing to confirm line numbers have not drifted.

    EDIT 1 — Import: Add ClientMenuCallbackResult, showClientRootMenu, handleClientMenuCallback to the import from ./telegram/handlers/client-menu (new import line, after the admin-menu import on line 30).

    EDIT 2 — parseCallbackData return union: Extend the return type annotation on parseCallbackData to include ClientMenuCallbackResult. The current union ends with MenuCallbackResult | null. Change it to MenuCallbackResult | ClientMenuCallbackResult | null.

    EDIT 3 — parseCallbackData body: Add the cmenu: regex match AFTER the existing menu: match block and BEFORE the final return null. Pattern from RESEARCH.md Research Area 2:
      const clientMenuMatch = data?.match(/^cmenu:([\w:]+?)(?::(\d+))?$/);
      if (clientMenuMatch) {
        return {
          clientMenuAction: clientMenuMatch[1],
          id: clientMenuMatch[2] ? Number(clientMenuMatch[2]) : undefined,
        };
      }
    The regex must use [\w:]+ (allows colons in sub-actions like book:confirm, cancel:yes) with a non-greedy tail before the optional numeric suffix group.

    EDIT 4 — handleCallbackQuery signature: Add business: Business as third parameter. Current signature: async function handleCallbackQuery(callbackQuery, senderTelegramId). New signature: async function handleCallbackQuery(callbackQuery: TelegramCallbackQuery, senderTelegramId: string, business: Business). Business is already imported in telegram.ts (it comes from database/queries on line 5).

    EDIT 5 — handleCallbackQuery body: Add client menu dispatch block immediately AFTER the menuAction branch (after line ~289 where the menuAction block returns) and BEFORE the client_cancel check. Per RESEARCH.md Research Area 3:
      if ('clientMenuAction' in parsed) {
        const clientResult = parsed as ClientMenuCallbackResult;
        if (callbackQuery.message?.message_id) {
          await editTelegramMessageReplyMarkup(senderTelegramId, callbackQuery.message.message_id, []);
        }
        await handleClientMenuCallback(clientResult, business, senderTelegramId);
        return;
      }
    No owner check is needed here — cross-tenant safety is guaranteed by the business object originating from the HMAC-verified webhook (not from callback_data).

    EDIT 6 — Call site update: On the line inside handleTelegramWebhookPost that calls handleCallbackQuery (currently: await handleCallbackQuery(update.callback_query, senderTelegramId)), add business as the third argument: await handleCallbackQuery(update.callback_query, senderTelegramId, business).

    EDIT 7 — /start pre-emption in handleFoundBusiness: In the client branch of handleFoundBusiness (the else branch after the owner check, currently lines 97-100), insert before the existing routeConversationMessage call:
      // CMENU-01: /start command — show structured keyboard, no Gemini round-trip (D-per-RESEARCH.md).
      if (messageText.trim() === '/start') {
        await showClientRootMenu(senderTelegramId, business);
        await markTelegramUpdateProcessed(updateId, business.id);
        return;
      }
    This is the sole intercept. Only the exact string "/start" (post-trim) is caught. Every other text falls through to routeConversationMessage unchanged — CMENU-05 preserved by the early-return pattern.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles clean. parseCallbackData return type includes ClientMenuCallbackResult. handleCallbackQuery has three parameters including business: Business. The /start intercept is in the client branch. The call site passes business as third arg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram → handleTelegramWebhookPost | HMAC-verified via X-Telegram-Bot-Api-Secret-Token; business resolved from webhook secret before any dispatch |
| callback_data → parseCallbackData | Regex-validated; only cmenu:([\w:]+?)(?::(\d+))?$ shape accepted; numeric id extracted as Number(), never used as businessId |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | cmenu: callback_data — spoofed action string | low | accept | clientMenuAction is a route selector only; no business data is read from it; all DB queries use business.id from the verified webhook |
| T-18-02 | Spoofing | Owner taps cmenu: button (no owner-exclusion in handleCallbackQuery) | low | accept | Owner sees their own client-facing menu — harmless; business is already HMAC-verified to be theirs; RESEARCH.md Pitfall 1 documents this as acceptable PoC behavior |
| T-18-03 | Information Disclosure | /start intercept leaks that business exists via non-404 response | low | accept | Business lookup already happens before /start check; webhook returns 200 regardless per Telegram protocol |
</threat_model>

<verification>
After both tasks complete:
- npx tsc --noEmit exits 0
- Grep for 'clientMenuAction' in src/webhooks/telegram.ts shows the parse arm and dispatch block
- Grep for '/start' in src/webhooks/telegram.ts shows the intercept in the client branch (not the owner branch)
- handleCallbackQuery in telegram.ts has three parameters visible via: grep -n 'async function handleCallbackQuery' src/webhooks/telegram.ts
</verification>

<success_criteria>
- Client sends /start → 4-button keyboard rendered with Greek labels, no Gemini call
- Client sends "Θέλω να κλείσω ραντεβού" → reaches routeConversationMessage (CMENU-05 intact)
- cmenu:root callback → root menu re-rendered
- cmenu:book callback → "Άγνωστη ενέργεια." (stub, filled by 18-02)
- TypeScript: npx tsc --noEmit exits 0
</success_criteria>

<output>
Create .planning/phases/18-client-menu/18-01-SUMMARY.md when done
</output>
