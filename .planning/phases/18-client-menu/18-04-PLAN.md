---
phase: 18-client-menu
plan: 04
type: execute
wave: 3
depends_on:
  - 18-02
  - 18-03
files_modified:
  - tests/webhooks/client-menu.test.ts
autonomous: true
requirements:
  - CMENU-01
  - CMENU-02
  - CMENU-03
  - CMENU-04
  - CMENU-05
must_haves:
  truths:
    - Client sends /start → showClientRootMenu is called, routeConversationMessage is NOT called
    - Client sends any other text → routeConversationMessage is called, showClientRootMenu is NOT called (CMENU-05)
    - parseCallbackData('cmenu:book') returns ClientMenuCallbackResult with clientMenuAction === 'book'
    - parseCallbackData('cmenu:cancel:yes:42') returns ClientMenuCallbackResult with clientMenuAction === 'cancel:yes' and id === 42
    - parseCallbackData does not break existing arms (approve_1, billing:client:1, menu:settings, slotless:req_approve:5 all still parse correctly)
    - cmenu:book:yes callback triggers bookSessionInstance
    - cmenu:cancel:yes callback triggers updateBookingStatus and restoreCredit when membership exists
  artifacts:
    - tests/webhooks/client-menu.test.ts (new test file)
  key_links:
    - Tests run with --testPathPattern=client-menu to avoid triggering the full suite (machine crashes on bare npm test per user memory)
    - All mocks use jest.mock at module level; no spying on internal private functions
---

<objective>
Integration tests for all Phase 18 routing paths: /start intercept, CMENU-05 free-text preservation, parseCallbackData union extension, booking flow, cancel flow, and the cross-ownership guard. Tests give a regression harness that confirms client menu additions do not break any existing callback dispatch arms.

Purpose: Prevent regressions. The parseCallbackData union is now six types wide; a misplaced regex could shadow existing arms. The /start intercept must not accidentally block other commands. The ownership guard in handleCancelExecute must remain effective.

Output: A single test file tests/webhooks/client-menu.test.ts with six test suites. All pass on npm test -- --testPathPattern="client-menu" --testTimeout=20000.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-client-menu/18-02-SUMMARY.md
@.planning/phases/18-client-menu/18-03-SUMMARY.md
@src/webhooks/telegram.ts
@src/telegram/handlers/client-menu.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write integration tests for parseCallbackData, /start intercept, and CMENU-05 free-text preservation</name>
  <files>tests/webhooks/client-menu.test.ts</files>
  <behavior>
    Suite A — parseCallbackData:
    - parseCallbackData('cmenu:book') → { clientMenuAction: 'book', id: undefined }
    - parseCallbackData('cmenu:cancel:yes:42') → { clientMenuAction: 'cancel:yes', id: 42 }
    - parseCallbackData('cmenu:book:confirm:9999') → { clientMenuAction: 'book:confirm', id: 9999 }
    - parseCallbackData('cmenu:root') → { clientMenuAction: 'root', id: undefined }
    - parseCallbackData('cmenu:balance') → { clientMenuAction: 'balance', id: undefined }
    - Existing arms not broken: parseCallbackData('approve_1') → { action: 'approve', bookingId: 1 }
    - Existing arms not broken: parseCallbackData('menu:settings') → { menuAction: 'settings', id: undefined }
    - Existing arms not broken: parseCallbackData('slotless:req_approve:5') → { action: 'slotless:req_approve', slotlessRequestId: 5 }
    - parseCallbackData('cmenu:') → null (no action segment)
    - parseCallbackData(undefined) → null

    Suite B — /start intercept in handleFoundBusiness client branch:
    - When messageText is '/start' and senderTelegramId !== business.ownerTelegramId → showClientRootMenu is called, routeConversationMessage is NOT called
    - When messageText is '/start' and senderTelegramId === business.ownerTelegramId → showAdminRootMenu path runs, NOT showClientRootMenu (owner branch pre-empts)
    - CMENU-05: when messageText is 'Θέλω να κλείσω ραντεβού' and senderTelegramId !== ownerTelegramId → routeConversationMessage IS called, showClientRootMenu is NOT called
    - CMENU-05: when messageText is '   /start   ' (padded with spaces) → showClientRootMenu IS called (trim() normalises)
  </behavior>
  <action>
    Import parseCallbackData from ../../src/webhooks/telegram using jest.requireActual or direct import.

    For /start intercept tests, mock handleFoundBusiness's dependencies at module level:
    - jest.mock('../../src/telegram/handlers/client-menu', ...) — mock showClientRootMenu as jest.fn().mockResolvedValue(undefined)
    - jest.mock('../../src/conversation/router', ...) — mock routeConversationMessage as jest.fn().mockResolvedValue(undefined)
    - jest.mock('../../src/database/queries', ...) — mock markTelegramUpdateProcessed as jest.fn().mockResolvedValue(undefined)
    - Do not mock handleFoundBusiness itself — call the exported function directly via the module or extract test helpers

    If handleFoundBusiness is not exported from telegram.ts, test the /start intercept behaviour indirectly via handleTelegramWebhookPost with a mocked Request/Response and mocked business lookup, using the existing test helper patterns from any existing tests in tests/webhooks/ — read those files first to match the pattern.

    The parseCallbackData tests are pure unit tests with no mocking needed — import and call directly.

    Never use bare npm test. Always: npm test -- --testPathPattern="client-menu" --testTimeout=20000
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="client-menu" --testTimeout=20000 2>&1 | tail -20</automated>
  </verify>
  <done>All Suite A and Suite B tests pass. parseCallbackData correctly narrows to ClientMenuCallbackResult for cmenu: prefixes. Existing arms still parse correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write integration tests for booking flow, cancel flow, and ownership guard</name>
  <files>tests/webhooks/client-menu.test.ts</files>
  <behavior>
    Suite C — Book flow (handleBookSessionExecute via handleClientMenuCallback):
    - When enforcement allows (checkEnforcementAndGetMembership returns allowed=true) and bookSessionInstance succeeds → Greek confirmation message sent to client
    - When enforcement blocks (returns allowed=false, message='Για να κάνετε κράτηση, χρειάζεστε ενεργή συνδρομή.') → that message is sent, bookSessionInstance is NOT called
    - When business.bookingMode === 'open_slots' → showBookSessionList sends fallback "chat" message, listSessions NOT called

    Suite D — Cancel flow (handleCancelExecute via handleClientMenuCallback):
    - Happy path: booking exists, booking.clientPhone === senderTelegramId, outside cutoff window → updateBookingStatus called with 'cancelled', confirmation sent
    - Credit restore: when findMembershipByBooking returns a membershipId → restoreCredit is called with that membershipId
    - No credit restore: when findMembershipByBooking returns null → restoreCredit is NOT called
    - Ownership guard: booking.clientPhone !== senderTelegramId → updateBookingStatus NOT called, ownership error message sent
    - Cutoff guard: cancellationCutoffEnabled=true, session is 1 hour away, cutoffHours=2 → updateBookingStatus NOT called, cutoff error message sent

    Suite E — parseCallbackData does not shadow cmenu: from existing arms:
    - parseCallbackData('billing:client:1') still returns BillingCallbackResult (firstId: 1)
    - parseCallbackData('renewal:approve:99') still returns RenewalCallbackResult (businessId: 99)
  </behavior>
  <action>
    Extend tests/webhooks/client-menu.test.ts with Suites C, D, E.

    For handleClientMenuCallback tests, import handleClientMenuCallback and mock all external dependencies at the top of the file using jest.mock:
    - ../../src/session/manager: mock listSessions, bookSessionInstance
    - ../../src/billing/enforcement: mock checkEnforcementAndGetMembership
    - ../../src/database/queries: mock findBookingByIdUnscoped, updateBookingStatus, findMembershipByBooking, listClientBookings, findServiceById
    - ../../src/billing/queries: mock restoreCredit, getClientActiveMembership
    - ../../src/telegram/client: mock sendTelegramMessage, sendTelegramMessageWithKeyboard, botTokenStore
    - ../../src/calendar/sync: mock deleteBookingFromCalendar

    Build a minimal Business fixture with relevant fields (id, name, ownerTelegramId, botToken, bookingMode, cancellationCutoffEnabled, cancellationCutoffHours, slotlessRequestsEnabled).

    Build a minimal Booking fixture with relevant fields (id, clientPhone, calendarDate, calendarTime, bookingStatus, businessId, serviceId).

    For cutoff guard test: set calendarDate to today's date string in Athens (or any date), calendarTime to a time 1 hour from now (or mock Date.now inside the test). Since hoursUntilSession uses the real clock, set calendarDate/calendarTime to a real near-future time or mock the hoursUntilSession helper by controlling the clock with jest.useFakeTimers.

    Run npm test -- --testPathPattern="client-menu" --testTimeout=20000 after adding each suite. Fix failures before adding the next suite.

    CRITICAL: NEVER run bare npm test — use --testPathPattern="client-menu" always.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="client-menu" --testTimeout=20000 2>&1 | tail -30</automated>
  </verify>
  <done>All Suites C, D, E pass. updateBookingStatus and bookSessionInstance calls verified via jest mock assertions. Ownership guard and cutoff guard prevent mutation when conditions are not met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks | All external I/O mocked at module boundary; no real DB or Telegram API calls in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-10 | Tampering | Test coverage gap allowing ownership bypass to ship undetected | medium | mitigate | Suite D explicitly asserts updateBookingStatus NOT called when booking.clientPhone !== senderTelegramId |
| T-18-11 | Tampering | parseCallbackData regex change shadows existing arm | medium | mitigate | Suite E asserts existing result types still parse correctly after cmenu: arm added |
</threat_model>

<verification>
Final check after both tasks:
- npm test -- --testPathPattern="client-menu" --testTimeout=20000 shows all tests passing, zero failures
- npx tsc --noEmit exits 0 (test file must also typecheck)
- grep -c 'it(' tests/webhooks/client-menu.test.ts shows at least 15 test cases covering all five suites
</verification>

<success_criteria>
- npm test -- --testPathPattern="client-menu" --testTimeout=20000 exits 0 with all tests green
- No regressions: npm test -- --testPathPattern="telegram" --testTimeout=20000 (if that test file exists) also remains green
- npx tsc --noEmit exits 0
- Test covers: parseCallbackData union extension, /start intercept, CMENU-05 free-text, booking enforcement gate, cancel ownership guard, cancel cutoff guard, credit restore logic, existing parseCallbackData arms unbroken
</success_criteria>

<output>
Create .planning/phases/18-client-menu/18-04-SUMMARY.md when done
</output>
