---
phase: 11-session-booking-flow
plan: 03
type: execute
wave: 3
depends_on:
  - "11-01"
  - "11-02"
files_modified:
  - tests/session-booking-flow.test.ts
autonomous: true
requirements:
  - SBOK-01
  - SBOK-02
  - SBOK-03
  - SBOK-04

must_haves:
  truths:
    - "npm test -- tests/session-booking-flow.test.ts --testTimeout=20000 exits 0 with all tests PASSING"
    - "SBOK-01: test confirms client booking via bookSessionInstance succeeds when capacity is available"
    - "SBOK-02: test confirms sessionsRemaining decrements by 1 and a session_deducted ledger row is inserted when bookSessionInstance is called with a finite membership"
    - "SBOK-03: test confirms rescheduleSessionTool rejects a new_session_instance_id whose sessionDate is past membership expiresAt with error:past_membership_expiry"
    - "SBOK-04: test confirms sequential multi-booking loop books all instances when allow_multi_booking=true and each successive booking decrements the membership counter correctly"
    - "Idempotency: replaying bookSessionInstance with same idempotencyKey does not double-deduct the membership counter"
    - "Capacity guard: two concurrent bookSessionInstance calls on the last slot return exactly one success and one full"
  artifacts:
    - tests/session-booking-flow.test.ts
  key_links:
    - "Test file follows TEST_DATABASE_URL + jest.resetModules() + require() pattern from billing-membership-creation.test.ts and session-assignment.test.ts"
    - "Tests call bookSessionInstance and rescheduleSessionTool handler functions directly — no Gemini mock needed for DB-layer correctness tests"
    - "Membership fixtures from tests/helpers/billing-fixtures.ts (insertTestPackage, insertTestMembership)"
    - "Session fixtures from tests/helpers/session-fixtures.ts (insertTestSessionCatalog, insertTestSessionInstance)"
---

<objective>
Create tests/session-booking-flow.test.ts with full integration test coverage for all four SBOK requirements. Tests run against the real test Postgres DB using the established TEST_DATABASE_URL + jest.resetModules() + require() pattern. The critical tests are: atomic deduction on booking (SBOK-02), membership expiry gate on reschedule (SBOK-03), and multi-booking sequential loop (SBOK-04).

Purpose: Nyquist completion — SBOK-01 through SBOK-04 each have at least one automated green test confirming correct DB behavior end-to-end.
Output: tests/session-booking-flow.test.ts with all tests passing; zero it.todo stubs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@tests/billing-membership-creation.test.ts
@tests/session-assignment.test.ts
@src/session/manager.ts
@src/conversation/function-executor.ts
@src/billing/queries.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/session-booking-flow.test.ts with SBOK-01 through SBOK-04 integration tests</name>
  <files>tests/session-booking-flow.test.ts</files>
  <read_first>
    tests/session-assignment.test.ts — full file: TEST_DATABASE_URL pattern, jest.resetModules(), require() imports, beforeAll/afterAll setup, insertTestBusiness + session fixture usage, unique phone pattern for isolation.
    tests/billing-session-deduction.test.ts — full file: withBusinessContext wrapper for deductSession, insertTestBooking helper, ledger row verification pattern (db.select on membershipLedger), insertTestMembership usage with sessionsRemaining override.
    tests/helpers/billing-fixtures.ts — insertTestPackage, insertTestMembership signatures.
    tests/helpers/session-fixtures.ts — insertTestSessionCatalog, insertTestSessionInstance signatures.
    src/session/manager.ts — bookSessionInstance updated signature (with activeMembership parameter) after Plan 11-01; understand what the 6th param accepts.
    src/conversation/function-executor.ts — rescheduleSessionTool: read the implemented handler to understand what inputs it needs for testing (booking_id, new_session_instance_id, context.business shape with bookingMode and allowMultiBooking).
    src/billing/queries.ts — getClientActiveMembership signature (for SBOK-03 expiry test setup), ActiveMembershipForDeduction interface.
  </read_first>
  <behavior>
    SBOK-01 (client books session via bookSessionInstance):
    - 'client books session: bookSessionInstance returns success when capacity available' — insert catalog (capacity=5), instance (bookedCount=0), membership (sessionsRemaining=3), call bookSessionInstance with membership; assert result.status === 'success' and result.bookingId > 0
    - 'booking blocked when session is full (SBOK-01 hard cap)' — set bookedCount=5 (= capacity), call bookSessionInstance; assert result.status === 'full'

    SBOK-02 (atomic credit deduction):
    - 'bookSessionInstance deducts 1 credit atomically: sessionsRemaining decrements and ledger row inserted' — start with sessionsRemaining=3; call bookSessionInstance with finite membership; after call: db.select memberships WHERE id=membership.id — assert sessionsRemaining=2; db.select membershipLedger WHERE bookingId=bookingId AND operationType='session_deducted' — assert 1 row with sessionsDeducted=1
    - 'unlimited membership (sessionsRemaining=null): booking succeeds, no ledger row inserted, no counter change' — insert membership with sessionsRemaining=null; call bookSessionInstance; assert result.status === 'success'; assert membershipLedger has 0 rows for this booking
    - 'no membership (activeMembership=null): booking still succeeds when enforcement policy=allow, no deduction' — pass null as activeMembership; assert booking inserts and result.status === 'success'; assert no ledger row
    - 'deduction is idempotent: replaying bookSessionInstance with same idempotencyKey does not double-deduct' — call bookSessionInstance twice with same idempotencyKey; assert sessionsRemaining = startingRemaining - 1 (deducted exactly once); assert exactly 1 session_deducted ledger row for the bookingId

    SBOK-03 (reschedule expiry gate):
    - 'rescheduleSessionTool blocks reschedule to session past membership expiry' — set up: insert membership with expiresAt = now + 10 days; insert two session instances (instanceA: date = today+5, instanceB: date = today+20 which is past expiry); book instanceA (via bookSessionInstance); call rescheduleSessionTool handler with booking_id = instanceA's booking, new_session_instance_id = instanceB; assert result.success === false and result.error === 'past_membership_expiry'
    - 'rescheduleSessionTool allows reschedule to session within membership expiry' — same setup but instanceB: date = today+8 (within expiry); assert result.success === true

    SBOK-04 (multi-booking):
    - 'multi-booking: books two sessions sequentially, decrements counter twice' — insert 2 instances (both with capacity>0); membership with sessionsRemaining=5; call bookSessionTool handler with session_instance_ids=[instanceA.id, instanceB.id] and business.allowMultiBooking=true; assert booked_count=2; assert sessionsRemaining=3 (5-2)
    - 'multi-booking disabled: returns error when allowMultiBooking=false on business' — call bookSessionTool with session_instance_ids array but business.allowMultiBooking=false; assert result.error === 'multi_booking_disabled'
    - 'multi-booking partial success: one full session does not block booking of other sessions in the array' — insert instanceA (capacity=1, bookedCount=1 i.e. full), instanceB (capacity=5, bookedCount=0); call bookSessionTool with [instanceA.id, instanceB.id]; assert booked_instance_ids=[instanceB.id], full_instance_ids=[instanceA.id]
  </behavior>
  <action>
    Create tests/session-booking-flow.test.ts as a new file. Follow the TEST_DATABASE_URL + jest.resetModules() + require() pattern EXACTLY as in session-assignment.test.ts.

    File header:
    // covers SBOK-01, SBOK-02, SBOK-03, SBOK-04
    // Integration tests against a REAL local Postgres connection.
    // Requires migrations 0006 (billing), 0007 (enforcement), 0010 (session catalog) to be applied.
    //
    // Setup (one-time, local dev machine):
    //   psql postgresql://manolis@localhost:5432/randevuclaw_test \
    //     -f migrations/0010_session_catalog_schema.sql

    TEST_DATABASE_URL pattern:
    const TEST_DATABASE_URL =
      process.env.SESSION_TEST_DATABASE_URL ??
      process.env.BILLING_TEST_DATABASE_URL ??
      'postgresql://manolis@localhost:5432/randevuclaw_test';

    const ORIGINAL_DATABASE_URL = process.env.DATABASE_URL;
    process.env.DATABASE_URL = TEST_DATABASE_URL;
    jest.resetModules();

    Require-based imports (NOT ES import — required for jest.resetModules() to work):
    const { db } = require('../src/database/db');
    const { eq, and } = require('drizzle-orm');
    const { memberships, membershipLedger, sessionInstances, sessionCatalog, bookings, services, businesses } = require('../src/database/schema');
    const { withBusinessContext, getConn } = require('../src/database/queries');
    const { bookSessionInstance } = require('../src/session/manager');
    const { insertTestBusiness } = require('./helpers/test-business');
    const { insertTestPackage, insertTestMembership } = require('./helpers/billing-fixtures');
    const { insertTestSessionCatalog, insertTestSessionInstance } = require('./helpers/session-fixtures');

    For SBOK-03 and SBOK-04 tests that call function-executor handler functions directly: require the handler from function-executor:
    const { executeTool } = require('../src/conversation/function-executor');

    afterAll:
    afterAll(() => {
      process.env.DATABASE_URL = ORIGINAL_DATABASE_URL;
    });

    Helper — build a minimal ToolContext for function-executor tests:
    function buildToolContext(business, clientPhone, idempotencyKey = 'test-idem-' + Date.now()) {
      return {
        business: {
          id: business.id,
          name: business.name,
          ownerTelegramId: null,
          enforcementPolicy: business.enforcementPolicy ?? 'allow',
          bookingMode: business.bookingMode ?? 'fixed_sessions',
          allowMultiBooking: business.allowMultiBooking ?? false,
        },
        clientPhone,
        requestId: 'test-req-' + Date.now(),
        idempotencyKey,
      };
    }

    For the SBOK-04 multi-booking test calling bookSessionTool: call executeTool('book_session', { business_id: businessId, session_instance_ids: [idA, idB] }, context). The handler reads context.business.allowMultiBooking.

    For the SBOK-03 reschedule test: call executeTool('reschedule_session', { business_id: businessId, booking_id: bookingId, new_session_instance_id: newInstanceId }, context).

    For SBOK-02 deduction tests: call bookSessionInstance directly (not via executeTool) so that the membership param can be controlled precisely.

    The activeMembership passed to bookSessionInstance in tests must be an object with { id, sessionsRemaining, expiresAt } matching the ActiveMembershipForDeduction interface. Use the row returned by insertTestMembership (which has these fields).

    Unique phone suffix pattern: `client-${Date.now()}-${Math.random().toString(36).slice(2)}`

    Separate describe block per requirement:
    - describe('SBOK-01: client session booking capacity enforcement', () => { ... })
    - describe('SBOK-02: atomic session credit deduction on booking', () => { ... })
    - describe('SBOK-03: reschedule expiry gate', () => { ... })
    - describe('SBOK-04: multi-session booking', () => { ... })

    Each describe block has its own beforeAll that inserts a fresh test business, service, catalog, and any needed membership. Use separate businessIds per describe block to prevent cross-test interference.

    For the SBOK-02 'deduction is idempotent' test: call bookSessionInstance with the same idempotencyKey twice. The second call will find the booking row via the fallback path (bookingRows.length === 0, existing booking fetched by requestId). Assert sessionsRemaining is startingRemaining - 1 (not startingRemaining - 2).

    For SBOK-03 setup: insert a booking for instanceA via bookSessionInstance, then pass the returned bookingId to reschedule_session args. The membership expiresAt must be set in the future but before instanceB's sessionDate — use insertTestMembership with expiresAt = new Date(Date.now() + 10 * 24 * 60 * 60 * 1000); instanceA.sessionDate = today+5 (within expiry); instanceB.sessionDate = today+20 (past expiry). Construct instanceB's sessionDate using isoDateInAthens utility or by adding days to today's ISO string manually.

    For SBOK-04 multi-booking: set business.allowMultiBooking=true on the test business row via direct db.update on businesses table in beforeAll. The businesses table already has the allowMultiBooking column (Phase 11 schema, confirmed in schema.ts line 71).

    Do NOT mock sendTelegramMessage in these tests — the handlers have try/catch around alert sends and a null ownerTelegramId in the test context will cause the alert to be skipped (the handler guards: if (context.business.ownerTelegramId) ...).
  </action>
  <verify>
    <automated>npm test -- tests/session-booking-flow.test.ts --testTimeout=20000</automated>
  </verify>
  <done>tests/session-booking-flow.test.ts has at least 11 passing integration tests covering SBOK-01 through SBOK-04; zero it.todo stubs; npm test exits 0 for this file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test DB → production DB | TEST_DATABASE_URL must point to randevuclaw_test (never production DATABASE_URL) |
| jest.mock → real Telegram API | No real Telegram calls — ownerTelegramId=null in all test contexts causes alert skip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-11 | Elevation | SBOK-02 idempotency test running against production DB | critical | mitigate | TEST_DATABASE_URL + jest.resetModules() pattern; same as session-assignment.test.ts |
| T-11-12 | Information Disclosure | Real client phone numbers in test fixtures | medium | mitigate | All test fixtures use synthetic phone numbers (client-{Date.now()} pattern) |
| T-11-13 | Denial of Service | SBOK-04 multi-booking test writing many rows to test DB | low | accept | Only 2 session instances per test; DB is single-purpose test DB; no production impact |
</threat_model>

<verification>
After plan completion:
- `npm test -- tests/session-booking-flow.test.ts --testTimeout=20000` exits 0 — all tests green
- Confirm SBOK-02: `grep -c "session_deducted" tests/session-booking-flow.test.ts` >= 1
- Confirm SBOK-03: `grep -c "past_membership_expiry" tests/session-booking-flow.test.ts` >= 1
- Confirm SBOK-04: `grep -c "multi_booking\|session_instance_ids" tests/session-booking-flow.test.ts` >= 2
- `npm test -- tests/session-assignment.test.ts tests/session-booking-flow.test.ts --testTimeout=20000` exits 0 (both session test files green together, no DB interference)
</verification>

<success_criteria>
- SBOK-01: booking succeeds when capacity available; returns full when session is full
- SBOK-02: sessionsRemaining decrements by 1 after booking; session_deducted ledger row present; unlimited membership (null) does not deduct; idempotent replay deducts exactly once
- SBOK-03: rescheduleSessionTool returns error:past_membership_expiry when new session date > membership expiry; allows reschedule within expiry
- SBOK-04: multi-booking books all instances sequentially; counter decrements once per booked session; partial success (some full) handled gracefully; multi_booking_disabled error when allowMultiBooking=false
- Zero it.todo stubs in test file
- npm test exits 0 for session-booking-flow.test.ts
</success_criteria>

<output>
Create `.planning/phases/11-session-booking-flow/11-03-SUMMARY.md` when done
</output>
