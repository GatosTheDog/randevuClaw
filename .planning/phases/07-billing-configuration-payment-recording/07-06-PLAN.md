---
phase: 07-billing-configuration-payment-recording
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/billing/queries.ts
  - src/telegram/handlers/payment-flow.ts
  - src/onboarding/ai-owner-agent.ts
  - src/billing/tools.ts
  - tests/billing-payment-flow.test.ts
autonomous: true
gap_closure: true
gap_ids: [G-07-6, G-07-5]
requirements:
  - BILL-03
  - PAY-01

must_haves:
  truths:
    - "getAllClientsForBusiness(businessId) queries clientBusinessRelationships directly with no booking join and no date filter, returning all-time clients ordered by createdAt desc"
    - "showClientSelection falls back to getAllClientsForBusiness when getRecentClientsForBusiness returns empty; shows those clients as an inline keyboard instead of sending an abort message"
    - "When both getRecentClientsForBusiness and getAllClientsForBusiness return empty, showClientSelection sends the Greek empty-state message and returns"
    - "deactivate_package FunctionDeclaration in OWNER_TOOLS uses package_name: string (not package_id: integer) to match the delete_service pattern"
    - "executeOwnerTool deactivate_package case resolves package_name via case-insensitive partial match against listPackages before calling handleDeactivatePackage with the resolved numeric id and matched package name"
    - "handleDeactivatePackage echoes the actual package name in its success reply when the optional packageName argument is provided"
    - "npm test -- tests/billing-payment-flow.test.ts exits 0 including the new fallback test"
  artifacts:
    - src/billing/queries.ts
    - src/telegram/handlers/payment-flow.ts
    - src/onboarding/ai-owner-agent.ts
    - src/billing/tools.ts
    - tests/billing-payment-flow.test.ts
  key_links:
    - "payment-flow.ts showClientSelection → getAllClientsForBusiness fallback path → keyboard shown with all-time client names or phone numbers"
    - "ai-owner-agent.ts deactivate_package case → listPackages(business.id) inside withBusinessContext → case-insensitive name match → handleDeactivatePackage(business.id, match.id, match.name)"
    - "billing/tools.ts handleDeactivatePackage: packageName param echoed in success string so Gemini can confirm correct package was deactivated"
---

<objective>
Close two UAT failures in Phase 7 billing:

G-07-6: showClientSelection aborts when no bookings exist in the last 30 days. Fix by adding getAllClientsForBusiness to billing/queries.ts and using it as a fallback in showClientSelection when the 30-day query returns empty.

G-07-5: deactivate_package deactivates the wrong package because Gemini receives package_id (integer) but has no way to know actual IDs. Fix by switching the FunctionDeclaration to package_name (string), resolving the name to an ID via case-insensitive partial match inside executeOwnerTool, and echoing the matched name in handleDeactivatePackage's success reply so Gemini can detect mismatches.

Purpose: These two fixes make the payment recording flow reachable for businesses without recent bookings, and make package deactivation reliable by removing the hallucinated-ID problem.
Output: Updated billing/queries.ts, payment-flow.ts, ai-owner-agent.ts, billing/tools.ts, and updated payment-flow test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-billing-configuration-payment-recording/07-CONTEXT.md
@.planning/phases/07-billing-configuration-payment-recording/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: G-07-6 — Add getAllClientsForBusiness fallback to payment flow</name>
  <files>src/billing/queries.ts, src/telegram/handlers/payment-flow.ts, tests/billing-payment-flow.test.ts</files>
  <action>
    G-07-6 root cause: showClientSelection (payment-flow.ts) hard-exits when getRecentClientsForBusiness returns empty because that query INNER JOINs bookings with a 30-day cutoff. clientBusinessRelationships (all-time clients) is never consulted as fallback.

    In src/billing/queries.ts:
    Add a new exported type AllTimeClient with fields clientBusinessRelationshipId: number, clientName: string | null, senderPhone: string. Add a new exported async function getAllClientsForBusiness(businessId: number) returning Promise<AllTimeClient[]>. The function uses getConn() (consistent with all other query functions in this file that respect withBusinessContext RLS) to select id as clientBusinessRelationshipId, clientName, and senderPhone from clientBusinessRelationships where businessId matches, ordered by desc(clientBusinessRelationships.createdAt). No booking join, no date filter. Add the type and function after the getRecentClientsForBusiness function (around line 265 in the recent-clients section).

    In src/telegram/handlers/payment-flow.ts:
    Add getAllClientsForBusiness to the import from ../../billing/queries. In showClientSelection, replace the hard-exit block (when clients.length === 0 sends the abort message) with a two-step fallback. Step 1: call getAllClientsForBusiness(businessId) inside withBusinessContext. Step 2: if the fallback result is also empty, send the Greek empty-state message "Δεν υπάρχουν εγγεγραμμένοι πελάτες." and return. Step 3: if the fallback returns clients, build an InlineKeyboard the same way as the primary path — each row is a single-button array where callback_data is "billing:client:{client.clientBusinessRelationshipId}" and label is client.clientName ?? client.senderPhone (senderPhone as fallback since all-time clients may have no name and no serviceNameFallback). Send with sendTelegramMessageWithKeyboard using the same prompt text "👤 Ποιος πελάτης έκανε πληρωμή;" and return. The original early-return path (clients returned by 30-day query) is unchanged.

    In tests/billing-payment-flow.test.ts:
    Three changes:
    (1) In the jest.mock('../src/billing/queries', ...) factory object, add getAllClientsForBusiness: jest.fn() alongside the existing mocked functions.
    (2) After the existing mock cast for mockGetRecentClients (currently around line 64), add: const mockGetAllClients = billingQueries.getAllClientsForBusiness as jest.Mock;
    (3) Update the existing test named "sends Greek empty-state message when no recent clients" (currently around line 148): add mockGetAllClients.mockResolvedValue([]) before the showClientSelection call (so the fallback also returns empty), and change the toHaveBeenCalledWith expectation to match the new all-empty message "Δεν υπάρχουν εγγεγραμμένοι πελάτες."
    (4) Add a new test "falls back to all-time clients keyboard when no recent bookings" inside the showClientSelection describe block: set mockGetRecentClients.mockResolvedValue([]) and mockGetAllClients.mockResolvedValue([{ clientBusinessRelationshipId: 300, clientName: 'Χρήστος', senderPhone: '+306900000099' }]). Call showClientSelection(BUSINESS_ID, OWNER_TELEGRAM_ID). Assert mockSendKeyboard was called once, that keyboard[0][0].callback_data equals 'billing:client:300', and that keyboard[0][0].text equals 'Χρήστος'.
  </action>
  <verify>
    <automated>cd /Users/manolis/Documents/RandevuClaw && npx tsc --noEmit && npm test -- tests/billing-payment-flow.test.ts --testTimeout=10000</automated>
  </verify>
  <done>
    getAllClientsForBusiness is exported from billing/queries.ts. showClientSelection shows an all-time client keyboard when 30-day query returns empty. When both queries return empty, sends "Δεν υπάρχουν εγγεγραμμένοι πελάτες." billing-payment-flow.test.ts passes including the new fallback test.
  </done>
</task>

<task type="auto">
  <name>Task 2: G-07-5 — Fix deactivate_package to name-based lookup</name>
  <files>src/onboarding/ai-owner-agent.ts, src/billing/tools.ts</files>
  <action>
    G-07-5 root cause: deactivate_package FunctionDeclaration requires package_id (integer). Gemini has no way to know numeric IDs since list_packages output omits IDs. Gemini hallucinates the ID, causing the wrong package to be deactivated. handleDeactivatePackage returns a generic success string with no package name echo so Gemini cannot detect the mismatch.

    In src/onboarding/ai-owner-agent.ts:
    (1) Add a new import statement: import { listPackages } from '../billing/queries'; — this is needed for the name-resolution lookup inside the deactivate_package case. Do not add any other symbols from billing/queries in this import.
    (2) In the ToolArgs interface: remove package_id?: number and add package_name?: string in its place (same line position, Phase 7 billing fields comment block).
    (3) In the OWNER_TOOLS array, find the deactivate_package tool definition and replace the package_id property (type 'integer') with a package_name property (type 'string', description "Όνομα πακέτου (partial match OK), π.χ. 'Μηνιαίο'" — matching the style of delete_service). Update the required array from ['package_id'] to ['package_name'].
    (4) In executeOwnerTool, replace the entire deactivate_package case body. The new implementation: extract packageName as String(args.package_name ?? '').trim(). If packageName is empty, return "Δεν δόθηκε όνομα πακέτου.". Wrap the remaining logic in withBusinessContext(business.id, async () => {...}): call listPackages(business.id) to get active packages, find the first match via packages.find(p => p.name.toLowerCase().includes(packageName.toLowerCase())), if no match return the Greek not-found message Δεν βρέθηκε ενεργό πακέτο με όνομα "${packageName}"., if match found call handleDeactivatePackage(business.id, match.id, match.name) and return the result.

    In src/billing/tools.ts:
    Update handleDeactivatePackage signature to add an optional third parameter packageName?: string after packageId. In the success branch (when deactivated is true): if packageName is truthy, return the Greek success string with the package name interpolated — Το πακέτο "${packageName}" απενεργοποιήθηκε. Υπάρχουσες συνδρομές δεν επηρεάζονται. — if packageName is undefined (backward-compatible path for direct calls without a name), return the original generic string unchanged. No other changes to handleDeactivatePackage.
  </action>
  <verify>
    <automated>cd /Users/manolis/Documents/RandevuClaw && npx tsc --noEmit && npm test -- tests/billing-package-deactivate.test.ts --testTimeout=10000</automated>
  </verify>
  <done>
    deactivate_package FunctionDeclaration uses package_name: string. executeOwnerTool deactivate_package case resolves name to ID via listPackages partial match. handleDeactivatePackage echoes package name when provided. billing-package-deactivate.test.ts still passes (backward-compatible — existing tests call without packageName and receive the generic success string). TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payment-flow.ts → clientBusinessRelationships | getAllClientsForBusiness uses getConn() which enforces RLS via withBusinessContext — only returns clients of the current business |
| Gemini NLU → deactivate_package case | package_name arrives from Gemini (untrusted); resolved via DB lookup against RLS-filtered active packages for this business only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-GC-01 | Information Disclosure | getAllClientsForBusiness | medium | mitigate | Uses getConn() inside withBusinessContext so RLS filters to current business only; no cross-tenant client data returned |
| T-07-GC-02 | Tampering | deactivate_package name resolution | low | accept | Gemini-provided package_name matches against active packages of the caller's own business (RLS-scoped). Worst case: partial match hits a different package in the same business — the success reply echoes the matched name so the owner sees which package was deactivated and can correct if wrong |
| T-07-GC-03 | Elevation | deactivate_package cross-tenant | high | mitigate | listPackages uses getConn() inside withBusinessContext scoped to business.id — cannot reach packages of a different business even with a crafted package_name |
</threat_model>

<verification>
After plan completion:
- npx tsc --noEmit exits 0 (all type changes compile cleanly)
- npm test -- tests/billing-payment-flow.test.ts --testTimeout=10000 exits 0 including fallback test
- npm test -- tests/billing-package-deactivate.test.ts --testTimeout=10000 exits 0 (backward-compatible — no test changes needed)
- grep output: src/billing/queries.ts contains getAllClientsForBusiness function export
- grep output: src/onboarding/ai-owner-agent.ts OWNER_TOOLS deactivate_package uses package_name not package_id
</verification>

<success_criteria>
- G-07-6 closed: payment recording flow shows client keyboard even when no bookings exist in last 30 days
- G-07-5 closed: deactivate_package resolves by name, not hallucinated ID; success reply echoes package name
- All billing tests green: npm test -- tests/billing-*.test.ts exits 0
</success_criteria>

<output>
Create `.planning/phases/07-billing-configuration-payment-recording/07-06-SUMMARY.md` when done.
</output>
