---
phase: 07-billing-configuration-payment-recording
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/webhooks/telegram.ts
  - .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md
autonomous: true
gap_closure: true
gap_ids: [G-07-2]
requirements:
  - BILL-01
  - PAY-01

must_haves:
  truths:
    - "TelegramCallbackQuery interface includes message?: { message_id: number } so the keyboard message ID is available in callback handlers"
    - "After billing:pkg_confirm branch, editTelegramMessageReplyMarkup is called to clear the Ναι/Όχι keyboard"
    - "After billing:pkg_cancel branch, editTelegramMessageReplyMarkup is called to clear the Ναι/Όχι keyboard"
    - "After billing:mem_confirm branch, editTelegramMessageReplyMarkup is called to clear the Ναι/Όχι keyboard"
    - "After billing:mem_cancel branch, editTelegramMessageReplyMarkup is called to clear the Ναι/Όχι keyboard"
    - "npx tsc --noEmit exits 0"
  artifacts:
    - src/webhooks/telegram.ts
    - .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md
  key_links:
    - "TelegramCallbackQuery.message?.message_id → editTelegramMessageReplyMarkup(senderTelegramId, callbackQuery.message.message_id, []) guarded by optional-chain check in each of the 4 terminal billing branches"
---

<objective>
Close UAT failure G-07-2: after the owner taps Ναι or Όχι on a billing confirmation keyboard (package creation or membership confirmation), the inline keyboard buttons remain visible on the message instead of disappearing.

Root cause: TelegramCallbackQuery interface is missing message?: { message_id: number } so the keyboard message ID is silently dropped. The 4 billing terminal branches (pkg_confirm, pkg_cancel, mem_confirm, mem_cancel) in handleCallbackQuery never call editTelegramMessageReplyMarkup. Booking branches already clear their keyboard at line 356-358 using ownerTelegramMessageId from DB; billing has no equivalent.

Fix: Extend TelegramCallbackQuery interface with message?: { message_id: number }, then add a guarded editTelegramMessageReplyMarkup call after each of the 4 terminal billing branches. Also update COVERAGE.md to change editMessageReplyMarkup from OPT-OUT to INTEGRATE (terminal billing steps only).

Purpose: Owner UX parity with the booking flow — pressing a confirmation button clears the button row so the owner cannot accidentally re-tap a stale keyboard.
Output: Updated src/webhooks/telegram.ts and updated COVERAGE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-billing-configuration-payment-recording/07-CONTEXT.md
@.planning/phases/07-billing-configuration-payment-recording/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: G-07-2 — Extend TelegramCallbackQuery and clear billing keyboard after terminal branches</name>
  <files>src/webhooks/telegram.ts, .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md</files>
  <action>
    G-07-2 root cause: TelegramCallbackQuery interface (lines 46-50 in telegram.ts) is missing message?: { message_id: number }. Without this field, the message_id from the callback payload is silently dropped and the billing terminal branches cannot reference it. The booking branches clear the keyboard using ownerTelegramMessageId fetched from the DB (line 356-358); billing has no such DB column, so it must use callbackQuery.message.message_id from the Telegram payload directly.

    In src/webhooks/telegram.ts, two changes:

    Change 1 — Interface extension: in the TelegramCallbackQuery interface (currently { id: string; from: TelegramFrom; data?: string }), add message?: { message_id: number } as a fourth optional field. The message field from Telegram contains the full message object; we only need message_id to call editMessageReplyMarkup, so the nested type is minimal.

    Change 2 — Keyboard clearing after each billing terminal branch: in the handleCallbackQuery function, inside the "firstId" in parsed block, add a keyboard-clearing call after each of the four terminal branches. editTelegramMessageReplyMarkup is already imported at line 17 from ../telegram/client. The call signature is editTelegramMessageReplyMarkup(senderTelegramId, messageId, []) where [] clears all buttons.

    The four terminal branches and what to add after each:

    After the billing:mem_confirm branch (after the await handleConfirmMembership(...) call): add a guarded keyboard clear using the optional-chain pattern — if callbackQuery.message?.message_id is defined (truthy), call await editTelegramMessageReplyMarkup(senderTelegramId, callbackQuery.message.message_id, []).

    After the billing:mem_cancel branch (after the await sendTelegramMessage(senderTelegramId, '❌ Ακυρώθηκε η πληρωμή.') call): add the same guarded keyboard-clearing call.

    After the billing:pkg_confirm branch (after the await handleConfirmPackage(...) call): add the same guarded keyboard-clearing call.

    After the billing:pkg_cancel branch (after the await handleCancelPackage(...) call): add the same guarded keyboard-clearing call.

    The intermediate branches (billing:client and billing:package) do NOT need keyboard clearing — they send a new keyboard message as the next step; clearing the previous keyboard is not required by the UAT and would change the UX in unintended ways.

    Use the optional-chain guard pattern (callbackQuery.message?.message_id) before each editTelegramMessageReplyMarkup call because Telegram marks message as optional in the callback_query spec. If message_id is absent (undefined), skip the clear silently — do not throw.

    In .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md:
    In Table 1 (Telegram Bot API), find the editMessageReplyMarkup row. Change its Decision cell from "OPT-OUT" to "INTEGRATE" and update its Reason cell to: "Applied to billing terminal confirmation steps only (billing:pkg_confirm, billing:pkg_cancel, billing:mem_confirm, billing:mem_cancel) — clears the Ναι/Όχι keyboard after owner taps either button so stale buttons do not linger. Intermediate steps (client selection, package selection) continue to send new messages. G-07-2."
  </action>
  <verify>
    <automated>cd /Users/manolis/Documents/RandevuClaw && npx tsc --noEmit</automated>
  </verify>
  <done>
    TelegramCallbackQuery interface has message?: { message_id: number }. Each of the 4 terminal billing branches (pkg_confirm, pkg_cancel, mem_confirm, mem_cancel) calls editTelegramMessageReplyMarkup guarded by callbackQuery.message?.message_id optional chain. TypeScript compiles without errors (the message field access is type-safe). COVERAGE.md editMessageReplyMarkup row updated to INTEGRATE.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram payload → callbackQuery.message.message_id | message_id comes from Telegram's server-signed payload; used to clear the caller's own keyboard in the same chat |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-GC-04 | Tampering | editTelegramMessageReplyMarkup with payload message_id | low | accept | Worst case: a crafted message_id in the callback payload causes editMessageReplyMarkup to fail with a Telegram API error (trying to edit a message the bot did not send). This is a no-op failure — no DB write occurs. The keyboard-clearing call is best-effort; a Telegram API error here does not affect the underlying business operation (package was already activated/cancelled). |
| T-07-GC-05 | Spoofing | Non-owner calling billing terminal callbacks | high | mitigate | Already mitigated upstream in handleCallbackQuery: findBusinessByOwnerTelegramId(senderTelegramId) runs before any billing branch (lines 235-240 in telegram.ts). If the caller is not the owner, the function returns early before reaching any terminal branch or keyboard-clearing call. |
</threat_model>

<verification>
After plan completion:
- npx tsc --noEmit exits 0
- grep -c 'message?.message_id' src/webhooks/telegram.ts returns >= 4 (one per terminal branch)
- COVERAGE.md editMessageReplyMarkup row shows INTEGRATE
</verification>

<success_criteria>
- G-07-2 closed: Ναι/Όχι keyboard disappears after owner taps either button on billing confirmations
- TypeScript compiles cleanly with the extended TelegramCallbackQuery interface
- COVERAGE.md accurately reflects that editMessageReplyMarkup is now used for terminal billing steps
</success_criteria>

<output>
Create `.planning/phases/07-billing-configuration-payment-recording/07-07-SUMMARY.md` when done.
</output>
