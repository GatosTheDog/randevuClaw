---
phase: 07-billing-configuration-payment-recording
plan: 03
type: execute
wave: 2
depends_on:
  - "07-02"
files_modified:
  - src/billing/queries.ts
  - src/database/queries.ts
  - tests/helpers/billing-fixtures.ts
  - tests/billing-membership-creation.test.ts
  - tests/billing-dst-arithmetic.test.ts
  - tests/billing-view-membership.test.ts
autonomous: true
requirements:
  - PAY-02
  - BILL-01
  - BILL-02
  - BILL-03
  - PAY-01
  - PAY-03
assumption_delta_decision: "no-change — selectable packages are a new feature introduction, not a model generalization"

must_haves:
  truths:
    - "src/billing/queries.ts exports 8 async functions covering full billing CRUD lifecycle"
    - "createMembership uses addCalendarDays from utils/timezone.ts to compute expiresAt in Europe/Athens timezone (DST-safe)"
    - "createMembership atomically inserts membership + membership_ledger row in a single db.transaction()"
    - "membership_ledger insert uses idempotencyKey format business_id:clientPhone:payment_recorded:purchaseDate to prevent duplicate ledger rows on webhook replay"
    - "insertClientBusinessRelationship in database/queries.ts accepts optional clientName parameter and upserts it on every call"
    - "billing-fixtures.ts exports insertTestPackage and insertTestMembership helpers that bypass the confirmation flow for test setup"
    - "npm test -- tests/billing-membership-creation.test.ts tests/billing-dst-arithmetic.test.ts --testTimeout=10000 exits 0 (green)"
  artifacts:
    - src/billing/queries.ts
    - tests/helpers/billing-fixtures.ts
  key_links:
    - "billing/queries.ts imports billingPackages, memberships, membershipLedger from src/database/schema.ts (07-02)"
    - "billing/queries.ts imports addCalendarDays and isoDateInAthens from src/utils/timezone.ts"
    - "billing/queries.ts uses getConn() from src/database/queries.ts for RLS-enabled connections"
    - "billing-fixtures.ts imports db (admin connection) from src/database/db.ts for test setup without RLS"
---

<objective>
Build the billing query layer: all database CRUD functions for packages, memberships, and ledger entries. Update the client relationship upsert to capture client_name. Create test fixtures and complete the PAY-02 and PAY-03 test suites.

Purpose: This query layer is the foundation for Wave 3 tool handlers and payment flow handlers. All DB operations in the billing domain go through these typed functions.
Output: src/billing/queries.ts (8 exported functions), updated src/database/queries.ts (client_name upsert), tests/helpers/billing-fixtures.ts, and green tests for membership creation DST arithmetic.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-billing-configuration-payment-recording/07-CONTEXT.md
@.planning/phases/07-billing-configuration-payment-recording/07-PATTERNS.md
@.planning/phases/07-billing-configuration-payment-recording/07-02-SUMMARY.md
@src/database/schema.ts
@src/database/queries.ts
@src/onboarding/queries.ts
@src/utils/timezone.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/billing/queries.ts and complete DST + membership tests</name>
  <files>
    src/billing/queries.ts,
    tests/billing-membership-creation.test.ts,
    tests/billing-dst-arithmetic.test.ts
  </files>
  <read_first>
    src/database/schema.ts — billingPackages, memberships, membershipLedger, clientBusinessRelationships, bookings, services table definitions and TypeScript inferred types
    src/onboarding/queries.ts — query function signature pattern: async function, typed return, getConn() usage, onConflictDoUpdate pattern
    src/database/queries.ts — getConn() export, withBusinessContext signature, db.transaction() usage
    src/utils/timezone.ts — isoDateInAthens and addCalendarDays function signatures and return types
    .planning/phases/07-billing-configuration-payment-recording/07-PATTERNS.md — Pattern 3 (rolling expiry with DST-safe arithmetic), Pattern 4 (immutable ledger), billing/queries.ts import pattern section
  </read_first>
  <behavior>
    - createPackage(businessId, data): inserts with isActive false, returns inserted row id — used for D-03 pending-confirmation flow
    - activatePackage(packageId): UPDATE SET is_active = true WHERE id = packageId AND is_active = false, returns boolean (true if row updated)
    - cancelPendingPackage(packageId): DELETE WHERE id = packageId AND is_active = false
    - listPackages(businessId): returns array of BillingPackage WHERE is_active = true ORDER BY createdAt DESC
    - deactivatePackage(packageId): UPDATE SET is_active = false WHERE id = packageId
    - getRecentClientsForBusiness(businessId, dayWindowDays): returns clients with bookings in the last dayWindowDays calendar days; result includes clientBusinessRelationshipId, clientName (nullable), serviceNameFallback (service name of most recent booking), lastBookingDateFormatted (YYYY-MM-DD)
    - createMembership(businessId, clientPhone, packageId): runs db.transaction with SELECT of package, compute expiresAt via isoDateInAthens + addCalendarDays, upsert membership (onConflictDoUpdate replacing existing active membership for same business+client), insert initial membershipLedger row with operationType 'payment_recorded' and idempotencyKey `${businessId}:${clientPhone}:payment_recorded:${purchaseDate}`, return { memberId, expiresAtDate, sessionsRemaining }
    - getClientActiveMembership(businessId, clientPhone): SELECT memberships JOIN billingPackages WHERE isActive = true AND expiresAt > NOW(), return { packageName, sessionsRemaining, expiresAt, isUnlimited: boolean } or null
    - DST test: addCalendarDays('2024-09-22', 30) === '2024-10-22' (Greek DST ends last Sunday of October — Sept 22 + 30 days does NOT cross DST, result Oct 22)
    - DST test: addCalendarDays('2024-10-25', 7) === '2024-11-01' (crosses Greek DST end Oct 27 2024, still correct)
  </behavior>
  <action>
    Create the src/billing/ directory and src/billing/queries.ts file.

    Import from drizzle-orm: eq, and, desc, sql, gt, lt. Import from src/database/schema: billingPackages, memberships, membershipLedger, clientBusinessRelationships, bookings, services. Import db from src/database/db (for admin operations like tx.insert inside transaction). Import getConn from src/database/queries (for RLS-enabled reads). Import isoDateInAthens and addCalendarDays from src/utils/timezone. Import logger from src/utils/logger.

    Export two TypeScript interfaces at the top of the file:
    - BillingPackage: matching the Drizzle inferred type of billingPackages table (id, businessId, name, priceCents, validDays, sessionCount: number | null, isActive, createdAt)
    - Membership: matching memberships table inferred type (id, businessId, clientPhone, packageId, purchaseDate, expiresAt, sessionsRemaining: number | null, isActive, createdAt)

    Also export type RecentClient: { clientBusinessRelationshipId: number; clientName: string | null; serviceNameFallback: string; lastBookingDateFormatted: string }

    Implement createPackage: insert into billingPackages using db.insert().values({ businessId, name, priceCents, validDays, sessionCount, isActive: false }).returning(); return rows[0].id as number.

    Implement activatePackage: use db.update(billingPackages).set({ isActive: true }).where(and(eq(billingPackages.id, packageId), eq(billingPackages.isActive, false))).returning(); return rows.length > 0.

    Implement cancelPendingPackage: use db.delete(billingPackages).where(and(eq(billingPackages.id, packageId), eq(billingPackages.isActive, false))).

    Implement listPackages: use getConn().select().from(billingPackages).where(and(eq(billingPackages.businessId, businessId), eq(billingPackages.isActive, true))).orderBy(desc(billingPackages.createdAt)); return typed array.

    Implement deactivatePackage: use db.update(billingPackages).set({ isActive: false }).where(eq(billingPackages.id, packageId)).

    Implement getRecentClientsForBusiness: compute a cutoff date string using isoDateInAthens applied to (new Date() minus dayWindowDays * 86400000 ms). Query: SELECT DISTINCT clientBusinessRelationships.id, clientBusinessRelationships.clientName, services.name as serviceNameFallback, bookings.calendarDate as lastBookingDate FROM clientBusinessRelationships JOIN bookings ON bookings.clientPhone = clientBusinessRelationships.senderPhone AND bookings.businessId = clientBusinessRelationships.businessId JOIN services ON services.id = bookings.serviceId WHERE clientBusinessRelationships.businessId = businessId AND bookings.calendarDate >= cutoffDate ORDER BY lastBookingDate DESC. Map to RecentClient array using the clientName and a formatted lastBookingDateFormatted field.

    Implement createMembership: use db.transaction(async (tx) => { ... }). Inside transaction: (a) fetch package via tx.select from billingPackages where id = packageId; (b) compute purchaseDate = isoDateInAthens(new Date()); (c) compute expiresAtDate = addCalendarDays(purchaseDate, pkg.validDays); (d) compute expiresAt = new Date(`${expiresAtDate}T23:59:59+02:00`) (end of day in Athens — note: use +03:00 during Greek DST summer, so it's safer to use the string date and let Postgres handle TZ — actually store it as a JavaScript Date using the computed string); (e) insert or update membership via tx.insert(memberships).values({ businessId, clientPhone, packageId, purchaseDate, expiresAt, sessionsRemaining: pkg.sessionCount, isActive: true }).onConflictDoUpdate({ target: [memberships.businessId, memberships.clientPhone], set: { packageId, purchaseDate, expiresAt, sessionsRemaining: pkg.sessionCount, isActive: true } }).returning(); (f) insert ledger row via tx.insert(membershipLedger).values({ membershipId: rows[0].id, operationType: 'payment_recorded', sessionsDeducted: 0, reason: 'Payment recorded by owner', idempotencyKey: `${businessId}:${clientPhone}:payment_recorded:${purchaseDate}` }); return { memberId: rows[0].id, expiresAtDate, sessionsRemaining: pkg.sessionCount }.

    Implement getClientActiveMembership: query memberships JOIN billingPackages WHERE memberships.businessId = businessId AND memberships.clientPhone = clientPhone AND memberships.isActive = true AND memberships.expiresAt > new Date(); return { packageName: pkg.name, sessionsRemaining, expiresAt, isUnlimited: sessionsRemaining === null } or null if no rows.

    Then complete tests/billing-membership-creation.test.ts and tests/billing-dst-arithmetic.test.ts. The billing-dst-arithmetic tests can import addCalendarDays directly from src/utils/timezone (no DB needed). The billing-membership-creation tests import insertTestBusiness from tests/helpers/test-business.ts, insertTestPackage from tests/helpers/billing-fixtures.ts (which will exist after Task 3), and createMembership from src/billing/queries.ts. For membership creation tests, use beforeAll to call insertTestBusiness, and test createMembership with real DB calls using withBusinessContext. Use jest.spyOn on isoDateInAthens to control the "today" date for expiry assertions. The DST test (billing-dst-arithmetic.test.ts) only needs addCalendarDays — no DB, no fixtures.
  </action>
  <acceptance_criteria>
    - `grep -c 'export async function' src/billing/queries.ts` returns >= 8
    - `grep -c 'addCalendarDays' src/billing/queries.ts` returns >= 1
    - `grep -c 'db.transaction' src/billing/queries.ts` returns >= 1
    - `grep -c 'idempotencyKey' src/billing/queries.ts` returns >= 1
    - `grep -c 'payment_recorded' src/billing/queries.ts` returns >= 1
    - `npx tsc --noEmit` exits 0
    - `npm test -- tests/billing-dst-arithmetic.test.ts --testTimeout=10000` exits 0 (no todos, all green)
    - DST test assertions confirm addCalendarDays('2024-09-22', 30) === '2024-10-22'
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test -- tests/billing-dst-arithmetic.test.ts --testTimeout=10000</automated>
  </verify>
  <done>src/billing/queries.ts exports 8 typed async functions; billing-dst-arithmetic tests pass green; TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update src/database/queries.ts — client_name upsert on every message (D-04)</name>
  <files>src/database/queries.ts</files>
  <read_first>
    src/database/queries.ts — full insertClientBusinessRelationship function (approximately lines 147-172); also review the function's callers to understand what parameters they currently pass
    src/database/schema.ts — clientBusinessRelationships table definition; confirm clientName column is present after Task 1 of 07-02
    .planning/phases/07-billing-configuration-payment-recording/07-PATTERNS.md — database/queries.ts section: "Modify pattern to upsert client_name" showing onConflictDoUpdate with clientName in the set clause
    .planning/phases/07-billing-configuration-payment-recording/07-CONTEXT.md — D-04: upsert clientName on every message (not skip), always reflect latest Telegram display name
  </read_first>
  <action>
    Modify insertClientBusinessRelationship in src/database/queries.ts to accept a third parameter clientName: string | undefined.

    Add the clientName field to the .values({ businessId, senderPhone, clientName, consentGiven: true, consentTimestamp: new Date() }) object.

    Change the existing .onConflictDoNothing() to .onConflictDoUpdate({ target: [clientBusinessRelationships.businessId, clientBusinessRelationships.senderPhone], set: { clientName } }) so that every call upserts the clientName even if the relationship row already exists. This implements D-04: always reflect the latest Telegram display name, never skip.

    Remove the existing "if rows[0] return rows[0]" / "existing =" fallback block since onConflictDoUpdate now always returns a row. The function body simplifies to: insert with onConflictDoUpdate, return rows[0].

    Add a JSDoc comment above the function citing D-04 and explaining that clientName is upserted (not skipped) to always reflect the latest Telegram from.first_name.

    The ClientBusinessRelationship interface exported from this file must be updated to include clientName: string | null if it does not already have it.

    The existing callers of insertClientBusinessRelationship (in webhooks/telegram.ts and possibly elsewhere) do not need to be changed immediately — the new parameter is optional (undefined is valid and upserts null). In 07-05, the caller in telegram.ts will be updated to pass from.first_name. For now, undefined as the third argument is backward-compatible.
  </action>
  <acceptance_criteria>
    - `grep -c 'clientName' src/database/queries.ts` returns >= 2 (in values and in set)
    - `grep -c 'onConflictDoUpdate' src/database/queries.ts` returns >= 1 in the insertClientBusinessRelationship function
    - `grep -c 'onConflictDoNothing' src/database/queries.ts` returns 0 in insertClientBusinessRelationship (replaced by onConflictDoUpdate)
    - `npx tsc --noEmit` exits 0 — existing callers compile without modification
    - `npm test` exits 0 — all pre-existing tests remain green (no regression from signature change)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test --testTimeout=10000</automated>
  </verify>
  <done>insertClientBusinessRelationship upserts clientName on every call; no existing tests broken; TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/helpers/billing-fixtures.ts and complete billing-view-membership stubs</name>
  <files>
    tests/helpers/billing-fixtures.ts,
    tests/billing-membership-creation.test.ts,
    tests/billing-view-membership.test.ts
  </files>
  <read_first>
    tests/helpers/test-business.ts — admin db usage for test setup, TypeScript interface pattern, JSDoc top comment convention
    src/billing/queries.ts — createPackage, createMembership signatures (just created in Task 1)
    src/database/schema.ts — billingPackages and memberships table column names for direct db.insert in fixtures
    src/database/db.ts — db import (admin connection, no RLS) for test setup writes
  </read_first>
  <behavior>
    - insertTestPackage(businessId, overrides): inserts billing package directly with isActive: true (bypasses D-03 confirmation for tests); defaults: name='Test Package', priceCents=5000, validDays=30, sessionCount=10; returns inserted BillingPackage row
    - insertTestMembership(businessId, clientPhone, packageId, overrides): inserts membership directly; defaults: purchaseDate=today, expiresAt=30 days from now, sessionsRemaining=10, isActive=true; returns inserted Membership row
    - billing-membership-creation tests: createMembership writes to both memberships and membership_ledger in one transaction; idempotencyKey is deterministic for same business+client+purchaseDate; onConflict replaces existing membership
    - billing-view-membership tests: getClientActiveMembership returns packageName, sessionsRemaining, expiresAt, isUnlimited; unlimited membership (sessionCount null) has isUnlimited: true and sessionsRemaining null; expired membership (expiresAt in past) returns null
  </behavior>
  <action>
    Create tests/helpers/billing-fixtures.ts following the exact style of tests/helpers/test-business.ts.

    Add a top comment citing "Phase 7: test fixture helpers for billing_packages and memberships tables" and explaining these helpers bypass the D-03 confirmation flow to directly insert rows for test setup.

    Export interface TestPackageOptions: { name?: string; priceCents?: number; validDays?: number; sessionCount?: number | null; isActive?: boolean }

    Export interface TestMembershipOptions: { purchaseDate?: string; expiresAt?: Date; sessionsRemaining?: number | null; isActive?: boolean }

    Export async function insertTestPackage(businessId: number, overrides?: TestPackageOptions): Promise<BillingPackage>: use db.insert(billingPackages).values({ businessId, name: overrides?.name ?? 'Test Package', priceCents: overrides?.priceCents ?? 5000, validDays: overrides?.validDays ?? 30, sessionCount: overrides?.sessionCount !== undefined ? overrides.sessionCount : 10, isActive: overrides?.isActive !== undefined ? overrides.isActive : true }).returning(); return rows[0] as BillingPackage.

    Export async function insertTestMembership(businessId: number, clientPhone: string, packageId: number, overrides?: TestMembershipOptions): Promise<Membership>: compute default purchaseDate as isoDateInAthens(new Date()), default expiresAt as new Date(Date.now() + 30 * 24 * 60 * 60 * 1000); use db.insert(memberships).values({ businessId, clientPhone, packageId, purchaseDate: overrides?.purchaseDate ?? purchaseDate, expiresAt: overrides?.expiresAt ?? expiresAt, sessionsRemaining: overrides?.sessionsRemaining !== undefined ? overrides.sessionsRemaining : 10, isActive: overrides?.isActive !== undefined ? overrides.isActive : true }).returning(); return rows[0] as Membership.

    Import BillingPackage and Membership types from src/billing/queries.ts. Import db from src/database/db. Import billingPackages and memberships from src/database/schema. Import isoDateInAthens from src/utils/timezone.

    After creating billing-fixtures.ts: update tests/billing-membership-creation.test.ts to replace the it.todo stubs with real test implementations. The tests should: (1) call insertTestBusiness to create a business; (2) call insertTestPackage to create a package; (3) call createMembership and verify the returned expiresAtDate is purchaseDate + validDays; (4) check the membership_ledger row was inserted with operationType 'payment_recorded'; (5) test idempotency: call createMembership twice with the same args — the membership_ledger should have only one row for payment_recorded (second call upserts membership but idempotencyKey collision on ledger is caught — note: the second call will fail on idempotencyKey UNIQUE if same purchaseDate; handle by expecting the expected behavior: one row).

    Update tests/billing-view-membership.test.ts to replace stubs with implementations calling getClientActiveMembership after setting up test data with insertTestBusiness + insertTestPackage + insertTestMembership.
  </action>
  <acceptance_criteria>
    - `grep -c 'export async function insertTestPackage' tests/helpers/billing-fixtures.ts` returns 1
    - `grep -c 'export async function insertTestMembership' tests/helpers/billing-fixtures.ts` returns 1
    - `npx tsc --noEmit` exits 0
    - `npm test -- tests/billing-membership-creation.test.ts tests/billing-view-membership.test.ts --testTimeout=10000` exits 0 (all green, no todos)
    - billing-view-membership test for unlimited membership passes (isUnlimited: true when sessionCount is null)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test -- tests/billing-membership-creation.test.ts tests/billing-view-membership.test.ts --testTimeout=10000</automated>
  </verify>
  <done>billing-fixtures.ts exports two fixture helpers; billing-membership-creation and billing-view-membership tests are green (no todos); TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner chat → billing/queries.ts | Tool handler args must be Zod-validated before reaching DB layer |
| DB transaction boundary | createMembership inserts two rows atomically — failure in either must roll back both |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Spoofing | Telegram callback_query ownership | high | accept (deferred) | Ownership validation implemented in 07-04 payment-flow.ts and 07-05 webhooks/telegram.ts |
| T-07-02 | Tampering | Gemini NLU injection via package name | high | mitigate | All billing/queries.ts functions use Drizzle's parameterized query builder — never string interpolation in SQL; owner-side Zod validation in tools.ts (07-04) validates before reaching this layer |
| T-07-03 | Elevation | Unauthorized billing ops | high | mitigate | listPackages and getClientActiveMembership use getConn() which inherits the RLS context set by withBusinessContext; callers in tools.ts must wrap in withBusinessContext |
| T-07-04 | Tampering | Double membership on webhook replay | high | mitigate | createMembership idempotencyKey format `${businessId}:${clientPhone}:payment_recorded:${purchaseDate}` is deterministic; UNIQUE constraint on membership_ledger idempotency_key causes second INSERT to fail, transaction rolls back the duplicate membership upsert |
| T-07-05 | Tampering | Price tampering in callback_data | medium | accept (deferred) | Price never stored in callback_data — getClientActiveMembership fetches price from DB by packageId; enforced in 07-04 payment-flow.ts |
| T-07-06 | Information Disclosure | Multi-tenant data leak | high | mitigate | All read functions use getConn() for RLS-enforced connection; businessId WHERE clause in all queries provides defense-in-depth even if RLS is misconfigured |
</threat_model>

<verification>
After plan completion:
- `npm test -- tests/billing-*.test.ts --testTimeout=10000` — billing-dst-arithmetic, billing-membership-creation, billing-view-membership are green; remaining 5 stub files are TODO (not yet implemented)
- `npm test` — full suite green (no regressions from database/queries.ts changes)
- `npx tsc --noEmit` — exits 0
</verification>

<success_criteria>
- src/billing/queries.ts with 8 exported typed async functions
- createMembership uses db.transaction() for atomic membership + ledger insert
- DST-safe expiry calculation confirmed by billing-dst-arithmetic.test.ts passing green
- src/database/queries.ts upserts clientName on every insertClientBusinessRelationship call
- tests/helpers/billing-fixtures.ts provides insertTestPackage and insertTestMembership
- billing-membership-creation.test.ts and billing-view-membership.test.ts pass green
- Full existing test suite remains green
</success_criteria>

<output>
Create `.planning/phases/07-billing-configuration-payment-recording/07-03-SUMMARY.md` when done.
</output>
