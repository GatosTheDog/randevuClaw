---
phase: 15-onboarding-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/onboarding/steps.ts
  - src/onboarding/router.ts
  - src/database/queries.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04

must_haves:
  truths:
    - OnboardingStep type in steps.ts gains four new literal values: 'config_booking_mode' | 'config_cancellation_cutoff' | 'config_slotless_requests' | 'config_last_session_threshold'
    - handleSvcMoreStep's 'no' path transitions to 'config_booking_mode' instead of calling handleActivate directly
    - Four new step handler functions exist in steps.ts: handleConfigBookingModeStep, handleConfigCancellationCutoffStep, handleConfigSlotlessRequestsStep, handleConfigLastSessionThresholdStep
    - Each config step has an explicit skip path (owner types 'παράλειψη' or 'skip' or any unrecognized text → use default and advance)
    - handleConfigLastSessionThresholdStep's next step is handleActivate (transitions to done)
    - dispatchOnboardingStep in router.ts routes all four new steps to their handlers
    - Business interface in queries.ts now includes lastSessionThresholdEnabled and lastSessionThresholdCount (if not already added by Phase 14)
  artifacts:
    - src/onboarding/steps.ts — 4 new OnboardingStep values, 4 new handler functions, svc_more transition updated
    - src/onboarding/router.ts — 4 new else-if branches for the new steps
    - src/database/queries.ts — lastSessionThresholdEnabled + lastSessionThresholdCount in Business (if Phase 14 already added them, verify they're present)
  key_links:
    - Config steps use db.update(businesses) directly with the admin db connection (not getConn/withBusinessContext — these are owner setup writes, not client-scoped reads)
    - Each step asks one question with clear default stated, e.g. "(προεπιλογή: open_slots)" or "(προεπιλογή: απενεργοποιημένο)"
    - The four steps form a linear chain: config_booking_mode → config_cancellation_cutoff → config_slotless_requests → config_last_session_threshold → handleActivate
---

<objective>
Add 4 optional config questions to the end of the onboarding flow, covering CONF-01 through CONF-04.

Each question has an explicit default and explicit skip path. Owners who skip all questions get the same defaults as pre-v1.3 (bookingMode='open_slots', cancellationCutoff=off, slotless=off, threshold=off).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: OnboardingStep type + four handler functions in steps.ts</name>
  <files>src/onboarding/steps.ts</files>
  <action>
In steps.ts, extend OnboardingStep type union with:
  | 'config_booking_mode'
  | 'config_cancellation_cutoff'
  | 'config_slotless_requests'
  | 'config_last_session_threshold'

Add necessary imports: `import { db } from '../database/db';` (if not already present), `import { businesses } from '../database/schema';`, `import { eq } from 'drizzle-orm';`.

Update handleSvcMoreStep's `isNo` path to transition to 'config_booking_mode' instead of calling handleActivate:
```typescript
} else if (isNo) {
  await updateOnboardingStep(session.id, 'config_booking_mode', null);
  await sendTelegramMessage(
    ownerTelegramId,
    'Θέλετε να χρησιμοποιήσετε πρόγραμμα τάξεων (ορισμένες ώρες) αντί για ελεύθερες θέσεις;\n' +
    'Απαντήστε "ναι" για πρόγραμμα τάξεων, "όχι" για ελεύθερες θέσεις, ή "παράλειψη" για προεπιλογή (ελεύθερες θέσεις).'
  );
}
```

Add four new handler functions:

--- handleConfigBookingModeStep ---
```typescript
export async function handleConfigBookingModeStep(
  session: OnboardingSession,
  business: Business,
  ownerTelegramId: string,
  text: string
): Promise<void> {
  const normalized = text.trim().toLowerCase();
  const isYes = normalized.includes('ναι') || normalized.includes('yes');
  const isNo = normalized.includes('όχι') || normalized.includes('no') || normalized.includes('παράλειψη') || normalized.includes('skip');

  if (isYes) {
    await db.update(businesses).set({ bookingMode: 'fixed_sessions' }).where(eq(businesses.id, business.id));
  }
  // Default (no or skip): keep 'open_slots' (already the default)

  await updateOnboardingStep(session.id, 'config_cancellation_cutoff', null);
  await sendTelegramMessage(
    ownerTelegramId,
    'Θέλετε όριο ακύρωσης κράτησης (χρόνος πριν από τον οποίο δεν επιστρέφεται πίστωση);\n' +
    'Απαντήστε π.χ. "ναι 8" για 8 ώρες, "όχι" για απενεργοποίηση, ή "παράλειψη".\n(προεπιλογή: απενεργοποιημένο)'
  );
}
```

--- handleConfigCancellationCutoffStep ---
```typescript
export async function handleConfigCancellationCutoffStep(
  session: OnboardingSession,
  business: Business,
  ownerTelegramId: string,
  text: string
): Promise<void> {
  const normalized = text.trim().toLowerCase();
  const isNo = normalized === 'όχι' || normalized === 'no' || normalized.includes('παράλειψη') || normalized.includes('skip');
  const match = text.match(/ναι\s+(\d+)/i);

  if (!isNo && match) {
    const hours = Math.min(168, Math.max(1, parseInt(match[1], 10)));
    await db.update(businesses)
      .set({ cancellationCutoffEnabled: true, cancellationCutoffHours: hours })
      .where(eq(businesses.id, business.id));
  }
  // Default (no or skip or unrecognized): keep disabled

  await updateOnboardingStep(session.id, 'config_slotless_requests', null);
  await sendTelegramMessage(
    ownerTelegramId,
    'Θέλετε να επιτρέπετε αιτήματα κράτησης όταν δεν υπάρχει διαθέσιμη θέση (αναμονή έγκρισης);\n' +
    'Απαντήστε "ναι" ή "όχι".\n(προεπιλογή: απενεργοποιημένο)'
  );
}
```

--- handleConfigSlotlessRequestsStep ---
```typescript
export async function handleConfigSlotlessRequestsStep(
  session: OnboardingSession,
  business: Business,
  ownerTelegramId: string,
  text: string
): Promise<void> {
  const normalized = text.trim().toLowerCase();
  const isYes = normalized.includes('ναι') || normalized.includes('yes');

  if (isYes) {
    await db.update(businesses)
      .set({ slotlessRequestsEnabled: true })
      .where(eq(businesses.id, business.id));
  }
  // Default (no, skip, unrecognized): keep disabled

  await updateOnboardingStep(session.id, 'config_last_session_threshold', null);
  await sendTelegramMessage(
    ownerTelegramId,
    'Θέλετε ειδοποίηση ανανέωσης όταν ένας πελάτης έχει λίγα εναπομείναντα μαθήματα;\n' +
    'Απαντήστε π.χ. "ναι 2" για ειδοποίηση στα 2 μαθήματα, "όχι" ή "παράλειψη".\n(προεπιλογή: απενεργοποιημένο)'
  );
}
```

--- handleConfigLastSessionThresholdStep ---
```typescript
export async function handleConfigLastSessionThresholdStep(
  session: OnboardingSession,
  business: Business,
  ownerTelegramId: string,
  text: string
): Promise<void> {
  const normalized = text.trim().toLowerCase();
  const isNo = normalized === 'όχι' || normalized === 'no' || normalized.includes('παράλειψη') || normalized.includes('skip');
  const match = text.match(/ναι\s+(\d+)/i);

  if (!isNo && match) {
    const count = Math.min(20, Math.max(1, parseInt(match[1], 10)));
    await db.update(businesses)
      .set({ lastSessionThresholdEnabled: true, lastSessionThresholdCount: count })
      .where(eq(businesses.id, business.id));
  }
  // Default (no or skip): keep disabled

  // All config questions done — proceed to activate.
  await handleActivate(session, business, ownerTelegramId);
}
```

Make sure imports are correct. The `db` import should come from '../database/db' and `businesses` from '../database/schema'.

Verify: npx tsc --noEmit 2>&1 | tail -5
  </action>
  <done>OnboardingStep union has 4 new values. handleSvcMoreStep 'no' path transitions to config_booking_mode. Four new handler functions exported. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: dispatchOnboardingStep in router.ts</name>
  <files>src/onboarding/router.ts</files>
  <action>
In router.ts, import the four new handler functions:
```typescript
import {
  handleNameStep,
  handleHoursQueryStep,
  handleHoursRangeStep,
  handleSvcNameStep,
  handleSvcPriceStep,
  handleSvcDurationStep,
  handleSvcMoreStep,
  handleConfigBookingModeStep,
  handleConfigCancellationCutoffStep,
  handleConfigSlotlessRequestsStep,
  handleConfigLastSessionThresholdStep,
} from './steps';
```

In dispatchOnboardingStep, add four else-if branches after the `svc_more` branch and before the `done` branch:

```typescript
} else if (step === 'config_booking_mode') {
  await handleConfigBookingModeStep(session, business, ownerTelegramId, messageText);
} else if (step === 'config_cancellation_cutoff') {
  await handleConfigCancellationCutoffStep(session, business, ownerTelegramId, messageText);
} else if (step === 'config_slotless_requests') {
  await handleConfigSlotlessRequestsStep(session, business, ownerTelegramId, messageText);
} else if (step === 'config_last_session_threshold') {
  await handleConfigLastSessionThresholdStep(session, business, ownerTelegramId, messageText);
} else if (step === 'done') {
```

Verify: npx tsc --noEmit 2>&1 | tail -5
  </action>
  <done>dispatchOnboardingStep routes all 4 new steps. TypeScript compiles.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit 2>&1 | tail -5
</verification>

<success_criteria>
- TypeScript compiles all modified files without errors
- OnboardingStep has 4 new literal values
- handleSvcMoreStep 'no' path goes to config_booking_mode
- Four handler functions exist in steps.ts with skip paths
- dispatchOnboardingStep handles all 4 new steps
- Owners who skip all config questions get the same defaults as pre-v1.3
</success_criteria>

<output>
Create .planning/phases/15-onboarding-extensions/15-01-SUMMARY.md when done
</output>
