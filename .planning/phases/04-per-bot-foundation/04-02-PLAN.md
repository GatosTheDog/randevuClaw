---
phase: "04"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/telegram/registry.ts
  - src/utils/logger.ts
  - tests/jest.setup.ts
autonomous: true
requirements:
  - BOT-03
  - BOT-04
must_haves:
  truths:
    - "telegraf@4.16.3 is in package.json dependencies"
    - "src/telegram/registry.ts exports getOrCreateBotInstance, getBotInstance, clearBotRegistry functions; module-level Map stores one Telegraf instance per webhookId (D-03)"
    - "src/utils/logger.ts redacts botToken and webhookSecret at field, wildcard, and config namespace levels; deprecated telegramBotToken and telegramWebhookSecret paths are removed (D-08)"
    - "tests/jest.setup.ts sets TEST_BOT_1_TOKEN, TEST_BOT_1_WEBHOOK_SECRET, TEST_BOT_1_WEBHOOK_ID, TEST_BOT_2_TOKEN, TEST_BOT_2_WEBHOOK_SECRET, TEST_BOT_2_WEBHOOK_ID via ??= operator (D-09); no longer sets TELEGRAM_BOT_TOKEN or TELEGRAM_WEBHOOK_SECRET"
  artifacts:
    - src/telegram/registry.ts
    - src/utils/logger.ts
    - tests/jest.setup.ts
  key_links:
    - "registry.ts Map<webhookId, Telegraf> is the lookup used by the refactored webhook handler (Plan 04-04)"
    - "logger.ts redact paths include botToken and webhookSecret so logger.info({ business }) never leaks secrets even if business object is logged (STATE.md blocker)"
    - "jest.setup.ts TEST_BOT_1_WEBHOOK_ID matches the webhookId used in telegram-webhook.test.ts mock setup (Plan 04-05)"
---

<objective>
Install Telegraf and establish the per-bot instance registry plus test/logger infrastructure.

Purpose: Plan 04-03 (queries refactor) and Plan 04-04 (webhook refactor) both import from registry.ts. The logger and test env setup must be done in Wave 1 so all later plans can rely on correct redaction and test env baseline.

Output: Telegraf installed; registry.ts singleton Map; pino redaction updated; jest.setup.ts updated with per-bot test vars.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-per-bot-foundation/04-CONTEXT.md
@.planning/phases/04-per-bot-foundation/04-RESEARCH.md
@.planning/phases/04-per-bot-foundation/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install telegraf + create src/telegram/registry.ts</name>
  <files>src/telegram/registry.ts, package.json</files>
  <read_first>
    - src/telegram/client.ts — understand import/export/logging patterns to replicate in registry.ts
    - .planning/phases/04-per-bot-foundation/04-CONTEXT.md §D-03 — locked: one Telegraf instance per bot in a Map
    - .planning/phases/04-per-bot-foundation/04-RESEARCH.md §"Pattern 1: Per-Bot Telegraf Registry" — canonical implementation
    - .planning/phases/04-per-bot-foundation/04-PATTERNS.md §"src/telegram/registry.ts" — imports pattern and full module structure
  </read_first>
  <action>
Install Telegraf:
Run `npm install telegraf@4.16.3` from the project root. The RESEARCH.md Package Legitimacy Audit confirms telegraf is [OK/Approved] — no checkpoint needed.

Create `src/telegram/registry.ts` as a new file. Module structure:

Imports:
- `import { Telegraf } from 'telegraf';`
- `import { logger } from '../utils/logger';`

Module-level singleton Map (D-03):
- `const botRegistry = new Map<string, Telegraf>();`
- Type alias for clarity: `// Map key is webhookId (UUID, not bot token — D-04)`

Export `getOrCreateBotInstance(webhookId: string, botToken: string): Telegraf`:
- Check `botRegistry.has(webhookId)` — return existing instance if found
- Create `new Telegraf(botToken)` — do NOT call `bot.launch()` in webhook mode (the webhook handler calls `.handleUpdate()` per D-03)
- Log `logger.info({ webhookId }, 'Registering new Telegraf instance')` — log webhookId (UUID), never botToken
- Store in Map and return

Export `getBotInstance(webhookId: string): Telegraf | undefined`:
- Return `botRegistry.get(webhookId)`

Export `clearBotRegistry(): void`:
- `botRegistry.clear()` — used in tests (beforeEach/afterEach) to reset state
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=telegram-client 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cat package.json | grep telegraf` shows telegraf version 4.16.3 or higher in dependencies
    - src/telegram/registry.ts exports getOrCreateBotInstance, getBotInstance, clearBotRegistry
    - `npx tsc --noEmit` exits 0 (no type errors in new file)
    - `npm test -- --testPathPattern=telegram-client` passes (send-path tests still green — D-02 honored, no signature changes)
    - logger.info calls in registry.ts log only webhookId, not botToken (inspect source)
  </acceptance_criteria>
  <done>telegraf installed; registry.ts exports three functions with singleton Map pattern; no bot token in log statements</done>
</task>

<task type="auto">
  <name>Task 2: Patch logger.ts + jest.setup.ts</name>
  <files>src/utils/logger.ts, tests/jest.setup.ts</files>
  <read_first>
    - src/utils/logger.ts — full file (21 lines), understand current redact paths before editing
    - tests/jest.setup.ts — full file (17 lines), understand current ??= assignments
    - .planning/phases/04-per-bot-foundation/04-CONTEXT.md §D-08, D-09 — locked env var changes
    - .planning/phases/04-per-bot-foundation/04-PATTERNS.md §"src/utils/logger.ts" and §"tests/jest.setup.ts"
  </read_first>
  <action>
LOGGER.TS — Update the pino redact.paths array. Three levels of redaction are maintained (field, wildcard, config namespace). Remove the deprecated paths for the old global Telegram token fields (D-08). Add the new per-bot secret paths (D-04, D-05):
- Remove from all three levels: `telegramBotToken`, `telegramWebhookSecret` (and their `*.` and `config.` prefixed variants)
- Add at all three levels: `botToken`, `webhookSecret` (and their `*.` and `config.` prefixed variants: `'*.botToken'`, `'*.webhookSecret'`, `'config.botToken'`, `'config.webhookSecret'`)
- Keep all other existing redaction paths unchanged (appSecret, databaseUrl, whatsappAccessToken, etc.)

JEST.SETUP.TS — Two changes:
1. Remove the two lines that set TELEGRAM_BOT_TOKEN and TELEGRAM_WEBHOOK_SECRET via ??= (D-08 — they are no longer in the Zod schema, though leaving them would be harmless; remove them for clarity)
2. Add six new TEST_BOT_* env var assignments at the end using the ??= pattern (D-09):
   - `process.env.TEST_BOT_1_TOKEN ??= 'test-bot-1-token';`
   - `process.env.TEST_BOT_1_WEBHOOK_SECRET ??= 'test-bot-1-webhook-secret';`
   - `process.env.TEST_BOT_1_WEBHOOK_ID ??= 'test-webhook-id-1';`
   - `process.env.TEST_BOT_2_TOKEN ??= 'test-bot-2-token';`
   - `process.env.TEST_BOT_2_WEBHOOK_SECRET ??= 'test-bot-2-webhook-secret';`
   - `process.env.TEST_BOT_2_WEBHOOK_ID ??= 'test-webhook-id-2';`
  </action>
  <verify>
    <automated>npm test 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes all existing tests (208 green — removing TELEGRAM_BOT_TOKEN from jest.setup.ts must not break any test since config.ts no longer requires it after Plan 04-01 Task 2)
    - `grep -c 'botToken' src/utils/logger.ts` outputs at least 3 (field, wildcard, config namespace levels)
    - `grep -c 'webhookSecret' src/utils/logger.ts` outputs at least 3
    - `grep -c 'TEST_BOT_1_TOKEN' tests/jest.setup.ts` outputs 1
    - `grep -c 'TEST_BOT_2_WEBHOOK_ID' tests/jest.setup.ts` outputs 1
    - `grep -c 'telegramBotToken' src/utils/logger.ts` outputs 0 (deprecated field removed)
  </acceptance_criteria>
  <done>All 208 tests pass; logger.ts redacts botToken/webhookSecret; jest.setup.ts provides TEST_BOT_* defaults; deprecated Telegram global token references removed from both files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| log output→operator | Pino serializes objects including business rows; redaction ensures botToken never reaches log sink |
| test env→config | jest.setup.ts must not inject TELEGRAM_BOT_TOKEN as required after config.ts stops requiring it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Information Disclosure | bot token in log output | high | mitigate | logger.ts redact paths include botToken at field, wildcard, and config namespace levels; registry.ts logs only webhookId UUID |
| T-04-05 | Tampering | shared Telegraf instance across bots | high | mitigate | Map registry enforces one Telegraf instance per webhookId; clearBotRegistry() in test teardown prevents cross-test contamination |
| T-04-SC | Tampering | npm telegraf install | low | accept | Package Legitimacy Audit in RESEARCH.md confirms telegraf 4.16.3 is [OK/Approved] — official Telegram Bot Framework, 200K+/week downloads, active maintenance |
</threat_model>

<verification>
After both tasks:
- `npm test` exits 0 (all 208 tests pass)
- `npx tsc --noEmit` exits 0
- `node -e "const r = require('./src/telegram/registry'); console.log(typeof r.getOrCreateBotInstance)"` outputs "function"
- `grep -c 'botToken' src/utils/logger.ts` ≥ 3
- `grep -c 'TEST_BOT_1_TOKEN' tests/jest.setup.ts` = 1
</verification>

<success_criteria>
- telegraf@4.16.3 in package.json
- registry.ts: getOrCreateBotInstance / getBotInstance / clearBotRegistry exported; Map singleton; no bot token in log output
- logger.ts: botToken/webhookSecret at three redaction levels; deprecated telegramBotToken paths removed
- jest.setup.ts: 6 TEST_BOT_* defaults added; TELEGRAM_BOT_TOKEN/TELEGRAM_WEBHOOK_SECRET removed
- All 208 tests still pass
</success_criteria>

## Artifacts This Phase Produces

| Symbol | Type | Location | Description |
|--------|------|----------|-------------|
| `getOrCreateBotInstance` | export function | `src/telegram/registry.ts` | Returns or creates Telegraf instance for a webhookId; logs only UUID never token |
| `getBotInstance` | export function | `src/telegram/registry.ts` | Read-only lookup of existing Telegraf instance |
| `clearBotRegistry` | export function | `src/telegram/registry.ts` | Empties the Map; used in test teardown |

<output>
Create `.planning/phases/04-per-bot-foundation/04-02-SUMMARY.md` when done
</output>
