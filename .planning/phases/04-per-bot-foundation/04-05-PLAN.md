---
phase: "04"
plan: "05"
type: execute
wave: 4
depends_on:
  - "04-04"
files_modified:
  - tests/telegram-webhook.test.ts
  - tests/rls-enforcement.test.ts
autonomous: true
requirements:
  - BOT-02
  - BOT-03
  - BOT-05
must_haves:
  truths:
    - "npx drizzle-kit push has been run and the three Phase 4 columns (bot_token, webhook_id, webhook_secret) exist in the live database and the test database"
    - "migrations/0003_phase4_per_bot.sql has been applied: randevuclaw_app role exists, RLS is enabled on 7 tables, FOR ALL policies are in place (D-11, D-12)"
    - "tests/telegram-webhook.test.ts routes all POST calls to /webhooks/telegram/:webhookId; mocks findBusinessByWebhookId and withBusinessContext instead of the v1.0 global-token mocks (D-04)"
    - "test for parallel bot routing: two distinct webhookId values each resolve to distinct businesses, both return 200 (BOT-02)"
    - "test for constant-time secret verification: valid webhookSecret returns 200; invalid returns 401; missing returns 401 (BOT-03, D-06)"
    - "tests/rls-enforcement.test.ts exists and has at least two passing integration tests: unscoped SELECT filtered to current business by RLS, and SET LOCAL context clears after transaction with no leakage (BOT-05, D-10)"
    - "npm test exits 0 — all existing 208 tests plus new tests are green"
  artifacts:
    - tests/telegram-webhook.test.ts
    - tests/rls-enforcement.test.ts
  key_links:
    - "Schema push (Task 1) must complete before rls-enforcement.test.ts can run — it requires the randevuclaw_app role and RLS policies to exist in the test database"
    - "telegram-webhook.test.ts mocks queries module as a whole (jest.mock); findBusinessByWebhookId and withBusinessContext must be added to the mocked function map"
    - "rls-enforcement.test.ts uses the real appDb connection (not mocked) — DATABASE_APP_URL must be set to a randevuclaw_app-role connection for RLS to actually enforce"
user_setup:
  - service: neon-postgres
    why: "RLS enforcement tests require appDb to connect as randevuclaw_app (non-superuser) for PostgreSQL to enforce RLS policies. Superuser connections bypass RLS."
    env_vars:
      - name: DATABASE_APP_URL
        source: "Set to postgresql://randevuclaw_app:<PASSWORD>@<NEON_HOST>/<DB_NAME>?sslmode=require — same host as DATABASE_URL but using the randevuclaw_app role credentials set when running migrations/0003_phase4_per_bot.sql. Add to .env.local and fly secrets."
---

<objective>
Apply schema migration to the live database, patch telegram-webhook.test.ts for the new per-bot routing model, and create rls-enforcement.test.ts to prove RLS isolation at the database layer.

Purpose: This plan closes the verification loop for Phase 4. The schema push makes the database schema match schema.ts. The test updates convert the 208-test suite to the new per-bot routing contract (BOT-02, BOT-03). The new RLS tests prove that withBusinessContext + RLS policies enforce tenant isolation at the PostgreSQL layer, not only in application code (BOT-05). All four success criteria from the ROADMAP must be satisfied before the phase is marked complete.

Output: Schema pushed to DB; telegram-webhook.test.ts updated for per-bot routing; rls-enforcement.test.ts created with passing integration tests; full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-per-bot-foundation/04-CONTEXT.md
@.planning/phases/04-per-bot-foundation/04-RESEARCH.md
@.planning/phases/04-per-bot-foundation/04-PATTERNS.md
@.planning/phases/04-per-bot-foundation/04-01-SUMMARY.md
@.planning/phases/04-per-bot-foundation/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: Apply schema push and RLS migration to database</name>
  <files>migrations/0003_phase4_per_bot.sql</files>
  <read_first>
    - migrations/0003_phase4_per_bot.sql — verify the file exists and contains all expected sections (CREATE ROLE, ENABLE ROW LEVEL SECURITY, CREATE POLICY, GRANT) before running anything
    - src/database/schema.ts — confirm botToken, webhookId, webhookSecret are in the businesses table definition (from Plan 04-01); drizzle-kit push applies the diff
    - .planning/phases/04-per-bot-foundation/04-CONTEXT.md §D-07, D-11, D-12 — verify what the migration must contain before applying
    - .planning/phases/04-per-bot-foundation/04-01-SUMMARY.md — confirm drizzle.config.ts path and any push flags established in Plan 04-01
  </read_first>
  <action>
This is a BLOCKING task. All test tasks (Tasks 2 and 3) MUST NOT run until this task completes successfully and verification passes.

Step 1 — Push Drizzle schema to database (adds the three Phase 4 columns):
Run `npx drizzle-kit push` from the project root. This reads drizzle.config.ts, connects to DATABASE_URL, and applies the diff between schema.ts and the live schema — adding bot_token, webhook_id (UNIQUE), webhook_secret to the businesses table. The command is idempotent: if columns already exist, it is a no-op.

If drizzle-kit prompts interactively, supply 'y' to confirm the schema push. If the push errors with "column already exists", verify the column is present and proceed.

Step 2 — Apply RLS infrastructure migration:
Run `psql "$DATABASE_URL" -f migrations/0003_phase4_per_bot.sql`. The migration uses DO $$ idempotency guards so re-running it is safe. This step creates the randevuclaw_app role, enables RLS on 7 tables, creates FOR ALL policies, and grants permissions.

If psql is not installed locally, use the Neon SQL console (web UI) to paste and execute the contents of migrations/0003_phase4_per_bot.sql. Add a note in the plan summary if manual execution was required.

Step 3 — Verify the migration applied correctly:
Run the following checks against DATABASE_URL:
- Columns exist: `psql "$DATABASE_URL" -c "\d businesses" | grep -E 'bot_token|webhook_id|webhook_secret'` — expect 3 lines
- Role exists: `psql "$DATABASE_URL" -c "SELECT rolname FROM pg_roles WHERE rolname = 'randevuclaw_app'"` — expect 1 row
- RLS enabled: `psql "$DATABASE_URL" -c "SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname='public' AND tablename IN ('businesses','messages','bookings','services','business_hours','client_business_relationships','conversation_turns')" | grep -c 't'` — expect at least 7 occurrences of 't' (true)
  </action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT column_name FROM information_schema.columns WHERE table_name='businesses' AND column_name IN ('bot_token','webhook_id','webhook_secret') ORDER BY column_name" 2>/dev/null | grep -c 'bot_token\|webhook_id\|webhook_secret'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command outputs 3 (all three Phase 4 columns exist in businesses table)
    - `psql "$DATABASE_URL" -c "SELECT rolname FROM pg_roles WHERE rolname = 'randevuclaw_app'" 2>/dev/null | grep -c 'randevuclaw_app'` outputs 1
    - `psql "$DATABASE_URL" -c "SELECT tablename FROM pg_tables WHERE schemaname='public' AND rowsecurity=true" 2>/dev/null | grep -c '.'` outputs at least 7
    - `npx tsc --noEmit` still exits 0 (schema push did not break TypeScript types)
  </acceptance_criteria>
  <done>businesses table has bot_token, webhook_id, webhook_secret columns; randevuclaw_app role exists; RLS enabled on 7 tables with FOR ALL policies; tsc still clean</done>
</task>

<task type="auto">
  <name>Task 2: Patch tests/telegram-webhook.test.ts — per-bot routing, HMAC, and parallel-bot tests</name>
  <files>tests/telegram-webhook.test.ts</files>
  <read_first>
    - tests/telegram-webhook.test.ts — read the FULL file (453 lines) before touching anything; identify every call to postWebhook, every mock declaration, the SECRET constant, and the describe block structure
    - src/webhooks/telegram.ts — confirm the refactored handler from Plan 04-04 (what it imports, what it calls, how verifyTelegramSecretToken was removed)
    - src/database/queries.ts — confirm findBusinessByWebhookId and withBusinessContext export signatures
    - .planning/phases/04-per-bot-foundation/04-CONTEXT.md §D-03, D-04, D-06, D-09
    - .planning/phases/04-per-bot-foundation/04-PATTERNS.md §"tests/telegram-webhook.test.ts" — canonical patch pattern for mock setup and new test cases
    - .planning/phases/04-per-bot-foundation/04-RESEARCH.md §"Phase Requirements → Test Map" rows for BOT-02 and BOT-03
    - tests/jest.setup.ts — confirm TEST_BOT_1_WEBHOOK_ID and TEST_BOT_1_WEBHOOK_SECRET default values (from Plan 04-02)
  </read_first>
  <action>
This task patches the existing test file. Read the full file first to understand every mock declaration and every test. Make targeted edits only where needed.

CHANGE 1 — Add KNOWN_BUSINESS Phase 4 fields:
The KNOWN_BUSINESS constant lacks botToken, webhookId, webhookSecret. Add these fields (they match TEST_BOT_1_* defaults from jest.setup.ts):
- `webhookSecret: 'test-bot-1-webhook-secret'` (matches process.env.TEST_BOT_1_WEBHOOK_SECRET ??= default)
- `webhookId: 'test-webhook-id-1'` (matches process.env.TEST_BOT_1_WEBHOOK_ID ??= default)
- `botToken: 'test-bot-1-token'` (matches process.env.TEST_BOT_1_TOKEN ??= default)

Add a KNOWN_BUSINESS_2 constant for the second test bot (TEST_BOT_2_* defaults):
- id: 2, name: 'Hair Salon Athens', slug: 'hair-salon-athens'
- botToken: 'test-bot-2-token', webhookId: 'test-webhook-id-2', webhookSecret: 'test-bot-2-webhook-secret'
- phoneNumberId: null, ownerTelegramId: '999999999', googleRefreshToken: null, agendaSentDate: null, createdAt: new Date()

CHANGE 2 — Update SECRET constant:
The existing `const SECRET = 'test-telegram-webhook-secret'` was the global secret (old config.telegramWebhookSecret). Update to the per-bot secret: `const SECRET = 'test-bot-1-webhook-secret'`. This matches KNOWN_BUSINESS.webhookSecret and TEST_BOT_1_WEBHOOK_SECRET default.

CHANGE 3 — Add mock declarations for new query functions:
After the existing mocked function declarations, add:
- `const mockedFindBusinessByWebhookId = queries.findBusinessByWebhookId as jest.MockedFunction<typeof queries.findBusinessByWebhookId>;`
- `const mockedWithBusinessContext = queries.withBusinessContext as jest.MockedFunction<typeof queries.withBusinessContext>;`

If verifyTelegramSecretToken was imported from src/webhooks/telegram (check if any existing test imports it), remove that import — it no longer exists in the refactored module.

CHANGE 4 — Update postWebhook helper signature:
The current helper posts to `/webhooks/telegram` without a webhookId. Change it to accept a webhookId parameter and post to `/webhooks/telegram/${webhookId}`:

Replace the current `async function postWebhook(body: object, secret: string | null = SECRET)` with:
`async function postWebhook(webhookId: string = 'test-webhook-id-1', body: object, secret: string | null = SECRET)`

Update the internal URL from `/webhooks/telegram` to `` `/webhooks/telegram/${webhookId}` ``.

CHANGE 5 — Update beforeEach to include new mocks:
In the existing `beforeEach`, add setup for the two new mocks so they have sensible defaults:
- `mockedFindBusinessByWebhookId.mockResolvedValue(KNOWN_BUSINESS);`
- `mockedWithBusinessContext.mockImplementation((businessId, callback) => callback());`

This ensures existing tests that don't set up these mocks explicitly still work — they receive a resolved KNOWN_BUSINESS and withBusinessContext calls through to the callback.

CHANGE 6 — Update existing test postWebhook call sites:
Every existing call to `postWebhook(body, secret)` must become `postWebhook('test-webhook-id-1', body, secret)` to match the new signature. Search every `postWebhook(make` and `postWebhook(make` call in the file and add the webhookId as the first argument. The default in the function signature handles tests that still omit it.

CHANGE 7 — Update Test 4 (wrong secret → HTTP status):
The refactored handler now returns 401 (not 403) for missing or invalid secrets. Update Test 4 assertions:
- `resMissing.status` → expect 401
- `resWrong.status` → expect 401

Also update the test name to reflect 401: "Test 4: missing/wrong secret token -> 401 ...".

CHANGE 8 — Add Test: parallel bot routing (BOT-02):
Add a new test case after the existing tests:

Name: 'Test: two distinct webhookIds each resolve to their own business, both return 200'
Setup: mock `mockedFindBusinessByWebhookId` to return KNOWN_BUSINESS on the first call and KNOWN_BUSINESS_2 on the second call using `.mockResolvedValueOnce`.
Post two requests: one to 'test-webhook-id-1' with SECRET, one to 'test-webhook-id-2' with KNOWN_BUSINESS_2's webhookSecret ('test-bot-2-webhook-secret').
Assertions: both responses are 200; `mockedFindBusinessByWebhookId` was called exactly twice with the respective webhookIds.

CHANGE 9 — Add Test: constant-time secret verification (BOT-03):
Add a new test case:

Name: 'Test: valid webhookSecret returns 200; invalid or missing returns 401'
Setup: `mockedFindBusinessByWebhookId.mockResolvedValue({ ...KNOWN_BUSINESS, webhookSecret: 'correct-32-char-secret-here-xx' });`
Post valid: webhookId 'test-webhook-id-1', any message update, secret 'correct-32-char-secret-here-xx' → expect 200.
Post invalid: same webhookId, same body, secret 'wrong-secret' → expect 401.
Post missing: same webhookId, same body, secret null → expect 401.

Note: the test only verifies HTTP response codes. It cannot directly verify constant-time comparison in a unit test — that invariant is enforced in the source (grep for timingSafeEqual) and documented in the threat model (T-04-10).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=telegram-webhook 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=telegram-webhook` exits 0; ALL telegram-webhook.test.ts tests pass (previously passing tests plus two new ones)
    - `grep -c 'findBusinessByWebhookId' tests/telegram-webhook.test.ts` outputs at least 2 (mock declaration + beforeEach setup)
    - `grep -c 'withBusinessContext' tests/telegram-webhook.test.ts` outputs at least 2 (mock declaration + beforeEach setup)
    - `grep -c 'webhookId' tests/telegram-webhook.test.ts` outputs at least 5 (KNOWN_BUSINESS field, postWebhook param, new test assertions)
    - `grep -c 'KNOWN_BUSINESS_2' tests/telegram-webhook.test.ts` outputs at least 1 (parallel bot test)
    - Secret-verification test expects 401 for invalid/missing secret (not 403)
    - `npm test` exits 0 (all 208 existing tests + new tests green)
  </acceptance_criteria>
  <done>telegram-webhook.test.ts routes to /:webhookId; mocks findBusinessByWebhookId + withBusinessContext; parallel-bot and HMAC tests added; all tests pass; 401 returned for invalid secrets</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/rls-enforcement.test.ts — RLS isolation integration tests</name>
  <files>tests/rls-enforcement.test.ts</files>
  <read_first>
    - tests/booking-queries.test.ts — read to understand integration test structure (beforeEach cleanup, db import, real DB queries, no jest.mock)
    - src/database/queries.ts — confirm withBusinessContext signature: (businessId: string | number, callback: () => Promise&lt;T&gt;) =&gt; Promise&lt;T&gt;
    - src/database/db.ts — confirm appDb export (from Plan 04-01); RLS tests must use appDb for policies to fire
    - src/database/schema.ts — confirm messages, businesses, bookings table shapes for test data
    - .planning/phases/04-per-bot-foundation/04-CONTEXT.md §D-10, D-11, D-12 — locked RLS behavior
    - .planning/phases/04-per-bot-foundation/04-PATTERNS.md §"tests/rls-enforcement.test.ts" — canonical test pattern with describe/beforeEach/it structure
    - .planning/phases/04-per-bot-foundation/04-RESEARCH.md §"Pitfall 2: Cross-Tenant Data Access" and §"Pitfall 4: Transaction Context Leakage"
  </read_first>
  <action>
Create a new file tests/rls-enforcement.test.ts. This is an INTEGRATION test: it uses the real database (not mocked). It ONLY passes when:
1. DATABASE_APP_URL is set to a connection string for the randevuclaw_app role (not superuser)
2. Migration 0003 has been applied (Task 1 of this plan)
3. RLS policies are active

If DATABASE_APP_URL is not set (falls back to admin connection), RLS will NOT be enforced (PostgreSQL superusers bypass RLS by default). Add a skip guard at the top of the describe block: if `!process.env.DATABASE_APP_URL`, call `test.skip` with a clear message: "Skipping RLS enforcement tests: DATABASE_APP_URL not set — set it to randevuclaw_app role connection to verify RLS isolation."

File structure (follow booking-queries.test.ts pattern exactly):

Imports:
- `import { db, appDb } from '../src/database/db';` — db for admin operations (inserts, cleanup), appDb for RLS-enforced operations
- `import { withBusinessContext } from '../src/database/queries';`
- `import { businesses, messages } from '../src/database/schema';`
- `import { eq } from 'drizzle-orm';`

beforeEach:
Clean up test data in dependency order (children first, then parents): delete from messages, then from businesses. Use admin `db` for cleanup (not appDb) so cleanup is never blocked by RLS.

TEST 1 — 'RLS blocks unscoped SELECT: withBusinessContext filters to current tenant only (BOT-05, D-10)':

a) Insert two business rows via admin `db`: {name: 'Test Business A', slug: 'test-biz-a'} and {name: 'Test Business B', slug: 'test-biz-b'}. Also set botToken, webhookId, webhookSecret for each (nullable fields, any non-null values work for test isolation). Capture returned ids (b1.id, b2.id).
b) Insert one message row for each business via admin `db` (use db.insert, not appDb — insert must bypass RLS since business context is not set yet): {businessId: b1.id, messageBody: 'msg-for-a', senderPhone: '111', whatsappMessageId: 'wa-a'} and {businessId: b2.id, messageBody: 'msg-for-b', senderPhone: '222', whatsappMessageId: 'wa-b'}.
c) Query messages within b1's context: `const b1Msgs = await withBusinessContext(b1.id, () => appDb.select().from(messages));`
d) Query messages within b2's context: `const b2Msgs = await withBusinessContext(b2.id, () => appDb.select().from(messages));`
e) Assert: `b1Msgs.length` is 1; `b1Msgs[0].businessId` is b1.id; `b2Msgs.length` is 1; `b2Msgs[0].businessId` is b2.id.
f) This proves: querying messages without a WHERE clause inside withBusinessContext returns ONLY the current tenant's rows (defense-in-depth: RLS, not just app-layer filtering).

TEST 2 — 'SET LOCAL context clears after transaction: sequential transactions are isolated (BOT-05, D-10)':

a) Insert two businesses (same pattern as Test 1). Insert one message per business.
b) First transaction: `const tx1 = await withBusinessContext(b1.id, () => appDb.select().from(messages));`
c) Second transaction: `const tx2 = await withBusinessContext(b2.id, () => appDb.select().from(messages));`
d) Assert: `tx1[0].businessId` is b1.id; `tx2[0].businessId` is b2.id.
e) This proves: SET LOCAL context from the first transaction does NOT leak into the second transaction (Pitfall 4 mitigation: SET LOCAL is transaction-scoped, auto-cleared on commit). If session-level SET were used instead of SET LOCAL, tx2 would see b1's data.

Note on test data: use distinct whatsappMessageId values per test to avoid unique constraint violations if beforeEach cleanup fails. Pattern: include the test number or a timestamp prefix.

Note on skipping: If DATABASE_APP_URL is unset and the tests run against the admin connection, both tests will pass trivially (superuser sees all rows). This is NOT a green result for BOT-05 — it means RLS was not tested. The skip guard makes this explicit rather than silently passing with wrong semantics.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=rls-enforcement 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=rls-enforcement` exits 0
    - If DATABASE_APP_URL is set: both tests pass, meaning RLS actually filtered rows
    - If DATABASE_APP_URL is NOT set: tests are SKIPPED (not failed) with a clear skip message
    - `grep -c 'withBusinessContext' tests/rls-enforcement.test.ts` outputs at least 2 (one per test)
    - `grep -c 'appDb' tests/rls-enforcement.test.ts` outputs at least 2 (used in select calls)
    - `grep -c 'DATABASE_APP_URL' tests/rls-enforcement.test.ts` outputs at least 1 (the skip guard)
    - `npm test` exits 0 (entire suite green, including these new tests)
  </acceptance_criteria>
  <done>rls-enforcement.test.ts exists with skip guard + 2 integration tests; when DATABASE_APP_URL points to randevuclaw_app role, both tests prove RLS isolates tenants at the DB layer; entire suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test DB → RLS policies | rls-enforcement.test.ts uses appDb (randevuclaw_app role); RLS policies must be active for tests to have meaning; superuser admin db bypasses RLS |
| Schema push → live DB | drizzle-kit push and psql migration run against DATABASE_URL (Neon live); idempotent guards prevent double-apply damage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-14 | Information Disclosure | RLS context leakage via SET (not SET LOCAL) | critical | mitigate | rls-enforcement.test.ts Test 2 explicitly verifies SET LOCAL behavior: sequential transactions with different businessIds remain isolated; a leaking session-level SET would cause Test 2 to fail |
| T-04-15 | Information Disclosure | RLS test silently passing with superuser connection | medium | mitigate | Skip guard in rls-enforcement.test.ts checks for DATABASE_APP_URL; if absent, tests skip with an explicit message rather than passing falsely; CI must set DATABASE_APP_URL for BOT-05 to be verified |
| T-04-16 | Tampering | Schema push applied to wrong database | medium | mitigate | drizzle-kit push reads DATABASE_URL from environment; verify DATABASE_URL before running; idempotent column additions (IF NOT EXISTS equivalent via drizzle-kit) prevent data loss on re-run |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No new npm packages in this plan; all test utilities (jest, supertest) already installed |
</threat_model>

<verification>
After all three tasks:
- `npm test` exits 0 — entire suite green (208 existing tests + new tests)
- `npx tsc --noEmit` exits 0
- `npm test -- --testPathPattern=telegram-webhook` exits 0 (all existing + 2 new tests)
- `npm test -- --testPathPattern=rls-enforcement` exits 0 (skip or pass depending on DATABASE_APP_URL)
- `psql "$DATABASE_URL" -c "\d businesses" 2>/dev/null | grep -c 'bot_token\|webhook_id\|webhook_secret'` outputs 3
- Phase 4 ROADMAP success criteria confirmed:
  1. All 208 existing tests pass (npm test output)
  2. Parallel bot routing test passes (two bots, no cross-contamination)
  3. HMAC secret test passes (invalid secret → 401, valid → 200)
  4. RLS enforcement test passes (unscoped SELECT filtered at DB layer when DATABASE_APP_URL set)
</verification>

<success_criteria>
- Schema push applied: bot_token, webhook_id, webhook_secret exist in live DB and test DB; randevuclaw_app role exists with RLS policies
- telegram-webhook.test.ts: routes to /:webhookId; mocks findBusinessByWebhookId + withBusinessContext; expects 401 (not 403) for invalid secrets; two new tests (parallel bot routing, HMAC verification) pass
- rls-enforcement.test.ts: 2 integration tests prove SET LOCAL scoping and cross-tenant SELECT filtering; skipped gracefully without DATABASE_APP_URL
- Full suite: npm test exits 0
</success_criteria>

## Artifacts This Phase Produces

| Symbol | Type | Location | Description |
|--------|------|----------|-------------|
| `rls-enforcement.test.ts` | integration test | `tests/rls-enforcement.test.ts` | Proves PostgreSQL RLS isolates tenants at DB layer; requires DATABASE_APP_URL |
| Schema columns (applied) | live DB state | Neon + test DB | bot_token, webhook_id (UNIQUE), webhook_secret on businesses table |
| randevuclaw_app role (applied) | live DB state | Neon + test DB | Non-superuser role with SELECT/INSERT/UPDATE/DELETE grants on business-scoped tables |
| RLS policies (applied) | live DB state | Neon + test DB | FOR ALL policies on 7 tables using current_setting('app.current_business_id', true)::INTEGER |

<output>
Create `.planning/phases/04-per-bot-foundation/04-05-SUMMARY.md` when done
</output>
