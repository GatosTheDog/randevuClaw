---
phase: 04-per-bot-foundation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/telegram-client.test.ts
  - tests/expiry-poller.test.ts
  - tests/telegram-webhook.test.ts
autonomous: true
gap_closure: true
requirements:
  - BOT-02
  - BOT-03
  - BOT-04
  - BOT-05
must_haves:
  truths:
    - All 5 tests in telegram-client.test.ts pass; each test body's API call runs inside botTokenStore.run so callTelegramApi finds a token in the AsyncLocalStorage store
    - Tests 2, 3, and 7 in expiry-poller.test.ts pass; the OWNER_BUSINESS_1 fixture has a non-null botToken so the CR-03 guard does not fire, findBusinessById returns the fixture for Tests 3 and 7 (now set in beforeEach), and botTokenStore.run calls through so sendTelegramMessage inside the run body executes
    - Tests 5 and 9 in telegram-webhook.test.ts pass; both answerCallbackQuery assertions use the WR-01 single-argument form
    - npm test produces no new test failures beyond the 5 pre-existing scheduler-agenda time-dependent ones
  artifacts:
    - tests/telegram-client.test.ts updated with botTokenStore import and run-wrapper in all 5 test bodies
    - tests/expiry-poller.test.ts updated with non-null botToken in OWNER_BUSINESS_1, findBusinessById default in beforeEach, and botTokenStore.run call-through in beforeEach
    - tests/telegram-webhook.test.ts updated with single-argument answerCallbackQuery assertions on lines 356 and 427
  key_links:
    - telegram-client.test.ts imports the real botTokenStore (no jest.mock of client module), so botTokenStore.run genuinely establishes the async context that callTelegramApi reads via getStore()
    - expiry-poller.test.ts mocks the entire client module, so botTokenStore.run is auto-mocked and must be explicitly wired to call through before the sendTelegramMessage assertions can ever be reached
---

<objective>
Fix 10 test failures across three test files introduced by code-review fixes CR-03 and WR-01
in Phase 4. No production code changes — only the three test files are modified.

Purpose: The Phase 4 code-review fixes (CR-03: botTokenStore wrapping in expiry-poller,
WR-01: single-arg answerCallbackQuery) changed observable behavior that three test files
were asserting against the old shape. This plan aligns the tests with the current production
code so the Phase 4 test-suite target is met.

Output: Three updated test files; npm test at the Phase 4 baseline (205 passing, 1 skipped,
5 pre-existing scheduler-agenda failures only).
</objective>

<execution_context>
@/Users/manolis/.claude/gsd-core/workflows/execute-plan.md
@/Users/manolis/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-per-bot-foundation/04-03-SUMMARY.md
@.planning/phases/04-per-bot-foundation/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap all five telegram-client.test.ts API calls in botTokenStore.run</name>
  <files>tests/telegram-client.test.ts</files>
  <action>
CR-03 changed callTelegramApi to throw when botTokenStore.getStore() returns undefined.
The test file imports the real client module (no jest.mock of ../src/telegram/client), so
botTokenStore.run is the real AsyncLocalStorage.run and establishes a genuine async context.

Step 1 — Add botTokenStore to the named import on line 1. The import block currently ends
with `editTelegramMessageReplyMarkup,`. Add `botTokenStore,` as a new entry immediately
before the closing `}` on the last import line.

Step 2 — Wrap the API call and all subsequent assertions in each of the five tests inside
`await botTokenStore.run('test-bot-token', async () => { ... });`. The setup code that
creates and assigns fetchMock (two lines at the top of each test body) stays OUTSIDE the
run wrapper; everything from the first `await` call on the production function through the
last `expect(...)` goes inside the wrapper. The pattern for each test is:

- Test 1 (sendTelegramMessage): after `global.fetch = fetchMock as unknown as typeof fetch;`,
  open `await botTokenStore.run('test-bot-token', async () => {`. Move the `const result =
  await sendTelegramMessage(...)` line and all six `expect`/`const` lines that follow into
  the callback body. Close with `});` before the test's closing `});`.

- Test 2 (sendTelegramMessageWithKeyboard): same pattern. The keyboard array definition and
  fetchMock setup stay outside. The `const result = await sendTelegramMessageWithKeyboard(...)`
  and all subsequent `expect`/`const` lines move inside.

- Test 3 (answerCallbackQuery): wrap `await answerCallbackQuery('cbq123', 'Booking
  επιβεβαιώθηκε');` and the three `const [url, options]`, `expect(url)`, and
  `expect(body).toMatchObject(...)` lines inside the run callback.

- Test 4 (editTelegramMessageReplyMarkup): wrap `await editTelegramMessageReplyMarkup('12345',
  42, []);` and the three subsequent `const [url, options]`, `expect(url)`, and
  `expect(body).toEqual(...)` lines inside the run callback.

- Test 5 (error shape): wrap the single `await expect(sendTelegramMessage('12345',
  'γεια')).rejects.toThrow('Bad Request');` line inside the run callback.

The fetchMock variable is defined before the run callback and captured by closure, so
`fetchMock.mock.calls[0]` and `fetchMock.toHaveBeenCalledTimes` assertions still work
correctly from inside the callback.

No changes to the describe block structure, afterEach, or any mock setup.
  </action>
  <verify>
    <automated>cd /Users/manolis/Documents/RandevuClaw && npx jest tests/telegram-client.test.ts --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <done>npx jest tests/telegram-client.test.ts exits 0 with 5 passing and 0 failing.</done>
</task>

<task type="auto">
  <name>Task 2: Fix expiry-poller.test.ts — non-null botToken fixture, findBusinessById default, and botTokenStore.run call-through</name>
  <files>tests/expiry-poller.test.ts</files>
  <action>
Three issues combine to cause Tests 2, 3, and 7 to fail (BOT-04, BOT-05):

Issue A — CR-03 guard: expiry-poller.ts calls `if (!business?.botToken) continue` before the
notification loop. OWNER_BUSINESS_1 has `botToken: null`, so the guard fires and
sendTelegramMessage is never reached.

Issue B — findBusinessById not mocked for Tests 3 and 7: the beforeEach does not set a
default return value for mockedFindBusinessById. Jest auto-mock returns undefined, which
makes `!business?.botToken` evaluate to true, firing the guard even before any fixture is
involved.

Issue C — botTokenStore.run is auto-mocked: jest.mock('../src/telegram/client') replaces all
exports including botTokenStore with jest.fn() equivalents. The auto-mocked run returns
undefined without invoking the callback, so sendTelegramMessage inside the run body is never
called even when the guard passes.

Fix A — On the OWNER_BUSINESS_1 constant (the line that reads `botToken: null,`), change
`null` to `'test-bot-token'`. This is a single character-level change inside the object
literal; all other fields stay unchanged.

Fix B — In the `beforeEach` inside `describe('runExpirySweep', ...)` (the one that calls
`jest.clearAllMocks()` and sets up mockedSendTelegramMessage and
mockedEditTelegramMessageReplyMarkup), add two new mock setup lines after the existing
`mockedEditTelegramMessageReplyMarkup.mockResolvedValue(undefined);` line:
  `mockedFindBusinessById.mockResolvedValue(OWNER_BUSINESS_1);`
  and
  `(telegramClient.botTokenStore.run as jest.Mock).mockImplementation((_: string, cb: () => Promise<unknown>) => cb());`

The second line wires botTokenStore.run to call the callback synchronously, matching the
pattern already established in telegram-webhook.test.ts. The `telegramClient` namespace is
already imported at the top of the file as `import * as telegramClient from
'../src/telegram/client'`.

Do NOT add these lines to the beforeEach inside `describe('startExpiryPoller', ...)` — those
tests mock listAllBusinessIds to return [] so findBusinessById and botTokenStore.run are
never reached; adding mocks there is harmless but unnecessary.

Test 2 already sets `mockedFindBusinessById.mockResolvedValue(OWNER_BUSINESS_1)` inside its
own body — this explicit per-test call takes precedence over the beforeEach default and
remains in place unchanged.
  </action>
  <verify>
    <automated>cd /Users/manolis/Documents/RandevuClaw && npx jest tests/expiry-poller.test.ts --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <done>npx jest tests/expiry-poller.test.ts exits 0 with all tests passing; Tests 2, 3, and 7 are no longer reported as failing.</done>
</task>

<task type="auto">
  <name>Task 3: Update two answerCallbackQuery assertions in telegram-webhook.test.ts</name>
  <files>tests/telegram-webhook.test.ts</files>
  <action>
WR-01 changed the webhook handler to call answerCallbackQuery(callbackQueryId) with no second
argument for both the approve/reject branch and the malformed-data branch. The production
function signature retains an optional text parameter; WR-01 simply stopped passing one.

Two test assertions still assert the old two-argument call shape and must be updated:

Test 5 (approve branch — line 356):
  Current:  `expect(mockedAnswerCallbackQuery).toHaveBeenCalledWith('cbq-1', expect.any(String));`
  Replace with: `expect(mockedAnswerCallbackQuery).toHaveBeenCalledWith('cbq-1');`

Test 9 (malformed callback data — line 427):
  Current:  `expect(mockedAnswerCallbackQuery).toHaveBeenCalledWith('cbq-1', undefined);`
  Replace with: `expect(mockedAnswerCallbackQuery).toHaveBeenCalledWith('cbq-1');`

No other lines in this file change. Do not alter the describe block structure, any other
`toHaveBeenCalledWith` assertion, or any of the beforeEach/afterEach blocks.
  </action>
  <verify>
    <automated>cd /Users/manolis/Documents/RandevuClaw && npx jest tests/telegram-webhook.test.ts --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <done>npx jest tests/telegram-webhook.test.ts exits 0; Tests 5 and 9 are no longer reported as failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → real ALS | Task 1 uses real AsyncLocalStorage.run — no network calls; scoped to test process only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-04-06-01 | Tampering | test/fixture data | low | accept | Test-only changes; no production data paths touched; no new npm installs |
</threat_model>

<verification>
After all three tasks complete, run the full test suite:

  cd /Users/manolis/Documents/RandevuClaw && npm test 2>&1 | tail -25

Expected outcome: only the 5 pre-existing scheduler-agenda time-dependent failures remain.
The three previously-failing suites (telegram-client, expiry-poller, telegram-webhook) all
report 0 failures.
</verification>

<success_criteria>
- npx jest tests/telegram-client.test.ts: 5 passing, 0 failing
- npx jest tests/expiry-poller.test.ts: all passing, 0 failing (Tests 2, 3, 7 no longer fail)
- npx jest tests/telegram-webhook.test.ts: all passing, 0 failing (Tests 5, 9 no longer fail)
- npm test: overall suite at Phase 4 target — no failures beyond the 5 pre-existing scheduler-agenda ones
</success_criteria>

<output>
Create `.planning/phases/04-per-bot-foundation/04-06-SUMMARY.md` when done.
</output>
