---
phase: 19-class-setup-onboarding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/conversation/ai-agent.ts
  - src/conversation/function-executor.ts
  - src/onboarding/ai-owner-agent.ts
  - src/scheduler/session-cancellation.ts
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-03
  - CLSS-05

must_haves:
  truths:
    - No Greek user-visible string in the four modified files contains the word σεζόν after this plan executes
    - All occurrences of σεζόν in tool description strings, confirmation messages, error messages, and notification messages are replaced with μάθημα or μαθήματα (plural) as appropriate
    - Internal TypeScript identifiers (variable names, function names, property keys) are NOT changed — only user-visible string literals
    - CLSS-05 is satisfied by the existing admin menu Classes sub-menu (menu:classes:create path in admin-menu.ts) — this plan adds a comment/note confirming that, no code changes needed for CLSS-05
  artifacts:
    - src/conversation/ai-agent.ts — tool description strings use μάθημα/μαθήματα
    - src/conversation/function-executor.ts — error/confirmation messages use μάθημα
    - src/onboarding/ai-owner-agent.ts — tool descriptions and result strings use μάθημα
    - src/scheduler/session-cancellation.ts — cancellation broadcast uses μάθημα
  key_links:
    - No regexp or runtime string logic targets σεζόν — all are static string literals; replacements are purely textual
---

<objective>
Replace every user-visible Greek occurrence of "σεζόν" with "μάθημα" (singular) or "μαθήματα" (plural) across the four files that contain it.

Purpose: Consistent Greek terminology across the entire bot so owners and clients see μάθημα/μαθήματα everywhere; σεζόν is the French loanword the codebase used before standardisation.

Output: Four edited source files with updated string literals; TypeScript identifiers unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/conversation/ai-agent.ts
@src/conversation/function-executor.ts
@src/onboarding/ai-owner-agent.ts
@src/scheduler/session-cancellation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace σεζόν in ai-agent.ts tool descriptions and system prompt</name>
  <files>src/conversation/ai-agent.ts</files>
  <action>
    Read src/conversation/ai-agent.ts in full before editing.

    Target lines (from grep audit — verify actual line numbers by reading first):

    In the list_sessions_for_client tool description (approx line 125):
    - "Επιστρέφει τις επερχόμενες διαθέσιμες σεζόν..." → replace "σεζόν" with "μαθήματα"
    - "Κάλεσέ το πριν από το book_session αν ο πελάτης δεν γνωρίζει τις ακριβείς λεπτομέρειες της σεζόν" → replace "σεζόν" with "μαθήματος"

    In the book_session tool description (approx line 137):
    - "Κλείνει συγκεκριμένη σεζόν για τον πελάτη." → "Κλείνει συγκεκριμένο μάθημα για τον πελάτη."
    - "list_sessions_for_client αν χρειάζεσαι να βρεις το ακριβές session_instance_id" — keep as-is (English identifier)
    - "Αν allow_multi_booking είναι ενεργό, μπορείς να στείλεις λίστα session_instance_ids για πολλαπλές κρατήσεις μαζί" — no σεζόν here, skip

    In the session_instance_id property description (approx line 142):
    - "ID της συγκεκριμένης σεζόν από list_sessions_for_client" → "ID του συγκεκριμένου μαθήματος από list_sessions_for_client"

    In the reschedule_session tool description (approx line 151):
    - "Αλλάζει μια κράτηση σεζόν σε διαφορετική σεζόν." → "Αλλάζει μια κράτηση μαθήματος σε διαφορετικό μάθημα."

    In the new_session_instance_id property description (approx line 157):
    - "ID της νέας σεζόν" → "ID του νέου μαθήματος"

    In the system prompt section for fixed_sessions mode (approx lines 209-212):
    - "ΣΤΑΘΕΡΕΣ ΣΕΖΟΝ" → "ΣΤΑΘΕΡΑ ΜΑΘΗΜΑΤΑ"
    - "τις διαθέσιμες σεζόν" → "τα διαθέσιμα μαθήματα"
    - "ΠΟΛΛΑΠΛΕΣ σεζόν" → "ΠΟΛΛΑΠΛΑ μαθήματα"

    Use Edit (scoped replacements) for each change. Read the file first to get exact strings.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/conversation/ai-agent.ts','utf8'); const hits=s.match(/σεζόν/g)||[]; console.log('σεζόν hits:',hits.length); if(hits.length>0){process.exit(1);}"</automated>
  </verify>
  <done>Zero occurrences of σεζόν remain in src/conversation/ai-agent.ts</done>
</task>

<task type="auto">
  <name>Task 2: Replace σεζόν in function-executor.ts error and confirmation messages</name>
  <files>src/conversation/function-executor.ts</files>
  <action>
    Read src/conversation/function-executor.ts in full before editing.

    Target occurrences (verify exact strings by reading the file):

    Line ~356 (cutoff warning):
    - "...αν ακυρώσετε τώρα (εντός ${cutoffHours} ωρών πριν τη σεζόν)..." → "...αν ακυρώσετε τώρα (εντός ${cutoffHours} ωρών πριν το μάθημα)..."

    Line ~544 (empty list message):
    - "Δεν υπάρχουν επερχόμενες σεζόν αυτή τη στιγμή." → "Δεν υπάρχουν επερχόμενα μαθήματα αυτή τη στιγμή."

    Line ~587 (enforcement message):
    - "Χρειάζεστε ενεργή συνδρομή για να κλείσετε σεζόν." → "Χρειάζεστε ενεργή συνδρομή για να κλείσετε μάθημα."

    Line ~631 (missing id message):
    - "Δεν δόθηκε αναγνωριστικό σεζόν." → "Δεν δόθηκε αναγνωριστικό μαθήματος."
    - "list_sessions_for_client για να δεις τις διαθέσιμες σεζόν." → "list_sessions_for_client για να δεις τα διαθέσιμα μαθήματα."

    Line ~640 (second enforcement message):
    - Same as line ~587 pattern — replace "σεζόν" with "μάθημα"

    Line ~647 (not found):
    - "Η σεζόν δεν βρέθηκε ή δεν είναι πλέον διαθέσιμη." → "Το μάθημα δεν βρέθηκε ή δεν είναι πλέον διαθέσιμο."

    Line ~660 (full):
    - "Η σεζόν είναι πλήρης." → "Το μάθημα είναι πλήρες."

    Line ~663 (not available):
    - "Η σεζόν δεν είναι διαθέσιμη" → "Το μάθημα δεν είναι διαθέσιμο"

    Line ~671 (booking confirmation owner alert):
    - "Νέα κράτηση σεζόν " → "Νέα κράτηση μαθήματος "

    Line ~702 (wrong reschedule type):
    - "Αυτή η κράτηση δεν αφορά σεζόν." → "Αυτή η κράτηση δεν αφορά μάθημα."

    Line ~719 (reschedule confirmation):
    - "Η νέα σεζόν (" → "Το νέο μάθημα ("

    Line ~757 (reschedule failure):
    - "Η νέα σεζόν είναι πλήρης." → "Το νέο μάθημα είναι πλήρες."
    - "Η νέα σεζόν δεν είναι διαθέσιμη." → "Το νέο μάθημα δεν είναι διαθέσιμο."

    Use Edit (scoped replacement) for each unique string. If two identical strings exist, handle them with replace_all: true or separate Edit calls with surrounding context to disambiguate.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/conversation/function-executor.ts','utf8'); const hits=s.match(/σεζόν/g)||[]; console.log('σεζόν hits:',hits.length); if(hits.length>0){process.exit(1);}"</automated>
  </verify>
  <done>Zero occurrences of σεζόν remain in src/conversation/function-executor.ts</done>
</task>

<task type="auto">
  <name>Task 3: Replace σεζόν in ai-owner-agent.ts and session-cancellation.ts</name>
  <files>src/onboarding/ai-owner-agent.ts, src/scheduler/session-cancellation.ts</files>
  <action>
    Read both files before editing.

    --- ai-owner-agent.ts ---

    Tool description strings (approx lines 233, 255, 283, 293, 299, 303, 312, 322, 326):
    - "...αν ένας πελάτης ακυρώσει εντός X ωρών πριν τη σεζόν..." → "...πριν το μάθημα..."
    - "Δημιουργεί επαναλαμβανόμενη σεζόν..." → "Δημιουργεί επαναλαμβανόμενο μάθημα..."
    - "~90 ημέρες σεζόν αυτόματα." → "~90 ημέρες μαθήματα αυτόματα."
    - "Χρησιμοποίησε όταν ο ιδιοκτήτης θέλει να ορίσει εβδομαδιαίο πρόγραμμα." — no change needed
    - "Εμφανίζει τις επερχόμενες σεζόν..." → "Εμφανίζει τα επερχόμενα μαθήματα..."
    - "Ακυρώνει συγκεκριμένη σεζόν." → "Ακυρώνει συγκεκριμένο μάθημα."
    - "Όλοι οι κρατημένοι πελάτες ειδοποιούνται αυτόματα." — no change
    - "Ημερομηνία σεζόν σε μορφή YYYY-MM-DD" → "Ημερομηνία μαθήματος σε μορφή YYYY-MM-DD"
    - "Ώρα σεζόν σε μορφή HH:MM" → "Ώρα μαθήματος σε μορφή HH:MM"
    - "Ορίζει συγκεκριμένο πελάτη σε σεζόν..." → "Ορίζει συγκεκριμένο πελάτη σε μάθημα..."
    - "Ημερομηνία σεζόν YYYY-MM-DD" → "Ημερομηνία μαθήματος YYYY-MM-DD"
    - "Ώρα σεζόν HH:MM" → "Ώρα μαθήματος HH:MM"

    Result/return message strings (approx lines 704, 711, 719, 736, 741, 744, 760, 772, 775, 781, 783, 832, 834):
    - "Δημιουργήθηκαν ${instanceCount} σεζόν για..." → "Δημιουργήθηκαν ${instanceCount} μαθήματα για..."
    - "Δεν υπάρχουν επερχόμενες σεζόν." → "Δεν υπάρχουν επερχόμενα μαθήματα."
    - "... και ${total - 20} ακόμα σεζόν." → "... και ${total - 20} ακόμα μαθήματα."
    - "Δεν βρέθηκε σεζόν στις..." → "Δεν βρέθηκε μάθημα στις..."
    - "Η σεζόν στις ... ήταν ήδη ακυρωμένη." → "Το μάθημα στις ... ήταν ήδη ακυρωμένο."
    - "Η σεζόν στις ... ακυρώθηκε." → "Το μάθημα στις ... ακυρώθηκε."
    - "Δεν βρέθηκε σεζόν" (second occurrence) → "Δεν βρέθηκε μάθημα"
    - "Η σεζόν είναι γεμάτη." → "Το μάθημα είναι γεμάτο."
    - "Σφάλμα: η σεζόν δεν είναι διαθέσιμη" → "Σφάλμα: το μάθημα δεν είναι διαθέσιμο"
    - "Ο ιδιοκτήτης σε όρισε στη σεζόν..." → "Ο ιδιοκτήτης σε όρισε στο μάθημα..."
    - "Ο πελάτης ... ορίστηκε στη σεζόν..." → "Ο πελάτης ... ορίστηκε στο μάθημα..."
    - "υπάρχουν ${sessions.length} υπάρχουσες σεζόν." → "υπάρχουν ${sessions.length} υπάρχοντα μαθήματα."
    - "Οι πελάτες θα βλέπουν πλέον μόνο τις ορισμένες σεζόν." → "Οι πελάτες θα βλέπουν πλέον μόνο τα ορισμένα μαθήματα."

    --- session-cancellation.ts (line ~102) ---
    - "Η σεζόν σας στις ${cancelled.sessionDate} ${cancelled.sessionTime} ακυρώθηκε. Παρακαλώ επικοινωνήστε μαζί μας για νέο ραντεβού."
      → "Το μάθημά σας στις ${cancelled.sessionDate} ${cancelled.sessionTime} ακυρώθηκε. Παρακαλώ επικοινωνήστε μαζί μας για νέο ραντεβού."

    Use Edit (scoped) for each change. Read each file first to confirm exact whitespace and surrounding context before applying edits.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['src/onboarding/ai-owner-agent.ts','src/scheduler/session-cancellation.ts'].forEach(f=>{const s=fs.readFileSync(f,'utf8'); const hits=s.match(/σεζόν/g)||[]; console.log(f,'σεζόν hits:',hits.length); if(hits.length>0){process.exit(1);}});"</automated>
  </verify>
  <done>
    - Zero occurrences of σεζόν in ai-owner-agent.ts and session-cancellation.ts
    - TypeScript identifiers (variable/function/property names) unchanged
    - npx tsc --noEmit passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source file edits | String-only changes; no logic altered |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-04 | Tampering | String replacement | low | accept | Changes are pure string literals with no runtime logic; TypeScript compilation confirms no identifier was broken |
</threat_model>

<verification>
1. Run all four node verification commands — each must print hits: 0
2. Run npx tsc --noEmit — must pass with no errors
3. Spot-check: grep -rn σεζόν src/ should return 0 results
</verification>

<success_criteria>
- grep -rn 'σεζόν' src/ returns no matches
- npx tsc --noEmit passes
- No TypeScript identifier renames (function/variable/property names unchanged)
</success_criteria>

<output>
Create `.planning/phases/19-class-setup-onboarding/19-02-SUMMARY.md` when done
</output>
