---
phase: 10-session-catalog-schema
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/session-creation.test.ts
  - tests/session-expansion.test.ts
  - tests/session-cancel.test.ts
  - tests/session-assignment.test.ts
  - tests/session-list.test.ts
autonomous: true
requirements:
  - CLSS-01
  - CLSS-02
  - CLSS-03
  - CLSS-04
  - CLSS-05

must_haves:
  truths:
    - "npm test -- tests/session-*.test.ts --testTimeout=10000 exits 0 with all items showing as TODO (no failures)"
    - "All 5 session test stub files exist in the tests/ directory"
    - "No test stub file imports from unbuilt modules (no compilation errors at Jest collection time)"
    - "Each stub file cites its CLSS requirement ID in a top comment"
  artifacts:
    - tests/session-creation.test.ts
    - tests/session-expansion.test.ts
    - tests/session-cancel.test.ts
    - tests/session-assignment.test.ts
    - tests/session-list.test.ts
  key_links:
    - "Each test file cites the requirement ID it covers in its top comment (e.g., '// covers CLSS-01')"
    - "it.todo stubs in session-assignment.test.ts explicitly name the capacity-race guard (SELECT FOR UPDATE) behavior"
    - "it.todo stubs in session-cancel.test.ts explicitly name the poller-notifies behavior (CLSS-03 async broadcast)"
---

<objective>
Create failing test stub files (it.todo format) for all Phase 10 session catalog behaviors covering CLSS-01 through CLSS-05. These stubs establish the Nyquist validation contract: subsequent plans fill in the tests as they build the implementation. (D-01: schema migration is a blocking Wave 0 gate — stubs must not import from unbuilt session modules.)

Purpose: Ensure every CLSS requirement has a corresponding automated test before any production code is written, per the Nyquist rule. Stubs are importable by Jest without TypeScript errors because they contain no imports from src/session/ (which does not yet exist).
Output: 5 test stub files in tests/ directory, all in it.todo format, zero failures on npm test run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-session-catalog-schema/10-RESEARCH.md
@.planning/phases/10-session-catalog-schema/10-PATTERNS.md
@tests/billing-membership-creation.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5 session test stub files (Wave 0 Nyquist stubs)</name>
  <files>
    tests/session-creation.test.ts,
    tests/session-expansion.test.ts,
    tests/session-cancel.test.ts,
    tests/session-assignment.test.ts,
    tests/session-list.test.ts
  </files>
  <read_first>
    tests/billing-membership-creation.test.ts — reference for the top-comment style (line 1: "// covers PAY-02"), describe() block structure, it.todo() naming conventions, and the no-import-from-unbuilt pattern
    .planning/phases/10-session-catalog-schema/10-RESEARCH.md — Validation Architecture section: Phase Requirements to Test Map table lists exact behavior names per requirement ID; Wave 0 Gaps section lists the full file inventory
  </read_first>
  <action>
    Create each of the 5 test stub files in the tests/ directory. Each file must follow the exact conventions from tests/billing-membership-creation.test.ts:

    1. Start with a top comment citing the requirement ID (e.g., "// covers CLSS-01") and a one-line description of what behavior it will test.
    2. Contain only describe() blocks and it.todo() stubs — no imports from src/session/ or any other unbuilt module. Stubs must be collectable by Jest without TypeScript errors.
    3. Use the behavior names from the RESEARCH.md Validation Architecture table.

    Specific describe/it.todo structure for each file:

    session-creation.test.ts (CLSS-01): describe('session catalog creation') with it.todo stubs for:
    - 'creates single session via chat: owner tool inserts catalog row and instance row atomically'
    - 'session creation returns catalogId and instanceCount in result object'
    - 'create_session with duplicate rrule on same business+service replaces existing via onConflictDoUpdate'
    - 'TypeScript interface: createSessionCatalogWithExpansion accepts businessId, serviceId, rruleString, startTime, capacity'

    session-expansion.test.ts (CLSS-02): describe('recurring session rrule expansion') with it.todo stubs for:
    - 'expands weekly Mon/Wed/Fri pattern to ~90 days of sessionInstances (between 36 and 42 instances)'
    - 'expansion is idempotent on replay: re-running with same rruleString inserts zero new rows (onConflictDoNothing)'
    - 'idempotencyKey format is catalog:{catalogId}:{sessionDate}:{sessionTime} for each instance'
    - 'expansion fails cleanly with thrown error when rruleString is invalid RFC 5545'
    - 'DST boundary: expansion across Oct 25 2026 (UTC+3 to UTC+2) produces correct wall-clock Athens dates'

    session-cancel.test.ts (CLSS-03): describe('session cancellation and broadcast') with it.todo stubs for:
    - 'cancel_session marks sessionInstance.isCancelled=true atomically'
    - 'cancel_session on already-cancelled instance is a no-op (idempotent, returns false)'
    - 'poller finds cancelled instances not yet notified and sends Greek message to each booked client'
    - 'poller dedup: sessionCancellationNotifications row prevents second notification send on poller re-run'
    - 'poller partial failure: one client send failure does not block other clients in same session'

    session-assignment.test.ts (CLSS-04): describe('direct client assignment to session') with it.todo stubs for:
    - 'assign_client_to_session inserts booking row with correct sessionInstanceId FK'
    - 'assign_client_to_session atomically increments sessionInstances.bookedCount by 1'
    - 'capacity race guard: two concurrent assignments on same full session — exactly one succeeds, one returns full'
    - 'assign_client_to_session on cancelled session returns conflict status, no booking inserted'
    - 'assigned client receives Greek notification via sendTelegramMessage after successful booking'

    session-list.test.ts (CLSS-05): describe('list upcoming sessions with booking counts') with it.todo stubs for:
    - 'listSessions aggregates bookedCount correctly for each sessionInstance'
    - 'listSessions excludes cancelled instances (isCancelled=true) from results'
    - 'listSessions excludes instances with sessionDate in the past'
    - 'listSessions result format includes sessionDate, sessionTime, bookedCount, capacity for each row'
    - 'listSessions returns empty array when no active upcoming sessions exist'
  </action>
  <verify>
    <automated>npm test -- tests/session-*.test.ts --testTimeout=10000</automated>
  </verify>
  <done>All 5 session test stub files exist in tests/; Jest reports them all as TODO suites with zero failures; npm test exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Install rrule package (only new dependency)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    package.json — current dependencies section to verify rrule is not already installed and to confirm Node.js version (must be 20+)
    .planning/phases/10-session-catalog-schema/10-RESEARCH.md — Package Legitimacy Audit table: rrule v2.8.1 is Approved; Standard Stack section: "npm install rrule" (no version pin needed, latest 2.8.x)
  </read_first>
  <action>
    Install the rrule package. This is the only new external dependency for Phase 10. The package legitimacy audit in 10-RESEARCH.md confirms rrule v2.8.1 is [VERIFIED: npm registry], 791K weekly downloads, RFC 5545 standard compliance, active maintenance (last commit Jun 2024), ~8 KB gzipped, no postinstall scripts.

    Run: npm install rrule

    After install, verify: npm view rrule version to confirm the installed version is 2.8.x. Then run npx tsc --noEmit to confirm no TypeScript compilation errors introduced by the new dependency's type definitions. Do not add any other packages in this task.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>rrule appears in package.json dependencies; npx tsc --noEmit exits 0; npm test exits 0 (full suite still green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test stubs → src modules | Test stubs must not import from unbuilt src/session/ — import errors contaminate Jest output |
| npm registry → local install | rrule package install from public npm registry (pre-audited as [VERIFIED]) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Spoofing | Telegram owner identity in session tools | high | accept (Wave 0) | Deferred — mitigated in 10-03 executeOwnerTool via existing ownerTelegramId guard pattern |
| T-10-02 | Tampering | Cross-tenant session creation (hacked businessId) | high | accept (Wave 0) | Deferred — WR-01 guard (explicit businessId FK chain) mitigated in 10-02 schema + 10-03 query layer |
| T-10-03 | Elevation | Session capacity bypass (concurrent overbooking) | high | accept (Wave 0) | SELECT FOR UPDATE guard mitigated in 10-03; session-assignment.test.ts capacity-race stub is the Nyquist contract |
| T-10-04 | Denial of Service | Duplicate session cancellation notifications | medium | accept (Wave 0) | sessionCancellationNotifications dedup table mitigated in 10-04 poller |
| T-10-SC | Tampering | npm install of unaudited package | high | mitigate | rrule pre-audited in 10-RESEARCH.md Package Legitimacy Audit — [VERIFIED: npm registry]; no blocking human checkpoint needed |
</threat_model>

<verification>
Run after plan completion: `npm test -- tests/session-*.test.ts --testTimeout=10000`
Expected output: 5 test suites, all items TODO, 0 failures, 0 errors.

Additionally verify: `npm test` (full suite) exits 0 — existing 320 tests remain green after rrule install.
</verification>

<success_criteria>
- 5 session test stub files exist in tests/ directory with correct CLSS requirement ID citations
- All stubs use it.todo() format with no imports from src/session/ (module does not exist yet)
- Jest runs all 5 stub files without compilation or runtime errors
- rrule appears in package.json; npx tsc --noEmit exits 0
- Full test suite (npm test) remains green
</success_criteria>

<output>
Create `.planning/phases/10-session-catalog-schema/10-01-SUMMARY.md` when done.
</output>
