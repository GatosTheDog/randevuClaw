---
phase: 10-session-catalog-schema
plan: 02
type: execute
wave: 1
depends_on:
  - "10-01"
files_modified:
  - src/database/schema.ts
  - migrations/0010_session_catalog_schema.sql
autonomous: false
requirements:
  - CLSS-01
  - CLSS-02
  - CLSS-03
  - CLSS-04
  - CLSS-05

must_haves:
  truths:
    - "npx tsc --noEmit exits 0 after schema.ts modifications"
    - "sessionCatalog, sessionInstances, slotlessRequests are exported from src/database/schema.ts"
    - "businesses table in schema.ts has all 7 new config columns (bookingMode, cancellationCutoffEnabled, cancellationCutoffHours, slotlessRequestsEnabled, lastSessionThresholdEnabled, lastSessionThresholdCount, allowMultiBooking)"
    - "bookings table in schema.ts has sessionInstanceId nullable FK to sessionInstances"
    - "migrations/0010_session_catalog_schema.sql exists with idempotent CREATE TABLE and ALTER TABLE statements for all 3 tables and 8 new columns"
    - "npx drizzle-kit push exits 0 — Neon DB has session_catalog, session_instances, slotless_requests tables and all new businesses columns"
    - "idempotency_key UNIQUE constraint exists on session_instances and slotless_requests in both schema.ts and live DB"
  artifacts:
    - "src/database/schema.ts with 3 new exported table constants and 8 new columns on existing tables"
    - "migrations/0010_session_catalog_schema.sql with idempotent SQL for all schema changes"
  key_links:
    - "schema.ts sessionCatalog table referenced by src/session/manager.ts (Wave 2)"
    - "schema.ts sessionInstances table referenced by src/session/manager.ts (Wave 2) and src/scheduler/session-cancellation.ts (Wave 3)"
    - "schema.ts slotlessRequests table schema foundation for Phase 13 (not queried in Phase 10)"
    - "businesses new columns referenced by src/onboarding/ai-owner-agent.ts switch cases (Wave 2)"
    - "bookings.sessionInstanceId FK referenced by bookSessionInstance atomic transaction (Wave 2)"
---

<objective>
Extend the Drizzle schema with 3 new tables (sessionCatalog, sessionInstances, slotlessRequests), 7 new business config columns, and a nullable sessionInstanceId FK on bookings. Create the SQL migration file and push to the Neon database. This schema is the blocking Wave 1 gate — no session code in Waves 2-4 can run until drizzle-kit push completes.

Purpose: All downstream work (session manager, owner tools, cancellation poller, and Phases 11-15) depends on these type definitions and live DB tables being in place.
Output: Updated src/database/schema.ts with new exports and columns, migrations/0010_session_catalog_schema.sql, and the live Neon DB with all schema changes applied.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-session-catalog-schema/10-RESEARCH.md
@.planning/phases/10-session-catalog-schema/10-PATTERNS.md
@src/database/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend src/database/schema.ts with session tables and new columns</name>
  <files>src/database/schema.ts</files>
  <read_first>
    src/database/schema.ts — full file: existing imports at top, table definitions end-of-file (last table is membershipExpiryNotifications), inline comment conventions (phase-cited JSDoc per column), uniqueIndex patterns
    .planning/phases/10-session-catalog-schema/10-RESEARCH.md — Drizzle Schema Design section: exact column definitions with rationale for sessionCatalog, sessionInstances, slotlessRequests; 7 New Business Config Columns subsection
    .planning/phases/10-session-catalog-schema/10-PATTERNS.md — Table Definition Pattern section: billingPackages analog, uniqueIndex WHERE clause patterns; Adding Nullable Columns to Businesses Table section: enforcementPolicy column as analog
  </read_first>
  <action>
    Make four distinct changes to src/database/schema.ts. Follow every existing convention: inline phase-cited JSDoc comments, nullable columns for backward compatibility on non-empty table, uniqueIndex with SQL WHERE clauses, and the same pgTable/serial/text/integer/boolean/timestamp/uniqueIndex imports already present.

    CHANGE 1 — Add 7 new columns to the businesses table (per D-01 through D-07 in phase architecture):
    Insert these columns into the businesses table definition, after the enforcementPolicy column (which is the last column before createdAt). Each column must have an inline JSDoc comment citing the Phase that introduces it and the purpose:

    - bookingMode: text('booking_mode').notNull().default('open_slots') — Phase 10 (CLSS-01): booking mode for this business. 'open_slots' = v1.2 behavior (availability-based, default); 'fixed_sessions' = class-schedule mode. NOT NULL with DEFAULT 'open_slots' — existing rows are safe after migration (preserve v1.2 behavior).
    - cancellationCutoffEnabled: boolean('cancellation_cutoff_enabled').notNull().default(false) — Phase 12 (CANC-01): whether the cancellation cutoff window is active. Default false — no cutoff unless owner explicitly enables.
    - cancellationCutoffHours: integer('cancellation_cutoff_hours').notNull().default(8) — Phase 12 (CANC-01): hours before session at which credit forfeiture kicks in. Default 8.
    - slotlessRequestsEnabled: boolean('slotless_requests_enabled').notNull().default(false) — Phase 13 (SLOT-01): whether clients can request bookings with no open slot. Default false.
    - lastSessionThresholdEnabled: boolean('last_session_threshold_enabled').notNull().default(false) — Phase 14 (RENW-01): whether the last-session renewal nudge is active. Default false.
    - lastSessionThresholdCount: integer('last_session_threshold_count').notNull().default(1) — Phase 14 (RENW-01): sessions remaining count that triggers the renewal nudge. Default 1.
    - allowMultiBooking: boolean('allow_multi_booking').notNull().default(false) — Phase 11 (SBOK-04): whether a client can book multiple sessions in one request. Default false.

    CHANGE 2 — Add nullable sessionInstanceId FK to the bookings table:
    In the existing bookings table definition, add sessionInstanceId after the rescheduledFromBookingId column. Use: sessionInstanceId: integer('session_instance_id') with no notNull() (nullable). Add JSDoc comment: Phase 10 (CLSS-01): nullable FK to session_instances; null for open_slots mode bookings (v1.2 and earlier); set for fixed_sessions mode bookings (v1.3+). The existing uniqueIndex on bookings must remain unchanged.

    CHANGE 3 — Add 3 new session tables at end of file after membershipExpiryNotifications:
    Add a section comment "--- Phase 10: Session Catalog & Schema ---" then define and export:

    sessionCatalog (table name 'session_catalog'): columns are id (serial primaryKey), businessId (integer notNull references businesses.id — RLS key: FK chain to businesses enforces ownership), serviceId (integer 'service_id' notNull references services.id), rruleString (text 'rrule_string' notNull — RFC 5545 recurrence rule string, e.g. "FREQ=WEEKLY;BYDAY=MO,WE,FR"), startTime (text 'start_time' notNull — wall-clock time "HH:MM" in Europe/Athens local, 24h), capacity (integer notNull — hard cap; sessions cannot overfill), isActive (boolean 'is_active' notNull default true — soft-delete: false = deactivated catalog entry), createdAt (timestamp 'created_at' notNull defaultNow). Table-level index: uniqueIndex('unique_active_catalog_per_business_service').on(table.businessId, table.serviceId).where(sql`is_active = true`) — one active catalog per (business, service); allows multiple inactive entries for same combination.

    sessionInstances (table name 'session_instances'): columns are id (serial primaryKey), catalogId (integer 'catalog_id' notNull references sessionCatalog.id — FK chain: instance → catalog → businesses → RLS), sessionDate (text 'session_date' notNull — ISO "YYYY-MM-DD" Europe/Athens wall-clock date), sessionTime (text 'session_time' notNull — "HH:MM" Europe/Athens wall-clock time, matches sessionCatalog.startTime convention), bookedCount (integer 'booked_count' notNull default 0 — denormalized for O(1) list_sessions queries; updated atomically on booking insert/cancel via sql`booked_count + 1`), isCancelled (boolean 'is_cancelled' notNull default false — soft-delete: owner cancelled this instance; preserves audit trail and idempotency on replay), idempotencyKey (text 'idempotency_key' notNull unique — format: "catalog:{catalogId}:{sessionDate}:{sessionTime}"; guards against duplicate instance creation on rrule expansion replay), createdAt (timestamp 'created_at' notNull defaultNow). Table-level index: uniqueIndex('unique_session_instance').on(table.catalogId, table.sessionDate, table.sessionTime) — one instance per (catalog, date, time); complementary constraint to idempotency_key.

    slotlessRequests (table name 'slotless_requests'): columns are id (serial primaryKey), businessId (integer 'business_id' notNull references businesses.id — RLS key), clientPhone (text 'client_phone' notNull — Telegram from.id stringified, consistent with bookings.clientPhone), requestedSessionDate (text 'requested_session_date' notNull — ISO "YYYY-MM-DD" Athens local), requestedSessionTime (text 'requested_session_time' notNull — "HH:MM" Athens local), serviceId (integer 'service_id' notNull references services.id), status (text notNull default 'pending' — 'pending' | 'approved' | 'rejected'), bookingId (integer 'booking_id' — nullable FK references bookings.id; set when owner approves and booking is created in Phase 13), idempotencyKey (text 'idempotency_key' notNull unique — format: "client:{clientPhone}:service:{serviceId}:{requestedSessionDate}:{requestedSessionTime}"; prevents duplicate inserts on webhook replay), createdAt (timestamp 'created_at' notNull defaultNow). Table-level index: uniqueIndex('unique_pending_slotless_per_client_service').on(table.businessId, table.clientPhone, table.serviceId, table.requestedSessionDate).where(sql`status = 'pending'`) — one pending request per (business, client, service, date); approved/rejected requests do not block new requests.

    All new tables must use the same pgTable, serial, text, integer, boolean, timestamp, uniqueIndex, sql imports already present at the top of the file. Do not add new import statements unless a genuinely new Drizzle function is needed (it should not be).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript compiles with zero errors; sessionCatalog, sessionInstances, slotlessRequests exported from src/database/schema.ts; businesses table has 7 new columns; bookings table has sessionInstanceId nullable FK.</done>
</task>

<task type="auto">
  <name>Task 2: Create migrations/0010_session_catalog_schema.sql</name>
  <files>migrations/0010_session_catalog_schema.sql</files>
  <read_first>
    migrations/0006_billing_schema.sql — migration file conventions: header comment block with purpose, section comments, idempotent DO $$ IF NOT EXISTS patterns, CREATE UNIQUE INDEX IF NOT EXISTS, GRANT statements for randevuclaw_app role and sequences
    migrations/0009_billing_rls.sql — most recent migration: confirm file naming sequence (next is 0010)
    migrations/0007_enforcement_policy.sql — pattern for ALTER TABLE ADD COLUMN IF NOT EXISTS with NOT NULL DEFAULT on a non-empty table
  </read_first>
  <action>
    Create migrations/0010_session_catalog_schema.sql as a manual SQL reference artifact. This file documents the schema changes for Phase 10 and is safe to apply via psql. drizzle-kit push in Task 3 applies from schema.ts directly; this file is idempotent documentation and recovery reference.

    Include a header comment: "Phase 10: Session Catalog and Schema — Add session_catalog, session_instances, slotless_requests tables; add 7 business config columns and sessionInstanceId FK on bookings. All statements are idempotent (IF NOT EXISTS / IF NOT EXISTS)."

    Section 1 (businesses table — 7 new columns, all NOT NULL with DEFAULT): Use ALTER TABLE businesses ADD COLUMN IF NOT EXISTS for each of the 7 columns, citing the Phase that introduces each:
    - booking_mode TEXT NOT NULL DEFAULT 'open_slots' (Phase 10)
    - cancellation_cutoff_enabled BOOLEAN NOT NULL DEFAULT FALSE (Phase 12)
    - cancellation_cutoff_hours INTEGER NOT NULL DEFAULT 8 (Phase 12)
    - slotless_requests_enabled BOOLEAN NOT NULL DEFAULT FALSE (Phase 13)
    - last_session_threshold_enabled BOOLEAN NOT NULL DEFAULT FALSE (Phase 14)
    - last_session_threshold_count INTEGER NOT NULL DEFAULT 1 (Phase 14)
    - allow_multi_booking BOOLEAN NOT NULL DEFAULT FALSE (Phase 11)

    Section 2 (bookings table — sessionInstanceId nullable FK): ALTER TABLE bookings ADD COLUMN IF NOT EXISTS session_instance_id INTEGER REFERENCES session_instances(id). Add a comment noting the forward reference: this column references session_instances which is created below; if applying manually run Section 4 first then re-run Section 2, or apply as a single transaction.

    Section 3 (session_catalog table): DO $$ BEGIN IF NOT EXISTS (SELECT FROM pg_tables WHERE tablename='session_catalog') THEN CREATE TABLE session_catalog (...) END IF; END $$. Columns: id SERIAL PRIMARY KEY, business_id INTEGER NOT NULL REFERENCES businesses(id), service_id INTEGER NOT NULL REFERENCES services(id), rrule_string TEXT NOT NULL, start_time TEXT NOT NULL, capacity INTEGER NOT NULL, is_active BOOLEAN NOT NULL DEFAULT TRUE, created_at TIMESTAMP NOT NULL DEFAULT NOW(). Add CHECK constraint: capacity > 0. After the DO block: CREATE UNIQUE INDEX IF NOT EXISTS unique_active_catalog_per_business_service ON session_catalog (business_id, service_id) WHERE is_active = true.

    Section 4 (session_instances table): DO $$ IF NOT EXISTS CREATE TABLE session_instances with columns id SERIAL PRIMARY KEY, catalog_id INTEGER NOT NULL REFERENCES session_catalog(id), session_date TEXT NOT NULL, session_time TEXT NOT NULL, booked_count INTEGER NOT NULL DEFAULT 0, is_cancelled BOOLEAN NOT NULL DEFAULT FALSE, idempotency_key TEXT NOT NULL UNIQUE, created_at TIMESTAMP NOT NULL DEFAULT NOW(). After: CREATE UNIQUE INDEX IF NOT EXISTS unique_session_instance ON session_instances (catalog_id, session_date, session_time).

    Section 5 (slotless_requests table): DO $$ IF NOT EXISTS CREATE TABLE slotless_requests with columns id SERIAL PRIMARY KEY, business_id INTEGER NOT NULL REFERENCES businesses(id), client_phone TEXT NOT NULL, requested_session_date TEXT NOT NULL, requested_session_time TEXT NOT NULL, service_id INTEGER NOT NULL REFERENCES services(id), status TEXT NOT NULL DEFAULT 'pending', booking_id INTEGER REFERENCES bookings(id), idempotency_key TEXT NOT NULL UNIQUE, created_at TIMESTAMP NOT NULL DEFAULT NOW(). Add CHECK constraint: status IN ('pending', 'approved', 'rejected'). After: CREATE UNIQUE INDEX IF NOT EXISTS unique_pending_slotless_per_client_service ON slotless_requests (business_id, client_phone, service_id, requested_session_date) WHERE status = 'pending'.

    Section 6 (role grants): GRANT SELECT, INSERT, UPDATE ON session_catalog TO randevuclaw_app; GRANT SELECT, INSERT, UPDATE ON session_instances TO randevuclaw_app; GRANT SELECT, INSERT, UPDATE ON slotless_requests TO randevuclaw_app; GRANT USAGE, SELECT ON SEQUENCE session_catalog_id_seq TO randevuclaw_app; GRANT USAGE, SELECT ON SEQUENCE session_instances_id_seq TO randevuclaw_app; GRANT USAGE, SELECT ON SEQUENCE slotless_requests_id_seq TO randevuclaw_app.
  </action>
  <verify>
    <automated>test -f migrations/0010_session_catalog_schema.sql &amp;&amp; grep -c 'session_catalog' migrations/0010_session_catalog_schema.sql</automated>
  </verify>
  <done>migrations/0010_session_catalog_schema.sql exists with all 6 sections; CREATE TABLE statements are idempotent; role grants present for randevuclaw_app; all 7 businesses ALTER TABLE columns documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Push schema to Neon DB via drizzle-kit push</name>
  <files><!-- no files modified — schema push applies existing schema.ts to the live DB --></files>
  <what-built>
    src/database/schema.ts now includes sessionCatalog, sessionInstances, slotlessRequests tables; 7 new columns on businesses (bookingMode, cancellationCutoffEnabled, cancellationCutoffHours, slotlessRequestsEnabled, lastSessionThresholdEnabled, lastSessionThresholdCount, allowMultiBooking); nullable sessionInstanceId FK on bookings. migrations/0010_session_catalog_schema.sql documents the equivalent SQL. This task applies the schema.ts changes directly to the Neon PostgreSQL database. This is the blocking Wave 1 gate — Waves 2, 3, and 4 cannot run until this checkpoint is approved.
  </what-built>
  <how-to-verify>
    1. Ensure DATABASE_URL is set in .env.local (Neon connection string)
    2. Run: npx drizzle-kit push
    3. drizzle-kit will prompt about creating new tables and adding columns — review the proposed changes (expected: 3 new tables + 8 new columns + 1 new nullable FK) and enter y to proceed
    4. Verify exit code is 0 with no errors
    5. Optional but recommended: connect to Neon DB console (https://console.neon.tech) and confirm session_catalog, session_instances, slotless_requests tables appear; businesses table has booking_mode column; bookings table has session_instance_id column
    6. Run npm test to confirm the full existing suite (320+ tests) remains green after schema push
  </how-to-verify>
  <verify>
    <automated>npm test --testTimeout=10000</automated>
    <human-check>Run npx drizzle-kit push; enter y at all prompts; confirm exit 0; verify session_catalog, session_instances, slotless_requests exist in Neon DB console; run npm test and confirm green.</human-check>
  </verify>
  <done>npx drizzle-kit push exits 0; session_catalog, session_instances, slotless_requests tables exist in Neon DB; businesses has all 7 new columns; bookings has session_instance_id; npm test passes green.</done>
  <resume-signal>Type "approved" when npx drizzle-kit push completes without errors and npm test passes</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → DB | drizzle-kit push runs with DATABASE_URL credentials — table additions and column adds are irreversible without a down-migration |
| bookings.sessionInstanceId → session_instances | Nullable FK; existing open_slots bookings have NULL; fixed_sessions bookings will have an ID |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Tampering | Cross-tenant session creation via hacked businessId | high | mitigate | businessId FK on sessionCatalog and slotlessRequests; RLS via randevuclaw_app role in Section 6 of migration; withBusinessContext enforced in Wave 2 query layer |
| T-10-03 | Elevation | Session capacity bypass via concurrent overbooking | high | accept (schema-only Wave) | SELECT FOR UPDATE guard deferred to Wave 2 session manager; bookedCount column is the denormalized sentinel |
| T-10-05 | Tampering | Status enum bypass on slotlessRequests | medium | mitigate | CHECK constraint status IN ('pending', 'approved', 'rejected') in migration SQL; Zod validation in Phase 13 handler |
| T-10-06 | Information Disclosure | Multi-tenant session data leak | high | mitigate | business_id FK on all 3 new tables; RLS via randevuclaw_app GRANT in Section 6; withBusinessContext enforced in Wave 2 |
| T-10-07 | Denial of Service | Duplicate session instances from rrule replay | medium | mitigate | idempotency_key TEXT NOT NULL UNIQUE on session_instances; prevents duplicate INSERT on replay via onConflictDoNothing |
| T-10-SC | Tampering | npm/drizzle-kit install chain | high | mitigate | drizzle-kit is existing tool (not new install); schema push via human checkpoint with blocking gate — human reviews proposed changes before confirming |
</threat_model>

<verification>
After schema push: `npm test` must pass with the existing test suite (320+ tests green). The 5 session test stubs from 10-01 remain in TODO state (no failures — they do not import from schema yet).

Spot-check schema: `npx drizzle-kit studio` (or Neon console) to verify session_catalog, session_instances, slotless_requests tables exist with correct column types and constraints. Verify businesses table has booking_mode column with default 'open_slots'.
</verification>

<success_criteria>
- src/database/schema.ts exports sessionCatalog, sessionInstances, slotlessRequests and has 7 new businesses columns + bookings.sessionInstanceId
- npx tsc --noEmit exits 0
- migrations/0010_session_catalog_schema.sql exists with idempotent CREATE TABLE and GRANT statements for all 3 tables and 8 new columns
- npx drizzle-kit push exits 0 — Neon DB contains all new tables and columns
- Full test suite (npm test) remains green after schema push
</success_criteria>

<output>
Create `.planning/phases/10-session-catalog-schema/10-02-SUMMARY.md` when done.
</output>
