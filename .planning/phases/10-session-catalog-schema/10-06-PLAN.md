---
phase: 10-session-catalog-schema
plan: 06
type: execute
wave: 4
depends_on:
  - "10-03"
  - "10-04"
  - "10-05"
files_modified:
  - tests/session-creation.test.ts
  - tests/session-expansion.test.ts
  - tests/session-cancel.test.ts
  - tests/session-assignment.test.ts
  - tests/session-list.test.ts
autonomous: true
requirements:
  - CLSS-01
  - CLSS-02
  - CLSS-03
  - CLSS-04
  - CLSS-05

must_haves:
  truths:
    - "npm test -- tests/session-*.test.ts --testTimeout=15000 exits 0 with all tests PASSING (no todos, no failures)"
    - "session-assignment.test.ts includes a concurrency test: two simultaneous calls to bookSessionInstance on the same full session return exactly one 'success' and one 'full'"
    - "session-cancel.test.ts includes a test verifying pollSessionCancellations sends to booked clients and inserts a dedup row preventing a second send"
    - "session-list.test.ts verifies listSessions returns sessionDate, sessionTime, bookedCount, capacity for each result row"
    - "session-expansion.test.ts verifies DST boundary expansion: Mon/Wed/Fri pattern crossing Oct 25 2026 produces correct wall-clock Athens dates"
    - "All 5 session test files have zero it.todo stubs remaining"
  artifacts:
    - tests/session-creation.test.ts
    - tests/session-expansion.test.ts
    - tests/session-cancel.test.ts
    - tests/session-assignment.test.ts
    - tests/session-list.test.ts
  key_links:
    - "session-assignment.test.ts capacity-race test uses Promise.all with two concurrent bookSessionInstance calls on same instance with bookedCount = capacity - 1"
    - "session-cancel.test.ts poller test stubs sendTelegramMessage via jest.mock and verifies call count and Greek message content"
    - "All test files follow the TEST_DATABASE_URL + jest.resetModules() + require() pattern from billing-membership-creation.test.ts"
---

<objective>
Replace all remaining it.todo stubs in the 5 session test files with real passing test implementations. The critical tests are: the capacity-race guard (SELECT FOR UPDATE preventing overbooking — CLSS-04), the poller dedup behavior (CLSS-03), and the DST boundary expansion correctness (CLSS-02). All CLSS requirements must have at least one green automated test before phase completion.

Purpose: Nyquist completion — the stubs written in Wave 0 are now filled with real implementations that run against the test DB and confirm correct behavior end to end.
Output: 5 fully green session test files, zero it.todo stubs, all CLSS-01 through CLSS-05 behaviors covered by automated tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-session-catalog-schema/10-RESEARCH.md
@.planning/phases/10-session-catalog-schema/10-PATTERNS.md
@tests/billing-membership-creation.test.ts
@src/session/manager.ts
@src/scheduler/session-cancellation.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill session-assignment.test.ts (CLSS-04) — capacity-race and direct assignment</name>
  <files>tests/session-assignment.test.ts</files>
  <read_first>
    tests/billing-membership-creation.test.ts — full file: TEST_DATABASE_URL pattern, jest.resetModules(), require() imports, beforeAll/afterAll setup, withBusinessContext usage; idempotency replay test pattern
    tests/helpers/session-fixtures.ts — insertTestSessionCatalog, insertTestSessionInstance signatures (created in 10-03)
    tests/helpers/test-business.ts — insertTestBusiness helper; understand how to insert a test service (services table fixture)
    src/session/manager.ts — bookSessionInstance function signature and return types (BookSessionResult: {status, bookingId?})
  </read_first>
  <behavior>
    - 'assign_client_to_session inserts booking row with correct sessionInstanceId FK': call bookSessionInstance with valid instanceId; verify returned bookingId exists in bookings table with correct sessionInstanceId via db.select()
    - 'assign_client_to_session atomically increments sessionInstances.bookedCount by 1': assert bookedCount row value before (0) and after (1) booking
    - 'capacity race guard: two concurrent assignments on same full session — exactly one succeeds, one returns full': set bookedCount = capacity - 1 on the instance, then call Promise.all([bookSessionInstance(...), bookSessionInstance(...)]) with different client phones; exactly one result must be 'success' and one must be 'full'
    - 'assign_client_to_session on cancelled session returns conflict status, no booking inserted': mark instance as isCancelled=true, call bookSessionInstance, assert result.status === 'conflict'; verify no booking row exists in DB
    - 'assigned client receives Greek notification via sendTelegramMessage after successful booking': verified via the owner tool test in session-creation (tool-level test, not repeated here); this stub is replaced with a unit assertion on bookSessionInstance return value only
  </behavior>
  <action>
    Replace all 5 it.todo stubs in session-assignment.test.ts with real implementations. Follow the TEST_DATABASE_URL + jest.resetModules() pattern from billing-membership-creation.test.ts exactly.

    Setup: beforeAll inserts a test business (insertTestBusiness), a test service (insert directly via admin db using services table from schema), a test session catalog (insertTestSessionCatalog), and a test session instance (insertTestSessionInstance). Store businessId, serviceId, catalogId, instanceId in closure variables.

    For the capacity-race test: update the test instance's bookedCount to capacity-1 (= 9 for default capacity=10) via a direct db.update() before the test. Then call Promise.all with two bookSessionInstance calls using different client phones. Assert exactly one 'success' and one 'full'. This test requires the REAL Neon test DB (SELECT FOR UPDATE must execute on a real Postgres instance — cannot mock it).

    For the cancelled session test: insert a separate session instance with isCancelled=true directly via insertTestSessionInstance({isCancelled: true}). Call bookSessionInstance against it. Assert result.status === 'conflict'.

    Use unique client phone suffixes per test: `test-client-${Date.now()}-${Math.random().toString(36).slice(2)}` to avoid idempotencyKey collisions between test runs.
  </action>
  <verify>
    <automated>npm test -- tests/session-assignment.test.ts --testTimeout=20000</automated>
  </verify>
  <done>session-assignment.test.ts has 4 passing real tests; capacity-race test verifies exactly-1-success with SELECT FOR UPDATE; zero it.todo stubs remain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill session-cancel.test.ts, session-list.test.ts, session-expansion.test.ts (CLSS-03, CLSS-05, CLSS-02 DST)</name>
  <files>
    tests/session-cancel.test.ts,
    tests/session-list.test.ts,
    tests/session-expansion.test.ts
  </files>
  <read_first>
    tests/session-assignment.test.ts — just filled in Task 1: use as style reference for this wave's test files
    src/scheduler/session-cancellation.ts — pollSessionCancellations function: understand what it queries and inserts so mocks can be set up correctly
    src/session/manager.ts — listSessions return type (SessionInstance[]), cancelSession boolean return
    tests/billing-membership-creation.test.ts — idempotency test pattern (call twice, second is rejected or no-ops)
  </read_first>
  <behavior>
    session-cancel.test.ts (CLSS-03):
    - 'cancel_session marks sessionInstance.isCancelled=true atomically': call cancelSession(businessId, instanceId); verify isCancelled=true in DB via db.select()
    - 'cancel_session on already-cancelled instance is a no-op (idempotent, returns false)': call cancelSession twice; second call returns false
    - 'poller finds cancelled instances not yet notified and sends Greek message to each booked client': mock sendTelegramMessage via jest.mock('../src/telegram/client'); insert cancelled instance with 2 booked clients (bookings rows); call pollSessionCancellations(); assert sendTelegramMessage called twice (once per client); assert sessionCancellationNotifications dedup row inserted
    - 'poller dedup: sessionCancellationNotifications row prevents second notification send on poller re-run': after first poller run, call pollSessionCancellations() again; assert sendTelegramMessage not called again (dedup row blocks second batch)
    - 'poller partial failure: one client send failure does not block other clients in same session': mock sendTelegramMessage to throw on first call, succeed on second; assert both clients were attempted and notification dedup row still inserted

    session-list.test.ts (CLSS-05):
    - 'listSessions aggregates bookedCount correctly for each sessionInstance': insert 2 session instances; book 1 client into instance A; call listSessions; assert instance A has bookedCount=1, instance B has bookedCount=0
    - 'listSessions excludes cancelled instances (isCancelled=true) from results': insert cancelled instance; call listSessions; assert result does not include cancelled instance
    - 'listSessions excludes instances with sessionDate in the past': insert instance with sessionDate = yesterday (isoDateInAthens on Date minus 1 day); call listSessions; assert not in result
    - 'listSessions result format includes sessionDate, sessionTime, bookedCount, capacity for each row': verify each result object has all 4 fields with correct types
    - 'listSessions returns empty array when no active upcoming sessions exist': call listSessions with no sessions in DB for this business; assert result is []

    session-expansion.test.ts (CLSS-02) — remaining stubs:
    - 'expands weekly Mon/Wed/Fri pattern to ~90 days of sessionInstances': already tested in 10-03 Task 2; verify result.instanceCount is between 36 and 42 (already passing — confirm still green)
    - 'idempotencyKey format is catalog:{catalogId}:{sessionDate}:{sessionTime} for each instance': after expansion, db.select on sessionInstances WHERE catalogId=X and verify first row's idempotencyKey matches expected format regex /^catalog:\d+:\d{4}-\d{2}-\d{2}:\d{2}:\d{2}$/
    - 'expansion fails cleanly with thrown error when rruleString is invalid RFC 5545': call createSessionCatalogWithExpansion with rruleString='INVALID' inside expect(...).rejects.toThrow(); assert no orphaned catalog row in DB after failure
    - 'DST boundary: expansion across Oct 25 2026 (UTC+3 to UTC+2) produces correct wall-clock Athens dates': call createSessionCatalogWithExpansion with FREQ=WEEKLY;BYDAY=SU starting from 2026-10-18, expand 14 days (expect instances on Oct 18 and Oct 25). For the Oct 25 instance, verify sessionDate='2026-10-25' (wall-clock Athens date — the DST transition day) and sessionTime='10:00' (startTime unchanged, not shifted to 09:00 by DST).
  </behavior>
  <action>
    Replace all remaining it.todo stubs in all three test files. Follow the TEST_DATABASE_URL + jest.resetModules() pattern. Each test file has its own beforeAll fixture setup.

    For session-cancel.test.ts poller tests: use jest.mock at the top of the file to mock src/telegram/client sendTelegramMessage. Reset mock between tests via beforeEach(() => jest.clearAllMocks()). For botTokenStore, mock botTokenStore.run to call the callback directly: mockBotTokenStore.run.mockImplementation((_token, cb) => cb()). Insert bookings rows directly via admin db for poller test setup (bypass Gemini/tool layer).

    For session-list.test.ts: use the admin db to insert sessionInstances and bookings directly for predictable state. Use unique business per describe block to avoid cross-test interference.

    For session-expansion.test.ts DST test: this is a pure functional test on the output of createSessionCatalogWithExpansion. The test does NOT need to inject mocked dates — use a fixed startDate of '2026-10-18' by passing it as a parameter override. Read the manager.ts implementation to check if createSessionCatalogWithExpansion accepts a startDate parameter or always uses isoDateInAthens(new Date()). If it always uses today, add an optional startDate parameter (defaulting to isoDateInAthens(new Date())) and update manager.ts accordingly — this is acceptable scope in Wave 4.

    For the 'expansion fails cleanly' test: call createSessionCatalogWithExpansion with rruleString='NOT_VALID_RRULE'. After the expected throw, do a db.select from sessionCatalog WHERE businessId=X to confirm no orphaned catalog row was committed.
  </action>
  <verify>
    <automated>npm test -- tests/session-cancel.test.ts tests/session-list.test.ts tests/session-expansion.test.ts --testTimeout=20000</automated>
  </verify>
  <done>All three test files are fully green with zero it.todo stubs; poller dedup test confirms second send is blocked; DST expansion test verifies Oct 25 wall-clock date is correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test DB → production DB | TEST_DATABASE_URL must point to randevuclaw_test DB (never production DATABASE_URL) |
| jest.mock → real Telegram API | sendTelegramMessage is mocked in poller tests — no real Telegram calls in test suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-20 | Elevation | Capacity-race test running against production DB | critical | mitigate | TEST_DATABASE_URL pattern (from billing-membership-creation.test.ts) ensures test isolation; jest.resetModules() forces module reload with test DB URL |
| T-10-21 | Information Disclosure | Real client phone numbers in test fixtures | medium | mitigate | All test fixtures use synthetic phone numbers (test-client-{Date.now()} pattern — never real numbers) |
| T-10-22 | Denial of Service | Concurrent capacity-race test causing table locks on test DB | low | accept | Test DB is single-purpose; SELECT FOR UPDATE with small row count resolves in <100ms; no production impact |
</threat_model>

<verification>
Final phase gate after all 6 plans complete:
- `npm test -- tests/session-*.test.ts --testTimeout=20000` exits 0 — all 5 files green, zero it.todo stubs
- `npm test --testTimeout=15000` exits 0 — full suite (320+ prior tests + new session tests) all green
- Manual smoke test: send "δείξε μου τις σεζόν" to owner bot — receives formatted list of upcoming sessions
- Manual smoke test: send "ακύρωσε σεζόν 2026-07-28 10:00" — receives Greek confirmation; poller (on next 6h run) sends client notification

Phase 10 CLSS requirement coverage:
- CLSS-01: session-creation.test.ts (catalog + instance created atomically) + create_recurring_session tool
- CLSS-02: session-expansion.test.ts (rrule expands ~90 days, idempotent, DST boundary)
- CLSS-03: session-cancel.test.ts (isCancelled flag + poller broadcast + dedup)
- CLSS-04: session-assignment.test.ts (atomic booking + capacity race SELECT FOR UPDATE)
- CLSS-05: session-list.test.ts (listSessions aggregation, filtering, format)
</verification>

<success_criteria>
- All 5 session test files have zero it.todo stubs
- session-assignment.test.ts capacity-race test: exactly one 'success' and one 'full' on concurrent bookSessionInstance against full session
- session-cancel.test.ts: poller dedup test confirms second sendTelegramMessage call blocked by sessionCancellationNotifications row
- session-expansion.test.ts: DST boundary test confirms Oct 25 2026 wall-clock Athens date is preserved by rrule expansion
- Full npm test suite exits 0
</success_criteria>

<output>
Create `.planning/phases/10-session-catalog-schema/10-06-SUMMARY.md` when done.
</output>
