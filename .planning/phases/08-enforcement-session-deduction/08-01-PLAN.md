---
phase: 08-enforcement-session-deduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/enforcement-session-deduction.test.ts
  - tests/booking-enforcement.test.ts
  - tests/enforcement-nlu.test.ts
  - tests/COVERAGE.md
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
  - ENFC-01
  - ENFC-02
  - ENFC-03

must_haves:
  truths:
    - Three test files exist with it.todo stubs covering every Phase 8 requirement
    - All stubs compile without importing from modules that do not yet exist
    - COVERAGE.md updated with Phase 8 test file entries
    - npm test runs without syntax errors against stub files
  artifacts:
    - tests/enforcement-session-deduction.test.ts
    - tests/booking-enforcement.test.ts
    - tests/enforcement-nlu.test.ts
    - tests/COVERAGE.md
  key_links:
    - Stub test files reference only Phase 7 modules that already exist (billing/queries, database/schema)
    - it.todo stubs use the exact test name strings that Wave 2-4 implementations will target
---

<objective>
Create test stub files for all Phase 8 requirements before any implementation begins.
All stubs use it.todo so they compile without importing modules that do not yet exist.
COVERAGE.md is updated to document Phase 8 test coverage.

Purpose: Nyquist compliance — every requirement has a named stub before implementation.
Output: Three test files with it.todo stubs + updated COVERAGE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-enforcement-session-deduction/08-RESEARCH.md

@tests/billing-membership-creation.test.ts
@tests/helpers/billing-fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enforcement session deduction test stubs (SESS-01 to SESS-04)</name>
  <files>tests/enforcement-session-deduction.test.ts</files>
  <read_first>
    - tests/billing-membership-creation.test.ts (jest.resetModules + require pattern, DB URL override)
    - tests/helpers/billing-fixtures.ts (insertTestPackage, insertTestMembership signatures)
    - tests/helpers/test-business.ts (insertTestBusiness signature)
    - src/billing/queries.ts (existing Membership interface, existing exports — check what exists before stubbing)
  </read_first>
  <action>
    Create tests/enforcement-session-deduction.test.ts as an it.todo stub file.

    Header comment: "covers SESS-01, SESS-02, SESS-03, SESS-04" and the same DB setup note as billing-membership-creation.test.ts.

    Use the jest.resetModules() + require() pattern from billing-membership-creation.test.ts to override DATABASE_URL to BILLING_TEST_DATABASE_URL (or 'postgresql://manolis@localhost:5432/randevuclaw_test').

    Include afterAll that restores ORIGINAL_DATABASE_URL.

    Declare two describe blocks — do NOT import from modules that don't yet exist:

    describe block 1: 'insertBookingWithSessionDeduction' — four it.todo stubs:
    - 'SESS-01: inserts booking and deducts 1 session atomically in same transaction'
    - 'SESS-01: concurrent bookings on same membership deduct exactly 1 session (race guard)'
    - 'SESS-03: unlimited membership (sessionCount=null) booking creates no ledger entry'
    - 'SESS-04: unlimited membership booking does not change sessionsRemaining'

    describe block 2: 'cancelBookingWithRefund' — three it.todo stubs:
    - 'SESS-02: cancellation within membership validity restores 1 session credit'
    - 'SESS-02: credit restore appends ledger entry with operationType credit_restored'
    - 'SESS-02/03: cancellation after membership expiry does not restore credit (sessions forfeited)'

    No imports from src/billing/enforcement.ts (does not exist yet). Only import from modules that already exist if needed — but since all are it.todo, no imports are required at all except jest type inference. Keep the file to plain describe/it.todo blocks plus the DB URL override at the top.
  </action>
  <verify>
    <automated>npx jest tests/enforcement-session-deduction.test.ts --passWithNoTests 2>&1 | grep -E "PASS|FAIL|todo|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/enforcement-session-deduction.test.ts
    - npx jest tests/enforcement-session-deduction.test.ts exits 0 with all tests shown as todo
    - No import of src/billing/enforcement.ts or any not-yet-existing module
    - All 7 it.todo strings match exactly the test names documented in 08-RESEARCH.md Phase Requirements → Test Map
  </acceptance_criteria>
  <done>npm test runs against the file with 0 failures and 7 todo stubs shown.</done>
</task>

<task type="auto">
  <name>Task 2: Create booking enforcement policy test stubs (ENFC-02, ENFC-03) and NLU policy tool stubs (ENFC-01)</name>
  <files>tests/booking-enforcement.test.ts, tests/enforcement-nlu.test.ts</files>
  <read_first>
    - tests/billing-nlu-parsing.test.ts (OWNER_TOOLS import pattern for NLU tool shape tests)
    - tests/function-executor.test.ts (function-executor mock pattern — check what mocks exist)
    - src/onboarding/ai-owner-agent.ts lines 37-199 (OWNER_TOOLS array — verify set_enforcement_policy does NOT exist yet)
  </read_first>
  <action>
    Create tests/booking-enforcement.test.ts as an it.todo stub file.

    Header comment: "covers ENFC-02, ENFC-03" and note that these require billing/enforcement.ts (Wave 2) and function-executor.ts modifications (Wave 3).

    No DATABASE_URL override needed — these stubs are pure it.todo with no DB calls.

    describe 'booking enforcement policy integration' — three it.todo stubs:
    - 'ENFC-02: block policy + no membership refuses booking and sends Greek refusal to client'
    - 'ENFC-03: flag policy + no membership allows booking and sends Greek alert to owner'
    - 'ENFC-02: block policy + active membership allows booking to proceed normally'

    ---

    Create tests/enforcement-nlu.test.ts as an it.todo stub file.

    Header comment: "covers ENFC-01" — owner sets enforcement policy via NLU tool.

    Import OWNER_TOOLS from src/onboarding/ai-owner-agent (this module already exists).

    describe 'set_enforcement_policy NLU tool' — four stubs:
    - it.todo for 'ENFC-01: set_enforcement_policy tool exists in OWNER_TOOLS'
    - it.todo for 'ENFC-01: tool rejects values other than block or flag'
    - it.todo for 'ENFC-01: setting policy to block persists to businesses.enforcement_policy in DB'
    - it.todo for 'ENFC-01: setting policy to flag persists to businesses.enforcement_policy in DB'

    Note: the OWNER_TOOLS import itself is safe — it already exists. Only the set_enforcement_policy tool case does not yet exist, but the import will succeed as the array is defined.
  </action>
  <verify>
    <automated>npx jest tests/booking-enforcement.test.ts tests/enforcement-nlu.test.ts --passWithNoTests 2>&1 | grep -E "PASS|FAIL|todo|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/booking-enforcement.test.ts exists with 3 it.todo stubs covering ENFC-02/03
    - tests/enforcement-nlu.test.ts exists with 4 it.todo stubs covering ENFC-01
    - Both files exit 0 with npx jest
    - enforcement-nlu.test.ts imports OWNER_TOOLS from src/onboarding/ai-owner-agent — which exists — and does not fail on import
  </acceptance_criteria>
  <done>npm test runs against both files with 0 failures and all stubs shown as todo.</done>
</task>

<task type="auto">
  <name>Task 3: Update COVERAGE.md with Phase 8 test entries</name>
  <files>tests/COVERAGE.md</files>
  <read_first>
    - tests/COVERAGE.md (existing content — append Phase 8 section, do not overwrite)
  </read_first>
  <action>
    Read tests/COVERAGE.md. Append a Phase 8 section at the end of the file.

    The section must list all three new test files with their requirement coverage:
    - tests/enforcement-session-deduction.test.ts — SESS-01, SESS-02, SESS-03, SESS-04
    - tests/booking-enforcement.test.ts — ENFC-02, ENFC-03
    - tests/enforcement-nlu.test.ts — ENFC-01

    Follow the exact format conventions already present in COVERAGE.md (heading style, table or list style — match whatever format exists).

    If COVERAGE.md does not exist, create it with the Phase 8 section only.
  </action>
  <verify>
    <automated>grep -c "enforcement-session-deduction\|booking-enforcement\|enforcement-nlu" tests/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - grep returns 3 (one match per file)
    - All three test files listed under Phase 8 with correct requirement IDs
  </acceptance_criteria>
  <done>COVERAGE.md contains Phase 8 entries for all three test files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → src modules | Test stubs import OWNER_TOOLS from existing ai-owner-agent.ts; no imports from unbuilt modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-W0-01 | Tampering | Test stub imports | low | mitigate | All stubs use it.todo — no runtime calls to unbuilt modules; compile-only import of OWNER_TOOLS which already exists |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages in Wave 0; all dependencies already installed in Phase 7 |
</threat_model>

<verification>
npm test -- tests/enforcement-session-deduction.test.ts tests/booking-enforcement.test.ts tests/enforcement-nlu.test.ts --passWithNoTests

All three files must exit 0 with 0 failures (all tests shown as pending/todo).
</verification>

<success_criteria>
- tests/enforcement-session-deduction.test.ts exists with 7 it.todo stubs (SESS-01/02/03/04)
- tests/booking-enforcement.test.ts exists with 3 it.todo stubs (ENFC-02/03)
- tests/enforcement-nlu.test.ts exists with 4 it.todo stubs (ENFC-01)
- tests/COVERAGE.md updated with Phase 8 section
- npm test exits 0 with all stubs shown as todo — no syntax errors
</success_criteria>

<output>
Create .planning/phases/08-enforcement-session-deduction/08-01-SUMMARY.md when done.
</output>

## Artifacts this phase plan produces

**New files:**
- `tests/enforcement-session-deduction.test.ts` — SESS-01/02/03/04 it.todo stubs
- `tests/booking-enforcement.test.ts` — ENFC-02/03 it.todo stubs
- `tests/enforcement-nlu.test.ts` — ENFC-01 it.todo stubs

**Modified files:**
- `tests/COVERAGE.md` — Phase 8 entries appended

**No new functions, types, or interfaces are defined yet** — all tests are it.todo stubs.
