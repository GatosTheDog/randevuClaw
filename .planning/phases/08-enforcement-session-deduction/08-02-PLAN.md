---
phase: 08-enforcement-session-deduction
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - migrations/0007_enforcement_policy.sql
  - src/database/schema.ts
  - src/database/queries.ts
autonomous: true
requirements:
  - SESS-01
  - ENFC-01
  - ENFC-02
  - ENFC-03

must_haves:
  truths:
    - migrations/0007_enforcement_policy.sql exists and is idempotent (ADD COLUMN IF NOT EXISTS)
    - businesses drizzle table definition in schema.ts has an enforcementPolicy column that maps to snake_case enforcement_policy with NOT NULL DEFAULT 'allow'
    - The Business TypeScript interface in queries.ts includes enforcementPolicy as a string field so callers can read it without type errors
    - randevuclaw_test local DB has the enforcement_policy column on the businesses table
    - Live Neon DB has the enforcement_policy column on the businesses table
    - TypeScript compilation passes without errors after both schema.ts and queries.ts are updated (both must change together — Pitfall 6 in RESEARCH.md)
    - "[FLAGGED-UNVERIFIED: ENFC migration ordering] Plan 04 code reads context.business.enforcementPolicy which comes from findBusinessByWebhookId — that function returns the Business interface. If the migration is not applied to the test DB before Plan 03 integration tests run, ts-jest will see the column but the test DB query will fail on missing column. Migration must be applied to randevuclaw_test in this plan's Task 2 before Plan 03 runs."
  artifacts:
    - migrations/0007_enforcement_policy.sql
    - src/database/schema.ts (businesses.enforcementPolicy column added)
    - src/database/queries.ts (Business.enforcementPolicy: string field added)
  key_links:
    - schema.ts enforcementPolicy column feeds the Business interface in queries.ts (both must change together)
    - Migration applied to randevuclaw_test unlocks Plan 03 integration tests
    - Migration applied to live Neon DB keeps production schema aligned
  prohibitions:
    - "[FLAGGED-UNVERIFIED] MUST update BOTH schema.ts AND queries.ts Business interface — updating only one causes TypeScript compile errors or silent undefined at runtime (Pitfall 6 in RESEARCH.md)"
    - "[FLAGGED-UNVERIFIED] Migration MUST use ADD COLUMN IF NOT EXISTS — plain ADD COLUMN errors if run twice (idempotency requirement for safe re-runs)"
---

<objective>
Schema extension for Phase 8 enforcement policy. Creates the migration SQL, adds the enforcementPolicy column to the Drizzle schema, updates the Business TypeScript interface, and applies the migration to both the local test database and the live Neon database.

Purpose: All Phase 8 production code reads businesses.enforcement_policy. This plan ensures the column exists at the DB layer before any query or handler code is written.

Output:
- migrations/0007_enforcement_policy.sql — ALTER TABLE businesses ADD COLUMN IF NOT EXISTS enforcement_policy TEXT NOT NULL DEFAULT 'allow' with CHECK constraint
- src/database/schema.ts — enforcementPolicy Drizzle column on businesses table
- src/database/queries.ts — enforcementPolicy: string on Business interface
- Migration applied to randevuclaw_test and live Neon DB
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-enforcement-session-deduction/08-CONTEXT.md
@.planning/phases/08-enforcement-session-deduction/08-PATTERNS.md
</context>

## Artifacts This Plan Produces

New files:
- `migrations/0007_enforcement_policy.sql` — ALTER TABLE businesses ADD COLUMN IF NOT EXISTS enforcement_policy TEXT NOT NULL DEFAULT 'allow' CONSTRAINT enforcement_policy_valid CHECK (enforcement_policy IN ('allow', 'block', 'flag')); GRANT UPDATE (enforcement_policy) ON businesses TO randevuclaw_app

Modified files:
- `src/database/schema.ts` — businesses drizzle table extended with `enforcementPolicy: text('enforcement_policy').notNull().default('allow')`
- `src/database/queries.ts` — Business interface extended with `enforcementPolicy: string`

<tasks>

<task type="auto">
  <name>Task 1: Create migration SQL + update schema.ts businesses table + update Business interface</name>
  <files>migrations/0007_enforcement_policy.sql, src/database/schema.ts, src/database/queries.ts</files>

  <read_first>
    - migrations/0006_billing_schema.sql — header comment pattern (lines 1–12), idempotency guards (ADD COLUMN IF NOT EXISTS), GRANT statement pattern at the bottom; copy this format for 0007
    - src/database/schema.ts — read the businesses table definition to identify the correct column insertion point and naming convention (camelCase Drizzle name → snake_case SQL via text('snake_case'))
    - src/database/queries.ts — lines 25–40: Business interface definition; identify where to append the new field
    - .planning/phases/08-enforcement-session-deduction/08-RESEARCH.md — Pattern 6 (migration SQL) and Pitfall 5/6 (ordering and dual-update requirement)
  </read_first>

  <action>
    Create migrations/0007_enforcement_policy.sql with:
    - Header comment block in the same format as 0006: migration name, purpose (Add enforcement_policy column to businesses table — Phase 8 D-07), how-to-apply instructions (psql $DATABASE_URL -f migrations/0007_enforcement_policy.sql), idempotency note (ADD COLUMN IF NOT EXISTS — safe to run multiple times)
    - Single ALTER TABLE statement: ALTER TABLE businesses ADD COLUMN IF NOT EXISTS enforcement_policy TEXT NOT NULL DEFAULT 'allow' CONSTRAINT enforcement_policy_valid CHECK (enforcement_policy IN ('allow', 'block', 'flag'))
    - GRANT statement: GRANT UPDATE (enforcement_policy) ON businesses TO randevuclaw_app
    - Planner discretion: include CHECK constraint (defense in depth — both DB and Zod app-layer validation per RESEARCH.md open question 1)

    In src/database/schema.ts, locate the businesses pgTable definition and add the enforcementPolicy column after the last existing column. Use the same pattern as other text columns: enforcementPolicy: text('enforcement_policy').notNull().default('allow'). Follow the camelCase→snake_case naming convention used throughout the file.

    In src/database/queries.ts, locate the Business interface (near line 25) and add enforcementPolicy: string as a new field. Position it after webhookSecret to keep billing-related fields grouped at the end of the interface.

    Both schema.ts and queries.ts MUST be updated in the same commit (Pitfall 6 in RESEARCH.md — updating only one causes compile errors or silent runtime undefined).
  </action>

  <acceptance_criteria>
    - migrations/0007_enforcement_policy.sql exists and contains the literal string "ADD COLUMN IF NOT EXISTS enforcement_policy"
    - migrations/0007_enforcement_policy.sql contains CHECK constraint with "enforcement_policy IN ('allow', 'block', 'flag')"
    - src/database/schema.ts businesses table definition includes "enforcementPolicy" (camelCase Drizzle field)
    - src/database/queries.ts Business interface includes "enforcementPolicy: string"
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && echo "TSC OK"</automated>
  </verify>

  <done>Migration file created; schema.ts and queries.ts updated; TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration to randevuclaw_test and live Neon DB</name>
  <files>migrations/0007_enforcement_policy.sql</files>

  <read_first>
    - migrations/0007_enforcement_policy.sql — just created in Task 1; confirms exact SQL to apply
    - .env or .env.local — DATABASE_URL value needed for live Neon DB psql invocation
  </read_first>

  <action>
    Apply the migration to the local test database randevuclaw_test:
    - Run: psql postgresql://manolis@localhost:5432/randevuclaw_test -f migrations/0007_enforcement_policy.sql
    - GRANT errors for the randevuclaw_app role are expected and harmless (same behavior as Phase 7 migrations — randevuclaw_app role does not exist on local test DB)

    Apply the migration to the live Neon database using DATABASE_URL from .env:
    - Load DATABASE_URL from .env (or .env.local if .env is absent) and run psql against it
    - Use: node -e "const {execSync}=require('child_process'); require('dotenv').config({path:require('fs').existsSync('.env')?'.env':'.env.local'}); execSync('psql \"'+process.env.DATABASE_URL+'\" -f migrations/0007_enforcement_policy.sql',{stdio:'inherit'})"
    - The migration is idempotent (IF NOT EXISTS guard) — safe to run even if already applied

    Verify the column exists on randevuclaw_test:
    - Run: psql postgresql://manolis@localhost:5432/randevuclaw_test -c "SELECT column_name, data_type, column_default FROM information_schema.columns WHERE table_name='businesses' AND column_name='enforcement_policy'"
    - Expected output: 1 row with column_name=enforcement_policy, data_type=text, column_default='allow'
  </action>

  <acceptance_criteria>
    - psql query above returns exactly 1 row for enforcement_policy on randevuclaw_test businesses table
    - `npx jest --testPathPattern="billing-session-deduction" --no-coverage` exits 0 (stubs still pass; schema in place for future integration tests)
    - `npx jest --no-coverage` exits 0 (full suite passes — no regressions from schema changes)
  </acceptance_criteria>

  <verify>
    <automated>psql postgresql://manolis@localhost:5432/randevuclaw_test -c "SELECT column_name FROM information_schema.columns WHERE table_name='businesses' AND column_name='enforcement_policy'" 2>&1 | grep -c "enforcement_policy"</automated>
  </verify>

  <done>Migration applied to both randevuclaw_test and live Neon DB; enforcement_policy column present; full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → PostgreSQL | Migration runs as database owner; CHECK constraint enforces allowed values at DB level |
| application → businesses column | Code reads enforcementPolicy from DB row; null-coalescing ensures backward compat for any rows that may lack the column (though DEFAULT 'allow' prevents this) |

## STRIDE Threat Register

```yaml
threats:
  - threat_id: T-08-03
    title: enforcement_policy column absent when Phase 8 code reads it
    asvs_ref: V5.1
    severity: high
    vector: Phase 8 code deployed before migration runs; businesses.enforcementPolicy is undefined at runtime
    impact: Null pointer or undefined coercion; enforcement logic silently disabled; possible server crash
    disposition: mitigate
    mitigation: "Migration applied in this plan (Task 2) before any Phase 8 handler code ships. DEFAULT 'allow' means existing rows are safe after migration runs. Plan 04 code uses null-coalescing (context.business.enforcementPolicy ?? 'allow') as a secondary guard per D-08."

  - threat_id: T-08-04
    title: Invalid enforcement_policy value stored in DB (bypasses Zod)
    asvs_ref: V5.3
    severity: low
    vector: Direct SQL write to businesses.enforcement_policy with a value not in ('allow','block','flag')
    impact: Enforcement logic receives unexpected value; defaults to 'allow' behavior (safe fallback)
    disposition: mitigate
    mitigation: "CHECK constraint in migration enforces allowed values at DB layer. Zod validation in Plan 05 enforces at app layer. Defense in depth — both layers required per RESEARCH.md open question 1."
```
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx jest --no-coverage` exits 0 (all existing tests green; 14 new it.todo pending from Plan 01)
- psql query confirms enforcement_policy column on randevuclaw_test businesses table
</verification>

<success_criteria>
- Migration SQL created with IF NOT EXISTS guard and CHECK constraint
- schema.ts and queries.ts both updated in sync
- enforcement_policy column present on randevuclaw_test and live Neon DB
- TypeScript compiles cleanly
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/08-enforcement-session-deduction/08-02-SUMMARY.md` when done
</output>
