---
phase: 08-enforcement-session-deduction
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/database/schema.ts
  - src/database/migrations/0005-enforcement-policy.sql
autonomous: false
requirements:
  - ENFC-01

must_haves:
  truths:
    - businesses table has enforcement_policy TEXT nullable column in schema.ts
    - Migration SQL file 0005-enforcement-policy.sql exists with ALTER TABLE statement
    - drizzle-kit push applied — column visible via SELECT on live DB
    - getEnforcementPolicy() can query the column without error after push
  artifacts:
    - src/database/schema.ts (enforcement_policy column added to businesses table)
    - src/database/migrations/0005-enforcement-policy.sql
  key_links:
    - schema.ts businesses.enforcementPolicy maps to DB column enforcement_policy
    - drizzle-kit push must complete before any Wave 2 query code runs
---

<objective>
Add the enforcement_policy TEXT nullable column to the businesses table in schema.ts,
write the migration SQL file, and push the schema change to the database.
This is the [BLOCKING] gate that must complete before any Wave 2 query functions
can query the column.

Purpose: ENFC-01 requires per-business enforcement policy stored in the database.
Output: schema.ts updated, 0005-enforcement-policy.sql written, drizzle-kit push applied.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-enforcement-session-deduction/08-PATTERNS.md

@src/database/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add enforcementPolicy column to businesses table in schema.ts + write migration SQL</name>
  <files>src/database/schema.ts, src/database/migrations/0005-enforcement-policy.sql</files>
  <read_first>
    - src/database/schema.ts (full file — read the businesses table definition, lines 12-44, to find the correct insertion point; read the Phase 7 billing table comments to match annotation style)
    - .planning/phases/08-enforcement-session-deduction/08-PATTERNS.md (schema extension pattern at top of file — exact column definition, nullable convention, comment format)
  </read_first>
  <action>
    In src/database/schema.ts: Add the enforcementPolicy column to the businesses pgTable definition,
    after the webhookSecret column and before the createdAt column.

    Column definition to insert (per D-01 from PATTERNS.md — nullable because businesses table is
    non-empty; existing rows get NULL which the app treats as 'flag' at query time per ENFC-01):

    - TypeScript name: enforcementPolicy
    - DB column name: enforcement_policy
    - Type: text()
    - Nullable (no .notNull())
    - No default at schema level — app provides 'flag' fallback in getEnforcementPolicy()
    - Comment block above the column (match existing column comment style):
      "Phase 8 (ENFC-01 — nullable: table is non-empty; NULL treated as 'flag' at query time).
      Per-business booking enforcement policy: 'block' (refuse clients without active membership)
      or 'flag' (allow booking and alert owner). Queried fresh on every booking attempt — no caching."

    Create src/database/migrations/0005-enforcement-policy.sql with:
    - Header comment identifying Phase 8 and ENFC-01
    - ALTER TABLE businesses ADD COLUMN enforcement_policy TEXT;
    - Comment explaining NULL means 'flag' default at application layer

    Do NOT add a SQL DEFAULT clause — existing rows keep NULL and the application handles the fallback.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/database/schema.ts compiles without TypeScript errors
    - businesses object in schema.ts contains enforcementPolicy: text('enforcement_policy') with no .notNull()
    - src/database/migrations/0005-enforcement-policy.sql exists and contains 'ALTER TABLE businesses ADD COLUMN enforcement_policy TEXT'
    - grep -c "enforcementPolicy" src/database/schema.ts returns 1
  </acceptance_criteria>
  <done>schema.ts compiles clean with enforcementPolicy column; migration SQL file written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Run drizzle-kit push to apply enforcement_policy column to live DB</name>
  <files>src/database/schema.ts</files>
  <action>
    Human action required — drizzle-kit push cannot run non-interactively for ADD COLUMN on a non-empty table.

    Steps to execute:
    1. Run: npx drizzle-kit push
    2. If prompted about non-destructive changes (ADD COLUMN only), confirm with 'y'
    3. Verify: npx drizzle-kit push exits 0 with no errors
    4. Optional DB check (Neon psql or Drizzle Studio):
       SELECT column_name FROM information_schema.columns
       WHERE table_name = 'businesses' AND column_name = 'enforcement_policy';
       Must return 1 row.
    5. Run: npm test -- tests/enforcement-session-deduction.test.ts --passWithNoTests
       Should still be 0 failures (all it.todo stubs, not affected by schema push).
  </action>
  <verify>
    <human-check>npx drizzle-kit push exits 0 and enforcement_policy column visible in live DB</human-check>
  </verify>
  <done>drizzle-kit push completed; enforcement_policy column exists in Neon businesses table.</done>
  <what-built>
    Task 1 added enforcementPolicy column to schema.ts and wrote 0005-enforcement-policy.sql.
    The schema change must be pushed to the Neon database before any code that queries
    businesses.enforcementPolicy can execute (including Wave 3 getEnforcementPolicy function).
  </what-built>
  <resume-signal>Type "schema pushed" after drizzle-kit push completes successfully, or describe any errors encountered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.ts → Neon DB | ALTER TABLE businesses ADD COLUMN: non-destructive, nullable column — safe on live data |
| application → enforcement_policy | Column is TEXT with no CHECK constraint — application enforces 'block'/'flag' enum via Zod at write time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02-01 | Tampering | enforcement_policy column | medium | mitigate | Zod validation z.enum(['block','flag']) applied in set_enforcement_policy handler (Wave 4) before any DB write; column accepts NULL which maps to 'flag' default at read time |
| T-08-02-02 | Repudiation | schema migration | low | mitigate | Migration SQL file 0005-enforcement-policy.sql committed to git for audit trail; drizzle-kit push is idempotent (ADD COLUMN IF NOT EXISTS semantics via Drizzle) |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; drizzle-kit already installed from Phase 7 |
</threat_model>

<verification>
1. npx tsc --noEmit exits 0 (schema.ts compiles clean)
2. grep -c "enforcementPolicy" src/database/schema.ts returns 1
3. src/database/migrations/0005-enforcement-policy.sql contains the ALTER TABLE statement
4. npx drizzle-kit push completes (human checkpoint above)
5. npm test -- tests/enforcement-session-deduction.test.ts exits 0 (stubs unaffected by schema push)
</verification>

<success_criteria>
- businesses table in schema.ts has enforcementPolicy text column (nullable, no default)
- Migration SQL 0005-enforcement-policy.sql written and committed
- enforcement_policy column visible in live Neon DB after drizzle-kit push
- No TypeScript compilation errors
</success_criteria>

<output>
Create .planning/phases/08-enforcement-session-deduction/08-02-SUMMARY.md when done.
</output>

## Artifacts this phase plan produces

**Modified files:**
- `src/database/schema.ts` — adds `enforcementPolicy: text('enforcement_policy')` to businesses table

**New files:**
- `src/database/migrations/0005-enforcement-policy.sql` — ALTER TABLE businesses ADD COLUMN enforcement_policy TEXT

**New schema symbol:**
- `businesses.enforcementPolicy` — TEXT nullable column, DB name: `enforcement_policy`
