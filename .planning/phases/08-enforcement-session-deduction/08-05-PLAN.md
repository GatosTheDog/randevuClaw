---
phase: 08-enforcement-session-deduction
plan: 05
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - src/onboarding/ai-owner-agent.ts
autonomous: true
requirements:
  - ENFC-01

must_haves:
  truths:
    - set_enforcement_policy tool exists in OWNER_TOOLS array with 'block'|'flag' enum
    - Tool description is in Greek matching the style of other OWNER_TOOLS entries
    - executeOwnerTool switch handles 'set_enforcement_policy' case
    - Valid policy ('block' or 'flag') is written to businesses.enforcementPolicy via db.update()
    - Invalid policy value returns Greek error string without any DB write
    - Policy update is logged with logger.info including businessId and policy
  artifacts:
    - src/onboarding/ai-owner-agent.ts (set_enforcement_policy tool + handler added)
  key_links:
    - OWNER_TOOLS array extended at line ~199 after view_client_membership entry
    - executeOwnerTool switch extended at line ~399 with new case
    - businesses table import already present in ai-owner-agent.ts (verify before adding)
---

<objective>
Add the set_enforcement_policy NLU tool to the owner agent so owners can configure
their booking enforcement policy via natural language Telegram chat.
This plan runs in parallel with 08-04 (both depend on Wave 3 only, not on each other).

Purpose: ENFC-01 — owner can set "block" or "flag" enforcement policy via chat immediately.
Output: OWNER_TOOLS extended with set_enforcement_policy; executeOwnerTool handles the case.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-enforcement-session-deduction/08-PATTERNS.md

@src/onboarding/ai-owner-agent.ts
@src/database/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add set_enforcement_policy to OWNER_TOOLS and executeOwnerTool in ai-owner-agent.ts</name>
  <files>src/onboarding/ai-owner-agent.ts</files>
  <read_first>
    - src/onboarding/ai-owner-agent.ts (full file — read OWNER_TOOLS array lines 37-199 to confirm view_client_membership is the last entry; read executeOwnerTool lines 278-420 to see the switch structure and identify where to add the new case; read existing imports block to confirm businesses is already imported from database/schema)
    - src/database/schema.ts (businesses table — verify enforcementPolicy column is present after Wave 2)
    - .planning/phases/08-enforcement-session-deduction/08-PATTERNS.md (ai-owner-agent.ts tool definition and handler blocks — exact shapes to replicate)
  </read_first>
  <behavior>
    - set_enforcement_policy tool appears in OWNER_TOOLS with Greek description and 'block'|'flag' enum
    - executeOwnerTool 'set_enforcement_policy' case: validates policy is 'block' or 'flag', returns Greek error if not
    - Valid policy: db.update(businesses).set({ enforcementPolicy: policy }).where(eq(businesses.id, business.id))
    - Success return: Greek confirmation string showing which policy was set and what it means
    - 'block' label: 'Άρνηση κρατήσεων χωρίς ενεργή συνδρομή'
    - 'flag' label: 'Αποδοχή κρατήσεων και ειδοποίηση ιδιοκτήτη'
    - logger.info({ businessId: business.id, policy }, 'enforcement_policy_updated') after DB write
  </behavior>
  <action>
    In src/onboarding/ai-owner-agent.ts:

    Step 1 — OWNER_TOOLS extension:
    Add the set_enforcement_policy tool object AFTER the view_client_membership entry (around line 199), before the closing bracket of the OWNER_TOOLS array.

    Tool shape (per PATTERNS.md):
    - type: 'function' as const
    - name: 'set_enforcement_policy'
    - description: 'Ορίζει την πολιτική επιβολής για αποδεκτές κρατήσεις: "block" (απόρριψη χωρίς ενεργή συνδρομή) ή "flag" (αποδοχή και ειδοποίηση ιδιοκτήτη).'
    - parameters.type: 'object'
    - parameters.properties.policy: { type: 'string', enum: ['block', 'flag'], description: 'Πολιτική: "block" για απόρριψη ή "flag" για ειδοποίηση' }
    - parameters.required: ['policy']

    Step 2 — executeOwnerTool switch case:
    Add case 'set_enforcement_policy': after the last existing case (around line 420, before the default case).

    Case body:
    - const policy = args.policy as string;
    - if (!['block', 'flag'].includes(policy)) return 'Άκυρη πολιτική. Χρησιμοποίησε "block" (άρνηση) ή "flag" (ειδοποίηση).';
    - await db.update(businesses).set({ enforcementPolicy: policy }).where(eq(businesses.id, business.id));
    - const policyLabel = policy === 'block' ? 'Άρνηση κρατήσεων χωρίς ενεργή συνδρομή' : 'Αποδοχή κρατήσεων και ειδοποίηση ιδιοκτήτη';
    - logger.info({ businessId: business.id, policy }, 'enforcement_policy_updated');
    - return 'OK: Πολιτική ορίστηκε σε "' + policyLabel + '".';
    - break; (required for switch cases that return, but since there is an explicit return, break is optional — match the pattern of other cases in the file)

    Step 3 — imports check:
    Verify businesses is already imported from ../database/schema — it should be from Phase 7. If not present, add it. Do NOT add a duplicate import. db and eq should already be imported — verify before adding.

    Step 4 — system prompt update:
    Check if the buildOwnerSystemPrompt function references what the owner can do. If it has a list of capabilities, add 'αλλαγή πολιτικής επιβολής κρατήσεων' (or similar) to the 'Κανόνες' list so Gemini knows about the new tool. Match the existing rule list format.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0
    - grep -c "set_enforcement_policy" src/onboarding/ai-owner-agent.ts returns at least 2 (in OWNER_TOOLS and in switch case)
    - grep -c "enforcementPolicy" src/onboarding/ai-owner-agent.ts returns at least 1 (DB write present)
    - grep -c "block.*flag\|flag.*block" src/onboarding/ai-owner-agent.ts returns at least 1 (validation logic present)
    - grep -c "enforcement_policy_updated" src/onboarding/ai-owner-agent.ts returns 1 (logger.info present)
    - npm test -- tests/billing-nlu-parsing.test.ts exits 0 (existing NLU tests still pass)
  </acceptance_criteria>
  <done>set_enforcement_policy in OWNER_TOOLS and handled in executeOwnerTool; TypeScript compiles clean; existing NLU tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner Telegram message → set_enforcement_policy | Owner identity verified at onboarding (Phase 5); Gemini NLU parses intent |
| Gemini tool args → executeOwnerTool | args.policy is untrusted Gemini output — validated against ['block','flag'] before any DB write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05-01 | Tampering | set_enforcement_policy with arbitrary policy value | medium | mitigate | Explicit allowlist check: !['block','flag'].includes(policy) returns Greek error before DB.update — Gemini-injected values outside the enum are rejected at application layer |
| T-08-05-02 | Repudiation | Policy change not logged | low | mitigate | logger.info({ businessId, policy }, 'enforcement_policy_updated') after every DB write — audit trail in structured logs |
| T-08-05-03 | Elevation of Privilege | Owner sets policy for another business | high | mitigate | business.id from withBusinessContext (RLS-enforced) — owner cannot supply an arbitrary businessId via chat; business is resolved from their authenticated bot token |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages |
</threat_model>

<verification>
npx tsc --noEmit exits 0
grep -c "set_enforcement_policy" src/onboarding/ai-owner-agent.ts returns at least 2
grep -c "enforcementPolicy" src/onboarding/ai-owner-agent.ts returns at least 1
npm test -- tests/billing-nlu-parsing.test.ts exits 0
</verification>

<success_criteria>
- set_enforcement_policy tool object in OWNER_TOOLS array with Greek description and enum ['block','flag']
- executeOwnerTool 'set_enforcement_policy' case validates policy and writes to DB
- Greek error returned for invalid policy without DB write
- Success returns Greek confirmation with policy label
- logger.info logged after DB write
- All existing ai-owner-agent NLU tests pass
</success_criteria>

<output>
Create .planning/phases/08-enforcement-session-deduction/08-05-SUMMARY.md when done.
</output>

## Artifacts this phase plan produces

**Modified files:**
- `src/onboarding/ai-owner-agent.ts` — OWNER_TOOLS extended with set_enforcement_policy; executeOwnerTool switch extended

**New NLU tool defined:**
- `set_enforcement_policy` — tool name in OWNER_TOOLS, handled in executeOwnerTool switch
