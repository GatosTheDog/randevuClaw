---
phase: 14-renewal-notification-extensions
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - tests/renewal-nudge.test.ts
autonomous: true
requirements:
  - RENW-01
  - RENW-02
  - RENW-03
  - RENW-04
  - RENW-05

must_haves:
  truths:
    - tests/renewal-nudge.test.ts exists with integration tests covering RENW-01 through RENW-05
    - Tests follow exact pattern of tests/cancellation-cutoff.test.ts (jest.resetModules + require, real DB, uniquePhone helper)
    - TypeScript compiles the test file without errors
    - RENW-01/02: setLastSessionThreshold stores enabled/count in DB
    - RENW-03: insertRenewalNudgeNotification is idempotent (same membershipId + date returns false)
    - RENW-05 path: findMembershipsAtThreshold returns memberships at threshold for enabled businesses
---

<objective>
Integration tests for Phase 14's renewal nudge query layer and sweep logic.

Write 6 tests covering RENW-01 through RENW-05 (query functions only — Telegram send is not tested here, keeping tests DB-only).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/renewal-nudge.test.ts</name>
  <files>tests/renewal-nudge.test.ts</files>
  <action>
Create tests/renewal-nudge.test.ts following the EXACT pattern of tests/cancellation-cutoff.test.ts.

Header:
```typescript
// covers RENW-01, RENW-02, RENW-03, RENW-04, RENW-05
// Integration tests against a REAL local Postgres connection.
// Requires migration 0012 (renewal_nudge_notifications table) to be applied.

const TEST_DATABASE_URL =
  process.env.SESSION_TEST_DATABASE_URL ??
  process.env.BILLING_TEST_DATABASE_URL ??
  'postgresql://manolis@localhost:5432/randevuclaw_test';

const ORIGINAL_DATABASE_URL = process.env.DATABASE_URL;
process.env.DATABASE_URL = TEST_DATABASE_URL;
jest.resetModules();
```

Use require() for imports (see eslint-disable in sister tests):
- db from ../src/database/db
- eq from drizzle-orm
- renewalNudgeNotifications, memberships, businesses from ../src/database/schema
- setLastSessionThreshold, findMembershipsAtThreshold, insertRenewalNudgeNotification from ../src/billing/queries
- insertTestBusiness from ./helpers/test-business
- insertTestPackage, insertTestMembership from ./helpers/billing-fixtures

afterAll: restore DATABASE_URL.

Helper: `function uniquePhone(): string { return 'renew-' + Date.now() + '-' + Math.random().toString(36).slice(2); }`

Tests inside `describe('Renewal Nudge Notifications', () => {...})`:

**Test 1 (RENW-01)**: 'setLastSessionThreshold enables threshold with count'
- insertTestBusiness
- setLastSessionThreshold(business.id, true, 3)
- Query DB: business has lastSessionThresholdEnabled=true, lastSessionThresholdCount=3

**Test 2 (RENW-02)**: 'setLastSessionThreshold can disable threshold'
- insertTestBusiness, enable, then disable
- setLastSessionThreshold(business.id, false, 1)
- Query DB: business has lastSessionThresholdEnabled=false

**Test 3 (RENW-03)**: 'insertRenewalNudgeNotification inserts and deduplicates'
- insertTestBusiness, insertTestPackage, insertTestMembership with sessionsRemaining: 2
- Call insertRenewalNudgeNotification(membership.id, '2026-08-01') → expect true
- Call again with same args → expect false (dedup)

**Test 4 (RENW-03/04)**: 'findMembershipsAtThreshold returns membership when at threshold'
- insertTestBusiness with lastSessionThresholdEnabled=true (set via db.update after insert), lastSessionThresholdCount=3
- insertTestPackage, insertTestMembership with sessionsRemaining: 2 (below threshold of 3)
- Call findMembershipsAtThreshold(business.id) → expect 1 result with clientPhone matching

**Test 5 (RENW-04)**: 'findMembershipsAtThreshold excludes memberships above threshold'
- Same setup but sessionsRemaining: 5 (above threshold of 3)
- findMembershipsAtThreshold → expect 0 results

**Test 6 (RENW-05)**: 'findMembershipsAtThreshold excludes inactive memberships'
- insertTestMembership with sessionsRemaining: 1, isActive: false (if billing-fixtures supports it; else manually update)
- findMembershipsAtThreshold → expect 0 results

afterEach: db.delete(renewalNudgeNotifications).where(eq(renewalNudgeNotifications.membershipId, membershipId)) where applicable. Use beforeAll per describe sub-block for business/service rows.

Run: npm test -- tests/renewal-nudge.test.ts --testTimeout=20000 2>&1 | tail -20
ECONNREFUSED is expected and acceptable — document in SUMMARY.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "renewal-nudge" | head -5</automated>
  </verify>
  <done>tests/renewal-nudge.test.ts exists. TypeScript compiles it without errors. 6 test cases present.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit 2>&1 | tail -5
</verification>

<success_criteria>
- tests/renewal-nudge.test.ts compiles without TypeScript errors
- 6 test cases covering RENW-01 through RENW-05
- Each test uses uniquePhone() to avoid cross-test contamination
</success_criteria>

<output>
Create .planning/phases/14-renewal-notification-extensions/14-03-SUMMARY.md when done
</output>
