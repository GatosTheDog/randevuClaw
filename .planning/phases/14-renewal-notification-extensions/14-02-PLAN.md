---
phase: 14-renewal-notification-extensions
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/scheduler/membership-expiry.ts
  - src/webhooks/telegram.ts
  - src/onboarding/ai-owner-agent.ts
autonomous: true
requirements:
  - RENW-01
  - RENW-02
  - RENW-03
  - RENW-04
  - RENW-05

must_haves:
  truths:
    - runMembershipExpirySweep also checks findMembershipsAtThreshold per business and, when threshold clients found, sends the owner a batch list with a Ναι/Όχι keyboard (RENW-04); insertRenewalNudgeNotification dedup prevents resending same day
    - When owner taps the 'Ναι' (send all) keyboard button, all threshold clients on the batch receive a Greek renewal nudge; 'Όχι' sends nothing
    - parseCallbackData handles 'renewal:approve:<batchKey>' and 'renewal:skip:<batchKey>' patterns
    - OWNER_TOOLS includes set_last_session_threshold (RENW-01/02) and send_renewal_reminder (RENW-05)
    - send_renewal_reminder sends a Greek renewal message to a single named client immediately (RENW-05)
    - set_last_session_threshold calls handleSetLastSessionThreshold from billing/tools.ts
  artifacts:
    - src/scheduler/membership-expiry.ts — extended to include threshold sweep (RENW-03/04)
    - src/webhooks/telegram.ts — parseCallbackData + handleCallbackQuery handle renewal batch approval
    - src/onboarding/ai-owner-agent.ts — OWNER_TOOLS + executeOwnerTool gain set_last_session_threshold and send_renewal_reminder
  key_links:
    - The threshold sweep in membership-expiry.ts runs inside the same per-business loop as the 7-day expiry sweep — one botTokenStore.run call per business covers both
    - batchKey for the keyboard callback is the businessId (one pending batch per business at a time is sufficient for PoC)
    - The client phone list for the renewal batch must be passed through the callback somehow; store it in a short-lived in-memory Map keyed by businessId (cleared after use or after 10 minutes); renewal callbacks fire quickly after the sweep message
---

<objective>
Wire the renewal nudge sweep into the existing membership expiry poller, add the Ναι/Όχι keyboard callback handler, and add owner tools for manual threshold control.

Three changes:
1. membership-expiry.ts: after 7-day sweep, run threshold sweep per business and send owner a batch list keyboard (RENW-03/04).
2. telegram.ts: parseCallbackData + handleCallbackQuery handle 'renewal:approve:<businessId>' and 'renewal:skip:<businessId>' patterns.
3. ai-owner-agent.ts: OWNER_TOOLS gain set_last_session_threshold and send_renewal_reminder; executeOwnerTool handles both (RENW-01/02/05).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Threshold sweep in membership-expiry.ts + in-memory batch store</name>
  <files>src/scheduler/membership-expiry.ts</files>
  <action>
Add import: `import { findMembershipsAtThreshold, insertRenewalNudgeNotification, getClientName } from '../billing/queries';`
Add import: `import { sendTelegramMessageWithKeyboard } from '../telegram/client';`
Add import: `import { isoDateInAthens } from '../utils/timezone';`

Add an in-memory batch store at module scope (cleared after use or after 10 minutes):

```typescript
// RENW-04: in-memory store of pending renewal batch lists awaiting owner approval.
// Keyed by businessId. Cleared after owner taps Ναι/Όχι or after 10 minutes.
export const pendingRenewalBatches = new Map<number, string[]>();
```

At the END of the per-business loop in runMembershipExpirySweep (after the 7-day expiry section, still inside the outer try), add the threshold sweep:

```typescript
// RENW-03/04: threshold sweep — find clients at last-session threshold.
if (business.lastSessionThresholdEnabled && business.ownerTelegramId) {
  try {
    const atThreshold = await findMembershipsAtThreshold(businessId);
    const todayAthens = isoDateInAthens(new Date());
    // Filter out memberships already nudged today.
    const toNudge: typeof atThreshold = [];
    for (const m of atThreshold) {
      const inserted = await insertRenewalNudgeNotification(m.id, todayAthens);
      if (inserted) toNudge.push(m);
    }
    if (toNudge.length > 0) {
      // Build named list for owner.
      const nameLines = await Promise.all(
        toNudge.map(async (m, i) => {
          const name = (await getClientName(businessId, m.clientPhone)) ?? m.clientPhone;
          return `${i + 1}. ${name} (${m.sessionsRemaining} μαθήματα)`;
        })
      );
      const listText = nameLines.join('\n');
      const batchMessage =
        `Πελάτες με λίγα εναπομείναντα μαθήματα:\n${listText}\n\nΘέλετε να σταλεί ειδοποίηση ανανέωσης σε όλους;`;
      // Store phones for the callback handler.
      pendingRenewalBatches.set(businessId, toNudge.map((m) => m.clientPhone));
      // Clear after 10 minutes if owner never taps.
      setTimeout(() => pendingRenewalBatches.delete(businessId), 10 * 60 * 1000);
      await botTokenStore.run(business.botToken, async () => {
        await sendTelegramMessageWithKeyboard(business.ownerTelegramId!, batchMessage, [
          [
            { text: '✅ Ναι, στείλε σε όλους', callback_data: `renewal:approve:${businessId}` },
            { text: '❌ Όχι', callback_data: `renewal:skip:${businessId}` },
          ],
        ]);
      });
      notificationCount += toNudge.length;
    }
  } catch (err) {
    logger.error({ err, businessId }, 'Renewal threshold sweep failed for business');
  }
}
```

Note: insertRenewalNudgeNotification is called BEFORE the keyboard is sent (at-most-once dedup, same pattern as expiry notifications). If sendTelegramMessageWithKeyboard fails, the dedup row is already committed and the sweep skips this business tomorrow — acceptable PoC tradeoff.

Verify: npx tsc --noEmit 2>&1 | tail -5
  </action>
  <done>pendingRenewalBatches Map exported. Threshold sweep block added inside per-business loop. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Renewal callback handler in telegram.ts</name>
  <files>src/webhooks/telegram.ts</files>
  <action>
Add import: `import { pendingRenewalBatches } from '../scheduler/membership-expiry';`
Add import: `import { findBusinessById } from '../database/queries';`

Add a new result type:
```typescript
export type RenewalCallbackResult = {
  action: 'renewal:approve' | 'renewal:skip';
  businessId: number;
};
```

Extend parseCallbackData to handle 'renewal:approve:<businessId>' and 'renewal:skip:<businessId>':

```typescript
const renewalMatch = data?.match(/^renewal:(approve|skip):(\d+)$/);
if (renewalMatch) {
  return {
    action: `renewal:${renewalMatch[1]}` as RenewalCallbackResult['action'],
    businessId: Number(renewalMatch[2]),
  };
}
```

Update parseCallbackData return type to include RenewalCallbackResult.

In handleCallbackQuery, after the slotless branch, add:

```typescript
if ('businessId' in parsed && 'action' in parsed && (parsed as RenewalCallbackResult).action?.startsWith('renewal:')) {
  const renewalResult = parsed as RenewalCallbackResult;
  // T-14-01: cross-tenant guard — verify sender owns the business referenced in callback_data.
  const ownerBusiness = await findBusinessByOwnerTelegramId(senderTelegramId);
  if (!ownerBusiness || ownerBusiness.id !== renewalResult.businessId) {
    logger.warn({ senderTelegramId, businessId: renewalResult.businessId }, 'renewal callback from non-owner, ignoring');
    return;
  }

  const phones = pendingRenewalBatches.get(renewalResult.businessId) ?? [];
  pendingRenewalBatches.delete(renewalResult.businessId);

  if (renewalResult.action === 'renewal:approve' && phones.length > 0) {
    const business = ownerBusiness;
    let sent = 0;
    for (const phone of phones) {
      try {
        await botTokenStore.run(business.botToken!, async () => {
          await sendTelegramMessage(
            phone,
            'Υπενθύμιση: Τα μαθήματά σας τελειώνουν σύντομα! Επικοινωνήστε για ανανέωση.'
          );
        });
        sent += 1;
      } catch (err) {
        logger.error({ err, phone, businessId: business.id }, 'Renewal nudge to client failed');
      }
    }
    await sendTelegramMessage(senderTelegramId, `✅ Στάλθηκαν ειδοποιήσεις σε ${sent} πελάτες.`);
  } else {
    await sendTelegramMessage(senderTelegramId, 'Παραλείφθηκε. Δεν στάλθηκαν ειδοποιήσεις.');
  }

  if (callbackQuery.message?.message_id) {
    await editTelegramMessageReplyMarkup(senderTelegramId, callbackQuery.message.message_id, []);
  }
  return;
}
```

Import botTokenStore from '../telegram/client' if not already imported.

Verify: npx tsc --noEmit 2>&1 | tail -5
  </action>
  <done>RenewalCallbackResult type added. parseCallbackData handles renewal patterns. handleCallbackQuery sends renewal nudges on approve. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 3: OWNER_TOOLS + executeOwnerTool for set_last_session_threshold and send_renewal_reminder</name>
  <files>src/onboarding/ai-owner-agent.ts</files>
  <action>
Add import: `import { handleSetLastSessionThreshold } from '../billing/tools';`
Add import: `import { getClientActiveMembership, getClientName } from '../billing/queries';`

In OWNER_TOOLS array, after list_slotless_requests, add:

```typescript
{
  type: 'function' as const,
  name: 'set_last_session_threshold',
  description: 'Ρυθμίζει την ειδοποίηση ανανέωσης συνδρομής όταν ένας πελάτης έχει λίγα μαθήματα.',
  parameters: {
    type: 'object',
    properties: {
      enabled: { type: 'boolean', description: 'true για ενεργοποίηση, false για απενεργοποίηση' },
      count: { type: 'number', description: 'Αριθμός εναπομεινάντων μαθημάτων που ενεργοποιεί την ειδοποίηση (1-20)' },
    },
    required: ['enabled', 'count'],
  },
},
{
  type: 'function' as const,
  name: 'send_renewal_reminder',
  description: 'Στέλνει άμεσα ειδοποίηση ανανέωσης συνδρομής σε έναν συγκεκριμένο πελάτη (RENW-05).',
  parameters: {
    type: 'object',
    properties: {
      client_phone: { type: 'string', description: 'Telegram ID ή τηλέφωνο πελάτη' },
    },
    required: ['client_phone'],
  },
},
```

In executeOwnerTool switch, before the default case, add:

```typescript
case 'set_last_session_threshold': {
  return withBusinessContext(business.id, () =>
    handleSetLastSessionThreshold(business.id, args as Record<string, unknown>)
  );
}

case 'send_renewal_reminder': {
  const clientPhone = String(args.client_phone ?? '').trim();
  if (!clientPhone) return 'Δεν δόθηκε αναγνωριστικό πελάτη.';
  const membership = await getClientActiveMembership(business.id, clientPhone);
  if (!membership) return `Ο πελάτης ${clientPhone} δεν έχει ενεργή συνδρομή.`;
  const clientName = (await getClientName(business.id, clientPhone)) ?? clientPhone;
  await sendTelegramMessage(
    clientPhone,
    `Υπενθύμιση: Τα μαθήματά σας τελειώνουν σύντομα! Επικοινωνήστε για ανανέωση.`
  );
  return `Η ειδοποίηση ανανέωσης στάλθηκε στον/στην ${clientName}.`;
}
```

Check that `getClientActiveMembership` is imported (may already be imported as part of billing/queries). If not, add it.
Check that `sendTelegramMessage` is imported (likely already present).

Verify: npx tsc --noEmit 2>&1 | tail -5
  </action>
  <done>set_last_session_threshold and send_renewal_reminder added to OWNER_TOOLS and executeOwnerTool. TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-14-01 | Spoofing | renewal callback — cross-owner | high | mitigate | ownerBusiness resolved from senderTelegramId; businessId from callback_data must match |
| T-14-02 | Repudiation | renewal nudge dedup | low | mitigate | insertRenewalNudgeNotification called before keyboard send; dedup row committed regardless of Telegram outcome |
| T-14-03 | DoS | in-memory batch store | low | accept | Map keyed by businessId, auto-cleared after 10 minutes; at most one entry per business |
</threat_model>

<verification>
npx tsc --noEmit 2>&1 | tail -5
</verification>

<success_criteria>
- TypeScript compiles all modified files without errors
- runMembershipExpirySweep sends threshold batch keyboard to owner when threshold clients found
- Renewal callback handler sends nudges to all approved clients
- OWNER_TOOLS has set_last_session_threshold and send_renewal_reminder
- executeOwnerTool handles both tools correctly
</success_criteria>

<output>
Create .planning/phases/14-renewal-notification-extensions/14-02-SUMMARY.md when done
</output>
