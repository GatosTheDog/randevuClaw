---
phase: 09-expiry-notifications-client-balance
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/billing/queries.ts
  - src/conversation/ai-agent.ts
  - src/conversation/function-executor.ts
  - tests/function-executor.test.ts
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
  - NOTF-03
  - NOTF-04

must_haves:
  truths:
    - "[FLAGGED ASSUMPTION NOTF-01] findMembershipsExpiringIn7Days uses isoDateInAthens + addCalendarDays for DST-safe 7-day window — assumed correct per RESEARCH.md A2; sweep in Plan 03 activates it"
    - "[FLAGGED ASSUMPTION NOTF-02] insertMembershipExpiryNotification with notificationType '7_day_owner' records the owner dedup row — assumed correct; sweep in Plan 03 issues the call"
    - "[FLAGGED ASSUMPTION NOTF-03] insertMembershipExpiryNotification returns true on first insert, false on UNIQUE conflict — assumed correct per RESEARCH.md A3 (Drizzle onConflictDoNothing returning behavior); verify in tests"
    - "[FLAGGED ASSUMPTION NOTF-04] checkMembershipBalanceTool returns the correct of three Greek messages based on membership state — assumed correct; 4 unit tests in this plan verify all three scenarios plus cross-tenant denial"
    - findMembershipsExpiringIn7Days and insertMembershipExpiryNotification are exported from src/billing/queries.ts and compile under npx tsc --noEmit
    - check_membership_balance tool definition exists in BOOKING_TOOLS array in src/conversation/ai-agent.ts
    - executeTool('check_membership_balance') routes to checkMembershipBalanceTool via the switch dispatcher in function-executor.ts
    - All 4 check_membership_balance it.todo stubs in tests/function-executor.test.ts are implemented and pass
    - All prior function-executor tests continue to pass
  artifacts:
    - src/billing/queries.ts (findMembershipsExpiringIn7Days + insertMembershipExpiryNotification + ExpiringMembership interface)
    - src/conversation/ai-agent.ts (check_membership_balance tool definition added to BOOKING_TOOLS)
    - src/conversation/function-executor.ts (CheckMembershipBalanceArgsSchema + checkMembershipBalanceTool + dispatcher case)
  key_links:
    - findMembershipsExpiringIn7Days(businessId) produces ExpiringMembership[] consumed by the sweep in Plan 03
    - insertMembershipExpiryNotification returns boolean; Plan 03 sweep gates Telegram send on return value true
    - checkMembershipBalanceTool calls getActiveMembershipForDeduction (already imported) — no new billing/queries import needed for the balance handler
    - formatExpiryDateGreek (from Plan 01 timezone.ts) imported by function-executor.ts for expiry date display in D-08 messages
  prohibitions:
    - "[FLAGGED] findMembershipsExpiringIn7Days MUST use db (not getConn()) — this is a sweep-context read, not a withBusinessContext RLS-scoped mutation; getConn() is reserved for write operations inside withBusinessContext"
    - "[FLAGGED] insertMembershipExpiryNotification MUST use db (not getConn()) — dedup inserts happen from the sweep, outside any withBusinessContext transaction"
    - "[FLAGGED] checkMembershipBalanceTool MUST NOT accept a client_phone parameter — tool always looks up context.clientPhone to prevent cross-client balance inspection (RESEARCH.md Open Question 4)"
    - botToken MUST NEVER appear in any logger call in this plan's changes
    - No fenced code blocks in action fields (directive prose only)
---

<objective>
Wave 1 — billing query layer extensions and client balance Gemini tool.

Purpose: Deliver the two query functions the sweep (Plan 03) needs (findMembershipsExpiringIn7Days + insertMembershipExpiryNotification) and the complete check_membership_balance Gemini tool (NOTF-04). Both tasks are independent (no file conflicts) and can be worked sequentially within a single plan.

Output:
- src/billing/queries.ts — ExpiringMembership interface + findMembershipsExpiringIn7Days() + insertMembershipExpiryNotification()
- src/conversation/ai-agent.ts — check_membership_balance tool definition added to BOOKING_TOOLS array
- src/conversation/function-executor.ts — CheckMembershipBalanceArgsSchema + checkMembershipBalanceTool() private function + case in switch dispatcher
- tests/function-executor.test.ts — 4 check_membership_balance it.todo stubs implemented and passing
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-expiry-notifications-client-balance/09-CONTEXT.md
@.planning/phases/09-expiry-notifications-client-balance/09-RESEARCH.md
@.planning/phases/09-expiry-notifications-client-balance/09-PATTERNS.md
@.planning/phases/09-expiry-notifications-client-balance/09-01-SUMMARY.md
</context>

## Artifacts This Plan Produces

New symbols in src/billing/queries.ts:
- `ExpiringMembership` — exported interface: { id: number; clientPhone: string; businessId: number; expiresAt: Date; sessionsRemaining: number | null }
- `findMembershipsExpiringIn7Days(businessId: number): Promise<ExpiringMembership[]>` — rolling window query, DST-safe
- `insertMembershipExpiryNotification(membershipId: number, notificationType: '7_day_client' | '7_day_owner', expiryDate: string): Promise<boolean>` — idempotent dedup insert

New symbols in src/conversation/function-executor.ts:
- `CheckMembershipBalanceArgsSchema` — Zod object schema: { business_id: z.number().int() }
- `checkMembershipBalanceTool(args, context)` — private async function, three-case Greek reply per D-08
- Dispatcher case: `'check_membership_balance'` added to executeTool switch

New entry in src/conversation/ai-agent.ts BOOKING_TOOLS:
- `check_membership_balance` tool definition with Greek description and business_id parameter

<tasks>

<task type="auto">
  <name>Task 1: Add findMembershipsExpiringIn7Days + insertMembershipExpiryNotification to billing/queries.ts</name>
  <files>src/billing/queries.ts</files>

  <read_first>
    - src/billing/queries.ts — read lines 1–20 (imports) and lines 540–577 (getClientActiveMembership, last function in file) to understand the current import block and where to append new exports
    - src/database/schema.ts — read lines 295–330 (memberships table + membershipExpiryNotifications table added in Plan 01) to confirm the column names used in the new table and the memberships.expiresAt field type
    - src/utils/timezone.ts — read lines 1–32 to confirm the signatures of isoDateInAthens(date: Date): string and addCalendarDays(isoDate: string, days: number): string used in the rolling window calculation
    - .planning/phases/09-expiry-notifications-client-balance/09-PATTERNS.md — read the Pattern 3 (rolling window query) and Pattern 2 (dedup insert) sections for the exact Drizzle query shape to replicate
  </read_first>

  <action>
    Step 1 — Update the drizzle-orm import in src/billing/queries.ts.

    The current import is `import { and, desc, eq, gt, gte, sql } from 'drizzle-orm'`. Add `lte` to this import. Result: `import { and, desc, eq, gt, gte, lte, sql } from 'drizzle-orm'`.

    Step 2 — Add membershipExpiryNotifications to the schema import in src/billing/queries.ts.

    The current schema import block includes billingPackages, memberships, membershipLedger, clientBusinessRelationships, bookings, services, businesses. Add membershipExpiryNotifications to this list.

    Step 3 — Append ExpiringMembership interface and two new exported functions after the getClientActiveMembership function (end of file).

    ExpiringMembership interface: export interface ExpiringMembership with fields id (number), clientPhone (string), businessId (number), expiresAt (Date), sessionsRemaining (number or null).

    findMembershipsExpiringIn7Days function:
    - Uses db (not getConn()) — sweep context is outside withBusinessContext; no RLS scoping needed for this read
    - Computes the 7-day window using isoDateInAthens and addCalendarDays (already imported): const now = new Date(); const nowIso = isoDateInAthens(now); const sevenDaysFromNowIso = addCalendarDays(nowIso, 7); const windowEnd = new Date with string template sevenDaysFromNowIso + 'T23:59:59+02:00'
    - Query selects id, clientPhone, businessId, expiresAt, sessionsRemaining from memberships
    - WHERE clause: eq(memberships.businessId, businessId), eq(memberships.isActive, true), lte(memberships.expiresAt, windowEnd), gt(memberships.expiresAt, now)
    - The gt(memberships.expiresAt, now) clause excludes already-expired memberships from triggering notifications (per RESEARCH.md Open Question 3 and PATTERNS.md Pattern 3)

    insertMembershipExpiryNotification function:
    - Parameters: membershipId (number), notificationType ('7_day_client' | '7_day_owner'), expiryDate (string)
    - Returns: Promise boolean (true if newly inserted, false if UNIQUE conflict)
    - Uses db.insert(membershipExpiryNotifications).values({ membershipId, notificationType, expiryDate }).onConflictDoNothing().returning({ id: membershipExpiryNotifications.id })
    - Returns result.length > 0
    - Uses db (not getConn()) — same reason as findMembershipsExpiringIn7Days
  </action>

  <acceptance_criteria>
    - grep "lte" src/billing/queries.ts returns the updated drizzle-orm import line with lte present
    - grep "membershipExpiryNotifications" src/billing/queries.ts returns at least 2 lines (schema import + usage in insertMembershipExpiryNotification)
    - grep "ExpiringMembership" src/billing/queries.ts returns at least 2 lines (interface declaration + function return type)
    - grep "findMembershipsExpiringIn7Days" src/billing/queries.ts returns at least 2 lines (export + function body)
    - grep "insertMembershipExpiryNotification" src/billing/queries.ts returns at least 2 lines (export + function body)
    - grep "gt(memberships.expiresAt, now)" src/billing/queries.ts returns the exclusion filter line inside findMembershipsExpiringIn7Days
    - npx tsc --noEmit exits 0
    - npm test -- tests/billing-session-deduction.test.ts --no-coverage --bail exits 0 (no regressions to existing billing tests)
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit && npm test -- tests/billing-session-deduction.test.ts --no-coverage --bail 2>&1 | tail -5</automated>
  </verify>

  <done>findMembershipsExpiringIn7Days and insertMembershipExpiryNotification are exported; lte imported; TypeScript compiles; billing tests green</done>
</task>

<task type="auto">
  <name>Task 2: Add check_membership_balance Gemini tool to ai-agent.ts + function-executor.ts + implement tests</name>
  <files>src/conversation/ai-agent.ts, src/conversation/function-executor.ts, tests/function-executor.test.ts</files>

  <read_first>
    - src/conversation/ai-agent.ts — read lines 38–130 (full BOOKING_TOOLS array) to see the existing tool definition shape (type, name, description, parameters with properties and required fields) and where to append the new tool entry
    - src/conversation/function-executor.ts — read the full file; understand the Zod schema pattern (CheckAvailabilityArgsSchema etc.), tool handler signature (args: Record unknown, context: ToolContext) → Promise Record unknown, the switch dispatcher pattern, and existing imports at lines 1–18
    - src/utils/timezone.ts — read lines 1–32 to confirm formatExpiryDateGreek is now exported (added in Plan 01) before importing it
    - tests/function-executor.test.ts — read the full file; find the check_membership_balance describe block (appended in Plan 01) with 4 it.todo stubs to implement; note the existing BUSINESS/CONTEXT test helper constants and mock variable names for getActiveMembershipForDeduction (mockedGetActiveMembership at line ~47)
  </read_first>

  <action>
    Step 1 — Add check_membership_balance to BOOKING_TOOLS in src/conversation/ai-agent.ts.

    In the BOOKING_TOOLS array, append a new tool entry after the last existing entry (list_client_bookings or equivalent). The entry shape follows the existing pattern: type 'function' as const, name 'check_membership_balance', description in Greek ('Ελέγχει το υπόλοιπο συνδρομής του πελάτη — αριθμός εναπομεινάντων μαθημάτων και ημερομηνία λήξης.'), parameters object with type 'object', properties containing business_id (type 'integer', description 'Το αναγνωριστικό της επιχείρησης'), required array containing 'business_id'.

    Step 2 — Extend src/conversation/function-executor.ts.

    Add formatExpiryDateGreek to the import from '../utils/timezone'. The current file does not import from timezone.ts; add: import { formatExpiryDateGreek } from '../utils/timezone';

    After the existing Zod schema declarations (CheckAvailabilityArgsSchema etc.), add: const CheckMembershipBalanceArgsSchema = z.object({ business_id: z.number().int() });

    After the listClientBookingsTool function (end of file), add the private async function checkMembershipBalanceTool(args: Record string unknown, context: ToolContext): Promise Record string unknown. The function:
    - Parses args with CheckMembershipBalanceArgsSchema.parse(args)
    - Calls getActiveMembershipForDeduction(context.business.id, context.clientPhone) — already imported at line 18; this function is called within the withBusinessContext transaction context active during all tool executions
    - If membership is null: returns { success: true, message: 'Δεν βρέθηκε ενεργή συνδρομή. Επικοινωνήστε με ' + context.business.name + ' για ανανέωση.' }
    - If membership.sessionsRemaining === null: returns { success: true, message: 'Η συνδρομή σας είναι απεριόριστων μαθημάτων και λήγει στις ' + formatExpiryDateGreek(membership.expiresAt) + '.' }
    - Otherwise: returns { success: true, message: 'Έχετε ' + membership.sessionsRemaining + ' μαθήματα απομείνει. Η συνδρομή σας λήγει στις ' + formatExpiryDateGreek(membership.expiresAt) + '.' }
    - No try/catch inside the handler — the outer executeTool() catch block handles all tool errors uniformly (existing pattern)

    In the executeTool switch statement, before the default: case, add: case 'check_membership_balance': return await checkMembershipBalanceTool(args, context);

    Step 3 — Implement the 4 it.todo stubs in tests/function-executor.test.ts.

    Inside the describe('check_membership_balance tool — NOTF-04') block added in Plan 01, replace each it.todo with a full it test body:

    Test 1 (no-membership message): Set mockedGetActiveMembership.mockResolvedValue(null). Call executeTool('check_membership_balance', { business_id: MOCK_BUSINESS_ID }, mockContext). Assert result.success is true and result.message contains 'Δεν βρέθηκε ενεργή συνδρομή' and contains the business name. MOCK_BUSINESS_ID must match the business.id in the existing BUSINESS/CONTEXT test helpers.

    Test 2 (unlimited message): Set mockedGetActiveMembership.mockResolvedValue({ id: 1, sessionsRemaining: null, expiresAt: new Date('2026-08-14T22:00:00Z') }). Call executeTool. Assert result.message contains 'απεριόριστων μαθημάτων' and contains '14/08/2026'.

    Test 3 (counted message): Set mockedGetActiveMembership.mockResolvedValue({ id: 2, sessionsRemaining: 5, expiresAt: new Date('2026-08-14T22:00:00Z') }). Call executeTool. Assert result.message contains '5 μαθήματα απομείνει' and contains '14/08/2026'.

    Test 4 (cross-tenant denial): Call executeTool('check_membership_balance', { business_id: 9999 }, mockContext) where mockContext.business.id is a different value (e.g., 1). Assert result.error is 'cross_tenant_denied'. The existing T-02-12 dispatcher-level check handles this before the tool handler runs.

    Add beforeEach(() => { mockedGetActiveMembership.mockReset(); }) inside the describe block to prevent mock state leaking between tests.

    The mock for billingQueries already exists in the file (jest.mock factory at line ~13); ensure getActiveMembershipForDeduction: jest.fn() is present in that factory. If it was added in the Wave 0 scaffolding, confirm and reuse; do not duplicate the jest.mock call.
  </action>

  <acceptance_criteria>
    - grep "check_membership_balance" src/conversation/ai-agent.ts returns at least 2 lines (name field + description field inside the tool object)
    - grep "CheckMembershipBalanceArgsSchema" src/conversation/function-executor.ts returns at least 2 lines (declaration + .parse call)
    - grep "formatExpiryDateGreek" src/conversation/function-executor.ts returns at least 2 lines (import + usage in handler)
    - grep "case 'check_membership_balance'" src/conversation/function-executor.ts returns the switch case line
    - grep "απεριόριστων μαθημάτων" src/conversation/function-executor.ts returns the unlimited message template line
    - npm test -- tests/function-executor.test.ts --no-coverage exits 0 with 4 new passing tests (the stubs are now implemented; zero pending in the check_membership_balance describe block)
    - npm test -- tests/function-executor.test.ts --no-coverage output contains no failing tests
    - npx tsc --noEmit exits 0
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit && npm test -- tests/function-executor.test.ts --no-coverage 2>&1 | tail -10</automated>
  </verify>

  <done>check_membership_balance tool registered in BOOKING_TOOLS; handler implemented in function-executor.ts; all 4 tests pass; tsc clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini → executeTool dispatcher | Gemini supplies tool name + args; cross-tenant check (T-02-12) validates business_id matches context.business.id before any handler runs |
| checkMembershipBalanceTool → billing DB | getActiveMembershipForDeduction uses getConn() (RLS-scoped), always filtered by context.business.id + context.clientPhone — no cross-tenant read possible |
| sweep query (findMembershipsExpiringIn7Days) → DB | Uses db (not getConn()), filtered by businessId in WHERE — business isolation maintained at application layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Tampering | check_membership_balance args.business_id | high | mitigate | Existing T-02-12 dispatcher check in executeTool() validates args.business_id === context.business.id before handler runs; cross-tenant denial tested in Test 4 |
| T-09-05 | Information Disclosure | checkMembershipBalanceTool context.clientPhone | medium | mitigate | Tool accepts only business_id param; clientPhone is always sourced from context (Telegram from.id), never from Gemini args — prevents client-phone injection for cross-client balance reads |
| T-09-06 | Tampering | insertMembershipExpiryNotification race | medium | mitigate | Postgres UNIQUE INDEX + onConflictDoNothing() atomically prevents duplicate notification rows even under concurrent sweep overlaps |
| T-09-07 | Logic Error | findMembershipsExpiringIn7Days DST window | low | mitigate | Uses isoDateInAthens() + addCalendarDays() (DST-safe) per RESEARCH.md Anti-Patterns; gt(expiresAt, now) excludes already-expired memberships |
</threat_model>

<verification>
After this plan:
- npm test exits 0 with all prior tests passing + 4 new check_membership_balance tests passing
- npx tsc --noEmit exits 0
- billing/queries.ts exports findMembershipsExpiringIn7Days and insertMembershipExpiryNotification
- function-executor.ts dispatches 'check_membership_balance' to checkMembershipBalanceTool
- ai-agent.ts BOOKING_TOOLS includes check_membership_balance with Greek description
</verification>

<success_criteria>
- findMembershipsExpiringIn7Days produces DST-safe rolling window with both lte(windowEnd) and gt(now) filters
- insertMembershipExpiryNotification returns true on first insert, false on UNIQUE conflict
- checkMembershipBalanceTool returns correct Greek message for all three D-08 scenarios
- Cross-tenant denial tested and passing
- All existing billing and function-executor tests green
</success_criteria>

<output>
Create `.planning/phases/09-expiry-notifications-client-balance/09-02-SUMMARY.md` when done
</output>
