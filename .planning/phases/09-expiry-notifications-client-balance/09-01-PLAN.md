---
phase: 09-expiry-notifications-client-balance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/database/schema.ts
  - migrations/0008_expiry_notifications.sql
  - src/utils/timezone.ts
  - tests/scheduler-expiry.test.ts
  - tests/function-executor.test.ts
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
  - NOTF-03
  - NOTF-04

must_haves:
  truths:
    - "[FLAGGED ASSUMPTION NOTF-01] Client receives a Greek Telegram notification 7 days before their membership expires, containing sessions remaining and expiry date — unclassified/unresolved from spec probe; implementation is planned in Plans 02–03"
    - "[FLAGGED ASSUMPTION NOTF-02] Owner receives a Greek Telegram notification 7 days before any client's membership expires, containing client name, sessions remaining, and expiry date — unclassified/unresolved from spec probe; implementation is planned in Plans 02–03"
    - "[FLAGGED ASSUMPTION NOTF-03] A given (membership_id, notification_type, expiry_date) triple fires at most one notification per membership per expiry event, enforced by UNIQUE constraint at DB level — unclassified/unresolved from spec probe; dedup table created in this plan"
    - "[FLAGGED ASSUMPTION NOTF-04] A client query produces an accurate Greek reply covering three scenarios (no membership, unlimited, counted sessions) — unclassified/unresolved from spec probe; tool implementation is planned in Plan 02"
    - schema.ts exports membershipExpiryNotifications table and the symbol compiles under npx tsc --noEmit
    - migrations/0008_expiry_notifications.sql contains CREATE TABLE and CREATE UNIQUE INDEX for membership_expiry_notifications
    - src/utils/timezone.ts exports formatExpiryDateGreek(date Date) string returning DD/MM/YYYY in Europe/Athens timezone
    - tests/scheduler-expiry.test.ts exists with exactly 6 it.todo stubs and zero imports from src/scheduler/membership-expiry.ts
    - tests/function-executor.test.ts gains a check_membership_balance describe block with exactly 4 it.todo stubs; all prior passing tests remain passing
  artifacts:
    - src/database/schema.ts (membershipExpiryNotifications table added)
    - migrations/0008_expiry_notifications.sql
    - src/utils/timezone.ts (formatExpiryDateGreek export added)
    - tests/scheduler-expiry.test.ts (new, 6 it.todo stubs)
    - tests/function-executor.test.ts (extended, 4 it.todo stubs)
  key_links:
    - membershipExpiryNotifications.membershipId FK → memberships.id (dedup table anchored to membership row)
    - UNIQUE INDEX unique_membership_expiry_notification on (membership_id, notification_type, expiry_date) enforces NOTF-03
    - formatExpiryDateGreek consumed by checkMembershipBalanceTool (Plan 02) and membership-expiry sweep (Plan 03)
  prohibitions:
    - "[FLAGGED] tests/scheduler-expiry.test.ts MUST NOT import runMembershipExpirySweep or startMembershipExpiryPoller from src/scheduler/membership-expiry.ts at the module level — that module does not exist yet and will cause ts-jest compile failure"
    - "[FLAGGED] tests/scheduler-expiry.test.ts MUST NOT import findMembershipsExpiringIn7Days or insertMembershipExpiryNotification from src/billing/queries.ts at the module level — those exports do not exist yet"
    - "[FLAGGED] tests/function-executor.test.ts new it.todo stubs MUST NOT call any function that does not already exist — stubs are empty pending markers only"
    - botToken MUST NEVER appear in any logger.error or logger.warn call (no PII/credentials in logs)
    - SQL injection protection: all DB queries MUST use Drizzle ORM parameterized queries, never string interpolation in WHERE clauses
---

<objective>
Wave 0 — schema foundation, formatting utility, and test scaffolding for Phase 9.

Purpose: Establish the database table for notification dedup, the shared date-formatting helper, and empty test stubs before any business logic is implemented. Mirrors the Phase 8 Wave 0 pattern (STATE.md: "it.todo stubs with no imports from unbuilt modules").

Output:
- src/database/schema.ts — membershipExpiryNotifications table with UNIQUE(membership_id, notification_type, expiry_date) per D-04
- migrations/0008_expiry_notifications.sql — raw SQL for Neon DB apply in Plan 03
- src/utils/timezone.ts — formatExpiryDateGreek(date: Date): string for DD/MM/YYYY Athens display per D-09
- tests/scheduler-expiry.test.ts — new file, 6 it.todo stubs covering NOTF-01/02/03 sweep behaviors
- tests/function-executor.test.ts — extended with describe('check_membership_balance tool') containing 4 it.todo stubs for NOTF-04
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-expiry-notifications-client-balance/09-CONTEXT.md
@.planning/phases/09-expiry-notifications-client-balance/09-RESEARCH.md
@.planning/phases/09-expiry-notifications-client-balance/09-PATTERNS.md
</context>

## Artifacts This Plan Produces

New files:
- `migrations/0008_expiry_notifications.sql` — CREATE TABLE membership_expiry_notifications + UNIQUE INDEX
- `tests/scheduler-expiry.test.ts` — it.todo stubs: "sends client Telegram notification when membership expires in 7 days (NOTF-01)", "sends owner Telegram notification with client name when membership expires in 7 days (NOTF-02)", "does NOT re-send when notification row already exists for same membership+type+expiryDate (NOTF-03 dedup)", "skips business when botToken is null (no Telegram context)", "continues to next business when one business sweep throws (per-business isolation)", "uses clientPhone as fallback when clientName is null in owner notification (Pitfall 5)"

Extended files:
- `src/database/schema.ts` — membershipExpiryNotifications table appended after membershipLedger (line 327)
- `src/utils/timezone.ts` — formatExpiryDateGreek(date: Date): string exported after addCalendarDays
- `tests/function-executor.test.ts` — new describe('check_membership_balance tool') block appended with 4 it.todo stubs

New symbols:
- `membershipExpiryNotifications` (Drizzle table object, exported from schema.ts)
- `formatExpiryDateGreek` (function, exported from timezone.ts)
- `unique_membership_expiry_notification` (UNIQUE INDEX name, in migration SQL and schema uniqueIndex)

Planner discretion (D-05): notification_type column stores `'7_day_client'` or `'7_day_owner'` as two distinct values rather than a single `'7_day'` value. This provides per-recipient dedup granularity — client and owner notifications can succeed or fail independently without one blocking retry of the other.

<tasks>

<task type="auto">
  <name>Task 1: Add membershipExpiryNotifications schema table + migration SQL + formatExpiryDateGreek utility</name>
  <files>src/database/schema.ts, migrations/0008_expiry_notifications.sql, src/utils/timezone.ts</files>

  <read_first>
    - src/database/schema.ts — read lines 295–327 (memberships + membershipLedger tables) to understand the existing table structure, pgTable/uniqueIndex/serial/integer/text/timestamp/boolean imports, and where to append the new table
    - src/database/schema.ts — read lines 237–265 (billingPackages table) to see the full uniqueIndex pattern with .on() call used in this codebase
    - src/utils/timezone.ts — read the full file (32 lines) to see isoDateInAthens and addCalendarDays export signatures before appending formatExpiryDateGreek
    - migrations/0007_enforcement_policy.sql — read to see the SQL style convention (column types, NOT NULL, DEFAULT, FK constraint naming) used in the most recent migration
  </read_first>

  <action>
    Step 1 — Extend src/database/schema.ts.

    After the closing brace of the membershipLedger table (around line 327), append the membershipExpiryNotifications table definition. The table definition uses pgTable('membership_expiry_notifications', ...) with the following columns: id (serial PK), membershipId (integer NOT NULL FK to memberships.id via .references(() => memberships.id)), notificationType (text NOT NULL — stores '7_day_client' or '7_day_owner' per the D-05 discretion choice), expiryDate (text NOT NULL — stores YYYY-MM-DD Athens calendar date produced by isoDateInAthens()), sentAt (timestamp NOT NULL with .defaultNow()), createdAt (timestamp NOT NULL with .defaultNow()). Add a table-level uniqueIndex named 'unique_membership_expiry_notification' on (table.membershipId, table.notificationType, table.expiryDate) — this is the NOTF-03 dedup constraint per D-04. No partial WHERE clause (unlike billingPackages) because dedup must be absolute per recipient per expiry event with no soft-delete carve-outs.

    Step 2 — Create migrations/0008_expiry_notifications.sql.

    Write raw SQL that creates the membership_expiry_notifications table and its UNIQUE INDEX. Use the same SQL conventions as 0007_enforcement_policy.sql (ALTER TABLE style for FK if applicable, or inline REFERENCES). The table DDL must match the Drizzle schema exactly: id SERIAL PRIMARY KEY, membership_id INTEGER NOT NULL REFERENCES memberships(id), notification_type TEXT NOT NULL, expiry_date TEXT NOT NULL, sent_at TIMESTAMP NOT NULL DEFAULT NOW(), created_at TIMESTAMP NOT NULL DEFAULT NOW(). Add: CREATE UNIQUE INDEX unique_membership_expiry_notification ON membership_expiry_notifications (membership_id, notification_type, expiry_date).

    Step 3 — Extend src/utils/timezone.ts.

    After the addCalendarDays function, append a new exported function formatExpiryDateGreek(date: Date): string. This function formats a Date value as DD/MM/YYYY in the Europe/Athens timezone using Intl.DateTimeFormat. Use 'en-GB' locale with timeZone: 'Europe/Athens', day: '2-digit', month: '2-digit', year: 'numeric', and no hour12 or time fields. This produces '14/08/2026' style output directly (en-GB locale formats as DD/MM/YYYY). Do not use manual string concatenation or other locales.
  </action>

  <acceptance_criteria>
    - grep -c "membershipExpiryNotifications" src/database/schema.ts returns at least 2 (export declaration + uniqueIndex usage)
    - grep "unique_membership_expiry_notification" src/database/schema.ts returns the uniqueIndex name
    - migrations/0008_expiry_notifications.sql exists and grep "membership_expiry_notifications" migrations/0008_expiry_notifications.sql returns at least 3 lines (CREATE TABLE + 2+ column lines + UNIQUE INDEX)
    - grep "unique_membership_expiry_notification" migrations/0008_expiry_notifications.sql returns the CREATE UNIQUE INDEX line
    - grep -c "formatExpiryDateGreek" src/utils/timezone.ts returns at least 2 (function declaration + export)
    - npx tsc --noEmit exits 0 (no TypeScript errors)
    - npm test -- tests/timezone.test.ts --bail exits 0 (no regressions to existing timezone tests)
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit && echo "TSC OK" && npm test -- tests/timezone.test.ts --bail --no-coverage 2>&1 | tail -5</automated>
  </verify>

  <done>schema.ts exports membershipExpiryNotifications; migration SQL exists; timezone.ts exports formatExpiryDateGreek; tsc passes</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/scheduler-expiry.test.ts + extend tests/function-executor.test.ts with it.todo stubs</name>
  <files>tests/scheduler-expiry.test.ts, tests/function-executor.test.ts</files>

  <read_first>
    - tests/expiry-poller.test.ts — read the full file; this is the closest structural analog: jest.mock setup for database/queries and telegram/client, per-business test patterns, botTokenStore mock
    - tests/scheduler-reminders.test.ts — read the full file; shows jest.setSystemTime() usage, per-business outer loop test shape, and how runReminderSweep is imported
    - tests/function-executor.test.ts — read the full file; understand the existing describe structure, mock declarations, BUSINESS/CONTEXT helper constants, and where to safely append the new describe block
  </read_first>

  <action>
    Step 1 — Create tests/scheduler-expiry.test.ts as a NEW file.

    Follow the mock setup pattern from tests/expiry-poller.test.ts. Add jest.mock declarations for '../src/database/queries', '../src/billing/queries', '../src/telegram/client', and '../src/utils/logger' at the top of the file, before any imports. Do NOT add a top-level import of runMembershipExpirySweep or startMembershipExpiryPoller — that module does not exist yet. Do NOT add a top-level import of findMembershipsExpiringIn7Days or insertMembershipExpiryNotification — those exports do not exist yet.

    Add a describe('runMembershipExpirySweep', () => { ... }) block containing exactly these 6 it.todo items:
    - it.todo('sends client Telegram notification when membership expires in 7 days (NOTF-01)')
    - it.todo('sends owner Telegram notification with client name when membership expires in 7 days (NOTF-02)')
    - it.todo('does NOT re-send when notification row already exists for same membership+type+expiryDate (NOTF-03 dedup)')
    - it.todo('skips business when botToken is null (no Telegram context)')
    - it.todo('continues to next business when one business sweep throws (per-business isolation)')
    - it.todo('uses clientPhone as fallback when clientName is null in owner notification (Pitfall 5)')

    Add a top-level comment: '// Phase 9: Membership expiry sweep tests — covers NOTF-01, NOTF-02, NOTF-03'

    Step 2 — Extend tests/function-executor.test.ts.

    Append a new describe block at the very END of the file, after all existing describe blocks. Do not modify, move, or delete any existing code.

    The new block: describe('check_membership_balance tool — NOTF-04', () => { ... }) containing exactly these 4 it.todo items:
    - it.todo('returns no-membership Greek message when getActiveMembershipForDeduction returns null (D-08 scenario 1)')
    - it.todo('returns unlimited-sessions Greek message when sessionsRemaining is null (D-08 scenario 2)')
    - it.todo('returns counted-sessions Greek message with N remaining when sessionsRemaining is a number (D-08 scenario 3)')
    - it.todo('returns cross_tenant_denied when args.business_id differs from context.business.id')

    Do NOT add any new jest.mock declarations to function-executor.test.ts in this task. The stubs must compile under the existing mock setup.
  </action>

  <acceptance_criteria>
    - tests/scheduler-expiry.test.ts exists
    - grep -c "it.todo" tests/scheduler-expiry.test.ts returns 6
    - grep -v "^//" tests/scheduler-expiry.test.ts | grep "membership-expiry\|findMembershipsExpiring\|insertMembershipExpiry" returns empty (no live imports from unbuilt modules)
    - npm test -- tests/scheduler-expiry.test.ts --no-coverage exits 0 with 6 pending tests (0 failed)
    - grep -c "it.todo" tests/function-executor.test.ts returns the prior count plus exactly 4
    - grep "check_membership_balance tool" tests/function-executor.test.ts returns the describe block title line
    - npm test -- tests/function-executor.test.ts --no-coverage exits 0 (all prior tests still pass; 4 new pending stubs)
  </acceptance_criteria>

  <verify>
    <automated>npm test -- tests/scheduler-expiry.test.ts tests/function-executor.test.ts --no-coverage 2>&1 | tail -10</automated>
  </verify>

  <done>Both test files compile and pass; scheduler-expiry.test.ts has 6 pending stubs; function-executor.test.ts has 4 new pending stubs; full test suite unaffected</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → DB | membershipExpiryNotifications table adds a FK to memberships — migration SQL must be applied to Neon DB before sweep runs (Plan 03 checkpoint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | UNIQUE INDEX definition | medium | mitigate | UNIQUE on (membership_id, notification_type, expiry_date) enforces NOTF-03 at DB level atomically; onConflictDoNothing() in Plan 02 ensures no race between check-and-insert |
| T-09-02 | Information Disclosure | formatExpiryDateGreek + timezone.ts | low | accept | Function is a pure Intl formatter; no PII touches logs; output is a date string (DD/MM/YYYY) with no membership ID or phone number |
| T-09-03 | Tampering | Test stub files | low | accept | Stubs contain no imports from unbuilt modules; ts-jest compile gate prevents broken stub patterns from entering the codebase |
</threat_model>

<verification>
After this plan:
- npm test exits 0 with no new failures (6 + 4 new pending it.todos are counted as pass)
- npx tsc --noEmit exits 0
- src/database/schema.ts exports membershipExpiryNotifications
- migrations/0008_expiry_notifications.sql exists and is syntactically valid SQL
- src/utils/timezone.ts exports formatExpiryDateGreek
</verification>

<success_criteria>
- Schema table defined with correct UNIQUE constraint covering (membership_id, notification_type, expiry_date)
- Migration SQL file ready for Plan 03 DB apply step
- formatExpiryDateGreek produces DD/MM/YYYY in Europe/Athens timezone
- 6 it.todo stubs in scheduler-expiry.test.ts with no unbuilt imports
- 4 it.todo stubs appended to function-executor.test.ts check_membership_balance describe block
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/09-expiry-notifications-client-balance/09-01-SUMMARY.md` when done
</output>
