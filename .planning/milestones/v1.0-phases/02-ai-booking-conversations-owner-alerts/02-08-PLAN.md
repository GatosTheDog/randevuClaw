---
phase: 02-ai-booking-conversations-owner-alerts
plan: 8
type: execute
wave: 1
depends_on: []
files_modified:
  - src/conversation/expiry-poller.ts
  - tests/expiry-poller.test.ts
autonomous: true
gap_closure: true
requirements: [OWNR-02]

must_haves:
  truths:
    - "CR-04: When one client's expiry notification fails during a sweep, every OTHER stale booking already flipped to 'expired' in that same sweep batch still gets its client notification and owner-button-clear attempted, not silently skipped and permanently un-notified"
  artifacts:
    - "src/conversation/expiry-poller.ts — per-booking try/catch inside runExpirySweep's inner loop"
  key_links:
    - "runExpirySweep's new per-booking try/catch isolates one booking's Telegram-send failure from the rest of the already-expired batch returned by expireStalePendingBookings, since a booking atomically flipped to 'expired' in the DB will never be revisited by a future sweep (its WHERE clause only selects still-pending_owner_approval rows)"
---

<objective>
Close CR-04: isolate each expired booking's client-notification-and-button-clear step inside runExpirySweep's inner loop in its own try/catch, so one Telegram send failure (network blip, rate limit, blocked chat) does not permanently silence notification for every OTHER booking already atomically flipped to 'expired' in that same sweep batch.

Purpose: Restore OWNR-02's "owner receives a WhatsApp/Telegram alert... in real time" reliability guarantee for the pending-booking expiry path — a booking that expireStalePendingBookings has already marked 'expired' can never be re-swept, so a skipped notification today is lost forever, not just delayed.
Output: src/conversation/expiry-poller.ts fixed and covered by a new isolation test; full regression suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-booking-conversations-owner-alerts/02-VERIFICATION.md
@.planning/phases/02-ai-booking-conversations-owner-alerts/02-REVIEW.md
@src/conversation/expiry-poller.ts
@tests/expiry-poller.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Isolate per-booking notification failures inside the expiry sweep (CR-04)</name>
  <files>src/conversation/expiry-poller.ts, tests/expiry-poller.test.ts</files>
  <read_first>
    - src/conversation/expiry-poller.ts — whole file (67 lines), especially runExpirySweep's inner `for (const booking of expired)` loop (lines 32-48)
    - tests/expiry-poller.test.ts — existing Tests 2, 3, 4 (notification/button-clear behavior and the existing per-business error isolation test)
    - .planning/phases/02-ai-booking-conversations-owner-alerts/02-REVIEW.md — CR-04 section (exact fix code and root-cause explanation)
  </read_first>
  <action>
    In `runExpirySweep`, wrap the body of the inner `for (const booking of expired)` loop — the `sendTelegramMessage` call, the `notifiedCount += 1` increment, and the conditional `editTelegramMessageReplyMarkup` button-clear block — in its own try/catch, nested inside the existing per-business try/catch. On catch, log via `logger.error({ err, bookingId: booking.id }, 'Failed to notify client of expired booking')` and let the loop continue to the next booking in the array — do not let one booking's failure abort the loop for the rest of that business's already-expired batch.

    Leave the outer per-business try/catch (around the `expireStalePendingBookings` call and its own `logger.error` call) completely unchanged — this is a second, nested layer of isolation, not a replacement.
  </action>
  <behavior>
    - `expireStalePendingBookings` returns TWO expired bookings for business 1; `sendTelegramMessage` is mocked to reject for the FIRST booking's clientPhone and resolve for the SECOND. Assert `sendTelegramMessage` was called for BOTH bookings (the loop was not aborted after the first failure), the returned `notifiedCount` is `1` (only the successfully-notified booking counts), and `logger.error` was called once, tagged with the failed booking's `bookingId`.
    - Confirm the existing Test 4 ("one business failing does not stop the sweep for others") still passes completely unchanged — this proves the new per-booking isolation is additive to, not a replacement for, the existing per-business isolation.
  </behavior>
  <verify>
    <automated>npx jest tests/expiry-poller.test.ts</automated>
    <human-check>Manually shorten EXPIRY_CUTOFF_MS (or backdate a booking's createdAt by 2+ hours) and let the poller's interval fire against a live Telegram bot + real Postgres. Confirm the client receives the Greek expiry message and the owner's original alert buttons are cleared. Then repeat with one client's chat deliberately unreachable (e.g. a blocked/invalid chat id) and confirm the OTHER pending bookings in the same sweep still get notified and their buttons still get cleared — proving CR-04's per-booking isolation end-to-end, not just via mocks.</human-check>
  </verify>
  <acceptance_criteria>
    - A `sendTelegramMessage` rejection for one booking in a multi-booking sweep batch does not prevent the next booking in that same batch from being notified.
    - `notifiedCount` only counts bookings whose notification actually succeeded.
    - All 6 existing tests plus the 1 new test pass.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>CR-04 is closed; expiry-sweep notification failures are isolated per-booking, matching the existing per-business isolation pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| expiry-poller.ts (server-internal timer, no external/user input) → Telegram outbound API | A transient outbound failure to one recipient must not affect delivery to any other recipient in the same batch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-26 | Denial of Service | expiry-poller.ts runExpirySweep (CR-04) | high | mitigate | Per-booking try/catch so one Telegram send failure never permanently silences notification and button-clearing for the rest of an already-'expired' batch (those rows can never be re-swept, since expireStalePendingBookings' WHERE clause only selects still-pending_owner_approval rows) |
</threat_model>

<verification>
Run the full regression suite (`npm test`) and `npx tsc --noEmit` after the task lands — must show 0 failures and 0 new TypeScript errors relative to the pre-gap-closure baseline, plus the 1 new isolation test.
</verification>

<success_criteria>
- A sweep batch of N stale bookings, where notification M fails, still attempts notification for all N-1 other bookings in that same batch.
- Existing per-business error isolation (Test 4) is unaffected.
</success_criteria>

<output>
Create `.planning/phases/02-ai-booking-conversations-owner-alerts/02-08-SUMMARY.md` when done
</output>
