---
phase: 02-ai-booking-conversations-owner-alerts
plan: 7
type: execute
wave: 1
depends_on: []
files_modified:
  - src/conversation/greek-preprocessor.ts
  - tests/greek-preprocessor.test.ts
autonomous: true
gap_closure: true
requirements: [BOOK-01, BOOK-03]

must_haves:
  truths:
    - "CR-05: Ordinary Greek 24-hour phrasing without an am/pm marker or time-of-day context word (e.g. 'στις 20', 'Παρασκευή στις 14') resolves to its own literal hour, never an invalid clock time like '32:00' or '26:00'"
  artifacts:
    - "src/conversation/greek-preprocessor.ts — resolveHourToTime's new hour>=13 guard"
    - "tests/greek-preprocessor.test.ts — 3 new corpus phrases covering bare hours 13-23"
  key_links:
    - "resolveHourToTime's new hour>=13 && hour<=23 guard runs BEFORE the existing bare-hour 12-hour heuristic (hour===12 / 8-11 / else+12), so an already-24h hour never reaches the +12 branch — the resulting valid time is what gets embedded in the [ΣΥΣΤΗΜΑ: ...] hint that Gemini's system prompt instructs it to trust for book_appointment/reschedule_appointment's calendar_time argument"
---

<objective>
Close CR-05: fix the Greek temporal preprocessor's bare-hour heuristic so ordinary 24-hour phrasing ("στις 20", "στις 14") never has 12 added a second time, which currently produces invalid clock times like "32:00"/"26:00" that flow straight into the Gemini-trusted system hint and can reach book_appointment's/reschedule_appointment's calendar_time argument.

Purpose: Restore BOOK-01 (booking via natural Greek chat) and BOOK-03 (checking availability) for the extremely common case of a client stating an ordinary 24-hour time with no am/pm marker.
Output: src/conversation/greek-preprocessor.ts fixed and covered by an extended 23-phrase corpus test; full regression suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-booking-conversations-owner-alerts/02-VERIFICATION.md
@.planning/phases/02-ai-booking-conversations-owner-alerts/02-REVIEW.md
@src/conversation/greek-preprocessor.ts
@tests/greek-preprocessor.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard the bare-hour heuristic against already-unambiguous 24-hour input (CR-05)</name>
  <files>src/conversation/greek-preprocessor.ts, tests/greek-preprocessor.test.ts</files>
  <read_first>
    - src/conversation/greek-preprocessor.ts — lines 76-103 (resolveHourToTime), lines 105-116 (resolveTime, the caller)
    - tests/greek-preprocessor.test.ts — the existing 20-phrase corpus, especially Tests 4, 8, 14 (the neighboring bare-hour boundary cases: hour 5, hour 11, hour 12)
    - .planning/phases/02-ai-booking-conversations-owner-alerts/02-REVIEW.md — CR-05 section (exact fix code and root-cause explanation)
  </read_first>
  <action>
    In `resolveHourToTime`, after the existing marker-present branch and the two time-of-day-context-word branches (`πρωι`; `απογευμα`/`μεσημερι`/`βραδυ`), and BEFORE the bare-hour heuristic's `if (hour === 12)` line, add a new guard: when `hour >= 13 && hour <= 23`, return `formatHour(hour, minutes)` immediately. Add a comment explaining this is CR-05's fix — an hour already in unambiguous 24-hour form must never have 12 added again by the heuristic below it.

    Leave every other branch completely unchanged: the marker-present logic, the `πρωι`/`απογευμα`/`μεσημερι`/`βραδυ` context-word logic, the bare-hour heuristic's `hour === 12` noon case, the `8 <= hour <= 11` morning case, and the final `return formatHour(hour + 12, minutes)` fallback for hours 1-7.
  </action>
  <behavior>
    - "Παρασκευή στις 14" → resolvedDate '2026-07-10' (next Friday from the corpus's fixed Wednesday reference), resolvedTime '14:00' (not '26:00').
    - "Παρασκευή στις 20" → resolvedDate '2026-07-10', resolvedTime '20:00' (not '32:00').
    - "στις 22" → no weekday/relative-day keyword present in the text, so resolvedDate is null; resolvedTime '22:00' (not '34:00').
    - Every existing corpus test (1-20, plus the annotation/robustness tests) keeps its exact current expected value unchanged — in particular Tests 4/8/14 (bare hours 5, 11, 12) must keep passing exactly as before, proving the new guard only changes behavior for hours 13-23.
  </behavior>
  <verify>
    <automated>npx jest tests/greek-preprocessor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `resolveHourToTime(20, '00', undefined, ...)` returns `'20:00'`, not `'32:00'`.
    - `resolveHourToTime(14, '00', undefined, ...)` returns `'14:00'`, not `'26:00'`.
    - All 23 corpus tests (20 original + 3 new) plus the annotation/robustness tests pass.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>CR-05 is closed; bare 24-hour Greek phrasing without an am/pm marker never produces an invalid clock time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client free-text message → greek-preprocessor.ts's annotatedText system hint → Gemini (instructed to trust the hint) → book_appointment/reschedule_appointment's calendar_time argument → DB write | An incorrectly-derived time string is embedded in text Gemini's own system prompt is told to ground its function-call arguments in, then flows unvalidated into a booking row |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-25 | Tampering | greek-preprocessor.ts resolveHourToTime (CR-05) | high | mitigate | Add an explicit 24-hour guard before the 12-hour heuristic so an invalid time (e.g. "32:00") can never be embedded in the Gemini-trusted system hint or reach the calendar_time argument of book_appointment/reschedule_appointment |
</threat_model>

<verification>
Run the full regression suite (`npm test`) and `npx tsc --noEmit` after the task lands — must show 0 failures and 0 new TypeScript errors relative to the pre-gap-closure baseline, plus the 3 new corpus tests.
</verification>

<success_criteria>
- Every bare-hour Greek phrase in the 13-23 range resolves to its own literal 24-hour time, with no double-application of the +12 heuristic.
- The original 20-phrase corpus (hours 1-12, marker-present forms, and time-of-day context words) is completely unaffected.
</success_criteria>

<output>
Create `.planning/phases/02-ai-booking-conversations-owner-alerts/02-07-SUMMARY.md` when done
</output>
