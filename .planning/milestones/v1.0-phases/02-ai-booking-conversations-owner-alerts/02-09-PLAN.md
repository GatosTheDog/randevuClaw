---
phase: 02-ai-booking-conversations-owner-alerts
plan: 9
type: execute
wave: 1
depends_on: []
files_modified:
  - src/database/queries.ts
  - src/webhooks/telegram.ts
  - tests/booking-queries.test.ts
  - tests/telegram-webhook.test.ts
autonomous: true
gap_closure: true
requirements: [OWNR-02]

must_haves:
  truths:
    - "WR-05: Two near-simultaneous Αποδοχή/Απόρριψη taps (or a Telegram-redelivered callback_query) on the SAME pending booking never both proceed to notify the client or run the reschedule cascade — only the request that wins the atomic status transition does"
    - "Aggregate (Truth #10, covered jointly with 02-06-PLAN.md's CR-02 fix): a booking can never be double-inserted or double-transitioned for the same business/date/time slot while an active hold already exists"
  artifacts:
    - "src/database/queries.ts — updateBookingStatusIfPending atomic compare-and-swap function"
    - "src/webhooks/telegram.ts — handleCallbackQuery uses the atomic update's return value as the sole gate for notify/cascade/button-clear, replacing the old read-then-check"
  key_links:
    - "handleCallbackQuery's notify/cascade/button-clear steps are now gated entirely on updateBookingStatusIfPending's return value (null = another request already won, or the booking was never pending), replacing the old two-step read-check-then-write race"
---

<objective>
Close WR-05: replace the owner-approval callback_query handler's read-then-write race (read booking.bookingStatus, check it in application code, THEN call updateBookingStatus separately) with an atomic compare-and-swap DB update, so two near-simultaneous taps on the same Αποδοχή/Απόρριψη button — or Telegram redelivering the same callback_query — can never both pass the pending-check and both notify the client / run the reschedule cascade.

Purpose: Close the last concurrency gap blocking ROADMAP.md's Phase 2 Success Criterion 5 ("owner can accept/reject... two clients attempting to book the exact same slot at the same time never both succeed") for OWNR-02's owner-approval surface, complementing 02-06-PLAN.md's CR-02 fix on the client-booking side.
Output: A new updateBookingStatusIfPending query function proven against a real Postgres connection, and src/webhooks/telegram.ts's handleCallbackQuery rewritten to use it as the sole concurrency gate; full regression suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-booking-conversations-owner-alerts/02-VERIFICATION.md
@.planning/phases/02-ai-booking-conversations-owner-alerts/02-REVIEW.md
@src/database/queries.ts
@src/webhooks/telegram.ts
@tests/booking-queries.test.ts
@tests/telegram-webhook.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an atomic compare-and-swap booking-status query (WR-05 foundation)</name>
  <files>src/database/queries.ts, tests/booking-queries.test.ts</files>
  <read_first>
    - src/database/queries.ts — `updateBookingStatus` (lines 315-317) and `findBookingByIdUnscoped` (lines 300-303), for the existing query-function conventions in this file
    - tests/booking-queries.test.ts — whole file, for the real-Postgres integration-test conventions (RUN_ID-prefixed unique data, beforeAll/afterAll cleanup, futureExpiry helper)
    - .planning/phases/02-ai-booking-conversations-owner-alerts/02-REVIEW.md — WR-05 section (exact fix code and root-cause explanation)
  </read_first>
  <action>
    Add a new exported function `updateBookingStatusIfPending(bookingId: number, newStatus: string): Promise<Booking | null>` directly below the existing `updateBookingStatus` function in src/database/queries.ts. It runs a single `db.update(bookings).set({ bookingStatus: newStatus }).where(and(eq(bookings.id, bookingId), eq(bookings.bookingStatus, 'pending_owner_approval'))).returning()` and returns `rows[0] ?? null`.

    Add a comment explaining that the WHERE clause itself is the concurrency guard: of two near-simultaneous callers, only the first to reach Postgres with this UPDATE finds a matching row still `pending_owner_approval` and gets it back; the second's WHERE clause matches zero rows and returns `null`, telling the caller "someone else already resolved this."
  </action>
  <behavior>
    - Insert a `pending_owner_approval` booking (via `queries.insertBooking`, following this file's existing RUN_ID-prefixed pattern); call `updateBookingStatusIfPending(id, 'confirmed')` — assert it returns the row with `bookingStatus: 'confirmed'`.
    - Call `updateBookingStatusIfPending` a SECOND time on that now-`confirmed` booking (simulating the loser of a race) — assert it returns `null`, and re-fetch the row via `findBookingById` to confirm `bookingStatus` is still `'confirmed'` (unchanged by the second no-op call, not reverted or duplicated).
  </behavior>
  <verify>
    <automated>npx jest tests/booking-queries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `updateBookingStatusIfPending` only mutates and returns a row when the booking is currently `pending_owner_approval`; otherwise it returns `null` with zero rows affected.
    - New tests pass against the real local test Postgres database, following this file's existing `TEST_DATABASE_URL`/`jest.resetModules()` convention.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The atomic compare-and-swap primitive exists and is proven against a real Postgres connection; Task 2 wires it into the webhook handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the read-then-write owner-approval race with the atomic compare-and-swap (WR-05)</name>
  <files>src/webhooks/telegram.ts, tests/telegram-webhook.test.ts</files>
  <read_first>
    - src/webhooks/telegram.ts — `handleCallbackQuery` (lines 96-166) in full
    - tests/telegram-webhook.test.ts — the "callback_query owner approval" describe block, especially Tests 5, 6, 7, 8, 12
    - .planning/phases/02-ai-booking-conversations-owner-alerts/02-REVIEW.md — WR-05 section (exact fix code and root-cause explanation)
  </read_first>
  <action>
    Import `updateBookingStatusIfPending` from `../database/queries` alongside the existing `updateBookingStatus` import in src/webhooks/telegram.ts — both stay in use: `updateBookingStatus` remains for the reschedule-cascade's cancel of the ORIGINAL booking (a different row, not itself subject to the same double-tap race).

    In `handleCallbackQuery`, DELETE the existing status pre-check block (`if (booking.bookingStatus !== 'pending_owner_approval') { logger.info(...); return; }`) — it is superseded by the atomic update below.

    Immediately after the existing ownership check (the `ownerTelegramId !== senderTelegramId` block), compute `const newStatus = parsed.action === 'approve' ? 'confirmed' : 'rejected';` and call `const updated = await updateBookingStatusIfPending(booking.id, newStatus);`. If `updated` is falsy, log via `logger.info({ bookingId: booking.id }, 'callback_query lost the race or booking already resolved, ignoring')` and return — this replaces the deleted pre-check's early-return behavior AND additionally closes the actual race window.

    Rewrite the approve/reject branches and the trailing button-clear block to read every booking field they need (`clientPhone`, `calendarDate`, `calendarTime`, `businessId`, `serviceId`, `rescheduledFromBookingId`, `ownerTelegramMessageId`) from `updated` instead of the earlier `booking` variable, since `updated` is the authoritative post-transition row. The reschedule cascade (`if (updated.rescheduledFromBookingId) { await updateBookingStatus(updated.rescheduledFromBookingId, 'cancelled'); }`) stays a plain, unconditional `updateBookingStatus` call — it targets a DIFFERENT booking row than the one just compare-and-swapped.
  </action>
  <behavior>
    - Test 5 (approve): mock `updateBookingStatusIfPending` to resolve with `{ ...PENDING_BOOKING, bookingStatus: 'confirmed' }` instead of mocking plain `updateBookingStatus`; assert `updateBookingStatusIfPending` was called with `(42, 'confirmed')`.
    - Test 6 (reject): mock `updateBookingStatusIfPending` to resolve with `{ ...PENDING_BOOKING, bookingStatus: 'rejected' }`; assert called with `(42, 'rejected')`; assert plain `updateBookingStatus` was NEVER called (no cascade on reject, and the primary transition no longer uses it).
    - Test 7 (reschedule approval cascade): mock `updateBookingStatusIfPending` to resolve with the confirmed new booking (`id: 99, rescheduledFromBookingId: 42`); assert it was called with `(99, 'confirmed')`; assert plain `updateBookingStatus` was still called with `(42, 'cancelled')` for the cascade.
    - Test 8 (reschedule rejection, no cascade): mock `updateBookingStatusIfPending` to resolve with the rejected new booking; assert plain `updateBookingStatus` was never called at all.
    - Test 12 (already-resolved re-tap): mock `updateBookingStatusIfPending` to resolve with `null` (simulating the atomic update finding no matching pending row); assert `sendTelegramMessage` and `editTelegramMessageReplyMarkup` were never called.
    - New Test 13: simulate a concurrent double-tap via two sequential `postWebhook` calls with different `update_id`s but the SAME `approve_42` callback data; mock `updateBookingStatusIfPending` with `.mockResolvedValueOnce({ ...PENDING_BOOKING, bookingStatus: 'confirmed' }).mockResolvedValueOnce(null)` so the first call "wins" and the second "loses"; assert `sendTelegramMessage` was called exactly ONCE in total across both requests, not twice — this is the automated regression proof for WR-05's race-condition fix.
  </behavior>
  <verify>
    <automated>npx jest tests/telegram-webhook.test.ts</automated>
    <human-check>Deploy or run the bot locally against a real Telegram bot token with two client sessions/windows open as the same owner. Create a pending booking (or use a fixture), open its Αποδοχή/Απόρριψη alert, and tap Αποδοχή as close together as possible from both windows (or rapid double-tap on one). Confirm only ONE client confirmation message is received, not two, and the owner's alert buttons visibly stop responding after the first successful tap.</human-check>
  </verify>
  <acceptance_criteria>
    - `handleCallbackQuery` no longer performs a separate read-then-check on `booking.bookingStatus`; the atomic `updateBookingStatusIfPending` call is the sole gate for whether notify/cascade/button-clear run.
    - The reschedule cascade still uses plain `updateBookingStatus` on the ORIGINAL booking id, unconditionally, once the new booking's compare-and-swap has succeeded.
    - All 12 existing telegram-webhook.test.ts owner-approval tests (updated per above) plus the 1 new concurrent-tap test pass.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>WR-05 is closed; the owner-approval callback_query path can no longer double-notify a client from two near-simultaneous taps or a redelivered callback_query.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Telegram owner client (authenticated via ownerTelegramId match, semi-trusted) → callback_query webhook → booking status DB mutation | Two deliveries of the same logical approve/reject action (double-tap, or Telegram's own redelivery) must resolve to exactly one state transition and exactly one client notification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-27 | Tampering | webhooks/telegram.ts handleCallbackQuery + database/queries.ts (WR-05) | high | mitigate | Atomic compare-and-swap (`updateBookingStatusIfPending`, `WHERE booking_status='pending_owner_approval'`) replaces the read-then-write race, so only one of two concurrent callback_query deliveries for the same booking can ever transition it and trigger notifications/cascade |
</threat_model>

<verification>
Run the full regression suite (`npm test`) and `npx tsc --noEmit` after both tasks land — must show 0 failures and 0 new TypeScript errors relative to the pre-gap-closure baseline, plus the 3 new/updated behaviors from Task 1 and the 1 new + 4 updated behaviors from Task 2.
</verification>

<success_criteria>
- Two concurrent callback_query deliveries for the same booking id and action never both result in a client notification — exactly one does.
- The reschedule-approval cascade (confirm new, cancel original) still fires correctly once, only for the winning request.
- A second tap on an already-resolved booking's buttons is silently ignored (200 OK, no mutation, no notification).
</success_criteria>

<output>
Create `.planning/phases/02-ai-booking-conversations-owner-alerts/02-09-SUMMARY.md` when done
</output>
