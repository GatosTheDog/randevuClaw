---
phase: 08-enforcement-session-deduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/billing-session-deduction.test.ts
  - tests/billing-enforcement-policy.test.ts
  - tests/function-executor.test.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
  - ENFC-01
  - ENFC-02
  - ENFC-03

must_haves:
  truths:
    - All three test files compile and run under ts-jest without errors; it.todo items are skipped (pending), not failures
    - No stub file imports a function from billing/queries.ts that does not yet exist (deductSession, restoreCredit, getActiveMembershipForDeduction, findMembershipByBooking, setBusinessEnforcementPolicy)
    - tests/function-executor.test.ts retains all pre-existing test cases and gains a new Phase 8 describe block appended at the end
    - "[FLAGGED-UNVERIFIED: SESS-01 idempotency] Idempotency probe assumes the 'booking:<bookingId>:deduction' key uniqueness is enforced at DB level by the existing idempotency_key UNIQUE constraint on membership_ledger — verify this constraint is present in the Phase 7 schema before implementing deductSession"
    - "[FLAGGED-UNVERIFIED: SESS-01 concurrency] Concurrency probe assumes withBusinessContext wraps ALL of bookAppointmentTool inside an appDb.transaction() — verify at telegram.ts line ~410 that withBusinessContext is entered before routeConversationMessage is called"
    - "[FLAGGED-UNVERIFIED: SESS-04 precision] sessionsRemaining is INTEGER on the DB; NULL signals unlimited — verify membership_ledger.sessionsDeducted accepts -1 for credit restore (no UNSIGNED constraint)"
  artifacts:
    - tests/billing-session-deduction.test.ts
    - tests/billing-enforcement-policy.test.ts
    - tests/function-executor.test.ts (extended)
  key_links:
    - billing-session-deduction.test.ts describe structure aligns with SESS-01/02/03/04 test cases (integration, real Postgres)
    - billing-enforcement-policy.test.ts describe structure aligns with ENFC-01 test cases (unit, jest.mock)
    - function-executor.test.ts Phase 8 describe block aligns with ENFC-02/03 and SESS-01/02/03/04 bookAppointmentTool + cancelAppointmentTool behaviors
  prohibitions:
    - "[FLAGGED-UNVERIFIED] billing-session-deduction.test.ts MUST NOT import deductSession, restoreCredit, getActiveMembershipForDeduction, or findMembershipByBooking at the module level — these functions do not yet exist and will cause ts-jest compile failure"
    - "[FLAGGED-UNVERIFIED] billing-enforcement-policy.test.ts MUST NOT import handleSetEnforcementPolicy or setBusinessEnforcementPolicy at the module level — these functions ship in Plan 05, not Plan 01"
---

<objective>
Wave 0 test scaffolding for Phase 8. Creates three test files (two new, one extended) with it.todo stub cases covering all seven phase requirements. No production code is touched.

Purpose: Establish the test scaffold before any implementation so ts-jest compilation stays green throughout the phase. Follows the project's established Wave 0 pattern (STATE.md: "it.todo stubs with no imports from unbuilt modules").

Output:
- tests/billing-session-deduction.test.ts — new integration test file with 5 it.todo stubs (SESS-01 × 2, SESS-02, SESS-03, SESS-04)
- tests/billing-enforcement-policy.test.ts — new unit test file with 3 it.todo stubs (ENFC-01 × 3)
- tests/function-executor.test.ts — extended with describe('Phase 8') block containing 6 it.todo stubs (ENFC-02, ENFC-03, SESS-01 deduction, SESS-04 unlimited, SESS-02 restore, null-restore skip)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-enforcement-session-deduction/08-CONTEXT.md
@.planning/phases/08-enforcement-session-deduction/08-RESEARCH.md
</context>

## Artifacts This Plan Produces

New files:
- `tests/billing-session-deduction.test.ts` — it.todo stubs: "deducts 1 session atomically on booking insert", "deduction is idempotent on replay (same idempotency_key)", "restores credit on cancel within validity window (SESS-02)", "no credit restore when membership expired at cancel time (SESS-03)", "unlimited membership: no deduction, no restore (SESS-04)"
- `tests/billing-enforcement-policy.test.ts` — it.todo stubs: "set_enforcement_policy persists policy to businesses table (ENFC-01)", "set_enforcement_policy returns Greek confirmation string", "set_enforcement_policy rejects invalid policy value with Greek error string"

Extended files:
- `tests/function-executor.test.ts` — new describe('Phase 8: enforcement + session deduction') block appended; 6 it.todo stubs

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/billing-session-deduction.test.ts with it.todo stubs</name>
  <files>tests/billing-session-deduction.test.ts</files>

  <read_first>
    - tests/billing-membership-creation.test.ts — preamble shape (TEST_DATABASE_URL, jest.resetModules, require pattern, afterAll DATABASE_URL restore); copy this structure exactly
    - tests/helpers/billing-fixtures.ts — available fixture helpers (insertTestPackage, TestMembershipOptions) for future test body reference
    - tests/helpers/test-business.ts — insertTestBusiness shape
  </read_first>

  <action>
    Create tests/billing-session-deduction.test.ts as a NEW file.

    Use the billing-membership-creation.test.ts preamble verbatim for the TEST_DATABASE_URL block and jest.resetModules() setup. The file should require only modules that already exist: db from '../src/database/db', eq from 'drizzle-orm', membershipLedger and memberships from '../src/database/schema', and withBusinessContext from '../src/database/queries'. Do NOT require deductSession, restoreCredit, getActiveMembershipForDeduction, or findMembershipByBooking — those functions do not exist yet.

    Include a top-level comment citing SESS-01 through SESS-04 as the covered requirements.

    Add these describe blocks with it.todo stubs only (no test body):
    - describe('session deduction — SESS-01') with: it.todo('deducts 1 session atomically on booking insert'), it.todo('deduction is idempotent on replay (same idempotency_key)')
    - describe('credit restore — SESS-02/SESS-03') with: it.todo('restores credit on cancel within validity window (SESS-02)'), it.todo('no credit restore when membership expired at cancel time (SESS-03)')
    - describe('unlimited membership — SESS-04') with: it.todo('unlimited membership: no deduction row, no counter change')

    Add afterAll(() => { process.env.DATABASE_URL = ORIGINAL_DATABASE_URL; }) to reset the env override.

    Total: exactly 5 it.todo() items. No implementation in any stub body.
  </action>

  <acceptance_criteria>
    - File exists at tests/billing-session-deduction.test.ts
    - File contains exactly 5 it.todo() calls (grep -c "it.todo" tests/billing-session-deduction.test.ts equals 5)
    - File does NOT contain any import or require of deductSession, restoreCredit, getActiveMembershipForDeduction, or findMembershipByBooking
    - `npx jest --testPathPattern="billing-session-deduction" --no-coverage` exits 0 with 5 pending tests
  </acceptance_criteria>

  <verify>
    <automated>npx jest --testPathPattern="billing-session-deduction" --no-coverage 2>&1 | tail -5</automated>
  </verify>

  <done>File exists, 5 it.todo stubs present, jest exits 0 with 5 pending (0 failed)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tests/billing-enforcement-policy.test.ts with it.todo stubs</name>
  <files>tests/billing-enforcement-policy.test.ts</files>

  <read_first>
    - tests/billing-payment-flow.test.ts — jest.mock pattern for '../src/billing/queries' and '../src/database/queries' (top-level mock declarations before any import)
    - tests/billing-package-creation.test.ts — jest.mock structure for billing/tools handler stubs
  </read_first>

  <action>
    Create tests/billing-enforcement-policy.test.ts as a NEW file.

    Include a top-level comment citing ENFC-01 as the covered requirement and that this is a unit test (jest.mock, no real DB).

    Add these jest.mock declarations at the top of the file (before any imports):
    - jest.mock('../src/billing/queries', ...) with a factory returning a mock object that includes setBusinessEnforcementPolicy: jest.fn() alongside all existing billing/queries exports as jest.fn(). Since setBusinessEnforcementPolicy does not yet exist, mock the module factory without importing it at the top level.
    - jest.mock('../src/utils/logger', ...) to silence logs.

    Add describe('handleSetEnforcementPolicy — ENFC-01') with:
    - it.todo('persists the chosen policy to the businesses table')
    - it.todo('returns a Greek confirmation string containing the policy name')
    - it.todo('returns a Greek error string without DB call when policy value is invalid')

    Total: exactly 3 it.todo() items.

    Do NOT import handleSetEnforcementPolicy from '../src/billing/tools' at the module level — that export does not yet exist. The actual require will go inside the filled-in test bodies in Plan 05.
  </action>

  <acceptance_criteria>
    - File exists at tests/billing-enforcement-policy.test.ts
    - Contains exactly 3 it.todo() calls
    - Does NOT contain a top-level import or require of handleSetEnforcementPolicy
    - `npx jest --testPathPattern="billing-enforcement-policy" --no-coverage` exits 0 with 3 pending tests
  </acceptance_criteria>

  <verify>
    <automated>npx jest --testPathPattern="billing-enforcement-policy" --no-coverage 2>&1 | tail -5</automated>
  </verify>

  <done>File exists, 3 it.todo stubs present, jest exits 0 with 3 pending (0 failed)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend tests/function-executor.test.ts with Phase 8 it.todo stubs</name>
  <files>tests/function-executor.test.ts</files>

  <read_first>
    - tests/function-executor.test.ts — read the full file to understand its existing describe structure, mock setup, BUSINESS/CONTEXT constants, and where to safely append the new describe block without disrupting existing tests
  </read_first>

  <action>
    Append a new describe block at the very END of tests/function-executor.test.ts, after all existing describe blocks. Do not move, modify, or delete any existing code.

    The new block is: describe('Phase 8: enforcement + session deduction', () => { ... })

    Inside, add these 6 it.todo() items:
    - it.todo('block policy: executeTool(book_appointment) returns Greek refusal and does NOT call insertBooking when client has no active membership')
    - it.todo('flag policy: executeTool(book_appointment) calls sendTelegramMessage with flag alert BEFORE sendTelegramMessageWithKeyboard when client has no active membership')
    - it.todo('finite membership: executeTool(book_appointment) calls deductSession(membershipId, bookingId, idempotencyKey) after insertBooking succeeds')
    - it.todo('unlimited membership (sessionsRemaining: null): executeTool(book_appointment) does NOT call deductSession')
    - it.todo('executeTool(cancel_appointment) calls restoreCredit(membershipId, bookingId, idempotencyKey) after updateBookingStatus when findMembershipByBooking returns a membershipId')
    - it.todo('executeTool(cancel_appointment) does NOT call restoreCredit when findMembershipByBooking returns null')

    Do NOT add any new jest.mock calls for billing/queries.ts in this task — those come in Plan 04 Task 3. The stubs must compile with only the existing mocks in place.
  </action>

  <acceptance_criteria>
    - tests/function-executor.test.ts has a new describe('Phase 8: enforcement + session deduction') block appended at the end
    - Exactly 6 new it.todo() items present in that describe block
    - All previously passing tests still pass: `npx jest --testPathPattern="function-executor" --no-coverage` exits 0 with the same count of passing tests as before PLUS 6 pending
    - No existing test case is modified, moved, or deleted
  </acceptance_criteria>

  <verify>
    <automated>npx jest --testPathPattern="function-executor" --no-coverage 2>&1 | tail -8</automated>
  </verify>

  <done>New describe block appended with 6 it.todo stubs; all existing function-executor tests still pass; jest exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → test DB | Integration test preamble sets DATABASE_URL to randevuclaw_test — ensure ORIGINAL_DATABASE_URL is restored in afterAll to prevent env pollution across test suites |

## STRIDE Threat Register

```yaml
threats:
  - threat_id: T-08-01-pre
    title: Concurrent session deduction race (two simultaneous bookings over-deducting)
    asvs_ref: V4.1
    severity: high
    vector: Two Telegram webhook deliveries arrive simultaneously for the same client; both read sessionsRemaining=1 before either decrements
    impact: Client books two sessions when only one remains; membership balance goes negative
    disposition: mitigate
    mitigation: "Plan 03 implements SELECT FOR UPDATE via .for('update') on membership row inside withBusinessContext transaction — serializes concurrent deductions. This plan creates the test stubs that will verify this behavior."

  - threat_id: T-08-02-pre
    title: Deduction replay on Telegram redelivery
    asvs_ref: V4.2
    severity: high
    vector: Telegram re-delivers the same webhook update (e.g., due to 5xx retry); bookAppointmentTool runs again on the same bookingId
    impact: Second deduction decrements sessionsRemaining twice for a single booking
    disposition: mitigate
    mitigation: "Plan 03 implements idempotency_key='booking:<bookingId>:deduction' UNIQUE constraint + onConflictDoNothing() in deductSession. This plan creates stub tests for idempotency verification."
```
</threat_model>

<verification>
Full test suite passes after this plan:
- `npx jest --no-coverage` exits 0 (no regressions; 8 new pending todos across 3 files)
</verification>

<success_criteria>
- tests/billing-session-deduction.test.ts created with 5 it.todo stubs
- tests/billing-enforcement-policy.test.ts created with 3 it.todo stubs
- tests/function-executor.test.ts extended with 6 it.todo stubs in Phase 8 describe block
- No new imports of unbuilt functions in any stub file
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/08-enforcement-session-deduction/08-01-SUMMARY.md` when done
</output>
