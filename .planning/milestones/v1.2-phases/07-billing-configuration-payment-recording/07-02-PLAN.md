---
phase: 07-billing-configuration-payment-recording
plan: 02
type: execute
wave: 1
depends_on:
  - "07-01"
files_modified:
  - src/database/schema.ts
  - migrations/0006_billing_schema.sql
autonomous: false
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
  - PAY-01
  - PAY-02
  - PAY-03
assumption_delta_decision: "no-change — selectable packages are a new feature introduction, not a model generalization"

must_haves:
  truths:
    - "npx tsc --noEmit exits 0 after schema.ts modifications"
    - "billingPackages, memberships, membershipLedger are exported from src/database/schema.ts"
    - "clientBusinessRelationships in schema.ts has a clientName column (text, nullable)"
    - "migrations/0006_billing_schema.sql exists with CREATE TABLE for all 3 new tables and ALTER TABLE for client_name"
    - "npx drizzle-kit push exits 0 — Neon DB has billing_packages, memberships, membership_ledger tables"
    - "idempotency_key UNIQUE constraint exists on membership_ledger in both schema.ts and the live DB"
    - "UNIQUE partial index WHERE is_active = true on memberships(business_id, client_phone) in schema.ts"
  artifacts:
    - "src/database/schema.ts with 3 new exported table constants"
    - "migrations/0006_billing_schema.sql with 5 sections (client_name, billing_packages, memberships, membership_ledger, grants)"
  key_links:
    - "schema.ts billingPackages table referenced by src/billing/queries.ts (Wave 2)"
    - "schema.ts memberships table referenced by src/billing/queries.ts (Wave 2)"
    - "schema.ts membershipLedger table referenced by src/billing/queries.ts (Wave 2)"
    - "clientBusinessRelationships.clientName consumed by src/database/queries.ts upsert (Wave 2)"
---

<objective>
Extend the Drizzle schema with the 3 new billing tables (billing_packages, memberships, membership_ledger) and add the client_name column to client_business_relationships. Create the SQL migration file and push the schema to the Neon database.

Purpose: This schema is the foundation for all Phase 7 work — Waves 2-4 query layers and tool handlers depend on these type definitions being in place.
Output: Updated src/database/schema.ts with 4 new exports, migrations/0006_billing_schema.sql, and the live Neon DB with all tables applied.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-billing-configuration-payment-recording/07-CONTEXT.md
@.planning/phases/07-billing-configuration-payment-recording/07-PATTERNS.md
@src/database/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend src/database/schema.ts with billing tables and client_name column</name>
  <files>src/database/schema.ts</files>
  <read_first>
    src/database/schema.ts — full file: understand existing import list at top, existing table definitions, comment conventions (phase-cited inline JSDoc), and the end of file after onboardingSessions
    .planning/phases/07-billing-configuration-payment-recording/07-PATTERNS.md — Schema section: clientBusinessRelationships with clientName, billingPackages, memberships, membershipLedger table shapes, and uniqueIndex patterns
    .planning/phases/07-billing-configuration-payment-recording/07-CONTEXT.md — D-02 (session_count null for unlimited), D-04 (clientName upsert), D-10 (one active membership per business+client), D-11 (immutable ledger idempotencyKey)
  </read_first>
  <action>
    Make two distinct changes to src/database/schema.ts:

    CHANGE 1 — Add clientName column to clientBusinessRelationships: In the existing clientBusinessRelationships table definition, add a new column clientName after senderPhone. Use the type text('client_name') with no notNull() (nullable). Add an inline JSDoc comment following the established phase-citation convention: comment must cite Phase 7, D-04, and state the column is captured from Telegram from.first_name on each message and upserted to reflect the latest display name. The uniqueIndex on (businessId, senderPhone) already present must remain unchanged.

    CHANGE 2 — Add Phase 7 billing tables at end of file: After the onboardingSessions table definition, add a comment block "--- Phase 7: Billing Configuration & Payment Recording ---". Then define and export three new Drizzle table constants:

    billingPackages (table name 'billing_packages'): columns are id (serial primaryKey), businessId (integer notNull references businesses.id), name (text notNull), priceCents (integer 'price_cents' notNull — price in euro cents, e.g. 8000 = €80.00), validDays (integer 'valid_days' notNull — validity period in days), sessionCount (integer 'session_count' — nullable, null means unlimited per D-02), isActive (boolean 'is_active' notNull default true), createdAt (timestamp 'created_at' notNull defaultNow). Table-level index: uniqueIndex('unique_active_package_name').on(table.businessId, table.name).where(sql`is_active = true`) — the WHERE clause prevents a pending-inactive package from blocking creation of a later active package with the same name. Add inline JSDoc comments on priceCents (cents convention), sessionCount (D-02 null = unlimited), and isActive (D-03 pending = false, active = true on confirmation).

    memberships (table name 'memberships'): columns are id (serial primaryKey), businessId (integer notNull references businesses.id), clientPhone (text 'client_phone' notNull — Telegram from.id stringified, consistent with bookings.clientPhone convention), packageId (integer 'package_id' notNull references billingPackages.id), purchaseDate (text 'purchase_date' notNull — ISO "YYYY-MM-DD" in Europe/Athens local time), expiresAt (timestamp 'expires_at' notNull — stored as TIMESTAMP WITH TIME ZONE; DST-safe per addCalendarDays utility), sessionsRemaining (integer 'sessions_remaining' — nullable, null means unlimited per D-02), isActive (boolean 'is_active' notNull default true), createdAt (timestamp 'created_at' notNull defaultNow). Table-level index: uniqueIndex('unique_active_membership').on(table.businessId, table.clientPhone).where(sql`is_active = true`) — enforces D-10 one active membership per (business, client) at DB level.

    membershipLedger (table name 'membership_ledger'): columns are id (serial primaryKey), membershipId (integer 'membership_id' notNull references memberships.id), operationType (text 'operation_type' notNull — e.g. 'payment_recorded', 'session_deducted', 'credit_restored'), sessionsDeducted (integer 'sessions_deducted' notNull default 0 — positive for deductions, negative for credits), bookingId (integer 'booking_id' references bookings.id — nullable, set in Phase 8 when deduction is tied to a booking), reason (text — nullable, human-readable audit note), idempotencyKey (text 'idempotency_key' notNull unique — UNIQUE inline constraint prevents duplicate rows on webhook replay, D-11), createdAt (timestamp 'created_at' notNull defaultNow). Table-level index: uniqueIndex('unique_ledger_idempotency').on(table.idempotencyKey) — explicit index to complement the inline unique constraint for query performance.

    All three new tables must follow the established file conventions: use the same pgTable, serial, text, integer, boolean, timestamp, uniqueIndex, sql imports already present at the top of the file. Do not add new import statements unless a new Drizzle function is genuinely needed (it should not be for these tables).
  </action>
  <acceptance_criteria>
    - `grep -c 'clientName' src/database/schema.ts` returns >= 1
    - `grep -c 'export const billingPackages' src/database/schema.ts` returns 1
    - `grep -c 'export const memberships' src/database/schema.ts` returns 1
    - `grep -c 'export const membershipLedger' src/database/schema.ts` returns 1
    - `grep -c 'idempotencyKey' src/database/schema.ts` returns >= 1
    - `grep -c 'is_active = true' src/database/schema.ts` returns >= 2 (partial index WHERE clause on both billingPackages and memberships)
    - `npx tsc --noEmit` exits 0 with no TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>TypeScript compiles with zero errors; four new schema items (clientName column, billingPackages, memberships, membershipLedger) are exported from src/database/schema.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Create migrations/0006_billing_schema.sql (manual SQL reference artifact)</name>
  <files>migrations/0006_billing_schema.sql</files>
  <read_first>
    migrations/0005_split_hours.sql — migration file format: header comment block, section comments, idempotent SQL patterns
    migrations/0004_phase5_onboarding.sql — GRANT patterns for randevuclaw_app role and sequence grants
    .planning/phases/07-billing-configuration-payment-recording/07-PATTERNS.md — SQL migration template (migrations/0006-billing-schema.sql section) with complete SQL for all 5 sections
    drizzle.config.ts — confirms out directory is ./migrations
  </read_first>
  <action>
    Create migrations/0006_billing_schema.sql as a manual SQL reference artifact. This file is for documentation and manual psql application; drizzle-kit push in Task 3 pushes from schema.ts directly. The file must be idempotent (safe to run multiple times).

    Include a header comment block citing the purpose ("Add billing_packages, memberships, membership_ledger tables and client_name column to client_business_relationships — Phase 7 D-01..D-11") and application instructions for psql.

    Section 1 (client_name column): ALTER TABLE client_business_relationships ADD COLUMN IF NOT EXISTS client_name TEXT with a comment citing D-04.

    Section 2 (billing_packages table): DO $$ IF NOT EXISTS (SELECT FROM pg_tables...) CREATE TABLE billing_packages with columns id SERIAL PRIMARY KEY, business_id INTEGER NOT NULL REFERENCES businesses(id), name TEXT NOT NULL, price_cents INTEGER NOT NULL, valid_days INTEGER NOT NULL, session_count INTEGER (nullable for unlimited), is_active BOOLEAN NOT NULL DEFAULT TRUE, created_at TIMESTAMP NOT NULL DEFAULT NOW(). After the DO block: CREATE UNIQUE INDEX IF NOT EXISTS unique_active_package_name ON billing_packages (business_id, name) WHERE is_active = true.

    Section 3 (memberships table): DO $$ IF NOT EXISTS CREATE TABLE memberships with columns id SERIAL PRIMARY KEY, business_id INTEGER NOT NULL REFERENCES businesses(id), client_phone TEXT NOT NULL, package_id INTEGER NOT NULL REFERENCES billing_packages(id), purchase_date TEXT NOT NULL (YYYY-MM-DD in Athens TZ), expires_at TIMESTAMP NOT NULL (TIMESTAMP WITH TIME ZONE DST-safe), sessions_remaining INTEGER (nullable), is_active BOOLEAN NOT NULL DEFAULT TRUE, created_at TIMESTAMP NOT NULL DEFAULT NOW(). After the DO block: CREATE UNIQUE INDEX IF NOT EXISTS unique_active_membership ON memberships (business_id, client_phone) WHERE is_active = true — enforces D-10.

    Section 4 (membership_ledger table): DO $$ IF NOT EXISTS CREATE TABLE membership_ledger with columns id SERIAL PRIMARY KEY, membership_id INTEGER NOT NULL REFERENCES memberships(id), operation_type TEXT NOT NULL, sessions_deducted INTEGER NOT NULL DEFAULT 0, booking_id INTEGER REFERENCES bookings(id), reason TEXT, idempotency_key TEXT NOT NULL UNIQUE, created_at TIMESTAMP NOT NULL DEFAULT NOW(). After the DO block: CREATE UNIQUE INDEX IF NOT EXISTS unique_ledger_idempotency ON membership_ledger (idempotency_key) — D-11 replay protection.

    Section 5 (role grants): GRANT SELECT, INSERT, UPDATE ON billing_packages TO randevuclaw_app; GRANT SELECT, INSERT, UPDATE ON memberships TO randevuclaw_app; GRANT SELECT, INSERT ON membership_ledger TO randevuclaw_app (no UPDATE — ledger is append-only); GRANT USAGE, SELECT ON SEQUENCE billing_packages_id_seq TO randevuclaw_app; GRANT USAGE, SELECT ON SEQUENCE memberships_id_seq TO randevuclaw_app; GRANT USAGE, SELECT ON SEQUENCE membership_ledger_id_seq TO randevuclaw_app.
  </action>
  <acceptance_criteria>
    - `test -f migrations/0006_billing_schema.sql` exits 0
    - `grep -c 'billing_packages' migrations/0006_billing_schema.sql` returns >= 3 (header, CREATE TABLE, GRANT)
    - `grep -c 'idempotency_key' migrations/0006_billing_schema.sql` returns >= 1
    - `grep -c 'randevuclaw_app' migrations/0006_billing_schema.sql` returns >= 3 (one per new table)
    - `grep -c 'IF NOT EXISTS' migrations/0006_billing_schema.sql` returns >= 3 (idempotent table creation)
    - `grep -c 'client_name' migrations/0006_billing_schema.sql` returns >= 1 (Section 1 ALTER TABLE)
  </acceptance_criteria>
  <verify>
    <automated>test -f migrations/0006_billing_schema.sql &amp;&amp; grep -c 'membership_ledger' migrations/0006_billing_schema.sql</automated>
  </verify>
  <done>migrations/0006_billing_schema.sql exists with all 5 sections; all CREATE TABLE statements are idempotent via IF NOT EXISTS; role grants present for randevuclaw_app.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Push schema to Neon DB via drizzle-kit push</name>
  <files><!-- no files modified — schema push applies existing schema.ts to the live DB --></files>
  <what-built>
    src/database/schema.ts now includes billingPackages, memberships, membershipLedger tables and clientName column on clientBusinessRelationships. migrations/0006_billing_schema.sql documents the equivalent SQL. This task applies the schema.ts changes directly to the Neon PostgreSQL database.
  </what-built>
  <action>
    Apply the Drizzle schema changes from schema.ts to the Neon database. Ensure DATABASE_URL is set in .env.local (the Neon connection string). Run: npx drizzle-kit push. If drizzle-kit prompts about creating UNIQUE indexes or altering existing tables, review the proposed changes and enter y to proceed. Confirm exit code is 0 with no error output. Run npm test to verify existing tests remain green after schema push.
  </action>
  <how-to-verify>
    1. Ensure DATABASE_URL is set (check .env.local — it should reference the Neon connection string)
    2. Run: npx drizzle-kit push
    3. If drizzle-kit prompts about creating UNIQUE indexes or altering existing tables, review the proposed changes and enter y to proceed (expected: 3 new tables + 1 new column)
    4. Verify exit code is 0 with no errors in the output
    5. Optional but recommended: connect to Neon DB console (https://console.neon.tech) and confirm billing_packages, memberships, membership_ledger tables appear, and client_business_relationships has a client_name column
    6. Run npm test to confirm the full existing suite (208+ tests) remains green after schema push
  </how-to-verify>
  <verify>
    <automated>npm test --testTimeout=10000</automated>
    <human-check>Run npx drizzle-kit push; enter y at any prompts; confirm exit 0; verify billing_packages, memberships, membership_ledger exist in Neon DB console; run npm test and confirm green.</human-check>
  </verify>
  <done>npx drizzle-kit push exits 0; billing_packages, memberships, membership_ledger tables exist in Neon DB; npm test passes green.</done>
  <resume-signal>Type "approved" when npx drizzle-kit push completes without errors and npm test passes</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → DB | drizzle-kit push runs with DATABASE_URL credentials — schema changes are irreversible without a down-migration |
| client_name → display layer | Telegram first_name field is user-controlled input stored in client_name column |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Spoofing | Telegram callback_query ownership | high | accept (deferred) | Mitigated in 07-05 via owner_telegram_id validation; schema has no auth mechanism |
| T-07-02 | Tampering | Gemini NLU injection via package name | high | mitigate | billingPackages uses parameterized Drizzle insert (never string concatenation); name column is text type without injection risk at schema level |
| T-07-03 | Elevation | Unauthorized billing ops | high | mitigate | RLS policy enforced via randevuclaw_app role grants; SET LOCAL app.current_business_id gates access at DB level via withBusinessContext in 07-03 |
| T-07-04 | Tampering | Double membership on webhook replay | high | mitigate | idempotency_key TEXT NOT NULL UNIQUE on membership_ledger table enforced at DB level; prevents duplicate INSERT on replay |
| T-07-05 | Tampering | Price tampering in callback_data | medium | accept (deferred) | Schema stores price in priceCents column server-side; callback_data encoding policy enforced in 07-04 payment-flow.ts |
| T-07-06 | Information Disclosure | Multi-tenant data leak | high | mitigate | business_id FK on all 3 new tables; RLS via randevuclaw_app role grants in Section 5 of migration; withBusinessContext enforced in 07-03 queries |
</threat_model>

<verification>
After schema push: `npm test` must pass with the existing test suite (208+ tests green). The 8 billing test stubs from 07-01 remain in TODO state (no failures).

Spot-check schema: `npx drizzle-kit studio` (or psql) to verify billing_packages, memberships, membership_ledger tables exist with correct column types and constraints.
</verification>

<success_criteria>
- src/database/schema.ts exports billingPackages, memberships, membershipLedger, and clientBusinessRelationships with clientName
- npx tsc --noEmit exits 0
- migrations/0006_billing_schema.sql exists with idempotent CREATE TABLE and GRANT statements
- npx drizzle-kit push exits 0 — Neon DB contains all 3 new tables and the client_name column
- Full test suite (npm test) remains green after schema push
</success_criteria>

<output>
Create `.planning/phases/07-billing-configuration-payment-recording/07-02-SUMMARY.md` when done.
</output>
