---
phase: 07-billing-configuration-payment-recording
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/billing-package-creation.test.ts
  - tests/billing-nlu-parsing.test.ts
  - tests/billing-package-list.test.ts
  - tests/billing-package-deactivate.test.ts
  - tests/billing-payment-flow.test.ts
  - tests/billing-membership-creation.test.ts
  - tests/billing-dst-arithmetic.test.ts
  - tests/billing-view-membership.test.ts
  - .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md
autonomous: true
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
  - PAY-01
  - PAY-02
  - PAY-03
assumption_delta_decision: "no-change — selectable packages are a new feature introduction, not a model generalization"

must_haves:
  truths:
    - "npm test -- tests/billing-*.test.ts --testTimeout=10000 exits 0 with all items showing as TODO (no failures)"
    - "All 8 billing test stub files exist in the tests/ directory"
    - "COVERAGE.md exists at .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md with Telegram and Gemini API coverage tables"
    - "No test stub file imports from unbuilt modules (no compilation errors)"
  artifacts:
    - tests/billing-package-creation.test.ts
    - tests/billing-nlu-parsing.test.ts
    - tests/billing-package-list.test.ts
    - tests/billing-package-deactivate.test.ts
    - tests/billing-payment-flow.test.ts
    - tests/billing-membership-creation.test.ts
    - tests/billing-dst-arithmetic.test.ts
    - tests/billing-view-membership.test.ts
    - .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md
  key_links:
    - "Each test stub file cites its requirement ID (BILL-0N or PAY-0N) in a top comment"
    - "COVERAGE.md lists answerCallbackQuery, sendMessage-with-keyboard, and all 5 Gemini billing tools as INTEGRATE"
---

<objective>
Create failing test stub files for all Phase 7 billing behaviors and write the API coverage matrix. These stubs establish the Nyquist validation contract: subsequent plans fill in the tests as they build the implementation.

Purpose: Ensure every billing requirement has a corresponding automated test before any production code is written, per the Nyquist rule.
Output: 8 test stub files (it.todo format, no imports from unbuilt modules) and COVERAGE.md documenting which Telegram and Gemini API capabilities Phase 7 integrates or opts out of.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-billing-configuration-payment-recording/07-CONTEXT.md
@.planning/phases/07-billing-configuration-payment-recording/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 8 billing test stub files (Wave 0 RED state)</name>
  <files>
    tests/billing-package-creation.test.ts,
    tests/billing-nlu-parsing.test.ts,
    tests/billing-package-list.test.ts,
    tests/billing-package-deactivate.test.ts,
    tests/billing-payment-flow.test.ts,
    tests/billing-membership-creation.test.ts,
    tests/billing-dst-arithmetic.test.ts,
    tests/billing-view-membership.test.ts
  </files>
  <read_first>
    tests/jest.setup.ts — test environment setup patterns and globals
    tests/helpers/test-business.ts — fixture helper style: top comment citing phase + purpose
    .planning/phases/07-billing-configuration-payment-recording/07-VALIDATION.md — per-task requirement → test mapping table
  </read_first>
  <action>
    Create each of the 8 test stub files in the tests/ directory. Each file must:

    1. Start with a top comment citing the requirement ID it covers (e.g., "// covers BILL-01") and a one-line description of what behavior it will test.
    2. Contain only describe() blocks and it.todo() stubs — no imports from src/billing/ or from unbuilt modules that do not yet exist. This keeps the stubs importable by Jest without TypeScript errors.
    3. Match the behavior names from 07-VALIDATION.md per-task verification table.

    Specific describe/it.todo structure for each file:

    billing-package-creation.test.ts (BILL-01): describe('package creation via NLU') with it.todo stubs for: 'creates package from NLU-parsed args (create_package tool)', 'echoes all 4 fields in Greek confirmation text before DB write (D-03)', 'inserts package with is_active false pending confirmation', 'activates package on billing:pkg_confirm callback', 'cancels and deletes pending package on billing:pkg_cancel callback'

    billing-nlu-parsing.test.ts (BILL-01): describe('package NLU parsing edge cases') with it.todo stubs for: 'parses "10 μαθήματα €50 30 μέρες" to session_count 10 and price_cents 5000', 'maps "απεριόριστες" keyword to session_count null', 'maps "απεριόριστο" keyword to session_count null', 'maps "χωρίς όριο" keyword to session_count null', 'maps "unlimited" keyword to session_count null', 'validates price_cents must be integer >= 0', 'validates valid_days must be integer >= 1'

    billing-package-list.test.ts (BILL-02): describe('list active packages') with it.todo stubs for: 'returns formatted Greek list of active packages', 'excludes deactivated packages from list', 'returns empty Greek message when no active packages'

    billing-package-deactivate.test.ts (BILL-03): describe('deactivate package') with it.todo stubs for: 'sets is_active to false on the target package', 'existing memberships referencing the package remain intact', 'deactivated package excluded from getRecentClients package selection'

    billing-payment-flow.test.ts (PAY-01): describe('payment recording flow') with it.todo stubs for: 'shows recent clients as inline keyboard buttons (last 30 days)', 'falls back to service+date label when client_name is null', 'callback_data for client button is billing:client:{id} under 64 bytes', 'shows active packages as inline keyboard buttons after client selected', 'callback_data for package button is billing:package:{clientId}:{packageId} under 64 bytes', 'shows Greek confirmation message after package selected', 'validates callback_query sender against owner_telegram_id before any billing op'

    billing-membership-creation.test.ts (PAY-02): describe('membership creation with rolling expiry') with it.todo stubs for: 'calculates expires_at as purchase_date + valid_days in Europe/Athens timezone', 'stores expires_at as TIMESTAMP WITH TIME ZONE', 'writes initial membership_ledger row with operation_type payment_recorded', 'idempotency_key prevents duplicate membership creation on replay', 'on conflict for same (business_id, client_phone) replaces existing membership'

    billing-dst-arithmetic.test.ts (PAY-02 DST edge case): describe('DST-safe date arithmetic') with it.todo stubs for: 'Sept 22 + 30 calendar days = Oct 22 (not Oct 23, crossing DST boundary)', 'Oct 25 + 30 calendar days = Nov 24 (post-DST end, Athens UTC+2 → UTC+3)', 'addCalendarDays uses noon-UTC anchor to avoid off-by-one on DST transition nights'

    billing-view-membership.test.ts (PAY-03): describe('view client membership') with it.todo stubs for: 'returns package name, sessions remaining, and expiry date in Greek reply', 'unlimited membership shows "Απεριόριστες" in reply', 'returns Greek not-found message when no active membership', 'excludes expired memberships from result'
  </action>
  <acceptance_criteria>
    - `ls tests/billing-*.test.ts | wc -l` returns 8 (all 8 files exist)
    - `npm test -- tests/billing-*.test.ts --testTimeout=10000 2>&1 | grep -c 'todo'` returns >= 20 (stubs showing as TODO)
    - `npm test -- tests/billing-*.test.ts --testTimeout=10000` exits 0 (no failures, only todos)
    - `grep -rL 'from.*billing' tests/billing-*.test.ts | wc -l` returns 8 (no billing imports in stubs)
    - Each file contains at least one describe() block and at least one it.todo() call
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/billing-*.test.ts --testTimeout=10000</automated>
  </verify>
  <done>All 8 test stub files exist; Jest reports them all as TODO suites with zero failures.</done>
</task>

<task type="auto">
  <name>Task 2: Create COVERAGE.md (Telegram + Gemini API billing surface)</name>
  <files>
    .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md
  </files>
  <read_first>
    .planning/phases/07-billing-configuration-payment-recording/07-CONTEXT.md — D-07 (billing tools extend ai-owner-agent), D-08 (keyboard mode for payment flow)
    .planning/phases/07-billing-configuration-payment-recording/07-RESEARCH.md — Standard Stack section (Gemini tool-calling surface), Architecture Patterns Pattern 1 (Gemini tool registration), Pattern 2 (inline keyboard)
  </read_first>
  <action>
    Create COVERAGE.md at .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md.

    The file must contain two Markdown tables documenting the API coverage decisions for Phase 7's billing scope.

    Table 1 — Telegram Bot API (billing-relevant surface): columns are capability, decision (INTEGRATE or OPT-OUT), reason. Include these rows: sendMessage (INTEGRATE — billing confirmations and replies to owner); sendMessage with reply_markup InlineKeyboardMarkup (INTEGRATE — client selection buttons and package selection buttons in payment flow, D-05/D-06); answerCallbackQuery (INTEGRATE — acknowledge button taps immediately before any DB operation to dismiss Telegram spinner); editMessageReplyMarkup (OPT-OUT — not needed, replace with new message on each step).

    Table 2 — Google Gemini API (billing-relevant surface): columns are capability, decision, reason. Include these rows: generateContent with tools (INTEGRATE — NLU for all 5 billing intents via existing ai-owner-agent.ts loop, D-07); function_declarations for create_package tool (INTEGRATE — parses name/price_cents/valid_days/session_count from owner Greek message, D-01); function_declarations for list_packages tool (INTEGRATE — triggers package list query, BILL-02); function_declarations for deactivate_package tool (INTEGRATE — triggers soft-delete, BILL-03); function_declarations for record_payment tool (INTEGRATE — detects payment intent, switches to keyboard mode, D-08); function_declarations for view_client_membership tool (INTEGRATE — triggers membership query, PAY-03); streaming (OPT-OUT — not needed for PoC, synchronous generateContent responses sufficient).

    Add a brief introduction paragraph above the tables stating "Phase 7 billing commands extend the existing Gemini NLU tool system in ai-owner-agent.ts (D-07). No new API clients are introduced; all calls go through existing @google/genai and telegraf instances."
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md` exits 0
    - `grep -c 'INTEGRATE' .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md` returns >= 7
    - `grep -c 'OPT-OUT' .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md` returns >= 2
    - `grep -c 'answerCallbackQuery' .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md` returns >= 1
    - `grep -c 'record_payment' .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md` returns >= 1
    - `grep -c 'view_client_membership' .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md` returns >= 1
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md &amp;&amp; grep -c 'INTEGRATE' .planning/phases/07-billing-configuration-payment-recording/COVERAGE.md</automated>
  </verify>
  <done>COVERAGE.md exists with both API tables; all 5 Gemini billing tool definitions and 3 Telegram capabilities marked INTEGRATE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test files → src modules | Test stubs must not import unbuilt modules — import errors would contaminate Jest output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Spoofing | Telegram callback_query ownership | high | accept (Wave 0) | Not applicable in test stubs — mitigated in 07-04 and 07-05 implementation plans |
| T-07-02 | Tampering | Gemini NLU injection via package name | high | accept (Wave 0) | Not applicable in test stubs — Zod validation mitigated in 07-04 |
| T-07-03 | Elevation | Unauthorized billing ops by non-owner | high | accept (Wave 0) | Not applicable in test stubs — RLS mitigated in 07-03 |
| T-07-04 | Tampering | Double membership on webhook replay | high | accept (Wave 0) | Not applicable in test stubs — idempotency_key UNIQUE mitigated in 07-02 schema |
| T-07-05 | Tampering | Price tampering in callback_data | medium | mitigate | billing-payment-flow.test.ts includes it.todo stub verifying callback_data contains only ID not price |
| T-07-06 | Information Disclosure | Multi-tenant data leak | high | accept (Wave 0) | Not applicable in test stubs — withBusinessContext RLS mitigated in 07-03 |
</threat_model>

<verification>
Run after plan completion: `npm test -- tests/billing-*.test.ts --testTimeout=10000`
Expected output: 8 test suites, all items TODO, 0 failures, 0 errors.
</verification>

<success_criteria>
- 8 billing test stub files exist in tests/ directory
- All stubs use it.todo() format with no imports from unbuilt modules
- Jest runs all 8 files without compilation or runtime errors
- COVERAGE.md exists with Telegram and Gemini billing capability tables
- Both INTEGRATE and OPT-OUT decisions documented for each API capability
</success_criteria>

<output>
Create `.planning/phases/07-billing-configuration-payment-recording/07-01-SUMMARY.md` when done.
</output>
