---
phase: 19-class-setup-onboarding
plan: 03
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/onboarding/__tests__/class-setup-steps.test.ts
autonomous: true
requirements:
  - CLSS-01
  - CLSS-02
  - CLSS-03
  - CLSS-04
  - CLSS-05

must_haves:
  truths:
    - Automated tests verify the Ναι skip path (class_setup_query → handleActivate, zero catalog rows)
    - Automated tests verify full happy path from class_setup_query through to handleActivate with session catalog row and instances written
    - Automated tests verify weekday parsing for 'καθημερινά' keyword and explicit comma-separated days
    - Automated tests verify invalid inputs at each step re-ask without advancing the step
    - handleConfigLastSessionThresholdStep branching is tested for both bookingMode='fixed_sessions' and 'open_slots'
  artifacts:
    - src/onboarding/__tests__/class-setup-steps.test.ts
  key_links:
    - Tests import handlers directly from src/onboarding/steps.ts
    - createSessionCatalogWithExpansion is mocked (jest.mock) so tests do not hit the DB
    - sendTelegramMessage and sendTelegramMessageWithKeyboard are mocked via jest.mock('../../../telegram/client')
---

<objective>
Write integration tests for the class setup onboarding step handlers added in Plan 19-01.

Purpose: Ensure skip path, happy path, weekday parsing, and invalid-input re-ask are all machine-verified before shipping.

Output: src/onboarding/__tests__/class-setup-steps.test.ts with passing tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/onboarding/steps.ts
@src/onboarding/router.ts
@src/session/manager.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write class-setup step unit tests</name>
  <files>src/onboarding/__tests__/class-setup-steps.test.ts</files>
  <behavior>
    - Test A — skip path: handleClassSetupQuery with 'όχι' text → handleActivate called, updateOnboardingStep NOT called with class_setup_service
    - Test B — advance path: handleClassSetupQuery with 'ναι' text → updateOnboardingStep called with 'class_setup_service'
    - Test C — service match: handleClassSetupServiceStep with text matching a service name → updates step to class_setup_weekdays
    - Test D — service no match: handleClassSetupServiceStep with unrecognized name → step NOT advanced, re-ask message sent
    - Test E — weekday parsing καθημερινά: handleClassSetupWeekdaysStep with 'καθημερινά' → collectedData.weekdays = ['Δευτέρα','Τρίτη','Τετάρτη','Πέμπτη','Παρασκευή']
    - Test F — weekday parsing explicit: handleClassSetupWeekdaysStep with 'Δευτέρα, Τετάρτη' → weekdays = ['Δευτέρα','Τετάρτη']
    - Test G — time valid: handleClassSetupTimeStep with '09:00' → advances to class_setup_capacity
    - Test H — time invalid: handleClassSetupTimeStep with 'abc' → step not advanced, error message sent
    - Test I — capacity valid + DB call: handleClassSetupCapacityStep with '4' → createSessionCatalogWithExpansion called with correct (businessId, serviceId, rrule, time, 4)
    - Test J — capacity invalid: handleClassSetupCapacityStep with '0' → step not advanced
    - Test K — class_setup_more Ναι → step resets to class_setup_service
    - Test L — class_setup_more Όχι → handleActivate called
    - Test M — handleConfigLastSessionThresholdStep with bookingMode='fixed_sessions' → advances to class_setup_query (not done)
    - Test N — handleConfigLastSessionThresholdStep with bookingMode='open_slots' → calls handleActivate directly
  </behavior>
  <action>
    Create src/onboarding/__tests__/class-setup-steps.test.ts.

    Look at an existing onboarding test file first to follow the same mock patterns. Check src/onboarding/__tests__/ for existing examples.

    Mock setup:
    - jest.mock('../../../database/db') — mock db
    - jest.mock('../../../telegram/client', () => ({ sendTelegramMessage: jest.fn(), sendTelegramMessageWithKeyboard: jest.fn() }))
    - jest.mock('../../../session/manager', () => ({ createSessionCatalogWithExpansion: jest.fn().mockResolvedValue({ catalogId: 1, instanceCount: 12 }), buildRRuleString: jest.fn().mockReturnValue('FREQ=WEEKLY;BYDAY=MO,WE'), listSessions: jest.fn().mockResolvedValue([]) }))
    - jest.mock('../../queries', () => ({ updateOnboardingStep: jest.fn(), activateBusiness: jest.fn() }))
    - jest.mock('../../../database/queries', () => ({ listServicesForBusiness: jest.fn().mockResolvedValue([{ id: 42, name: 'Pilates Reformer', price: 2000, durationMin: 60 }]), findServiceById: jest.fn().mockResolvedValue({ id: 42, name: 'Pilates Reformer', price: 2000, durationMin: 60 }) }))

    Helper fixtures:
    - buildSession(step, collectedData): OnboardingSession-shaped object with id=1, currentStep=step, collectedData=JSON.stringify(collectedData)
    - buildBusiness(bookingMode?): Business-shaped object with id=99, bookingMode=bookingMode??'fixed_sessions', botToken='tok', webhookId=null, webhookSecret=null (and any other required fields derived from the Business type)

    All tests use OWNER_TELEGRAM_ID = 'owner-123' as the chat ID.

    For Test A and L (handleActivate path), note that handleActivate calls:
    - unregisterBotWebhook, registerBotWebhook (mock these via jest.mock('../../../telegram/client') adding those exports as jest.fn())
    - activateBusiness from './queries' (already mocked)
    - db.update(...) — already mocked via jest.mock('../../../database/db')
    - config.webhookBaseUrl must be set: mock '../../../config' returning { webhookBaseUrl: 'https://example.com', geminiApiKey: 'key' }

    Write tests as Jest describe/it blocks. The test file must not import jest.config from the global scope — it uses the project's existing Jest config.

    IMPORTANT: Do not run the full test suite. Run only this file:
    npm test -- --testPathPattern="class-setup-steps" --testTimeout=20000
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="class-setup-steps" --testTimeout=20000</automated>
  </verify>
  <done>
    - All 14 test cases (A through N) pass
    - No TypeScript errors: npx tsc --noEmit
    - Zero mocked functions produce unexpected calls
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures | All DB and Telegram calls mocked; no real network or DB access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-05 | Information Disclosure | Test file | low | accept | Test fixtures contain only dummy tokens and IDs; no real credentials in test code |
</threat_model>

<verification>
npm test -- --testPathPattern="class-setup-steps" --testTimeout=20000

All 14 tests must pass. No skipped or pending tests.
</verification>

<success_criteria>
- Test file runs to completion with 14 passing tests and 0 failures
- npx tsc --noEmit passes
</success_criteria>

<output>
Create `.planning/phases/19-class-setup-onboarding/19-03-SUMMARY.md` when done
</output>
