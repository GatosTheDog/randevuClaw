---
phase: 19-class-setup-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/onboarding/steps.ts
  - src/onboarding/router.ts
autonomous: true
requirements:
  - CLSS-01
  - CLSS-02
  - CLSS-03
  - CLSS-04

must_haves:
  truths:
    - When onboarding reaches class_setup_query the owner sees a Ναι/Όχι prompt asking if they want to set up a class schedule
    - Owner who taps Ναι is walked through class_setup_service → class_setup_weekdays → class_setup_time → class_setup_capacity and session instances are persisted via createSessionCatalogWithExpansion before advancing to done
    - Owner who taps Όχι (skip) at class_setup_query goes directly to handleActivate without creating any session catalog rows
    - Owner can add another class after the first (class_setup_more prompt) or stop; all choices lead eventually to handleActivate
    - OnboardingStep type contains every new step string; dispatchOnboardingStep routes all new steps
  artifacts:
    - src/onboarding/steps.ts — updated with new step handlers
    - src/onboarding/router.ts — dispatch covers all new step names
  key_links:
    - handleConfigLastSessionThresholdStep in steps.ts now calls handleClassSetupQuery instead of handleActivate directly
    - handleClassSetupSkip / handleClassSetupMore both eventually reach handleActivate
    - createSessionCatalogWithExpansion imported from session/manager at top of steps.ts (already imported? check — if not, add import)
---

<objective>
Add the class schedule setup step cluster to the onboarding flow.

Purpose: Owners who use fixed_sessions mode need an in-onboarding guided path to define their first recurring class before the business goes live. Per CLSS-01/02/03/04, this is a guided multi-turn dialog; skip is explicit (Ναι/Όχι button).

Output: New OnboardingStep variants and handler functions in steps.ts; dispatchOnboardingStep in router.ts extended to route them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-class-setup-onboarding/19-01-PLAN.md
@src/onboarding/steps.ts
@src/onboarding/router.ts
@src/session/manager.ts
@src/database/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add class_setup_* steps to OnboardingStep type and implement handlers</name>
  <files>src/onboarding/steps.ts</files>
  <behavior>
    - handleClassSetupQuery: text contains 'ναι'/'yes' → advance to class_setup_service; text contains 'όχι'/'no' → call handleActivate directly (skip path, CLSS-04)
    - handleClassSetupServiceStep: owner names the service for the class; must match an existing service for the business (listServicesForBusiness lookup, case-insensitive prefix match); on no match send numbered list and re-ask; on match store serviceId in collectedData.classSetup.serviceId and advance to class_setup_weekdays
    - handleClassSetupWeekdaysStep: parse comma-separated Greek weekday names from input; accept 'καθημερινά'/'daily' keyword mapping to ['Δευτέρα','Τρίτη','Τετάρτη','Πέμπτη','Παρασκευή']; accept 'καθε' + weekday list; at least 1 valid weekday required or re-ask; store weekdays array in collectedData.classSetup.weekdays; advance to class_setup_time
    - handleClassSetupTimeStep: accept 'HH:MM' 24-hour format (same RANGE_REGEX structure as existing code); on valid advance to class_setup_capacity storing startTime in collectedData.classSetup.startTime; on invalid re-ask
    - handleClassSetupCapacityStep: accept positive integer 1-99; store in collectedData.classSetup.capacity; call createSessionCatalogWithExpansion(business.id, serviceId, rruleString, startTime, capacity); send Greek confirmation "X μαθήματα δημιουργήθηκαν για [service] [weekdays] [time]"; advance to class_setup_more
    - handleClassSetupMoreStep: Ναι → reset classSetup in collectedData and advance to class_setup_service; Όχι → call handleActivate
    - buildRRuleString is already imported from session/manager — use it to build the rrule from stored weekdays array
  </behavior>
  <action>
    In src/onboarding/steps.ts:

    1. Extend the OnboardingStep union type (lines 22-46) to include the new variants in order after config_last_session_threshold and before 'done':
       'class_setup_query' | 'class_setup_service' | 'class_setup_weekdays' | 'class_setup_time' | 'class_setup_capacity' | 'class_setup_more'

    2. Extend CollectedData interface to include an optional classSetup field:
       classSetup?: { serviceId?: number; weekdays?: string[]; startTime?: string; capacity?: number }

    3. Add import for listServicesForBusiness from '../database/queries' (check if already imported; if not, add it).

    4. Add import for createSessionCatalogWithExpansion and buildRRuleString from '../session/manager'.

    5. Implement handleClassSetupQuery (exported async function):
       - Parses Ναι/Όχι the same pattern as existing yes/no handlers
       - Ναι: updateOnboardingStep(session.id, 'class_setup_service', null); send message listing existing services by name with instruction "Για ποιο μάθημα/υπηρεσία θέλετε να ορίσετε πρόγραμμα; (π.χ. Reformer Pilates)"
       - Όχι: call await handleActivate(session, business, ownerTelegramId) directly — per CLSS-04 skip path
       - Unrecognized: re-send Ναι/Όχι keyboard

    6. Implement handleClassSetupServiceStep (exported async function):
       - Call listServicesForBusiness(business.id) to get available services
       - Case-insensitive substring match on service.name vs text.trim()
       - If no match: send numbered list "Διαθέσιμες υπηρεσίες:\n1. X\n2. Y\nΠοια υπηρεσία;" and return (do not advance)
       - If exactly one match OR owner types a number 1-N: store that service's id in collectedData.classSetup.serviceId; advance to class_setup_weekdays; send "Ποιες μέρες; (π.χ. Δευτέρα, Τετάρτη, Παρασκευή ή 'καθημερινά' για Δευτ-Παρ)"

    7. Implement handleClassSetupWeekdaysStep (exported async function):
       - Parse text for Greek weekday names using GREEK_DAY_NAMES (already declared in steps.ts) keys; OR handle the keyword 'καθημερινά' as shorthand for weekdays 1-5 names
       - Collect matched weekday name strings (not indices) into an array
       - At least 1 required; else re-ask "Δεν αναγνωρίστηκαν μέρες. Παρακαλώ γράψτε π.χ. Δευτέρα, Τετάρτη, Παρασκευή:"
       - Store weekdays array in collectedData.classSetup.weekdays; advance to class_setup_time; send "Τι ώρα; (π.χ. 09:00)"

    8. Implement handleClassSetupTimeStep (exported async function):
       - Use existing TIME_ONLY_REGEX: /^([01]\d|2[0-3]):[0-5]\d$/ — create this constant near the top of the step handlers section (it is the first half of RANGE_REGEX but anchored to just one time)
       - On valid: store startTime in collectedData.classSetup.startTime; advance to class_setup_capacity; send "Πόσες θέσεις; (π.χ. 4)"
       - On invalid: send "Μη έγκυρη ώρα. Χρησιμοποιήστε μορφή ΩΩ:ΛΛ (π.χ. 09:00):"

    9. Implement handleClassSetupCapacityStep (exported async function):
       - Parse positive integer 1-99 from text
       - On invalid: send "Μη έγκυρος αριθμός. Εισάγετε 1-99:" and return
       - Retrieve collectedData.classSetup — if serviceId/weekdays/startTime are missing, log error and reset to class_setup_query with error message (defensive guard)
       - Call buildRRuleString(weekdays, startTime) to build rruleString
       - Call createSessionCatalogWithExpansion(business.id, serviceId, rruleString, startTime, capacity)
       - Send confirmation: "Δημιουργήθηκαν X μαθήματα για [serviceName] [startTime] — [weekdays joined comma]. Θέλετε να προσθέσετε άλλο μάθημα;"
       - To get serviceName, call findServiceById(business.id, serviceId) — add import if needed
       - Advance to class_setup_more with YES_NO_BUTTONS keyboard

    10. Implement handleClassSetupMoreStep (exported async function):
        - Ναι: clear classSetup (set to {}) in collectedData; advance to class_setup_service; send service-name prompt
        - Όχι: call handleActivate(session, business, ownerTelegramId)
        - Unrecognized: re-send Ναι/Όχι keyboard "Θέλετε να προσθέσετε άλλο μάθημα;"

    11. Modify handleConfigLastSessionThresholdStep (existing function at bottom of steps.ts): currently ends with `await handleActivate(...)`. Change it to instead call a new inline helper:
        - If business.bookingMode === 'fixed_sessions': advance to class_setup_query and send Ναι/Όχι keyboard asking "Θέλετε να ορίσετε τώρα τo πρόγραμμα μαθημάτων; (μπορείτε να το κάνετε αργότερα από το μενού)"
        - Else (open_slots): call handleActivate directly as before

    Note: findServiceById must be imported from '../database/queries' (check existing imports first). The function signature is findServiceById(businessId: number, serviceId: number): Promise<Service | null>.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="onboarding/steps" --testTimeout=20000</automated>
  </verify>
  <done>
    - OnboardingStep type includes class_setup_query, class_setup_service, class_setup_weekdays, class_setup_time, class_setup_capacity, class_setup_more
    - All 6 handler functions are exported from steps.ts with correct signatures
    - handleConfigLastSessionThresholdStep branches on bookingMode before calling handleActivate
    - TypeScript compiles without errors: npx tsc --noEmit
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire class_setup_* steps in dispatchOnboardingStep</name>
  <files>src/onboarding/router.ts</files>
  <action>
    In src/onboarding/router.ts:

    1. Add imports for the 6 new handler functions at the top import block (from './steps'):
       handleClassSetupQuery, handleClassSetupServiceStep, handleClassSetupWeekdaysStep, handleClassSetupTimeStep, handleClassSetupCapacityStep, handleClassSetupMoreStep

    2. In dispatchOnboardingStep, add new else-if branches after the existing config_last_session_threshold branch and before the step === 'done' branch:

       else if (step === 'class_setup_query') {
         await handleClassSetupQuery(session, business, ownerTelegramId, messageText);
       } else if (step === 'class_setup_service') {
         await handleClassSetupServiceStep(session, business, ownerTelegramId, messageText);
       } else if (step === 'class_setup_weekdays') {
         await handleClassSetupWeekdaysStep(session, business, ownerTelegramId, messageText);
       } else if (step === 'class_setup_time') {
         await handleClassSetupTimeStep(session, business, ownerTelegramId, messageText);
       } else if (step === 'class_setup_capacity') {
         await handleClassSetupCapacityStep(session, business, ownerTelegramId, messageText);
       } else if (step === 'class_setup_more') {
         await handleClassSetupMoreStep(session, business, ownerTelegramId, messageText);
       }

    3. Confirm TypeScript compiles: npx tsc --noEmit
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - dispatchOnboardingStep handles all 6 new step names without falling through to the unknown-step logger
    - No TypeScript errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner Telegram → onboarding handler | Free-text owner input parsed for weekday names, times, capacity integers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | handleClassSetupCapacityStep | medium | mitigate | Validate capacity is 1-99 integer before calling createSessionCatalogWithExpansion; guard against missing collectedData fields before DB write |
| T-19-02 | Elevation of Privilege | handleClassSetupServiceStep | medium | mitigate | listServicesForBusiness uses businessId from the session context (owner-owned), never from user text; cross-tenant lookup not possible |
| T-19-03 | Denial of Service | handleClassSetupCapacityStep expansion | low | accept | createSessionCatalogWithExpansion already limits expansion to 90 days; rrule expansion is bounded |
</threat_model>

<verification>
1. Run `npx tsc --noEmit` — no errors
2. Trigger onboarding manually or via test, set bookingMode to fixed_sessions, confirm class_setup_query step is reached after config_last_session_threshold
3. Open_slots mode: confirm class_setup_query is NOT reached (goes straight to done)
</verification>

<success_criteria>
- OnboardingStep type has 6 new step variants and TypeScript accepts it
- New handler functions compile and dispatch correctly
- Skip path (Όχι) creates zero session catalog rows
- Non-skip path calls createSessionCatalogWithExpansion with correct (businessId, serviceId, rruleString, startTime, capacity) args
</success_criteria>

<output>
Create `.planning/phases/19-class-setup-onboarding/19-01-SUMMARY.md` when done
</output>
