---
phase: 20-client-escalation
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/webhooks/telegram.ts
  - tests/client-escalation.test.ts
autonomous: true
requirements:
  - ESCL-03

must_haves:
  truths:
    - Admin can tap "Εγκρίνω εξαίρεση" on the escalation message and the system creates a booking that bypasses the enforcement gate — but only if the session instance still has a free slot at the time of tap
    - Admin can tap "Απάντηση πελάτη" and the system sends a pre-composed prompt message to the admin asking them to type a reply; the next admin message goes to the client (via CMENU-05 free-text flow, not implemented here — this plan delivers only the reply prompt)
    - The escl:approve and escl:reply callback_data arms are parsed by parseCallbackData and returned as EscalationCallbackResult with discriminant escalationAction
    - Full-capacity at approval time returns a clear Greek message to the admin ("Δεν ήταν δυνατή η εξαίρεση: το μάθημα παραμένει πλήρες.")
    - Cross-tenant guard: approve handler re-derives the business from senderTelegramId (findBusinessByOwnerTelegramId), never from callback_data IDs
  artifacts:
    - src/webhooks/telegram.ts — EscalationCallbackResult type + parseCallbackData escl: arm + handleCallbackQuery escl arm
    - tests/client-escalation.test.ts — integration tests for parseCallbackData escl arms and approve/reply handler behaviours
  key_links:
    - parseCallbackData regex: /^escl:(approve|reply):(\d+)(?::(\d+))?$/ with discriminant escalationAction
    - approve handler calls bookSessionInstance with activeMembership=null (bypass enforcement, respect capacity DB lock)
    - reply handler sends a fixed Greek prompt to admin chatId; client ID stored in parsed result for future wiring
    - handleCallbackQuery dispatches on 'escalationAction' in parsed before any other arm
---

<objective>
Add the admin-side escalation response: parse escl: callback_data, implement the
approve-exception handler (calls bookSessionInstance directly, respects capacity),
and implement the reply-to-client prompt handler. Add integration tests covering
the new parseCallbackData arm and both handler paths.

Purpose: Completes ESCL-03 — admin can act inline on escalation notifications without
leaving the bot chat.

Output: Updated telegram.ts with EscalationCallbackResult + new handleCallbackQuery arm;
new tests/client-escalation.test.ts covering the full escalation round-trip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/webhooks/telegram.ts
@src/telegram/escalation.ts
@src/telegram/handlers/client-menu.ts
@src/session/manager.ts
@src/database/queries.ts
@tests/admin-menu.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EscalationCallbackResult type + parseCallbackData escl: arm to telegram.ts</name>
  <files>src/webhooks/telegram.ts</files>
  <action>
    Edit src/webhooks/telegram.ts — three changes:

    1. Add EscalationCallbackResult type near the other result types (after RenewalCallbackResult):

         export type EscalationCallbackResult = {
           escalationAction: 'approve' | 'reply';
           /** instanceId — present only for approve */
           instanceId?: number;
           /** clientTelegramId to notify or approve for */
           clientTelegramId: string;
         };

    2. Add EscalationCallbackResult to the parseCallbackData return union type annotation.

    3. Add the escl: arm inside parseCallbackData, after the renewalMatch block and before the menuMatch block:

         // Phase 20: escalation callback pattern
         // escl:approve:<instanceId>:<clientTelegramId>  — approve exception (instanceId present)
         // escl:reply:<clientTelegramId>                 — send reply prompt to admin
         const escalationMatch = data?.match(/^escl:(approve|reply):(\d+)(?::(\d+))?$/);
         if (escalationMatch) {
           const action = escalationMatch[1] as 'approve' | 'reply';
           if (action === 'approve') {
             // approve form: escl:approve:<instanceId>:<clientTelegramId>
             return {
               escalationAction: 'approve',
               instanceId: Number(escalationMatch[2]),
               clientTelegramId: escalationMatch[3] ?? '',
             };
           } else {
             // reply form: escl:reply:<clientTelegramId>
             return {
               escalationAction: 'reply',
               clientTelegramId: String(escalationMatch[2]),
             };
           }
         }

    The regex /^escl:(approve|reply):(\d+)(?::(\d+))?$/ handles both shapes:
      - escl:approve:99:123456789  → group1=approve, group2=99(instanceId), group3=123456789(clientId)
      - escl:reply:123456789       → group1=reply,   group2=123456789(clientId), group3=undefined

    Callback_data budget check: escl:approve:9999999:999999999 = 33 bytes — well under 64.

    Do not alter any existing parse arms.

    Also add EscalationCallbackResult to the escalation import at the top of the file
    (import the type from the new escalation module — only the type is needed here,
    no runtime import required since TypeScript can use 'import type'):

         import type { EscalationCallbackResult } from '../telegram/escalation';

    Wait — EscalationCallbackResult is defined IN telegram.ts itself (not in escalation.ts),
    so no import is needed. The type is defined and exported directly in telegram.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    parseCallbackData returns EscalationCallbackResult for escl:approve:* and escl:reply:* strings; existing arms untouched; TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add escl: handler arm in handleCallbackQuery + integration tests</name>
  <files>src/webhooks/telegram.ts, tests/client-escalation.test.ts</files>
  <action>
    PART A — handleCallbackQuery dispatch arm (src/webhooks/telegram.ts):

    Add import at the top of the file (after existing imports):

         import { bookSessionInstance } from '../session/manager';
         import { findClientBusinessRelationship } from '../database/queries';

    (Check if bookSessionInstance is already imported — if so, skip that line.)

    Inside handleCallbackQuery, add an escalation arm BEFORE the 'menuAction' in parsed check:

         // Phase 20: escalation callback routing (ESCL-03)
         // Discriminant: 'escalationAction' in result → EscalationCallbackResult
         // Cross-tenant guard: business re-derived from senderTelegramId via findBusinessByOwnerTelegramId.
         if ('escalationAction' in parsed) {
           const escl = parsed as EscalationCallbackResult;
           const ownerBusiness = await findBusinessByOwnerTelegramId(senderTelegramId);
           if (!ownerBusiness) {
             logger.warn({ senderTelegramId }, 'escl callback from unregistered owner, ignoring');
             return;
           }

           if (escl.escalationAction === 'approve') {
             // Approve-exception: book instanceId for clientTelegramId bypassing enforcement,
             // still subject to capacity DB lock (bookSessionInstance's SELECT FOR UPDATE).
             if (!escl.instanceId || !escl.clientTelegramId) {
               await sendTelegramMessage(senderTelegramId, 'Σφάλμα: ελλιπή δεδομένα εξαίρεσης.');
               return;
             }

             // Resolve serviceId from instanceId (same pattern as handleBookSessionExecute)
             const instanceRow = await db
               .select({ serviceId: sessionCatalog.serviceId })
               .from(sessionInstances)
               .innerJoin(sessionCatalog, eq(sessionInstances.catalogId, sessionCatalog.id))
               .where(eq(sessionInstances.id, escl.instanceId))
               .limit(1);
             const serviceId = instanceRow[0]?.serviceId;

             if (serviceId === undefined) {
               await sendTelegramMessage(senderTelegramId, 'Το μάθημα δεν βρέθηκε.');
               return;
             }

             const idempotencyKey = `escl:approve:${escl.clientTelegramId}:${escl.instanceId}`;
             const result = await bookSessionInstance(
               ownerBusiness.id,
               escl.instanceId,
               escl.clientTelegramId,
               serviceId,
               idempotencyKey,
               null   // activeMembership=null: bypass enforcement, use no-deduction path
             );

             if (!result || result.status === 'full') {
               await sendTelegramMessage(
                 senderTelegramId,
                 'Δεν ήταν δυνατή η εξαίρεση: το μάθημα παραμένει πλήρες.'
               );
               return;
             }

             // Notify client of approval
             try {
               await botTokenStore.run(ownerBusiness.botToken!, async () => {
                 await sendTelegramMessage(
                   escl.clientTelegramId,
                   'Η κράτησή σας εγκρίθηκε από τον διαχειριστή! Θα σας δούμε σύντομα.'
                 );
               });
             } catch (err) {
               logger.error({ err }, 'escl approve: client notification failed (best-effort)');
             }
             await sendTelegramMessage(senderTelegramId, 'Εξαίρεση εγκρίθηκε. Η κράτηση δημιουργήθηκε.');

           } else {
             // Reply action: prompt the admin to type a message to the client.
             // The actual message relay is handled by the existing free-text flow (CMENU-05).
             // This plan delivers only the reply prompt — future wiring can intercept the
             // next admin message and forward it.
             await sendTelegramMessage(
               senderTelegramId,
               `Γράψε το μήνυμα που θέλεις να στείλεις στον πελάτη (${escl.clientTelegramId}) και αποστολή.`
             );
           }

           if (callbackQuery.message?.message_id) {
             await editTelegramMessageReplyMarkup(senderTelegramId, callbackQuery.message.message_id, []);
           }
           return;
         }

    Make sure the Drizzle imports are available in this file — db, sessionInstances, sessionCatalog, eq
    are already imported (check the existing handleBookSessionExecute which uses them via client-menu.ts;
    in telegram.ts they may not be imported yet). Add:

         import { db } from '../database/db';
         import { sessionInstances, sessionCatalog } from '../database/schema';
         import { eq } from 'drizzle-orm';

    Only add those that are not already imported at the top of telegram.ts. The existing file already
    imports eq and db in some paths — verify before adding to avoid duplicate imports.

    PART B — Integration tests (tests/client-escalation.test.ts):

    Create tests/client-escalation.test.ts. Pattern follows admin-menu.test.ts exactly (mock all
    external modules, no real DB or Telegram API calls).

    Test file header (replace backticks with their meaning — do not use fenced code blocks):
      Description: Integration tests for Phase 20 client escalation — ESCL-01, ESCL-02, ESCL-03.
      Never run bare npm test — machine crashes. Use: npm test -- --testPathPattern="client-escalation" --testTimeout=20000

    Mocks: jest.mock for '../src/database/queries', '../src/telegram/client',
    '../src/telegram/registry', '../src/billing/queries', '../src/session/manager',
    '../src/onboarding/queries', '../src/database/db', '../src/database/schema',
    '../src/telegram/escalation'.

    Test groups and cases:

    GROUP 1: parseCallbackData — EscalationCallbackResult arm
      - 'escl:approve:99:123456789' → { escalationAction: 'approve', instanceId: 99, clientTelegramId: '123456789' }
      - 'escl:reply:123456789'      → { escalationAction: 'reply', instanceId: undefined, clientTelegramId: '123456789' }
      - 'escl:approve:1:' → escalationAction: 'approve', clientTelegramId: '' (edge case: empty client id from malformed data)
      - existing menu: arm not broken: 'menu:settings' still returns menuAction discriminant
      - existing cmenu: arm not broken: 'cmenu:book' still returns clientMenuAction discriminant

    GROUP 2: buildEscalationKeyboard (from '../src/telegram/escalation')
      Note: escalation.ts is mocked, so import buildEscalationKeyboard from the actual module
      in a test that does NOT mock it — use jest.unmock or import directly. Alternatively,
      test buildEscalationKeyboard in a separate describe block that imports the module without mocking.
      Simplest approach: do NOT mock escalation.ts for this group; mock only its dependencies
      (telegram/client and database/queries).
      - With instanceId: keyboard has two buttons: approve + reply
      - Without instanceId: keyboard has one button: reply only
      - Approve callback_data: starts with 'escl:approve:'
      - Reply callback_data: starts with 'escl:reply:'

    GROUP 3: sendEscalationToAdmin behavior
      - When business.botToken is missing, function returns without calling sendTelegramMessageWithKeyboard
      - When business.ownerTelegramId is missing, function returns without calling sendTelegramMessageWithKeyboard
      - When both are present, sendTelegramMessageWithKeyboard is called once via botTokenStore.run
      - Message text contains the clientTelegramId or clientName (from mocked findClientBusinessRelationship)
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="client-escalation" --testTimeout=20000</automated>
  </verify>
  <done>
    All three test groups pass; parseCallbackData returns EscalationCallbackResult for both escl: shapes; approve handler calls bookSessionInstance with activeMembership=null; reply handler sends the Greek prompt; TypeScript compiles without errors (npx tsc --noEmit).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| escl: callback_data → approve handler | instanceId and clientTelegramId arrive from callback_data — IDs only, no user data; business re-derived from owner's authenticated senderTelegramId |
| Approve exception → bookSessionInstance | activeMembership=null bypasses enforcement but the SELECT FOR UPDATE capacity check in bookSessionInstance still runs — full class stays full |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04 | Elevation of Privilege | escl:approve: bypasses enforcement | high | mitigate | Cross-tenant guard: ownerBusiness re-derived from senderTelegramId (findBusinessByOwnerTelegramId) before any booking mutation; a non-owner who crafts an escl:approve: callback_data is rejected at the owner check |
| T-20-05 | Tampering | instanceId in escl:approve: callback_data | medium | mitigate | instanceId is only used to resolve serviceId; actual capacity enforcement runs inside bookSessionInstance's SELECT FOR UPDATE — cannot book a full class regardless of what ID is in callback_data |
| T-20-06 | Spoofing | clientTelegramId in escl:reply: callback_data | low | accept | Admin explicitly chose to reply to this client; the free-text relay in CMENU-05 was admin-initiated; no automated message is sent to the client from the reply arm in this phase |
| T-20-07 | Denial of Service | Spamming escl: buttons | low | accept | Idempotency key on bookSessionInstance prevents duplicate bookings from repeated taps; capacity lock prevents overbooking |
</threat_model>

<verification>
- npx tsc --noEmit passes with no errors
- npm test -- --testPathPattern="client-escalation" --testTimeout=20000 — all tests pass
- npm test -- --testPathPattern="telegram-webhook" --testTimeout=20000 — existing tests unbroken
- npm test -- --testPathPattern="admin-menu" --testTimeout=20000 — existing tests unbroken
</verification>

<success_criteria>
1. parseCallbackData returns { escalationAction: 'approve', instanceId, clientTelegramId } for escl:approve: strings
2. parseCallbackData returns { escalationAction: 'reply', clientTelegramId } for escl:reply: strings
3. Approve handler calls bookSessionInstance with activeMembership=null; returns full-class message if capacity exhausted at approval time
4. Reply handler sends Greek prompt to admin (no message sent to client in this phase)
5. All existing parseCallbackData arms (booking, billing, slotless, renewal, menu, cmenu) continue to parse correctly
6. Test suite passes with no crashes
</success_criteria>

<output>
Create `.planning/phases/20-client-escalation/20-02-SUMMARY.md` when done
</output>
