// covers PAY-03
// Tests for querying and displaying a client's active membership:
// sessions remaining, expiry date, unlimited flag, and not-found / expired states.

describe('view client membership', () => {
  it.todo('returns package name, sessions remaining, and expiry date in Greek reply');
  it.todo('unlimited membership shows "Απεριόριστες" in reply');
  it.todo('returns Greek not-found message when no active membership');
  it.todo('excludes expired memberships from result');
});
