// covers BILL-02
// Tests for listing active billing packages via the list_packages tool:
// only active packages are returned; empty state is handled gracefully.

describe('list active packages', () => {
  it.todo('returns formatted Greek list of active packages');
  it.todo('excludes deactivated packages from list');
  it.todo('returns empty Greek message when no active packages');
});
